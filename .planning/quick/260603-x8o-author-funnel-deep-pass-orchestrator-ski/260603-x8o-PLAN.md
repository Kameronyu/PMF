---
phase: quick-260603-x8o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/funnel-deep-pass/SKILL.md
  - prompts/funnel-deep-pass.md
autonomous: true
requirements:
  - QUICK-260603-x8o
must_haves:
  truths:
    - "Running /funnel-deep-pass gives the operator a single self-contained orchestrator runbook — no need to open prompts/funnel-deep-pass.md to execute the workflow"
    - "The SKILL.md states plainly which pipeline steps are DETERMINISTIC (script/hook — enforced) vs JUDGMENT (agent — model call), with the two deterministic enforcement gates named (analyzer-context assembly + validate-analyzer verbatim-substring reject)"
    - "Both agent prompts (Router + Section Analyzer) live verbatim inside SKILL.md and nowhere else after retirement"
    - "prompts/funnel-deep-pass.md no longer contains the §ORCHESTRATION prose or either agent prompt — only §SCHEMA + closed enums + §DETERMINISTIC SCAFFOLD + a one-line pointer to the skill"
    - "The skill documents its chain position (/market-selection → /funnel-deep-pass → funnel-vectorize → /copywrite) and its precondition (a chosen space-map.json cell + assembled ads)"
  artifacts:
    - path: ".claude/skills/funnel-deep-pass/SKILL.md"
      provides: "Orchestrator skill: frontmatter + runbook + enforcement map + folded Router & Section Analyzer prompts + precondition checks + copy-paste deterministic chain + spawn templates + /copywrite hand-off + schema pointer"
      min_lines: 200
      contains: "name: funnel-deep-pass"
    - path: "prompts/funnel-deep-pass.md"
      provides: "Retired source: keeps §SCHEMA + enums + §DETERMINISTIC SCAFFOLD; agent prompts + orchestration replaced by a pointer"
      contains: ".claude/skills/funnel-deep-pass/SKILL.md"
  key_links:
    - from: ".claude/skills/funnel-deep-pass/SKILL.md"
      to: "prompts/funnel-deep-pass.md §SCHEMA"
      via: "pointer reference for the schema/controlled-vocab (not duplicated)"
      pattern: "funnel-deep-pass\\.md"
    - from: ".claude/skills/funnel-deep-pass/SKILL.md"
      to: "tools/funnel-analyzer-context.js + tools/hooks/validate-analyzer.js"
      via: "enforcement-map rows marking deterministic assembly + PostToolUse reject gate"
      pattern: "validate-analyzer\\.js"
---

<objective>
Author `.claude/skills/funnel-deep-pass/SKILL.md` — a new orchestrator skill that consolidates the funnel deep-pass workflow into a single source of truth, modeled on `.claude/skills/copywrite/SKILL.md`. Then retire the now-relocated orchestration prose + both agent prompts from `prompts/funnel-deep-pass.md`, leaving a pointer and keeping only the schema/scaffold there.

Purpose: Today the deep-pass workflow is split between an unstructured `prompts/funnel-deep-pass.md` and the operator's head. The operator's core worry is honor-system vs deterministic file reads — the skill must mark each pipeline step DETERMINISTIC (enforced) vs JUDGMENT (model call) so it is unambiguous that the Section Analyzer can never silently skip reading the funnel body (the bytes are embedded by the orchestrator; validate-analyzer.js rejects output whose verbatim_refs aren't literal substrings). After this, each fact lives in exactly one place: orchestration + agent prompts in the skill, schema + controlled vocab in the .md.

Output: One new SKILL.md + an edited `prompts/funnel-deep-pass.md` (agent prompts + orchestration removed, pointer added, schema/scaffold kept).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# THE TEMPLATE — match this orchestrator voice/structure exactly
@.claude/skills/copywrite/SKILL.md

# Sibling skill — note read_first + "DR bundle NOT auto-injected; you MUST Read it" lesson
@.claude/skills/market-selection/SKILL.md

# THE WORKFLOW BEING CONSOLIDATED — source for the verbatim agent prompts (Router lines 194-246, Section Analyzer lines 250-477), §ORCHESTRATION (179-191), §DETERMINISTIC SCAFFOLD (148-177), §SCHEMA (37-145)
@prompts/funnel-deep-pass.md

# Co-owns the schema + the DR-injection rule (§11)
@prompts/_specs/funnel-analysis-collection-spec.md

<verified_pipeline_contract>
All scripts/hooks EXIST and are verified (confirmed at plan time). Use these CLI signatures LITERALLY in the runbook:

- node tools/funnel-assemble.js <ads-json> [--competitor=<name>] [--source-type=DTC|crowdfunding] [--out=./funnels] [--crowdfund-lps=<file>]
- node tools/crowdfund-fetch.js   (SPA/Cloudflare render for crowdfunding LPs — feeds --crowdfund-lps)
- node tools/funnel-clean.js <funnel-package.json|dir> [--out=<dir>]
- node tools/funnel-score.js <funnel-package.json|dir> [--out=<dir>]
- node tools/funnel-analyzer-context.js --funnel=<funnel_id> [--space=<space>] [--clean=<path>] [--max-chars=<n>] [--out=<path>]
    (emits the assembled [DR bundle] + [<funnel_copy>] block to stdout or --out; exit 1 on empty/bad input)
- node tools/hooks/inject-dr.js   (regenerates prompts/_generated/section-analyzer-dr-context.md — the 6-file DR bundle; analyzer-context.js depends on it; regenerate if DR sources changed)
- node tools/funnel-store.js --space=<space> --funnel=<pkg.json> --beliefs=<records.json>
    OR batch: --space=<space> --dir=<scored-dir> --beliefs-dir=<analyzer-out-dir>
- node tools/funnel-vectorize.js --space=<space> [--out=<path>]   (builds runs/<space>/funnels/_index.json — the RAG index /copywrite consumes)
- tools/hooks/validate-analyzer.js — PostToolUse reject on Section Analyzer Write (verbatim-substring gate, overflow-belief rule, closed-vocab reject, position rule, single-funnel discipline)

PER-FUNNEL LOOP (from funnel-deep-pass.md §ORCHESTRATION):
  for each funnel: Router agent sets routing_flag → funnel-analyzer-context.js assembles block → spawn Section Analyzer with block embedded → validate-analyzer.js gates the Write → funnel-store.js persists.
  Then once: funnel-vectorize.js.
</verified_pipeline_contract>

<retirement_boundary>
In prompts/funnel-deep-pass.md (grep-confirmed at plan time):
- KEEP lines 1–177: header + ## SCHEMA (6a/6b) + closed enums + ## DETERMINISTIC SCAFFOLD
- REMOVE lines 179–477: ## ORCHESTRATION (per-funnel loop) + ## AGENT 1 — ROUTER + ## AGENT 2 — SECTION ANALYZER
- Replace the removed block with a one-line pointer to .claude/skills/funnel-deep-pass/SKILL.md
</retirement_boundary>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author .claude/skills/funnel-deep-pass/SKILL.md (orchestrator skill)</name>
  <files>.claude/skills/funnel-deep-pass/SKILL.md</files>
  <action>
Create `.claude/skills/funnel-deep-pass/SKILL.md`. Model the VOICE and STRUCTURE on `.claude/skills/copywrite/SKILL.md` (and borrow the read_first + "NOT auto-injected, you MUST Read it" framing from `market-selection/SKILL.md`). It is an orchestrator skill — write in the "You are the orchestrator running in the main loop" register; mark deterministic steps as "deterministic script — not an agent"; state plainly that "hooks do not fire inside subagents — injection is YOUR step."

Required sections, in this order:

1. **FRONTMATTER** (YAML, like copywrite): `name: funnel-deep-pass` + a multi-line `description:` that says: this is the DEEP-PASS COLLECTION ORCHESTRATOR; per funnel it (a) routes scope (Router agent), (b) deterministically assembles [DR bundle + cleaned funnel body] and EMBEDS it in the Section Analyzer's spawn prompt, (c) lets validate-analyzer.js reject off-contract output, (d) persists belief records; once done it vectorizes the store for /copywrite. State it runs AFTER /market-selection has picked an NTP cell and ads are assembled.

2. **Lead note** (like copywrite's "> WIRING FILE" callout): a one-line callout that the SCHEMA + controlled vocab are NOT duplicated here — they are co-owned by `prompts/funnel-deep-pass.md §SCHEMA` + `tools/hooks/validate-analyzer.js` + the spec, and this skill points back to them (rationale: triplicating the schema creates drift).

3. **## What this is** — the orchestrator's job: for each funnel in the assembled set, run the per-funnel judgment+deterministic loop, then vectorize once. There IS a parallel fan-out concern (N funnels) but the loop is per-funnel; spawn one Router + one Section Analyzer per funnel.

4. **## Chain position** — document verbatim: `/market-selection (pick NTP)` → `/funnel-deep-pass (this)` → `funnel-vectorize` → `/copywrite`. State the precondition plainly: a chosen `space-map.json` cell (giving `run_transformation` + `niche` + `source_type` per competitor) AND assembled ads must already exist.

5. **## ENFORCEMENT MAP — deterministic (enforced) vs judgment (model call)** — THE OPERATOR'S CORE WORRY. A table with columns `Step | Kind | Mechanism | What enforces it`. One row per pipeline step. Mark as DETERMINISTIC (script/hook): funnel-assemble, crowdfund-fetch, funnel-clean, funnel-score, funnel-analyzer-context (assembly), inject-dr (bundle regen), validate-analyzer (PostToolUse), funnel-store, funnel-vectorize. Mark as JUDGMENT (agent/model call): Router, Section Analyzer — ONLY these two. Below the table, state the three load-bearing truths PLAINLY (these are the operator's worry, stated as fact):
   - The Section Analyzer NEVER `Read`s files: `funnel-analyzer-context.js` deterministically assembles [DR bundle via inject-dr.js] + [cleaned funnel body in `<funnel_copy>`] and the orchestrator EMBEDS those bytes into the spawn prompt (this is exactly what quick task 260603-wfz built — cite it).
   - `validate-analyzer.js` (PostToolUse) REJECTS analyzer output whose `verbatim_refs[].text` aren't literal substrings of the funnel copy → an analyzer that ignored the body CANNOT pass. Deterministic gate, not trust.
   - Only Router + Section Analyzer are judgment agents; assemble / clean / score / store / vectorize + both hooks are deterministic.

6. **## PRECONDITION CHECK (run once)** — copy-pasteable bash, like copywrite's. Check the space-map cell exists and ads are assembled; check `prompts/_generated/section-analyzer-dr-context.md` exists (else `node tools/hooks/inject-dr.js`). If missing, STOP and tell operator what's absent — do not fabricate funnels.

7. **## THE DETERMINISTIC CHAIN (copy-paste bash)** — the assemble→clean→score sequence using the LITERAL CLI signatures from `<verified_pipeline_contract>` above. Each command preceded by one line of what it does and that it is deterministic. Include the crowdfunding branch (crowdfund-fetch → --crowdfund-lps). End this section at "scored funnel packages ready."

8. **## PER-FUNNEL LOOP (orchestration)** — the judgment loop. For EACH funnel:
   a. Spawn the **Router** agent (template in §9) → get `routing_flag`.
   b. Run `node tools/funnel-analyzer-context.js --space=<space> --funnel=<funnel_id>` to assemble the embedded block (deterministic — NOT the agent's job). Capture its stdout/--out.
   c. Spawn the **Section Analyzer** with that block embedded directly in the Task prompt (template in §9). State: the analyzer receives the BYTES; it does not Read anything. validate-analyzer.js gates its Write.
   d. `node tools/funnel-store.js ...` persists the belief records.
   Then ONCE after all funnels: `node tools/funnel-vectorize.js --space=<space>` builds the RAG index /copywrite consumes.

9. **## AGENT PROMPTS (folded in verbatim — these live nowhere else)** — two fenced sub-blocks:
   - **### AGENT 1 — ROUTER (spawn template)** — paste the FULL Router prompt VERBATIM from `prompts/funnel-deep-pass.md` lines 196–246 (between the ``` fences). Above it, a one-line "spawn this as a cheap-model subagent; fill INPUT from the funnel package + space-map cell."
   - **### AGENT 2 — SECTION ANALYZER (spawn template)** — paste the FULL Section Analyzer prompt VERBATIM from `prompts/funnel-deep-pass.md` lines 252–476 (between the ``` fences), INCLUDING the SECURITY/untrusted-data-boundary block. Above it, a one-line "spawn this as a quality-model subagent AFTER analyzer-context.js has assembled the block; embed the assembled [DR bundle + <funnel_copy>] block where the prompt expects it." Preserve the prompt's own FALLBACK-ONLY note (regenerate via inject-dr.js + Read) untouched.
   COPY THESE VERBATIM — do not paraphrase, rewrite, or 'improve' the agent prompts. They are load-bearing and hook-validated.

10. **## HAND-OFF TO /copywrite** — after vectorize, `runs/<space>/funnels/_index.json` exists; tell the operator to run `/copywrite` next with the same `<space>`. One sentence.

11. **## SCHEMA + CONTROLLED VOCAB (pointer, not duplicated)** — point to `prompts/funnel-deep-pass.md §SCHEMA` (6a funnel-level fields, 6b belief-instance records) + closed enums (CLAIM_TYPE / EXECUTION_TYPE / PROOF_TIER / MOVE / ROUTING_FLAG / BELIEF_ID_ANCHORS) + §DETERMINISTIC SCAFFOLD. State validate-analyzer.js enforces these enums; do not restate the values here.

Write naturally in the orchestrator register — do NOT pad to hit a line count. The two folded agent prompts alone are ~250 lines, so the file will be well over the 200-line floor.
  </action>
  <verify>
    <automated>test -f .claude/skills/funnel-deep-pass/SKILL.md && grep -q "name: funnel-deep-pass" .claude/skills/funnel-deep-pass/SKILL.md && grep -q "ENFORCEMENT MAP" .claude/skills/funnel-deep-pass/SKILL.md && grep -q "validate-analyzer.js" .claude/skills/funnel-deep-pass/SKILL.md && grep -q "AGENT 1 — ROUTER" .claude/skills/funnel-deep-pass/SKILL.md && grep -q "AGENT 2 — SECTION ANALYZER" .claude/skills/funnel-deep-pass/SKILL.md && grep -q "market-selection" .claude/skills/funnel-deep-pass/SKILL.md && grep -q "funnel-vectorize" .claude/skills/funnel-deep-pass/SKILL.md && grep -q "funnel-analyzer-context.js" .claude/skills/funnel-deep-pass/SKILL.md && grep -q "funnel-deep-pass.md" .claude/skills/funnel-deep-pass/SKILL.md && echo PASS</automated>
  </verify>
  <done>
SKILL.md exists with: frontmatter (name: funnel-deep-pass), the SCHEMA-not-duplicated lead note, What-this-is, Chain position, ENFORCEMENT MAP table + the three plain-stated truths, precondition check, copy-paste deterministic chain (literal CLI signatures), per-funnel loop, BOTH agent prompts folded verbatim, /copywrite hand-off, and a schema pointer to funnel-deep-pass.md §SCHEMA. The verify grep chain passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Retire relocated orchestration + agent prompts from prompts/funnel-deep-pass.md</name>
  <files>prompts/funnel-deep-pass.md</files>
  <action>
Edit `prompts/funnel-deep-pass.md` per `<retirement_boundary>`:

- KEEP lines 1–177 untouched: the header, `## SCHEMA` (6a + 6b), the `### Closed enums` block, and `## DETERMINISTIC SCAFFOLD`. These are the schema/controlled-vocab/scaffold that the skill points BACK to — they stay here as the single source of truth for the schema.
- REMOVE lines 179–477 entirely: `## ORCHESTRATION — per-funnel loop`, `## AGENT 1 — ROUTER` (full prompt), and `## AGENT 2 — SECTION ANALYZER` (full prompt). These have been relocated verbatim into the skill; relocation is non-lossy (they lived nowhere else).
- REPLACE the removed block with a one-line pointer section. Use a heading + one line, e.g.:

  ```
  ## ORCHESTRATION + AGENT PROMPTS — RELOCATED

  The per-funnel orchestration runbook and BOTH agent prompts (Router + Section Analyzer) now
  live in `.claude/skills/funnel-deep-pass/SKILL.md` (single source of truth). This file keeps
  only the §SCHEMA + closed enums + §DETERMINISTIC SCAFFOLD, which the skill points back to.
  ```

Implementation note governing this retirement (stated in locked decision #4): `tools/funnel-analyzer-context.js` builds the CONTEXT block (DR bundle + funnel body), NOT the analyzer's instruction text — the analyzer prompt text is orchestrator-owned, so removing it from this .md breaks no script. The two hooks (inject-dr.js, validate-analyzer.js) reference the SCHEMA/enums (kept) and the generated DR bundle, not the prose agent prompts (removed). Confirm with the grep below that no tool/hook sources the removed prose before finalizing.
  </action>
  <verify>
    <automated>! grep -q "AGENT 1 — ROUTER" prompts/funnel-deep-pass.md && ! grep -q "AGENT 2 — SECTION ANALYZER" prompts/funnel-deep-pass.md && grep -q "## SCHEMA" prompts/funnel-deep-pass.md && grep -q "DETERMINISTIC SCAFFOLD" prompts/funnel-deep-pass.md && grep -q "RELOCATED" prompts/funnel-deep-pass.md && grep -q ".claude/skills/funnel-deep-pass/SKILL.md" prompts/funnel-deep-pass.md && echo "PASS no-tool-refs:" && ! grep -rEl "funnel-deep-pass\.md" tools/ 2>/dev/null | grep . && echo "PASS"</automated>
  </verify>
  <done>
prompts/funnel-deep-pass.md no longer contains either agent prompt or the orchestration runbook prose; it retains §SCHEMA, the closed enums, and §DETERMINISTIC SCAFFOLD; the removed block is replaced by a RELOCATED pointer naming `.claude/skills/funnel-deep-pass/SKILL.md`. No script under tools/ references the removed prose (grep confirms tools/ does not source funnel-deep-pass.md for the agent prompts). Verify grep chain passes.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `.claude/skills/funnel-deep-pass/SKILL.md` exists with all 11 required sections (frontmatter, lead note, What this is, Chain position, ENFORCEMENT MAP, precondition, deterministic chain, per-funnel loop, both folded agent prompts, /copywrite hand-off, schema pointer).
- The two agent prompts are byte-for-byte the ones from the old `prompts/funnel-deep-pass.md` (manual spot-check: open both, diff the Router INPUT block and the Section Analyzer SECURITY block — they must match).
- `prompts/funnel-deep-pass.md` is now schema + scaffold + pointer only; the agent prompts appear in EXACTLY ONE place (the skill).
- Each fact lives in exactly one place: schema/vocab in the .md, orchestration + agent prompts in the skill.
- No code under tools/ was touched; this is doc/skill authoring only.
</verification>

<success_criteria>
- Operator can run /funnel-deep-pass and execute the full collection workflow from the skill alone, without opening the source .md (except to consult the schema, which the skill points to).
- The DETERMINISTIC vs JUDGMENT distinction is unambiguous in the ENFORCEMENT MAP, and the three honor-system-killing truths (analyzer never Reads / validate-analyzer rejects non-substring verbatim / only 2 judgment agents) are stated as plain fact.
- The Router + Section Analyzer prompts exist verbatim in the skill and NOWHERE ELSE (retirement non-lossy).
- prompts/funnel-deep-pass.md keeps the schema/scaffold as the single source of truth the skill references; no schema duplication.
- The chain (/market-selection → /funnel-deep-pass → funnel-vectorize → /copywrite) and precondition (chosen space-map.json cell + assembled ads) are documented.
</success_criteria>

<output>
After completion, create `.planning/quick/260603-x8o-author-funnel-deep-pass-orchestrator-ski/260603-x8o-SUMMARY.md`.
</output>
