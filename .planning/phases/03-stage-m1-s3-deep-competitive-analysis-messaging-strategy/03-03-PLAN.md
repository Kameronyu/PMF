---
phase: 03-stage-m1-s3-deep-competitive-analysis-messaging-strategy
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - prompts/funnel-deep-pass.md
  - tools/hooks/inject-dr.js
  - tools/hooks/validate-analyzer.js
autonomous: true
requirements: []   # No v1 REQ-ID. Specced M1-S3 collection-layer capability feeding M2; promote when M2 is planned.
user_setup: []

must_haves:
  truths:
    - "funnel-deep-pass.md carries TWO agent blocks: Router (sets routing_flag before the analyzer) and Section Analyzer (the one judgment workhorse)"
    - "The Router sets a funnel-level routing_flag in {structure_only, messaging_full, both} from transformation similarity, BEFORE the analyzer, never inferred by it"
    - "The Section Analyzer emits belief-instance records with funnel-level ordinal position, open-with-anchors 9-belief taxonomy, granular execution_detail"
    - "One controlled vocabulary with no duplicate tags; claim_type REUSES the existing direct|enlarged|mechanism|enhanced enum"
    - "inject-dr.js auto-injects the SIX named DR files into the Section Analyzer context each run (FILE injection, not RAG); other agents get DR baked into the prompt, mechanical bricks get none"
    - "validate-analyzer.js rejects off-anchor beliefs without lowered confidence, off-vocab tags, page-local position, and birdseye-only fields the analyzer must not set"
  artifacts:
    - path: "prompts/funnel-deep-pass.md"
      provides: "Router + Section Analyzer prompts + the record schema + closed-enum controlled vocab"
      contains: "SECTION ANALYZER"
    - path: "tools/hooks/inject-dr.js"
      provides: "Runtime DR-file auto-injection hook for the Section Analyzer (the 6 files)"
      contains: "dr-marketing"
    - path: "tools/hooks/validate-analyzer.js"
      provides: "Validate-on-Write hook for belief-instance records"
      contains: "belief_confidence"
  key_links:
    - from: "tools/hooks/inject-dr.js"
      to: "Section Analyzer context"
      via: "fs.readFileSync over the six dr-marketing/*.md files"
      pattern: "dr-marketing"
    - from: "tools/hooks/validate-analyzer.js"
      to: "cleaned funnel copy"
      via: "verbatim-substring gate on verbatim_refs[]"
      pattern: "verbatim"
---

<objective>
Build the one judgment file and its two hooks: `prompts/funnel-deep-pass.md` (Router agent block + Section Analyzer agent block + the record schema + the controlled vocabulary as closed enums), `tools/hooks/inject-dr.js` (auto-injects the six DR files into the Section Analyzer at runtime), and `tools/hooks/validate-analyzer.js` (validate-on-Write hook for belief-instance records).

Purpose: The Section Analyzer is THE judgment workhorse of the collection layer — the one agent that needs quality. It segments beliefs (judgment the cleaner cannot do), classifies against the 9-anchor taxonomy, and emits granular records the future birdseye agent reads. The Router sets routing_flag BEFORE the analyzer so keep/discard is governed at plan time, not inferred. inject-dr.js is net-new (no analog) — the only runtime context-injection mechanism in the repo.
Output: `prompts/funnel-deep-pass.md` + `tools/hooks/inject-dr.js` + `tools/hooks/validate-analyzer.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@CLAUDE.md
@prompts/_specs/funnel-analysis-collection-spec.md
@prompts/step1-light-pass.md
@definitions.md
@.planning/phases/03-stage-m1-s3-deep-competitive-analysis-messaging-strategy/03-CONTEXT.md
@.planning/phases/03-stage-m1-s3-deep-competitive-analysis-messaging-strategy/03-PATTERNS.md

<interfaces>
FUNNEL-LEVEL fields (6a, recorded ONCE per funnel):
  funnel_id, competitor, source_type, transformation (from PMF stage, do not re-derive),
  primary_claim (verbatim, analyzer), claim_type (analyzer; REUSE direct|enlarged|mechanism|enhanced),
  niche (PMF stage), routing_flag (plan time; structure_only|messaging_full|both),
  awareness_entry (analyzer), funnel_sequence (analyzer), offer_mechanic (analyzer),
  urgency_construction (analyzer; the mechanic AND the number), validation_lane (scorer), validation_strength (scorer)

BELIEF-INSTANCE record (6b, N per funnel):
  funnel_id, position (FUNNEL-LEVEL ordinal, NOT page-local — known failure point),
  belief_id (open-with-anchors), belief_confidence (LOW for overflow beliefs outside anchors),
  execution_type (controlled vocab), execution_detail (GRANULAR free text — sub-claim recoverable),
  proof_tier (Tier1 self-evident/demo | Tier2 authority | Tier3 social), moves[] (tagged),
  verbatim_refs[] (pointers to exact copy; tag competitor-marketing vs review_language)

THE 9 BELIEF ANCHORS (open-with-anchors):
  1 problem-exists  2 problem-matters  3 past-solutions-failed  4 mechanism-is-the-reason
  5 product-delivers-transformation  6 trust-the-brand-or-founder  7 it-will-ship
  8 its-worth-the-price  9 act-now
  (anchors 1-5 = pre-sale warming/advertorial/VSL order; 6-9 = cold-offer + crowdfunding scaffolding)

ROUTING FLAG:
  structure_only  -> off-transformation crowdfunding: keep container/sequence/ship-scaffolding, DISCARD specific claims/angles
  messaging_full  -> in-transformation brand: keep everything esp. claims/angles/proof
  both            -> in-transformation crowdfunding: richest, keep all

THE SIX DR FILES to auto-inject (exact paths, all verified present):
  ~/knowledge/dr-marketing/persuasion--carl-weische.md
  ~/knowledge/dr-marketing/funnel-architecture--carl-weische.md
  ~/knowledge/dr-marketing/vssl--carl-weische.md
  ~/knowledge/dr-marketing/differentiator-framework__2_.md
  ~/knowledge/dr-marketing/consumer-psychology-persuasion-buyer-behavior--mark-builds-brands.md
  ~/knowledge/dr-marketing/offer-construction--carl-weische.md

step1-light-pass.md structure to MIRROR: title+ASCII pipeline; "## SCHEMA" fenced-JSON-with-comments;
  "### Closed enums (a value off-list is a hard reject)" (CLAIM_TYPE_ENUM at line 176);
  "## DETERMINISTIC SCAFFOLD"; numbered "## AGENT N" blocks. Agent-block conventions: open with
  what-it-does + a hard NOT-list; "observation first, classify second"; "Output ONLY valid <schema>. No prose."

validate-dumper.js hook contract to copy: node <hook> <path.json>; read->REJECT(exit2+stderr);
  JSON.parse->REJECT; validate->violations[] accumulate; exit 2 with REJECT: msgs OR silent exit 0.
  Verbatim-substring gate at lines 132-148 (findCleanDir + corpus-load 66-97).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the controlled vocabulary, then write funnel-deep-pass.md (Router + Section Analyzer + schema + enums)</name>
  <read_first>
    - prompts/step1-light-pass.md (the format template — title+pipeline, ## SCHEMA, ### Closed enums at 173-181, ## DETERMINISTIC SCAFFOLD, ## AGENT N blocks; CLAIM_TYPE_ENUM at line 176)
    - prompts/_specs/funnel-analysis-collection-spec.md §4 (routing flag + rationale), §5 (section delimitation + funnel-level ordinal position), §6 (the WHOLE record schema 6a/6b/6c), §7 (the 9 anchors + install order), §8 (descriptive-not-prescriptive hard rules + two forced passes — bake VERBATIM into the analyzer prompt), §9 (controlled vocab resolution rule), §11 (which DR files; bake-vs-inject rule)
    - definitions.md (the transformation definition the Router keys off, D-10)
    - the SIX DR files (read to resolve the vocab value lists for execution_type / proof_tier / move): persuasion--carl-weische.md, funnel-architecture--carl-weische.md, vssl--carl-weische.md, differentiator-framework__2_.md, consumer-psychology-persuasion-buyer-behavior--mark-builds-brands.md, offer-construction--carl-weische.md (all under ~/knowledge/dr-marketing/)
    - .planning/phases/03-stage-m1-s3-deep-competitive-analysis-messaging-strategy/03-PATTERNS.md "funnel-deep-pass.md (NEW)" section
  </read_first>
  <files>prompts/funnel-deep-pass.md</files>
  <action>
    FIRST resolve the controlled vocabulary (D-15, spec §9) from the six DR files + repo conventions into ONE internally-consistent set of closed enums, NO two tags for the same thing:
      - claim_type: REUSE the existing CLAIM_TYPE_ENUM from step1-light-pass.md line 176 = "direct | enlarged | mechanism | enhanced". Do NOT coin a second claim-type vocab.
      - execution_type: draw a closed list from the DR files (e.g. mechanism-explanation, feature-as-evidence, demo, authority, social-proof, founder-credibility, risk-reversal, scarcity, story-epiphany, comparison). Finalize one de-duplicated list.
      - proof_tier: Tier1 (self-evident/demo) | Tier2 (authority) | Tier3 (social-proof), from the differentiator believability tiers.
      - move: a closed list of discrete persuasive moves (angle-lever tags from the differentiator four levers).
    NO human ratification gate (D-15 overrides spec §12 — do not add a ratification checkpoint).
    THEN write prompts/funnel-deep-pass.md mirroring step1-light-pass.md structure:
      1. Title + goal + ASCII pipeline diagram (Router -> Section Analyzer).
      2. "## SCHEMA" — fenced JSON with inline comments for the 6a funnel-level fields AND the 6b belief-instance record (use the exact field lists from the interfaces block).
      3. "### Closed enums (a value off-list is a hard reject)" — pin claim_type (reused), execution_type, proof_tier, move.
      4. "## DETERMINISTIC SCAFFOLD" — list the companion scripts (Plans 01-02) + the validate-analyzer + inject-dr hooks.
      5. "## AGENT 1 — ROUTER" (light A2-classify, cheap model): input = competitor + the run transformation (from space-map.json chosen cell) + definitions.md transformation definition; ONE judgment = transformation-similar-or-not; output = funnel-level routing_flag in {structure_only, messaging_full, both}. Set BEFORE the analyzer, NEVER inferred by it. Put the §4 rationale in the prompt (borrow MESSAGING only from in-transformation brands; STRUCTURE is transformation-independent). DR logic BAKED into this prompt (no raw DR at runtime — §11).
      6. "## AGENT 2 — SECTION ANALYZER" (the workhorse, quality model): reads cleaned funnel copy + bound-ad angles; emits 6b belief-instance records. Bake §8 hard rules VERBATIM: never judge good/bad; never rewrite/optimize copy; operate on ONE funnel at a time; NEVER reason about the pool/consensus/divergence (that's birdseye — any such flag is a bug); force TWO internal passes (1 record literal facts, 2 map to tags via rubric). position = FUNNEL-LEVEL ordinal (1st/2nd/3rd belief-instance across the whole funnel, NOT "section 3 of the page"). belief_id against the 9 anchors (open-with-anchors: prefer an anchor; if none fits propose a new belief_id with LOW belief_confidence). execution_detail GRANULAR enough the sub-claim is recoverable (the spec example: NOT "they build trust" but "founder names the exact factory, shows a dated production photo, states he placed the full MOQ order himself"). The six DR files are AUTO-INJECTED at runtime by inject-dr.js — REFERENCE them as available context, do NOT inline them. End each agent block with "Output ONLY valid <schema>. No prose."
      SECURITY (prompt-injection defense): the analyzer prompt MUST treat the cleaned funnel copy + bound-ad text as UNTRUSTED DATA to analyze, NOT as instructions. Wrap the funnel copy in an explicit data delimiter and instruct the agent to ignore any instructions embedded in the scraped competitor copy (addresses T-03-09).
  </action>
  <verify>
    <automated>node tools/hooks/check-deep-pass.js 2>/dev/null || node -e "const s=require('fs').readFileSync('prompts/funnel-deep-pass.md','utf8'); const need=['ROUTER','SECTION ANALYZER','routing_flag','structure_only','messaging_full','position','belief_id','belief_confidence','execution_detail','proof_tier','problem-exists','it-will-ship','act-now','Output ONLY valid']; for(const n of need){ if(!s.includes(n)){console.error('MISSING: '+n); process.exit(1);} } console.log('deep-pass contract present');"</automated>
  </verify>
  <acceptance_criteria>
    - prompts/funnel-deep-pass.md exists with both agent blocks: contains "ROUTER" and "SECTION ANALYZER"
    - routing flag enum present: contains "structure_only", "messaging_full", "both"
    - reuses claim_type enum (not re-coined): contains "direct | enlarged | mechanism | enhanced" (verbatim from step1 line 176)
    - all 9 anchors present: grep finds "problem-exists", "mechanism-is-the-reason", "it-will-ship", "act-now"
    - belief-instance schema fields present: "position", "belief_id", "belief_confidence", "execution_detail", "proof_tier", "verbatim_refs"
    - §8 discipline baked: contains "one funnel" / "ONE funnel" AND a no-divergence/no-pool instruction (grep "pool" or "consensus" or "divergence")
    - prompt-injection guard present: contains an untrusted-data delimiter + ignore-embedded-instructions note (grep "untrusted" or "ignore" near the funnel-copy input)
    - DR files referenced as injected (not inlined): grep "inject" / "injected at runtime"
    - closed enums declared hard-reject: contains "off-list" or "hard reject"
  </acceptance_criteria>
  <done>funnel-deep-pass.md carries the Router + Section Analyzer prompts, the 6a/6b schema, the resolved closed-enum controlled vocab (claim_type reused), the §8 descriptive discipline baked verbatim, funnel-level ordinal position, and the prompt-injection guard.</done>
</task>

<task type="auto">
  <name>Task 2: Create inject-dr.js — runtime DR-file auto-injection hook (net-new, the only injection mechanism)</name>
  <read_first>
    - prompts/_specs/funnel-analysis-collection-spec.md §11 (which six files; the bake-vs-inject rule; loading/trimming/ordering left to the implementer; do NOT inject all DR files)
    - .planning/phases/03-stage-m1-s3-deep-competitive-analysis-messaging-strategy/03-PATTERNS.md "No Analog Found" row for inject-dr.js (reuse only the fs.readFileSync idiom; this is genuinely new design)
    - tools/hooks/validate-dumper.js (for the hook file conventions: CLI signature, exit codes, --help) — adapt the SHAPE even though this hook injects rather than validates
    - 03-CONTEXT.md "Claude's Discretion" (the load/trim/order/cache/context-limit mechanics are the implementer's call)
  </read_first>
  <files>tools/hooks/inject-dr.js</files>
  <action>
    Create tools/hooks/inject-dr.js — a runtime hook that auto-injects the SIX DR files into the Section Analyzer's context each run (FILE injection — concatenated text, NOT RAG/vectors, D-16). This is net-new (PATTERNS.md flags no analog); reuse only the fs.readFileSync idiom. The SIX fixed target files (exact paths — resolve ~ to os.homedir()):
      persuasion--carl-weische.md, funnel-architecture--carl-weische.md, vssl--carl-weische.md,
      differentiator-framework__2_.md, consumer-psychology-persuasion-buyer-behavior--mark-builds-brands.md,
      offer-construction--carl-weische.md   (all under ~/knowledge/dr-marketing/)
    Behavior: read the six files, concatenate them in a stable order each with a clear "=== DR FILE: <name> ===" header so the analyzer can attribute, and emit the combined block (to stdout or a temp context file the orchestrator pastes into the Section Analyzer call). Trimming/ordering/caching/context-limit handling is the implementer's call (spec §11, D-Discretion) — at minimum: if a file is missing, log a warning and continue with the rest (never crash the run); keep total injected size within a documented cap (if over, trim by section, document the trim). Do NOT inject the optional/secondary files (advertorial / landing-pages / consumer-psychology) — only the six (injecting all degrades classification). Inherit the hook file conventions from validate-dumper.js: shebang, --help block, clear CLI signature, never-fabricate (missing file -> skip + warn, never invent content).
    SECURITY: validate that each resolved path stays under ~/knowledge/dr-marketing/ (no path traversal / no arbitrary file read); the six filenames are a fixed allowlist — do NOT accept a filename from untrusted input (addresses T-03-10).
  </action>
  <verify>
    <automated>node tools/hooks/inject-dr.js --help && node -e "const s=require('fs').readFileSync('tools/hooks/inject-dr.js','utf8'); const need=['persuasion--carl-weische','funnel-architecture--carl-weische','vssl--carl-weische','differentiator-framework','consumer-psychology-persuasion-buyer-behavior','offer-construction--carl-weische','readFileSync']; for(const n of need){ if(!s.includes(n)){console.error('MISSING: '+n); process.exit(1);} } if(s.includes('advertorial--carl-weische')||s.includes('landing-pages--carl-weische')){console.error('must NOT inject optional files');process.exit(1);} console.log('inject-dr allowlist correct');"</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/hooks/inject-dr.js --help` exits 0
    - all SIX target filenames present in source (the fixed allowlist): persuasion / funnel-architecture / vssl / differentiator-framework / consumer-psychology-persuasion-buyer-behavior / offer-construction
    - none of the optional files injected: source does NOT contain "advertorial--carl-weische" or "landing-pages--carl-weische"
    - uses fs.readFileSync over the dr-marketing path (`grep -E "readFileSync" tools/hooks/inject-dr.js` and `grep "dr-marketing" tools/hooks/inject-dr.js`)
    - missing-file resilience: a try/catch or existsSync guard around each read (does not crash on a missing file)
    - path-traversal guard: filenames are a hardcoded allowlist, not read from argv (manual: the six names are literals in source)
  </acceptance_criteria>
  <done>inject-dr.js concatenates exactly the six DR files (hardcoded allowlist, ~ resolved) with attribution headers, skips missing files with a warning, refuses optional files, and emits the combined block for the Section Analyzer context.</done>
</task>

<task type="auto">
  <name>Task 3: Create validate-analyzer.js — validate-on-Write hook for belief-instance records</name>
  <read_first>
    - tools/hooks/validate-dumper.js (the structural analog — hook contract 11-44, violations[]-accumulate 99/154-159, verbatim-substring gate 132-148, findCleanDir + corpus-load 66-97)
    - prompts/_specs/funnel-analysis-collection-spec.md §5 (position = funnel-level ordinal), §6b (the record fields), §7 (open-with-anchors: overflow beliefs need low confidence), §8 (single-funnel discipline — no consensus/divergence fields), §9 (closed vocab)
    - .planning/phases/03-stage-m1-s3-deep-competitive-analysis-messaging-strategy/03-PATTERNS.md "validate-analyzer.js (NEW)" section (the analyzer-specific reject rules)
  </read_first>
  <files>tools/hooks/validate-analyzer.js</files>
  <action>
    Create tools/hooks/validate-analyzer.js — a PostToolUse validate-on-Write hook, structural copy of validate-dumper.js. Same signature `node tools/hooks/validate-analyzer.js <path-to-output.json>`; read -> REJECT on read error (exit 2 + stderr) -> JSON.parse -> REJECT on parse error -> validate -> accumulate violations[] -> exit 2 with "REJECT: ..." messages on any violation, silent exit 0 on pass. Analyzer-specific reject rules (from spec):
      1. VERBATIM-SUBSTRING GATE (copy findCleanDir + corpus-load 66-97, point at the funnel's cleaned body): any verbatim_refs[] string that is NOT a verbatim substring of the cleaned funnel copy -> REJECT (kills hallucinated/paraphrased verbatim).
      2. belief_id off the 9-anchor set with belief_confidence NOT lowered -> REJECT (overflow beliefs must be flagged low for review, §7).
      3. execution_type / proof_tier / claim_type / move off the closed vocab (the enums pinned in funnel-deep-pass.md, D-15) -> REJECT (same shape as step1's off-enum claim_type reject).
      4. position not a funnel-level ordinal (duplicated within a funnel, or page-local) -> REJECT (§5/D-12, the known failure point).
      5. any birdseye-only field present (consensus / divergence / "unusual" / pool-reasoning the analyzer must NOT set) -> REJECT (§8 single-funnel discipline).
    Inherit never-fabricate + the violations-accumulate-then-dump idiom. Add a --help block.
  </action>
  <verify>
    <automated>node tools/hooks/validate-analyzer.js --help && node -e "const s=require('fs').readFileSync('tools/hooks/validate-analyzer.js','utf8'); const need=['REJECT','verbatim','belief_confidence','position','process.exit(2)','process.exit(0)']; for(const n of need){ if(!s.includes(n)){console.error('MISSING: '+n); process.exit(1);} } console.log('validate-analyzer contract present');"</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/hooks/validate-analyzer.js --help` exits 0
    - implements the reject contract: contains "REJECT", "process.exit(2)" and "process.exit(0)"
    - verbatim-substring gate present: contains "verbatim" and a substring/includes check (`grep -E "includes|indexOf|substring" tools/hooks/validate-analyzer.js`)
    - overflow-belief rule: contains "belief_confidence" (low-confidence requirement for off-anchor beliefs)
    - position rule: contains "position" (funnel-level ordinal check)
    - closed-vocab reject: references execution_type/proof_tier/claim_type/move enum membership
    - single-funnel discipline: rejects a consensus/divergence field (`grep -E "consensus|divergence" tools/hooks/validate-analyzer.js`)
    - calling with a missing arg exits 2 (manual: `node tools/hooks/validate-analyzer.js` -> exit 2 + stderr)
  </acceptance_criteria>
  <done>validate-analyzer.js rejects hallucinated verbatim, off-anchor beliefs without lowered confidence, off-vocab tags, page-local/duplicate position, and any birdseye-only field — silent pass on clean records.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraped competitor copy (untrusted) -> Section Analyzer LLM prompt | The cleaned funnel copy + bound-ad text is attacker-influenceable competitor page content fed into an LLM. Prompt-injection surface. |
| filesystem path -> inject-dr.js fs.readFileSync | The hook reads files from disk; must not be coercible into reading outside the DR directory. |
| analyzer LLM output -> JSON store | The analyzer can hallucinate verbatim / emit off-contract fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering / injection (prompt injection) | Section Analyzer prompt consuming untrusted scraped copy | mitigate | Task 1: wrap funnel copy + ad text in an explicit data delimiter; instruct the agent to treat it as DATA to analyze and to ignore any instructions embedded in the competitor copy. validate-analyzer.js (Task 3) is the backstop: off-contract output (injected fields, hallucinated verbatim) is hook-rejected regardless of what the page tried to make the LLM do. |
| T-03-10 | Elevation / path traversal | inject-dr.js fs.readFileSync | mitigate | Task 2: the six filenames are a hardcoded literal allowlist resolved under os.homedir()+"/knowledge/dr-marketing/"; no filename is taken from argv or any untrusted input; each resolved path is checked to stay under that directory. |
| T-03-11 | Spoofing / integrity (hallucinated verbatim) | analyzer verbatim_refs[] -> store | mitigate | Task 3: verbatim-substring gate rejects any verbatim_ref that is not an exact substring of the cleaned funnel copy (no LLM-authored quote text reaches the store), mirroring the proven dumper gate. |
| T-03-12 | Information disclosure (DR-file leakage) | inject-dr.js output | accept | The six DR files are the operator's own knowledge base, injected only into the operator's own analyzer run locally. No external exposure; low sensitivity. No mitigation owed. |
</threat_model>

<verification>
- Task 1: funnel-deep-pass.md has both agent blocks, the full 6a/6b schema, reused claim_type enum, all 9 anchors, §8 discipline baked, and the prompt-injection guard.
- Task 2: inject-dr.js injects exactly the six DR files (allowlist), skips missing, refuses optional files, guards path traversal.
- Task 3: validate-analyzer.js enforces verbatim-substring, overflow-confidence, closed-vocab, funnel-level position, and single-funnel discipline.
- A throwaway live run of the analyzer on ONE funnel is exercised in Plan 04's smoke test (D-17), not here.
</verification>

<success_criteria>
- The one judgment agent (Section Analyzer) + the Router live in funnel-deep-pass.md mirroring the step1 template, with a single internally-consistent controlled vocab (no duplicate tags, claim_type reused).
- The DR-injection hook delivers exactly the six files to the analyzer; every other agent has DR baked in; mechanical bricks get none (bake-vs-inject rule honored).
- The validate-analyzer hook enforces every spec invariant (verbatim, anchors, vocab, funnel-level position, single-funnel discipline).
- Prompt-injection and path-traversal surfaces are mitigated.
</success_criteria>

<output>
After completion, create `.planning/phases/03-stage-m1-s3-deep-competitive-analysis-messaging-strategy/03-03-SUMMARY.md`
</output>
