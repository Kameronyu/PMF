---
phase: 03-stage-m1-s3-deep-competitive-analysis-messaging-strategy
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - tools/funnel-store.js
  - .planning/ROADMAP.md
  - .planning/phases/03-stage-m1-s3-deep-competitive-analysis-messaging-strategy/03-DEBUG-RUN-NOTES.md
autonomous: false
requirements: []   # No v1 REQ-ID. Specced M1-S3 collection-layer capability feeding M2; promote when M2 is planned.
user_setup: []

must_haves:
  truths:
    - "funnel-store.js writes one funnel_package + N belief records per funnel as structured JSON under runs/<space>/funnels/<funnel_id>.json"
    - "Field names / tag formats / file naming conform to Phase-1 repo conventions; store is RAG-ready but NOT vectors"
    - "ROADMAP Phase-3 Goal + Success Criteria describe the COLLECTION-LAYER deliverable; birdseye merge + Gate 2 explicitly deferred; spec pointer updated"
    - "03-DEBUG-RUN-NOTES.md scaffold exists (empty, mirrors 01-DEBUG-RUN-NOTES.md), to be filled on the first real run"
    - "A plumbing smoke test proves ad->LP cluster -> LP render -> cleaner -> scorer end-to-end on 1-2 real brands, and the Section Analyzer emits belief records on ONE funnel (throwaway)"
  artifacts:
    - path: "tools/funnel-store.js"
      provides: "S4 store — namespaced per-funnel JSON under runs/<space>/funnels/ (D-16)"
      contains: "funnels"
    - path: ".planning/ROADMAP.md"
      provides: "Phase-3 section rewritten to the collection-layer scope (D-01)"
      contains: "collection layer"
    - path: ".planning/phases/03-stage-m1-s3-deep-competitive-analysis-messaging-strategy/03-DEBUG-RUN-NOTES.md"
      provides: "Debug-notes scaffold (D-18)"
      contains: "Debug Run Notes"
  key_links:
    - from: "tools/funnel-score.js + funnel-deep-pass.md (Plans 02/03)"
      to: "tools/funnel-store.js"
      via: "funnel_package + belief_records[] written to runs/<space>/funnels/"
      pattern: "belief_records|funnels"
    - from: "all bricks (Plans 01-03)"
      to: "live smoke test"
      via: "re-pull 1-2 real brands with destination_url end-to-end"
      pattern: "destination_url"
---

<objective>
Close the collection layer: build `funnel-store.js` (S4 store), rewrite the ROADMAP Phase-3 section to the collection-layer scope (D-01), scaffold `03-DEBUG-RUN-NOTES.md` (D-18), and run the build-validation plumbing smoke test on live data (D-17).

Purpose: The store is the downstream artifact the future birdseye agent and the vector RAG both read. The ROADMAP still describes the OLD pre-shrink scope (the merge + Gate 2 + birdseye with the wrong spec pointer) — it must be corrected so the roadmap reflects what was actually built and explicitly defers birdseye. The smoke test proves the brick string plumbs end-to-end on real data without picking a market (the real methodology-debug pass waits for a market, D-02).
Output: `tools/funnel-store.js` + corrected `.planning/ROADMAP.md` + `03-DEBUG-RUN-NOTES.md` scaffold + a verified live smoke-test pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@CLAUDE.md
@prompts/_specs/funnel-analysis-collection-spec.md
@.planning/phases/03-stage-m1-s3-deep-competitive-analysis-messaging-strategy/03-CONTEXT.md
@.planning/phases/03-stage-m1-s3-deep-competitive-analysis-messaging-strategy/03-PATTERNS.md
@.planning/phases/01-stage-m1-s1-light-pass/01-DEBUG-RUN-NOTES.md

<interfaces>
funnel-store.js analog: fetch.js writeRaw (lines 372-381, write-with-sidecar idiom) + the repo run-output layout.
D-16: output under runs/<space>/funnels/<funnel_id>.json (NOT repo-root). One funnel = one funnel_package + N belief records.
Phase-1 JSON conventions to conform to:
  - top-level wrapper object with a slug/identifier + a records array (e.g. ads/<slug>.json: {slug, brand_query, status, resolved_advertiser, active_ad_count, ads:[...]})
  - underscore-prefixed meta keys inline: _provenance / _note (space-map.json convention, step1 lines 53-57)
  - JSON.stringify(obj, null, 2) everywhere
  - preserve granular execution_detail + verbatim_refs[] (with review_language vs competitor-marketing tags) — vectorizable, NOT vectorized

The pipeline to smoke-test (Plans 01-03): adlib-one.js (destination_url) -> funnel-assemble.js (cluster+render) ->
  funnel-clean.js -> funnel-score.js -> [Router + Section Analyzer via funnel-deep-pass.md + inject-dr.js + validate-analyzer.js] -> funnel-store.js
Real run data available: runs/arduview/* (in-transformation only; ads at ./ads/). Use a known brand from there for the re-pull.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create funnel-store.js — namespaced per-funnel JSON store (S4)</name>
  <read_first>
    - tools/fetch.js writeRaw (lines 372-381, write-with-sidecar idiom)
    - prompts/_specs/funnel-analysis-collection-spec.md §6 (record schema the store persists) + §10 (birdseye-completeness — confirm every field is present so nothing is missing for the future birdseye agent)
    - prompts/step1-light-pass.md lines 53-57, 123-124 (the _provenance/_note underscore-meta convention)
    - .planning/phases/03-stage-m1-s3-deep-competitive-analysis-messaging-strategy/03-PATTERNS.md "funnel-store.js (NEW)" section (the repo-root vs namespaced-path seam)
  </read_first>
  <files>tools/funnel-store.js</files>
  <action>
    Create tools/funnel-store.js. It takes the funnel_package (with validation stamp from funnel-score.js) + the belief_records[] (from the Section Analyzer) and writes ONE JSON file per funnel: runs/<space>/funnels/<funnel_id>.json (D-16, namespaced — NOT repo-root). Each file = the 6a funnel-level fields + a belief_records[] array (the N 6b records). CONFORM field NAMES + JSON format to Phase-1 conventions (note the seam, PATTERNS.md: Phase-1 run output lives at repo ROOT but the new store is namespaced — conform naming, write to the namespaced path):
      - top-level wrapper: { funnel_id, competitor, source_type, transformation, niche, routing_flag, primary_claim, claim_type, awareness_entry, funnel_sequence, offer_mechanic, urgency_construction, validation_lane, validation_strength, _provenance, belief_records: [...] }
      - underscore-meta keys (_provenance with source + ISO timestamp; _note where useful) per the space-map.json convention
      - JSON.stringify(obj, null, 2)
      - preserve granular execution_detail + verbatim_refs[] verbatim (with review_language vs competitor-marketing tags) — RAG-ready but NOT vectors (vectorization is downstream/JIT, D-16; S4 != the RAG).
    Create the runs/<space>/funnels/ directory if absent. Inherit resilient-batch + a sidecar log + --help + one-line summary. Market-agnostic: <space> is a parameter (D-02), nothing hardcoded.
    §10 BIRDSEYE-COMPLETENESS CHECK: before finishing, confirm the stored record carries every field the future birdseye agent needs (belief_id, funnel-level position, validation_strength + validation_lane, primary_claim + claim_type, granular execution_detail, proof_tier + verbatim_refs). Do NOT add any birdseye-computed field (consensus/divergence) — those are correctly absent from collection.
  </action>
  <verify>
    <automated>node tools/funnel-store.js --help && node -e "const s=require('fs').readFileSync('tools/funnel-store.js','utf8'); const need=['funnels','belief_records','_provenance','JSON.stringify','funnel_id']; for(const n of need){ if(!s.includes(n)){console.error('MISSING: '+n); process.exit(1);} } if(s.includes('consensus')||s.includes('divergence')){console.error('store must NOT add birdseye fields');process.exit(1);} console.log('store contract present');"</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/funnel-store.js --help` exits 0
    - writes under runs/<space>/funnels/ (`grep "funnels" tools/funnel-store.js` and a path join with the space parameter)
    - one funnel = one file with funnel_package fields + belief_records[] (`grep "belief_records" tools/funnel-store.js`)
    - conforms to repo JSON conventions: `JSON.stringify(obj, null, 2)` + `_provenance` underscore-meta present
    - preserves execution_detail + verbatim_refs (`grep -E "execution_detail|verbatim_refs" tools/funnel-store.js`)
    - does NOT add birdseye-computed fields: source contains neither "consensus" nor "divergence"
    - <space> is a parameter, not hardcoded (no literal "arduview"/"eink" market in the write path)
  </acceptance_criteria>
  <done>funnel-store.js writes one repo-convention-conforming JSON file per funnel (funnel_package + belief_records[]) under runs/<space>/funnels/, RAG-ready but not vectorized, with no birdseye fields, fully parameterized on <space>.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the ROADMAP Phase-3 section reflects collection-layer scope (D-01)</name>
  <read_first>
    - .planning/ROADMAP.md (Phase-3 section — ALREADY rewritten to collection-layer scope during planning on 2026-06-03; this task now VERIFIES it, only re-rewriting if the automated check fails)
    - .planning/phases/03-stage-m1-s3-deep-competitive-analysis-messaging-strategy/03-CONTEXT.md <domain> + <deferred> (the shrunk scope + the birdseye deferral note)
    - prompts/_specs/funnel-analysis-collection-spec.md §1 + §10 (the collection-layer deliverable, the deferred birdseye)
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>
    NOTE: The Phase-3 ROADMAP rewrite was ALREADY DONE during planning (2026-06-03). First run the automated check below — if it passes, this task is a no-op (the section is already correct); record it complete without editing. ONLY if the check fails (e.g. a later edit regressed the section) do the rewrite described below.

    If a rewrite IS needed, rewrite the "### Phase 3: Stage M1-S3" section of .planning/ROADMAP.md to the COLLECTION-LAYER deliverable (D-01). Concretely:
      - Goal: replace the two-lens/merge/deployable-plan/Gate-2 prose with: the collection-layer brick string (Scraper/Assembler -> Cleaner -> Validation Scorer -> Router -> Section Analyzer -> Store) produces validated belief-instance records + funnel-level fields, market-agnostic and ready-to-run; the birdseye merge + Gate 2 are explicitly DEFERRED to a later phase.
      - Status / spec pointer: change from "SPECCED (prompts/_specs/deep-market-analysis-framework.md)" to point at "prompts/_specs/funnel-analysis-collection-spec.md" as the authoritative build spec (note deep-market-analysis-framework.md is retained for the strategic "why" only).
      - Success Criteria: replace the 4 merge-oriented criteria with collection-layer ones: (1) the brick string is built (each component mapped to its brick: scripts for fetch/clean/score/store, agents for Router + Section Analyzer, the DR-injection hook); (2) ads bind to LPs by normalized destination URL into funnel_packages, two validation currencies stamped without normalization; (3) the Section Analyzer emits belief-instance records (9-anchor open-with-anchors taxonomy, funnel-level ordinal position, granular execution_detail) that satisfy the §10 birdseye-completeness check; (4) everything is market-agnostic and proven to plumb end-to-end via the D-17 smoke test; birdseye merge + Gate 2 deferred.
      - Plans: update from "TBD" to the 4 plans (03-01 binding spine, 03-02 mechanical bricks, 03-03 judgment agent + hooks, 03-04 store + roadmap + debug scaffold + smoke test).
      - Add a DEFERRED birdseye-synthesis phase note (per CONTEXT <deferred>): "Birdseye synthesis agent (A4->D1: merge / spine+divergences / dead-ground+whitespace / awareness calibration / Gate 2) — own later phase; add to the roadmap when planning it." Place it either in the Phase-3 section as a deferred note or in the existing deferred area.
      - Keep the GSD "### Phase N" format intact (the tooling resolves stages off it).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/ROADMAP.md','utf8'); const m=s.match(/### Phase 3:[\s\S]*?(?=### Phase 4:)/); if(!m){console.error('Phase 3 section not found');process.exit(1);} const sec=m[0]; const need=['collection','funnel-analysis-collection-spec.md','belief-instance','deferred']; for(const n of need){ if(!new RegExp(n,'i').test(sec)){console.error('MISSING in Phase 3 section: '+n); process.exit(1);} } if(/deep-market-analysis-framework\.md.*Status|Status.*deep-market-analysis-framework\.md/i.test(sec) && !/funnel-analysis-collection-spec/i.test(sec)){console.error('stale spec pointer');process.exit(1);} console.log('roadmap Phase-3 rewritten');"</automated>
  </verify>
  <acceptance_criteria>
    - the "### Phase 3" section mentions "collection layer" / "collection-layer"
    - spec pointer updated: the Phase-3 section references "funnel-analysis-collection-spec.md"
    - Goal no longer claims it produces the merged deployable plan / runs Gate 2 as in-scope (those are marked deferred)
    - Success Criteria reference belief-instance records + funnel fields + market-agnostic + birdseye-completeness
    - a birdseye-synthesis DEFERRED note exists (grep "birdseye" + "defer")
    - Plans line lists the 4 plans (not "TBD")
    - GSD "### Phase 3:" header still present (format preserved)
  </acceptance_criteria>
  <done>ROADMAP Phase-3 Goal + Success Criteria + spec pointer + Plans reflect the collection-layer deliverable; birdseye merge + Gate 2 are explicitly deferred with a note to add the synthesis phase later.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold 03-DEBUG-RUN-NOTES.md (D-18)</name>
  <read_first>
    - .planning/phases/01-stage-m1-s1-light-pass/01-DEBUG-RUN-NOTES.md (the analog structure to mirror — section headers: Task 1 reference seed, Task 2 pipeline run + self-audit / stages / hooks fired / debug breaks, Task 3 Kam's verdict, Verdict)
    - .planning/phases/03-stage-m1-s3-deep-competitive-analysis-messaging-strategy/03-CONTEXT.md D-17 + D-18 (the scaffold is empty now, filled on the first real deep-analysis run)
  </read_first>
  <files>.planning/phases/03-stage-m1-s3-deep-competitive-analysis-messaging-strategy/03-DEBUG-RUN-NOTES.md</files>
  <action>
    Create 03-DEBUG-RUN-NOTES.md as an EMPTY SCAFFOLD mirroring 01-DEBUG-RUN-NOTES.md's section structure, adapted to the deep-analysis pipeline. Title it for the deep-analysis reference run. Include placeholder sections (left empty / marked "TO BE FILLED on the first real deep-analysis run, deferred until a market exists — D-02/D-17"):
      - Reference market/seed (DECISION CHECKPOINT: locked) — empty until a market is picked
      - Pipeline run + self-audit: stages completed (assemble -> clean -> score -> route -> analyze -> store), hooks fired/rejected (validate-analyzer, inject-dr), debug breaks surfaced (esp. the known failure points: URL normalization splatter, funnel-level position vs page-local, two-currency normalization, belief over-classification)
      - Kam's belief-tagging verdict (HUMAN-VERIFY CHECKPOINT) — does the analyzer tag beliefs the way Kam would
      - Verdict
    This is a scaffold ONLY — do NOT run a methodology pass or fill content (that waits for a real market).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/03-stage-m1-s3-deep-competitive-analysis-messaging-strategy/03-DEBUG-RUN-NOTES.md','utf8'); const need=['Debug Run Notes','self-audit','Verdict','deferred']; for(const n of need){ if(!new RegExp(n,'i').test(s)){console.error('MISSING: '+n); process.exit(1);} } console.log('debug-notes scaffold present');"</automated>
  </verify>
  <acceptance_criteria>
    - file 03-DEBUG-RUN-NOTES.md exists with a "Debug Run Notes" title
    - mirrors the 01 analog sections: a pipeline-run/self-audit section, a human-verify verdict section, a final Verdict
    - explicitly marked deferred / "TO BE FILLED on the first real run" (grep "defer")
    - lists the known failure points to watch (grep at least one of: "normalization", "position", "currency")
    - NO filled run content (it is an empty scaffold)
  </acceptance_criteria>
  <done>03-DEBUG-RUN-NOTES.md exists as an empty scaffold mirroring the 01 analog, with the deep-analysis stages + known failure points stubbed, marked deferred until a real market.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Build-validation plumbing smoke test on live data (D-17)</name>
  <read_first>
    - prompts/_specs/funnel-analysis-collection-spec.md §2b (the binding the smoke test exercises) + §10 (birdseye-completeness the analyzer output must satisfy)
    - .planning/phases/03-stage-m1-s3-deep-competitive-analysis-messaging-strategy/03-CONTEXT.md D-17 (the smoke-test definition + the throwaway analyzer-verify)
    - the run output at ./ads/ + runs/arduview/ (a known real brand to re-pull)
  </read_first>
  <files>(no files authored — exercises the Plan 01-03 + Task 1 tooling on live data; writes only throwaway smoke output)</files>
  <action>
    Run the full collection-layer brick string end-to-end on LIVE data (no market picked — D-17), as the automated step BEFORE the human verdict:
      1. node tools/adlib-one.js <known-brand>  -> confirm destination_url populated on ads.
      2. node tools/funnel-assemble.js          -> confirm >=1 funnel_package with rendered landing_page_body + bound ads (cluster + render plumbed).
      3. node tools/funnel-clean.js then node tools/funnel-score.js -> confirm section-marked clean copy + validation_lane/validation_strength stamp.
      4. Run the Section Analyzer (funnel-deep-pass.md AGENT 2, with inject-dr.js feeding DR context) on ONE funnel TEMPORARILY -> confirm it emits >=1 belief record that PASSES validate-analyzer.js. THROWAWAY output (discard — not a methodology deliverable, D-17).
      5. node tools/funnel-store.js              -> confirm a runs/<space>/funnels/<funnel_id>.json file is written.
    Surface any failure as a readable artifact (which stage / which brand). Do NOT judge belief-tagging quality — that is the deferred methodology pass (D-02). Then present the results to Kam for the plumbing verdict.
  </action>
  <verify>Each pipeline stage runs without error on 1-2 live brands; destination_url populated; a funnel_package assembled; validation stamped; a schema-valid belief record emitted (passes validate-analyzer.js); a store JSON file written.</verify>
  <done>The brick string plumbs end-to-end on live data and Kam confirms the plumbing (not the methodology); throwaway analyzer output discarded.</done>
  <what-built>
    The full collection-layer brick string from Plans 01-03 + the store from Task 1:
      adlib-one.js (now with destination_url) -> funnel-assemble.js (cluster + Playwright LP render) ->
      funnel-clean.js -> funnel-score.js -> [Router + Section Analyzer via funnel-deep-pass.md, with
      inject-dr.js feeding DR context and validate-analyzer.js gating output] -> funnel-store.js.
    BEFORE this checkpoint, the executor runs the plumbing smoke test AUTOMATICALLY on LIVE data (no market needed, D-17):
      1. Re-pull 1-2 real brands (e.g. a known Arduview-pool brand) with `node tools/adlib-one.js <brand>` and confirm the new destination_url field is populated on the ads.
      2. Run `node tools/funnel-assemble.js` to prove ad->LP clustering by normalized URL + LP render produces funnel_package(s).
      3. Run funnel-clean.js -> funnel-score.js on those packages; confirm section-marked clean copy + a validation_lane/validation_strength stamp.
      4. Run the Section Analyzer (via funnel-deep-pass.md, with inject-dr.js) on ONE funnel TEMPORARILY, just to verify it EMITS belief records that pass validate-analyzer.js. This output is THROWAWAY (discard it — NOT a methodology deliverable, D-17). The real methodology-debug pass is deferred until a market exists (D-02).
      5. Run funnel-store.js on the smoke output to confirm it writes runs/<space>/funnels/<funnel_id>.json.
    Any failure surfaces as a readable artifact (which stage, which brand) — do not silently pass.
  </what-built>
  <how-to-verify>
    1. Confirm the re-pulled ads JSON shows a non-null `destination_url` on at least one ad (the binding key works on live data).
    2. Confirm at least one funnel_package was assembled with a rendered landing_page_body and a bound ad set (cluster + render plumbed).
    3. Confirm the cleaner produced section-marked verbatim and the scorer stamped validation_lane + validation_strength.
    4. Confirm the Section Analyzer emitted >= 1 belief-instance record that PASSED validate-analyzer.js (the throwaway record — its CONTENT quality is NOT judged here, only that the plumbing emits a schema-valid record).
    5. Confirm funnel-store.js wrote a runs/<space>/funnels/<funnel_id>.json file.
    This proves the brick string plumbs end-to-end on live data WITHOUT picking a market. Do NOT judge whether the analyzer tags beliefs "correctly" — that is the deferred methodology pass.
  </how-to-verify>
  <resume-signal>Type "approved" if the plumbing runs end-to-end (destination_url populated, funnel_package assembled, validation stamped, a schema-valid belief record emitted, store file written), or describe which stage broke.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analyzer/scorer output -> funnel-store.js write | Structured records written to the filesystem; funnel_id flows into a file path. |
| live external pages -> the smoke test (re-uses Plan 01/02 render) | The smoke test renders real external LPs (inherits the SSRF/DoS surface mitigated in Plans 01-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Tampering / path traversal | funnel-store.js writing runs/<space>/funnels/<funnel_id>.json | mitigate | Sanitize funnel_id + <space> before using them in the write path (allow only [a-z0-9._-], strip "/" and ".."), so a hostile funnel_id (derived from a scraped URL) cannot escape runs/<space>/funnels/. |
| T-03-14 | Denial of service / SSRF | smoke-test LP renders | mitigate | The smoke test reuses funnel-assemble.js / crowdfund-fetch.js, which already carry the SSRF host-blocklist + render timeouts from Plans 01-02. No new surface; inherited mitigations apply. Smoke test is scoped to 1-2 known brands (low volume). |
| T-03-15 | Information disclosure | runs/<space>/funnels/*.json containing scraped competitor copy + reviews | accept | Local operator store of public competitor marketing copy + public on-page reviews; no PII pipeline, no external publication. Low sensitivity. No mitigation owed. |
</threat_model>

<verification>
- Task 1: funnel-store.js writes repo-conforming per-funnel JSON under runs/<space>/funnels/, no birdseye fields, path-sanitized.
- Task 2: ROADMAP Phase-3 rewritten to collection-layer scope with corrected spec pointer + birdseye deferral.
- Task 3: 03-DEBUG-RUN-NOTES.md scaffold exists, empty, deferred.
- Task 4: the brick string plumbs end-to-end on 1-2 live brands; a throwaway schema-valid belief record is emitted; human confirms the plumbing (not the methodology).
</verification>

<success_criteria>
- The collection layer is complete and ready-to-run, market-agnostic: store writes the final records, the ROADMAP reflects the actual scope, the debug scaffold awaits a market, and the plumbing is proven on live data.
- Birdseye merge + Gate 2 are explicitly deferred in the roadmap.
- No methodology judgment is made here (deferred to the first real-market run, D-02/D-17).
</success_criteria>

<output>
After completion, create `.planning/phases/03-stage-m1-s3-deep-competitive-analysis-messaging-strategy/03-04-SUMMARY.md`
</output>
