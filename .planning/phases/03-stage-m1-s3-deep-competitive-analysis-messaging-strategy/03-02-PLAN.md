---
phase: 03-stage-m1-s3-deep-competitive-analysis-messaging-strategy
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - tools/crowdfund-fetch.js
  - tools/funnel-clean.js
  - tools/funnel-score.js
autonomous: true
requirements: []   # No v1 REQ-ID. Specced M1-S3 collection-layer capability feeding M2; promote when M2 is planned.
user_setup: []

must_haves:
  truths:
    - "crowdfund-fetch.js parses Currency-B stats: amount_raised, backer_count, funded_vs_failed, delivered_vs_not"
    - "funnel-clean.js reads landing_page_body from a funnel_package and emits section-marked verbatim clean copy"
    - "funnel-clean.js preserves on-page review blocks tagged review_language (does NOT decide belief boundaries)"
    - "funnel-score.js stamps validation_lane (A or B) and validation_strength per funnel, NEVER normalized into one number"
    - "A funnel running ads INTO a crowdfunding page carries BOTH lanes"
  artifacts:
    - path: "tools/crowdfund-fetch.js"
      provides: "Currency-B stat parser (D-07) on top of the existing render skeleton"
      contains: "amount_raised"
    - path: "tools/funnel-clean.js"
      provides: "Section-marked clean copy + on-page review preservation (D-11 cleaner stays dumb)"
      contains: "review_language"
    - path: "tools/funnel-score.js"
      provides: "Two-currency validation_strength + validation_lane, no normalization (D-09)"
      contains: "validation_lane"
  key_links:
    - from: "tools/funnel-assemble.js (Plan 01)"
      to: "tools/funnel-clean.js"
      via: "landing_page_body field on funnel_package"
      pattern: "landing_page_body"
    - from: "tools/funnel-assemble.js (Plan 01)"
      to: "tools/funnel-score.js"
      via: "bound_ads[].run_length_days + crowdfunding_stats"
      pattern: "run_length_days|crowdfunding_stats"
---

<objective>
Build the three mechanical post-binding bricks that consume the `funnel_package` from Plan 01: (1) extend `crowdfund-fetch.js` with a Currency-B stat parser, (2) create `funnel-clean.js` (section-marked verbatim cleaner that stays dumb), (3) create `funnel-score.js` (two-currency validation scorer, no normalization).

Purpose: These are deterministic scripts per the brick model (CLAUDE.md: fetch/clean/score = scripts, never agents). The cleaner marks structure but does NOT decide belief boundaries (that is the Analyzer's judgment job in Plan 03). The scorer keeps the two validation currencies as separate lanes — collapsing them into one number destroys the signal the birdseye agent needs (D-09).
Output: `tools/crowdfund-fetch.js` (extended) + `tools/funnel-clean.js` (new) + `tools/funnel-score.js` (new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@CLAUDE.md
@prompts/_specs/funnel-analysis-collection-spec.md
@.planning/phases/03-stage-m1-s3-deep-competitive-analysis-messaging-strategy/03-CONTEXT.md
@.planning/phases/03-stage-m1-s3-deep-competitive-analysis-messaging-strategy/03-PATTERNS.md

<interfaces>
<!-- funnel_package shape this plan CONSUMES (produced by Plan 01 funnel-assemble.js): -->
funnel_package {
  funnel_id, competitor, source_type, landing_page_url, landing_page_body /*raw rendered html/text*/,
  bound_ads: [ { ad_creative_ref, primary_text, headline, cta_text, started_running_date, impression_bucket, platforms } ],
  crowdfunding_stats: { amount_raised, backer_count, funded_vs_failed, delivered_vs_not } | null
}

<!-- crowdfund-fetch.js: render skeleton at lines 51-152 (goto -> CF-clear -> networkidle -> expand -> document.body.innerText). Currently DUMPS .html/.txt/.png (136-143), parses NOTHING. --type= multi-page: campaign|comments|updates|risks|faq (lines 37, 118-127). -->
<!-- Tolerant multi-pattern regex idiom to copy for the Currency-B parser: fetch.js extractTrendSeries (lines 199-247) — try several patterns in order, return null on no-match, never throw. -->
<!-- funnel-clean.js analog: clean.js stripToText(html) (lines 45-71) kills script/style/nav/header/footer, strips tags, decodes entities, collapses ws. Provenance header (109-110). Per-file try/catch batch (106-124). -->
<!-- funnel-score.js analog: fetch.js classifyTrendShape (lines 263-291) — pure-arithmetic no-LLM classifier with a documented-threshold block (254-262). daysBetween/toISO helpers: adlib-one.js 86-94. -->
<!-- Currency A: max_run_duration_days = Math.max(...bound_ads.map(a=>a.run_length_days)); 60+ days ~= top 11% anti-fluke floor; x impression_bucket x variant_count. SURFACE the floor, do NOT gate (anti_fluke precedent, step1 lines 159-161). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend crowdfund-fetch.js with a Currency-B stat parser</name>
  <read_first>
    - tools/crowdfund-fetch.js (the file being extended — render skeleton 51-152, dumps-nothing at 136-143, --type= multi-page at 37/118-127)
    - tools/fetch.js extractTrendSeries (lines 199-247, the tolerant multi-pattern regex cascade idiom)
    - prompts/_specs/funnel-analysis-collection-spec.md §3 Currency B (lines 110-117 — the four stats + why committed money beats modeled traffic)
    - .planning/phases/03-stage-m1-s3-deep-competitive-analysis-messaging-strategy/03-PATTERNS.md "crowdfund-fetch.js (EXTEND)" section
  </read_first>
  <files>tools/crowdfund-fetch.js</files>
  <action>
    KEEP the render skeleton (lines 51-152) intact and the `--type=` multi-page convention. ADD a Currency-B parser that runs after the text extraction (~line 131) on `--type=campaign`, parsing these four fields from the rendered text/HTML into a `crowdfunding_stats` object (D-07, spec §3):
      - `amount_raised`
      - `backer_count`
      - `funded_vs_failed` (did it hit goal — funded vs failed)
      - `delivered_vs_not` (did a PRIOR campaign by this creator actually ship — the strongest durability signal; this comes from the creator profile/history, likely a separate `--type=` fetch against the creator page)
    Use the tolerant multi-pattern regex cascade from fetch.js extractTrendSeries: Kickstarter / Indiegogo / Crowd Supply each render these numbers differently, so try a KS pattern -> Indiegogo pattern -> generic fallback (`$[\d,]+ pledged`, `[\d,]+ backers`), return `null` on no-match, NEVER throw. Inherit never-fabricate: unparseable stat = `null`, never a guess.
    Emit `crowdfunding_stats` into the script's JSON output so funnel-assemble.js / funnel-score.js can attach it to the funnel_package. Do NOT remove the existing dump behavior (comments/updates feed verbatim_refs review-language later).
  </action>
  <verify>
    <automated>node tools/crowdfund-fetch.js --help && node -e "const s=require('fs').readFileSync('tools/crowdfund-fetch.js','utf8'); for (const f of ['amount_raised','backer_count','funded_vs_failed','delivered_vs_not','crowdfunding_stats']) if(!s.includes(f)){console.error('MISSING '+f);process.exit(1);} console.log('Currency-B parser present');"</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/crowdfund-fetch.js --help` exits 0 (render skeleton preserved)
    - source contains all four Currency-B fields: `amount_raised`, `backer_count`, `funded_vs_failed`, `delivered_vs_not`
    - source emits a `crowdfunding_stats` object (`grep "crowdfunding_stats" tools/crowdfund-fetch.js`)
    - parser uses a multi-pattern cascade returning null on no-match (`grep -E "null" near the parse path; no throw on unparsed stat`)
    - existing render skeleton + `--type=` convention untouched (`grep -E "campaign|comments|updates" tools/crowdfund-fetch.js`)
  </acceptance_criteria>
  <done>crowdfund-fetch.js parses amount_raised/backer_count/funded_vs_failed/delivered_vs_not into a crowdfunding_stats object via a tolerant pattern cascade; render skeleton preserved; unparseable = null.</done>
</task>

<task type="auto">
  <name>Task 2: Create funnel-clean.js — section-marked verbatim cleaner (stays dumb)</name>
  <read_first>
    - tools/clean.js (analog — stripToText 45-71, provenance header 109-110, per-file try/catch batch 106-124)
    - prompts/_specs/funnel-analysis-collection-spec.md §1 + §5 (cleaner marks structure, does NOT decide belief boundaries) + §6c (on-page reviews captured into verbatim_refs tagged review_language; do NOT build a separate review-mining pipeline)
    - .planning/phases/03-stage-m1-s3-deep-competitive-analysis-messaging-strategy/03-PATTERNS.md "funnel-clean.js (NEW)" section
  </read_first>
  <files>tools/funnel-clean.js</files>
  <action>
    Create `tools/funnel-clean.js`. Copy the `stripToText(html)` core from clean.js (lines 45-71): kill `<script>/<style>/<nav>/<header>/<footer>`, strip remaining tags, decode entities, collapse whitespace — the "dumb regex, swappable later" brick. Differences from clean.js:
      - Read `landing_page_body` from the `funnel_package` (one funnel = one body), NOT `corpus/<slug>/raw/*.html`.
      - Emit SECTION-MARKED clean copy: extend stripToText to insert structural section markers (e.g. on heading boundaries / major block breaks) rather than flatten everything to one blob. These are STRUCTURAL markers only — the cleaner does NOT decide belief boundaries (spec §5: the Analyzer cuts beliefs; the cleaner stays dumb, D-11).
      - PRESERVE on-page review blocks verbatim rather than treating them as footer chrome; tag them so they land in `verbatim_refs[]` as `review_language` downstream (spec §6c). Do NOT build any review-mining / scoring — just preserve + tag the blocks.
    Keep the provenance header convention (clean.js 109-110: `<!-- source: ... | cleaned: <ISO> -->`). Inherit per-funnel try/catch batch (one bad funnel never aborts the batch). CLI arg parse + `--help` + one-line summary. `JSON.stringify(obj, null, 2)` for emit.
  </action>
  <verify>
    <automated>node tools/funnel-clean.js --help && node -e "const s=require('fs').readFileSync('tools/funnel-clean.js','utf8'); for (const f of ['landing_page_body','review_language','stripToText']) if(!s.includes(f)){console.error('MISSING '+f);process.exit(1);} console.log('cleaner contract present');"</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/funnel-clean.js --help` exits 0
    - reads `landing_page_body` (`grep "landing_page_body" tools/funnel-clean.js`)
    - tags review blocks `review_language` (`grep "review_language" tools/funnel-clean.js`)
    - emits section markers (some marker token present, e.g. a `## ` / `[SECTION` / `section_marker` — `grep -E "section|SECTION" tools/funnel-clean.js`)
    - kills script/style/nav (`grep -E "script|style|nav" tools/funnel-clean.js` — the strip targets)
    - preserves provenance header (`grep -E "source:|cleaned:" tools/funnel-clean.js`)
    - NO belief classification logic (does not import/reference belief_id/anchor — `! grep -q "belief_id" tools/funnel-clean.js`)
  </acceptance_criteria>
  <done>funnel-clean.js reads landing_page_body, emits section-marked verbatim clean copy, preserves+tags on-page reviews as review_language, and contains NO belief-boundary judgment.</done>
</task>

<task type="auto">
  <name>Task 3: Create funnel-score.js — two-currency validation scorer (no normalization)</name>
  <read_first>
    - tools/fetch.js classifyTrendShape (lines 263-291, the pure-arithmetic no-LLM classifier) + its documented-threshold block (254-262)
    - tools/adlib-one.js daysBetween/toISO (lines 86-94, copyable arithmetic)
    - prompts/_specs/funnel-analysis-collection-spec.md §3 (the WHOLE section — two currencies, cross-currency rule, no normalization)
    - .planning/phases/03-stage-m1-s3-deep-competitive-analysis-messaging-strategy/03-PATTERNS.md "funnel-score.js (NEW)" section + the anti-fluke "surface, don't gate" precedent
  </read_first>
  <files>tools/funnel-score.js</files>
  <action>
    Create `tools/funnel-score.js` — a pure-arithmetic no-LLM scorer (CLAUDE.md: deterministic -> script), modeled on classifyTrendShape. It reads a `funnel_package`, computes validation, and STAMPS `validation_lane` + `validation_strength` onto it. TWO currencies, NEVER normalized into one number (D-09, spec §3):
      - Currency A (ad-fed, `validation_lane: "A"`): `max_run_duration_days = Math.max(...bound_ads.map(a => a.run_length_days))` (the spine; 60+ days ~= top 11%, anti-fluke floor) x `impression_bucket` (multiplier) x `variant_count` (multiplier).
      - Currency B (crowdfunding, `validation_lane: "B"`): carry `amount_raised`, `backer_count`, `funded_vs_failed`, `delivered_vs_not` from `crowdfunding_stats`. delivered_vs_not (prior campaign shipped) = strongest durability signal.
      - A funnel running Meta ads INTO a crowdfunding page has BOTH lanes — record BOTH (validation_lane can carry both; do not pick one).
    Write the 60-day anti-fluke floor and the bucket multipliers as a DOCUMENTED constant block (classifyTrendShape style). SURFACE the floor number, do NOT hard-gate — the scorer stamps strength, it never drops weak funnels (anti_fluke "surface don't gate" precedent, step1 lines 159-161). NO SEMrush/Similarweb traffic (modeled, weak, gated) — committed money / longevity only. Inherit resilient-batch + `--help` + one-line summary + `JSON.stringify(obj, null, 2)`.
  </action>
  <verify>
    <automated>node tools/funnel-score.js --help && node -e "const s=require('fs').readFileSync('tools/funnel-score.js','utf8'); for (const f of ['validation_lane','validation_strength','max_run_duration_days','amount_raised']) if(!s.includes(f)){console.error('MISSING '+f);process.exit(1);} if(/normaliz/i.test(s) && !/no.?normaliz|never normaliz|not normaliz/i.test(s)){console.error('WARN check normalization');} console.log('scorer contract present');"</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/funnel-score.js --help` exits 0
    - stamps both `validation_lane` and `validation_strength` (both grep-present)
    - Currency A math present: `max_run_duration_days`, `variant_count`, `impression_bucket` referenced
    - Currency B fields carried: `amount_raised`, `backer_count`, `funded_vs_failed`, `delivered_vs_not`
    - two lanes are NOT collapsed: no single combined score field merging A and B (manual: `validation_strength` is lane-keyed, not one scalar across both)
    - documented 60-day floor constant present (`grep -E "60|anti.?fluke" tools/funnel-score.js`)
    - does NOT hard-drop funnels (no filter/early-return that discards low-strength funnels)
    - pure arithmetic, no LLM call (`! grep -qiE "anthropic|openai|fetch.*api|claude" tools/funnel-score.js`)
  </acceptance_criteria>
  <done>funnel-score.js stamps validation_lane + validation_strength using Currency A (run-duration x bucket x variants) and/or Currency B (raise/backers/funded/delivered) without merging the two lanes, surfaces (never gates) the anti-fluke floor, and uses no LLM.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crowdfunding platform page -> crowdfund-fetch.js render + parse | Untrusted external HTML/JS rendered via Playwright (same SSRF/DoS surface as Plan 01's LP render). |
| funnel_package.landing_page_body (untrusted) -> funnel-clean.js | Untrusted competitor copy processed by regex; preserved verbatim (incl. reviews) into the store. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information disclosure (SSRF) | crowdfund-fetch.js Playwright goto on a hand-fed crowdfunding LP URL | mitigate | Apply the SAME private/loopback/link-local/metadata host blocklist + http(s)-only scheme check as T-03-01 before goto. Hand-fed LPs are operator-supplied but still validated (operator could paste a hostile URL). |
| T-03-06 | Denial of service | crowdfund-fetch.js render of a hostile/slow crowdfunding page | mitigate | Keep the existing CF-clear loop bounded + goto/networkidle timeouts; per-page try/catch; never throw on a parse miss (return null). |
| T-03-07 | Tampering / injection (ReDoS) | funnel-clean.js regex strippers + crowdfund-fetch.js Currency-B regex cascade on attacker-controlled text | mitigate | Use bounded/linear regex patterns (avoid catastrophic backtracking — no nested unbounded quantifiers on untrusted input); the stripToText patterns are inherited from clean.js (already linear). Cap input size before regex if body is pathologically large. |
| T-03-08 | Injection (deferred downstream) | review_language / verbatim copy preserved raw -> later fed to analyzer LLM | accept (mitigated in Plan 03) | Reviews/copy preserved verbatim BY DESIGN (§6c). Prompt-injection from this untrusted text is mitigated at the analyzer boundary in Plan 03. funnel-clean.js never executes the content; it only marks + preserves. |
</threat_model>

<verification>
- Task 1: crowdfund-fetch.js emits crowdfunding_stats with all four Currency-B fields; render skeleton intact; SSRF guard on goto.
- Task 2: funnel-clean.js emits section-marked verbatim with reviews tagged review_language; no belief classification.
- Task 3: funnel-score.js stamps both lanes without merging; surfaces (not gates) the floor; no LLM.
</verification>

<success_criteria>
- The three mechanical bricks consume the Plan 01 funnel_package and produce: parsed Currency-B stats, section-marked verbatim clean copy with preserved reviews, and a two-lane validation stamp.
- All three are deterministic scripts (no agents, no LLM) per the brick model.
- The two validation currencies stay separate (no normalization) — the load-bearing D-09 invariant.
</success_criteria>

<output>
After completion, create `.planning/phases/03-stage-m1-s3-deep-competitive-analysis-messaging-strategy/03-02-SUMMARY.md`
</output>
