---
phase: 17
slug: lp-builder-implement-arduview-landing-and-deposit-pages-from
status: draft
shadcn_initialized: false
preset: none
created: 2026-06-04
instance: arduview
design_system: glasshouse
supersedes:
  - runs/arduview/site/STYLE-LOCK.md
  - runs/arduview/BUILD-FEEDBACK.md#1
stylesheet: runs/arduview/site/styles.css   # LINKED, never re-authored (D-03)
---

# Phase 17 — UI Design Contract (Arduview instance · "Glasshouse")

> Visual and interaction contract for the Arduview LP-builder instance. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **Scope note (D-02):** This is the per-instance *visual contract* the reusable LP-builder consumes. It is the Arduview instance — bootstrapped from the already-locked "Glasshouse" system, not invented here. The builder is product-agnostic; this file is one of its inputs.
>
> **Authority (D-10):** Once this file exists it is the **single design source**. It SUPERSEDES `STYLE-LOCK.md` and `BUILD-FEEDBACK.md §1`; those become inputs-only and are never re-read by the builder. This document must stand alone — everything the builder and checker need is here.
>
> **Reuse rule (D-03):** The builder composes/edits HTML against the prebuilt, locked `styles.css`. It needs class *names* (below), never the CSS body. It NEVER authors or re-authors CSS.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (hand-authored static HTML + one locked stylesheet) |
| Preset | not applicable |
| Component library | none — bespoke "Glasshouse" CSS at `runs/arduview/site/styles.css` (LINKED, never re-authored) |
| Icon library | none — Unicode glyphs only (`▣ ✓ ↩️ ✉️ 🔒 →`), inline. No emoji-as-icon beyond these established marks |
| Fonts (Google Fonts, free) | **Doto** (dot-matrix display/labels) · **Space Grotesk** (headlines, wt 400–500) · **IBM Plex Mono** (body/specs/FAQ) |
| Surfaces | flip black ⇄ white per band — never blend, no gradients on content |
| Casing | lowercase for nav / labels / CTAs / eyebrows / pills / chips; locked headline copy left verbatim (sentence case as written) |

Font load (verbatim, in `<head>` of every page):
```
https://fonts.googleapis.com/css2?family=Doto:wght@400;700;800;900&family=Space+Grotesk:wght@400;500;600&family=IBM+Plex+Mono:ital,wght@0,400;0,500;1,400&display=swap
```

---

## Spacing Scale

The locked system is **fluid** (`clamp()`-based), not a fixed 8-point token set. These are the locked CSS variables and rhythm values — the builder uses these primitives, never invents one-off margins.

| Token | Value | Usage |
|-------|-------|-------|
| `--gutter` | `clamp(20px, 5vw, 64px)` | Horizontal page padding (`.wrap`, `.nav`, `.footer`) |
| `--section-y` | `clamp(72px, 12vh, 160px)` | Vertical rhythm between full sections (`.section`) |
| `--section-y` (tight) | `clamp(40px, 7vh, 88px)` | `.section--tight` (founder band) |
| `--maxw` | `1120px` | Content max width; hero overrides to `1320px` |
| split/founder/offer gap | `clamp(28px, 5vw, 72px)` | Two-column gap |
| inter-element rhythm | 8 / 14 / 18 / 22 / 24 / 26 / 28 / 34 / 48 / 56 px | Discrete margins already used in the reference; stay on these steps |

Exceptions: spacing is fluid by design (responsive viewport units) — the "multiples of 4" rule does not apply to the clamp tokens. The builder must reuse the tokens/classes above and not introduce arbitrary pixel margins outside the discrete step set.

---

## Typography

Three faces, locked roles. Heading weight tops out at 500 (TE light-grotesque restraint) — never bold a headline.

| Role | Class | Family | Size | Weight | Line Height | Notes |
|------|-------|--------|------|--------|-------------|-------|
| Body | `.body` / `body` | IBM Plex Mono | **17px** (min — see HARD-3) | 400 | 1.65 | Base body |
| Lead | `.lead` | IBM Plex Mono | `clamp(1.08rem, 1.7vw, 1.24rem)` | 400 | 1.72 | Intro paragraphs; max 60ch |
| Eyebrow / label / pill / chip-key / save-flag | `.eyebrow` `.pill` `.offer__tag` `.save-flag` | Doto | 11–13px | 700 | — | letter-spacing 0.1–0.18em, lowercase, cyan on dark |
| Sub-head | `.h-sub` | Space Grotesk | `clamp(1.3rem, 2.6vw, 1.9rem)` | 400 | 1.04 | |
| Section head | `.h-section` | Space Grotesk | `clamp(1.9rem, 4.4vw, 3.2rem)` | 500 | 1.04 | max 16ch |
| Hero head | `.h-hero` | Space Grotesk | `clamp(2.5rem, 5.2vw, 4.6rem)` | 500 | 1.04 | hero override; max 14ch |
| Price (new) | `.price-new` | Space Grotesk | `clamp(2.6rem, 6vw, 4rem)` | 500 | — | |

Headings: `line-height: 1.04`, `letter-spacing: -0.02em`. Body/lead: `line-height: 1.65–1.72`.

---

## Color

60/30/10 reading: black/white surfaces alternate as the dominant frame; greyscale ramp is the secondary; cyan glow is the strict <5% accent. **The product photos carry the real color (TE principle) — cyan is punctuation only.**

| Role | Value | Usage |
|------|-------|-------|
| Dominant surface — dark | `#0a0a0b` (`--black`) | Hero, "wow"/hardware bands, deposit page, footer. Where transparency reads. |
| Dominant surface — light | `#ffffff` (`--white`) + `#f6f6f4` (`--paper`) | Editorial/spec bands ("what it is", FAQ) |
| Raised dark surface | `#131316` (`--black-2`) | Founder band, raised panels |
| Secondary text on light | `#44444a` (`--grey-1`) | Muted copy on white |
| Secondary text on dark | `#b8b8be` (`--grey-2`) | Muted copy on black |
| Hairlines | `#e6e6e3` (`--line-light`) / `#26262b` (`--line-dark`) | Rules, borders |
| **Accent — screen-glow cyan** | `#2be5ff` (`--glow`) | RESERVED list below |
| Accent dim | `rgba(43,229,255,0.14)` (`--glow-dim`) | Focus rings, glow shadows |

**Accent (cyan `#2be5ff`) reserved for — explicit list, <5% of any view, one highlight per section:**
- the dot motif (pill status dot, eyebrow lead-rule, FAQ `// ` markers, `lockins`/`chip` ticks)
- the in-copy highlight class `.hl` (exactly one per section)
- interactive accent: button fill, input focus ring/border, link hover glow
- eyebrows, proof-chip values, `save-flag` background, wordmark `®`

**Accent NEVER on:** body copy, headline body text, decorative fills beyond the above. Never a second accent color (no second hue, ever).

Destructive: **none.** This phase has no destructive actions (LP + deposit funnel only). No destructive palette declared.

---

## HARD CONSTRAINTS — page-craft rules (ported VERBATIM from BUILD-FEEDBACK §1, D-11)

These are non-negotiable, machine-checkable rules. A ui-checker MUST be able to enforce each. They override any softer guidance above on conflict.

### HARD-1 — Context-aware accent: NO black-on-black, EVER (recurred twice — highest-priority rule)
The accent/highlight color MUST be **context-aware** — readable on BOTH light and dark surfaces. **No text on a same-value background, ever.**
- Symptom 1 that triggered this rule: hero headline highlight "see right through" rendered black-on-black.
- Symptom 2 (same bug): deposit "$5 deposit" highlight black-on-black.
- Root cause: the highlight class defaulted to dark ink and was patched for only *some* dark containers.
- Enforcement: any `.hl` (or equivalent highlight) inside a dark surface (`.hero`, `.section--dark`, `.deposit-hero`, `--black`/`--black-2` bg) MUST resolve to cyan glow (`color: var(--glow)`, no dark border) — never inherit `--ink`. On light surfaces `.hl` is ink with a cyan underline. The locked CSS already encodes both branches; the builder must not place a highlight in a container where the cascade leaves it same-value.
- **Checker test:** for every highlighted text run, computed text color ≠ computed background color (contrast must clear, not collapse).

### HARD-2 — CTA legibility: black-on-accent, readable face (NOT the display font)
CTA text MUST be **black (`#000`) on the cyan accent fill**, in a **readable face — IBM Plex Mono (`var(--mono)`), weight 500**. NEVER the Doto dot-matrix/display font; it is unreadable at button size.
- Locked: `.btn { background: var(--glow); color: #000; font-family: var(--mono); font-weight: 500; }`.
- **Checker test:** every primary CTA button has accent background, black text, and a non-Doto font-family.

### HARD-3 — 17px body minimum
Base body font size is **17px minimum**. Secondary-text contrast is raised: greys darkened on light (`--grey-1 #44444a`), lightened on dark (`--grey-2 #b8b8be`). Leads bumped up. No body or secondary copy below 17px equivalent except the conventional fine-print classes (`.cta__fine` 13px, `.nav__meta` 12px, `.paywall`/`.offer__secure` 12px, `.chip` 13px) which are deliberate sub-body labels.
- **Checker test:** `body { font-size: 17px }`; no primary reading text under 17px.

### HARD-4 — Per-page fold budget (state explicitly; never leave to the builder)
**LP (`index.html`) hero above the fold:** title + CTA + video. Subheader sits BELOW the fold. **Video is the centerpiece — ≥ half the hero, bigger than instinct.**
**Deposit (`deposit.html`) above the fold:** image + title + CTA + the value (51% off). Image beside the title (no scroll to see it). CTA up top. Description may drop below.
- **Checker test:** above-the-fold DOM of each page contains exactly the budgeted elements (and the hero video occupies ≥50% of hero area on LP).

### HARD-5 — Explicit element order per breakpoint (state both, never infer)
- **LP desktop:** headline + CTA left, video right; subheader full-width below.
- **LP mobile:** title → CTA → video → subheader. Hero media full-bleed (edge-to-edge) on mobile.
- **Deposit desktop:** copy (tag → title → price-row → value → CTA → secure note) left, image right.
- **Deposit mobile:** image on top (capped height) → title → price → CTA → (reassurance below).
- Breakpoints: `≤860px` (stack two-cols, deposit image to top, hero stacks + full-bleed video), `≤520px` (CTA form goes column, full-width input + button).
- **Checker test:** DOM/CSS order matches the stated order at each breakpoint.

### HARD-6 — Copy hierarchy / risk-reversal (deposit), no repetition
Tighten copy but **do not delete any risk-reversal move** (deposit credits toward price; fully refundable; 30-day money-back). **Do not repeat yourself** — each risk-reversal point appears once, in one zone. **Value (51% off) above the fold; reassurance as the user scrolls.**
- **Checker test:** each risk-reversal point appears exactly once; the 51% value is above the fold.

### HARD-7 — Additive restraint (BUILD-FEEDBACK §2, builder behavior, D-05)
The builder renders the brief in the locked visual system and adds **no customer-facing content the brief didn't ask for — in words or in chrome.** Every visible word comes from the copy brief (PAGE-SPEC). No image captions/figcaptions, no invented eyebrows/kickers, no badge labels, no helper microcopy, no "what is this" tags, no decorative UI the brief didn't call for.
- The rule applies ONLY to customer-facing/visible content. Non-visible strings are unconstrained: `alt`, `aria-*`, `data-*`, class names, file names, code comments — be descriptive there.
- Conventional unavoidable functional strings (form placeholders e.g. `your email`, button states) are fine, kept minimal.
- Also avoid the generic visual AI-tells: gratuitous gradients, emoji-as-icons, three-identical-feature-cards, centered-everything defaults. (The locked Glasshouse system already fights these — `figcaptions` are CSS-hidden via `.frame__tag { display: none }`.)
- **Checker test:** no figcaptions rendered; no visible string absent from the PAGE-SPEC copy brief; no decorative chrome beyond the locked component set.

> **REJECTED — do NOT encode:** BUILD-FEEDBACK §3 image-numbering convention is rejected as brittle. Do not add image-N labels, numbering, or position semantics to the contract.

---

## Component Inventory (locked class names — builder composes against these)

Builder needs names only; CSS body lives in the linked `styles.css` and is never re-authored.

| Component | Class(es) | Surface | Notes |
|-----------|-----------|---------|-------|
| Page shell | `.wrap` `.section` `.section--dark` `.section--light` `.section--paper` `.section--tight` | both | dark bands get auto dot-grid texture via `::before` |
| Nav | `.nav` `.wordmark` `.reg` `.nav__meta` | dark | sticky, blurred; no buy button (email capture is the only LP conversion) |
| Eyebrow | `.eyebrow` | both | Doto, cyan, lead-rule; on light add inline `color:#0a0a0b` |
| Hero | `.hero` `.hero__top` `.hero__lead-col` `.h-hero` `.hero__cta` `.hero__video` `.hero__video-el` `.hero__sub` | dark | grid `0.82fr 1.3fr`; video right |
| Trust pills | `.pills` `.pill` `.pill .dot` | dark | Doto labels, cyan status dot |
| Proof chips | `.chips` `.chip` `.chip b` `.chip .tick` | dark | inline stats |
| CTA / email capture | `.cta` `.cta__form` `.cta__input` `.btn` `.btn--lg` `.btn--ghost` `.cta__fine` `.cta__done` | both | `.is-done` success state; CTA = black-on-cyan (HARD-2) |
| Media frame | `.frame` `.frame--dark` `.frame--light` `.frame img` | both | `.frame__tag` is CSS-hidden (captions removed, HARD-7) |
| Two-col split | `.split` `.split--text-first` `.split__copy` `.split__media` | both | stacks ≤860px |
| Feature rows | `.features` `.feature` `.feature__k` `.feature__v` `.kicker` | light | NOT three identical cards — keyed rows |
| Founder | `.founder` `.founder__photo` `.letter` `.badges` `.badge` `.bracket` | dark-2 | bracketed scaffold; fill from interview |
| FAQ | `.faq` `.faq__item` `.faq__q` `.faq__a` | light | |
| Footer | `.footer` `.footer__row` | dark | |
| Deposit offer | `.deposit-hero` `.offer` `.offer__copy` `.offer__tag` `.offer__title` `.offer__value` `.offer__cta` `.offer__secure` `.offer__img` | dark | above-fold; HARD-4/5 |
| Price | `.price-row` `.price-old` `.price-new` `.save-flag` | dark | |
| Reassurance | `.reassure` `.reassure__h` `.lockins` `.lockins li b` `.lockins .tick` `.pay-strip` `.trust-blocks` `.trust-block` `.paywall` | dark | below-fold; each point once (HARD-6) |
| Motion | `.reveal` `.reveal.in` | both | honors `prefers-reduced-motion` |
| Hero video placeholder | `.video-ph` (+ children) | dark | swap to `<video autoplay muted loop playsinline poster>` when clip is ready |

---

## Tokenization (builder output contract — D-04)

The builder emits **tokens and stable filenames, never live URLs** (keeps it pure/testable with zero live accounts). Downstream `hydrate` + `asset-upload.js` fill them.

| Token | Filled by | Stage |
|-------|-----------|-------|
| `{{KLAVIYO_FORM_ACTION}}` | `hydrate` (script) from provisioning config | post-build |
| `{{SHOPIFY_CHECKOUT}}` | `hydrate` (script) — replaces `data-checkout` href `#` | post-build |
| `src="assets/img/<name>.jpg"` | `asset-upload.js` backfills `cdn.shopify.com/...` from `url-map.json` | post-build |

Builder leaves stable filenames (already does: `assets/img/transparency.jpg`, `maker.jpg`, `gameplay.jpg`, `scale.jpg`, `deposit.jpg`, `kickstarter.png`, `payment-icons.jpg`, `hero-poster.jpg`, `assets/video/hero.mp4`). Asset input contract: optimized ~1600px / q82 (D-13, upstream asset-emit — not a builder/Arduview decision).

---

## Copywriting Contract (Arduview instance — sourced from reference build / COPY-DRAFT → PAGE-SPEC)

Visible copy is owned by PAGE-SPEC (the copy brief); the builder authors none of it (HARD-7). These are the locked instance values used by the proven reference build.

| Element | Copy |
|---------|------|
| LP primary CTA (hero) | `get early access →` |
| LP CTA (mid) | `lock in early access →` |
| LP CTA (final) | `get early access →` |
| Deposit primary CTA | `reserve for $5 →` |
| LP CTA fine print | `Founders get Arduview in August 2026. Everyone else waits until 2027.` |
| Email success state | `✓ locked in. founders ship aug 2026.` / `✓ done. watch your inbox.` |
| Email input placeholder | `your email` |
| Deposit value (above fold, stated once) | `Your $5 deposit comes straight off the $146 founder price. Fully refundable until it ships.` |
| Deposit secure note | `🔒 secure checkout · no account needed · card details never touch this page` |
| Risk-reversal (each once, below fold) | founder price $146 locked · ships first Aug 2026 · 30-day money-back |
| Empty state | not applicable — static marketing pages, no data-driven empty states |
| Error state | not applicable — front-end email capture stub + hosted Shopify checkout; no in-page error surface in scope |
| Destructive confirmation | not applicable — no destructive actions in this phase |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| n/a — no shadcn / no component registry | none | not applicable (no `components.json`; bespoke locked CSS, no third-party blocks) |

No third-party registries declared. Registry vetting gate: not applicable.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS — additive restraint (HARD-7), risk-reversal once (HARD-6), CTAs per contract
- [ ] Dimension 2 Visuals: PASS — fold budget (HARD-4), element order per breakpoint (HARD-5), no captions/AI-tell chrome
- [ ] Dimension 3 Color: PASS — single cyan accent <5%, context-aware (HARD-1, no black-on-black), CTA black-on-accent (HARD-2)
- [ ] Dimension 4 Typography: PASS — 3 locked faces, heading wt ≤500, 17px body min (HARD-3)
- [ ] Dimension 5 Spacing: PASS — fluid clamp tokens reused, no arbitrary margins
- [ ] Dimension 6 Registry Safety: PASS — not applicable, no third-party blocks

**Approval:** pending
