---
phase: 17-lp-builder-implement-arduview-landing-and-deposit-pages-from
plan: 06
type: execute
wave: 3
depends_on: [02, 03, 04, 05]
files_modified:
  - runs/arduview/site/index.html
  - runs/arduview/site/deposit.html
autonomous: false
requirements: [LPB-08]
user_setup:
  - service: klaviyo
    why: "Email capture form action — {{KLAVIYO_FORM_ACTION}} token"
    env_vars: []
    dashboard_config:
      - task: "Create a Klaviyo list + embed/hosted form; copy its form action URL"
        location: "Klaviyo Dashboard → Sign-up forms / List → embed → form action"
  - service: shopify
    why: "$5 deposit checkout — {{SHOPIFY_CHECKOUT}} token"
    env_vars: []
    dashboard_config:
      - task: "Create the $5 deposit product; copy its cart permalink (e.g. /cart/<variant>:1)"
        location: "Shopify Admin → Products → $5 deposit → variant cart permalink"
must_haves:
  truths:
    - "The lp-builder agent has composed runs/arduview/site/index.html + deposit.html as tokenized markup matching the proven reference, with {{KLAVIYO_FORM_ACTION}} in the email form action and {{SHOPIFY_CHECKOUT}} in the deposit data-checkout href"
    - "lp-ui-check.js PASSES the builder output (HARD-1..7 gate exit 0)"
    - "After lp-hydrate.js (operator's two pasted strings) the HTML has zero stray {{...}} integration tokens"
    - "asset-upload.js has backfilled CDN URLs for assets/img/<name>.jpg (or the gap is logged, not fabricated)"
    - "The Arduview LP + deposit page are deployed live to surge and load (HTTP 200) — the workflow's validation instance"
  artifacts:
    - path: "runs/arduview/site/index.html"
      provides: "Builder-composed, checked, hydrated Arduview LP"
      contains: "KLAVIYO_FORM_ACTION"
    - path: "runs/arduview/site/deposit.html"
      provides: "Builder-composed, checked, hydrated Arduview deposit page"
      contains: "data-checkout"
  key_links:
    - from: "lp-builder output → lp-ui-check → lp-hydrate → asset-upload → surge deploy"
      to: "live arduview-see-through.surge.sh"
      via: "the full no-rework pipeline run, no agent re-opening HTML after the builder"
      pattern: "surge"
---

<objective>
Run the full LP-builder pipeline end-to-end on the Arduview instance (LPB-08) — the workflow's VALIDATION.
Builder composes tokenized HTML → lp-ui-check gates (HARD-1..7) → operator pastes the two provisioning
strings → lp-hydrate fills tokens → asset-upload backfills CDN → surge deploys live. This proves the whole
pipeline: one writer, frozen contracts, no re-edits, a live page.

Purpose: D-12 — the Arduview instance deployed live IS the workflow's validation. This is the UAT gate Kam
reads (verification is UAT, not unit tests — STATE.md).
Output: composed + checked + hydrated `runs/arduview/site/index.html` + `deposit.html`; a live surge URL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-INTERFACES.md
@.planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-UI-SPEC.md
@runs/arduview/site/PAGE-SPEC.md
@.claude/skills/lp-builder/SKILL.md

<interfaces>
<!-- The pipeline order is fixed (INTERFACES): no agent re-opens HTML after the builder. -->
1. /lp-builder (agent) reads 17-UI-SPEC.md + runs/arduview/site/PAGE-SPEC.md → writes tokenized
   runs/arduview/site/index.html + deposit.html (links styles.css; emits {{TOKENS}} + assets/img/<name>.jpg).
2. node tools/lp-ui-check.js --site=runs/arduview/site   → MUST exit 0 (FAIL loops back to /lp-builder only).
3. operator pastes Klaviyo + Shopify strings into runs/arduview/site/integration.json (CHECKPOINT).
4. node tools/lp-hydrate.js --site=runs/arduview/site    → fills the two tokens; zero stray {{...}}.
5. node tools/asset-upload.js --space=arduview --url-map=<path>  → CDN backfill (or logged gap, never fabricated).
6. python3 runs/arduview/_tooling/surge_drive.py runs/arduview/site arduview-see-through.surge.sh  → live URL.
Reference target: runs/arduview/site/index.html (268 ln) + deposit.html (97 ln) — the proven output to match.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the builder → checker loop to produce gated tokenized Arduview HTML</name>
  <read_first>
    - .planning/phases/17-.../17-INTERFACES.md (the full stage order + each seam's invariants)
    - .claude/skills/lp-builder/SKILL.md (the builder agent contract — invoke it as specified)
    - .planning/phases/17-.../17-UI-SPEC.md (visual contract the builder composes against)
    - runs/arduview/site/PAGE-SPEC.md (the Arduview content contract the builder reads)
    - runs/arduview/site/index.html + deposit.html (the proven reference output to match — confirm parity after the build)
  </read_first>
  <files>runs/arduview/site/index.html, runs/arduview/site/deposit.html</files>
  <action>
    Invoke the `/lp-builder` agent (per `.claude/skills/lp-builder/SKILL.md`) to compose
    `runs/arduview/site/index.html` + `deposit.html` from `17-UI-SPEC.md` + `runs/arduview/site/PAGE-SPEC.md`.
    The builder MUST: link `styles.css` (emit the verbatim <head> font-load block), emit
    `{{KLAVIYO_FORM_ACTION}}` in the email `<form action="...">` and `{{SHOPIFY_CHECKOUT}}` in the deposit
    `<a ... href="{{SHOPIFY_CHECKOUT}}" data-checkout>`, leave stable `assets/img/<name>.jpg` filenames, render
    only PAGE-SPEC visible copy (no invented chrome), and place each `.hl` per PAGE-SPEC's pre-marked
    highlights (no black-on-black).

    Then run the gate: `node tools/lp-ui-check.js --site=runs/arduview/site`. It MUST exit 0. If it exits 1,
    the FAIL loops back to `/lp-builder` ONLY (the sole re-writer) — re-compose the flagged block, re-check.
    Do NOT hand-edit the HTML outside the builder (that would violate the one-writer rule). Iterate
    builder→check until exit 0.

    Confirm parity with the proven reference: the section set, classes, and copy match the reference
    index.html/deposit.html for the Arduview instance (the reference is the target, not a file to diff
    byte-for-byte — the builder regenerates it tokenized).
  </action>
  <verify>
    <automated>grep -q 'action="{{KLAVIYO_FORM_ACTION}}"' runs/arduview/site/index.html && grep -q '{{SHOPIFY_CHECKOUT}}' runs/arduview/site/deposit.html && grep -q 'data-checkout' runs/arduview/site/deposit.html && grep -q 'href="styles.css"' runs/arduview/site/index.html && node tools/lp-ui-check.js --site=runs/arduview/site && echo "BUILDER+CHECK OK"</automated>
  </verify>
  <acceptance_criteria>
    - `runs/arduview/site/index.html` contains `action="{{KLAVIYO_FORM_ACTION}}"` on the email form.
    - `runs/arduview/site/deposit.html` contains `{{SHOPIFY_CHECKOUT}}` in a `data-checkout` link.
    - Both pages link `styles.css` (grep `href="styles.css"`) and do NOT inline a `<style>` block re-authoring the design system.
    - `node tools/lp-ui-check.js --site=runs/arduview/site` exits 0 (HARD-1..7 gate passes).
    - No live integration URL is present pre-hydrate (grep finds the `{{...}}` tokens, not a `kmail`/`myshopify` URL yet).
  </acceptance_criteria>
  <done>
    Builder-composed, gate-passing tokenized Arduview HTML exists, matching the reference for the instance,
    with both integration tokens in their fixed DOM slots and the locked CSS linked (not re-authored).
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator provisions Klaviyo + Shopify and pastes the two strings into integration.json</name>
  <what-built>
    The tokenized Arduview HTML is built and passes the HARD-1..7 gate. The only thing missing to go live is
    the two provisioning strings, which require human-only dashboard actions (no CLI/API in v1 — D-07).
  </what-built>
  <how-to-verify>
    1. In Klaviyo: create a list + sign-up form; copy the form's ACTION URL (the hosted-form POST target).
    2. In Shopify: create the $5 deposit product; copy its cart permalink (e.g.
       `https://<store>.myshopify.com/cart/<variant>:1`).
    3. Paste both into `runs/arduview/site/integration.json`, replacing the `PASTE_..._HERE` placeholders:
       ```json
       { "KLAVIYO_FORM_ACTION": "<your klaviyo form action>", "SHOPIFY_CHECKOUT": "<your shopify cart permalink>" }
       ```
    4. (Klaviyo embed-mode note, RESEARCH Open Q3: v1 assumes a hosted-form-action POST. If your Klaviyo
       setup needs a JS fetch instead, say so — that is a separate script.js change.)
    Confirm: `node -e "const c=require('./runs/arduview/site/integration.json'); console.log(c)"` shows real
    URLs, not `PASTE_` placeholders.
  </how-to-verify>
  <resume-signal>Type "provisioned" once both real strings are pasted, or "skip-live" to deploy with unfilled tokens (page builds; checkout/email inert until pasted).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Hydrate tokens, backfill CDN, deploy live, verify the page loads</name>
  <read_first>
    - .planning/phases/17-.../17-INTERFACES.md (the post-build substitution order: hydrate → asset-upload → deploy; never re-open HTML with an agent)
    - tools/lp-hydrate.js (the hydrate CLI — invoke with --site)
    - tools/asset-upload.js lines 29-94 (the asset-upload CLI — --space + --url-map; D-08, already built)
    - runs/arduview/_tooling/surge_drive.py (the parameterized deploy driver — invoke with site + domain args)
    - runs/arduview/site/integration.json (the operator-filled config the hydrate reads)
  </read_first>
  <files>runs/arduview/site/index.html, runs/arduview/site/deposit.html</files>
  <action>
    Run the deterministic post-build pass IN ORDER (no agent re-opens the HTML):

    1. `node tools/lp-hydrate.js --site=runs/arduview/site`. After it runs, assert zero stray integration
       tokens remain IF the operator provisioned: `grep -E '\{\{(KLAVIYO_FORM_ACTION|SHOPIFY_CHECKOUT)\}\}'`
       returns nothing. (If the operator chose "skip-live", the tokens remain by design — that is a logged
       WARN, not a failure.)

    2. `node tools/asset-upload.js --space=arduview --url-map=<url-map.json path>` IF a url-map.json exists
       (CDN URLs only exist after the manual Shopify Files image upload — D-08). If no url-map yet, the
       `assets/img/<name>.jpg` filenames stay local (the page still deploys; images load from the local
       `assets/` dir). Never fabricate a CDN URL (asset-upload already enforces this).

    3. Deploy: `python3 runs/arduview/_tooling/surge_drive.py runs/arduview/site arduview-see-through.surge.sh`
       (the parameterized driver from Plan 03). The PTY feeds the throwaway surge creds.

    4. Verify live: `curl -sI https://arduview-see-through.surge.sh` returns HTTP 200, and
       `curl -sI https://arduview-see-through.surge.sh/deposit.html` returns 200.

    If deploy hits a surge auth error, that is an authentication gate — surface it and retry after the
    operator authenticates (do not pre-plan around it).
  </action>
  <verify>
    <automated>node tools/lp-hydrate.js --site=runs/arduview/site; curl -sI https://arduview-see-through.surge.sh | head -1 | grep -q "200" && curl -sI https://arduview-see-through.surge.sh/deposit.html | head -1 | grep -q "200" && echo "LIVE OK"</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/lp-hydrate.js --site=runs/arduview/site` runs and writes `_hydrate-log.txt`.
    - If provisioned: `grep -E '\{\{(KLAVIYO_FORM_ACTION|SHOPIFY_CHECKOUT)\}\}' runs/arduview/site/*.html` returns nothing (zero stray integration tokens).
    - `curl -sI https://arduview-see-through.surge.sh` returns HTTP 200; `/deposit.html` returns HTTP 200.
    - The live LP shows the hero with video and the email form; the deposit page shows the $5 offer (Kam confirms in Task 4).
  </acceptance_criteria>
  <done>
    Tokens hydrated (or logged un-provisioned), CDN backfilled (or local), Arduview deployed live, both pages
    return HTTP 200.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Kam verifies the live Arduview LP + deposit page (UAT gate)</name>
  <what-built>
    The full pipeline ran: /lp-builder composed tokenized HTML → lp-ui-check gated HARD-1..7 → lp-hydrate
    filled the integration tokens → (asset-upload backfilled CDN) → surge deployed live. No agent re-opened
    the HTML after the builder.
  </what-built>
  <how-to-verify>
    1. Open https://arduview-see-through.surge.sh in a browser.
       - Hero: headline + CTA + video, video ≥ half the hero (HARD-4); the "see right through" highlight is
         CYAN, not black-on-black (HARD-1).
       - Email form submits to the Klaviyo action (if provisioned).
       - No invented captions/eyebrows/chrome (HARD-7); CTA is black-on-cyan, readable face not Doto (HARD-2).
    2. Open https://arduview-see-through.surge.sh/deposit.html.
       - Above fold: image + title + CTA + the 51%-off value (HARD-4). The "$5 deposit" highlight is cyan.
       - The `reserve for $5 →` CTA navigates to the Shopify checkout (if provisioned).
       - Risk-reversal points appear once each (HARD-6).
    3. Confirm parity with the proven reference build — same look, no regressions.
  </how-to-verify>
  <resume-signal>Type "approved" if the live pages match the contract, or describe the specific HARD-rule violation / copy issue to loop back to /lp-builder.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → integration.json → HTML | The operator's two pasted strings are substituted into the live public page. |
| local site dir → public surge URL | Deploy pushes the static site to a public host. |
| surge host creds | Throwaway plaintext account drives the deploy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-06-01 | Information disclosure | Unfilled `{{TOKEN}}` shipped live | mitigate | Task 3 greps for stray integration tokens after hydrate when provisioned; lp-hydrate logs `[STRAY]`. If the operator chose skip-live, un-provisioned tokens are an explicit, logged decision, not an accident. |
| T-17-06-02 | Spoofing/Tampering | Wrong/malicious URL pasted into integration.json injected into the live page | accept | integration.json is trusted operator input (16-05 T-16-05-03 "accept"); the Task-4 Kam UAT review is the backstop (he confirms the form/checkout target). |
| T-17-06-03 | Information disclosure | Plaintext surge creds used in deploy | accept | Throwaway host account, documented; not expanded. Acceptable v1 posture (RESEARCH Runtime State). |
| T-17-06-04 | Information disclosure (V5) | `--space`/`--site`/`--url-map` argv into write paths during the run | mitigate | hydrate/asset-upload already apply the S2 path sanitizer to argv segments (Plans 03 + the existing asset-upload). |
</threat_model>

<verification>
- Builder output passes lp-ui-check (exit 0); both tokens in fixed DOM slots; styles.css linked not re-authored.
- After hydrate (provisioned): zero stray integration tokens.
- Live: arduview-see-through.surge.sh + /deposit.html both return HTTP 200.
- Kam UAT approval (Task 4) — the phase gate.
</verification>

<success_criteria>
The full no-rework pipeline runs end-to-end and produces the live Arduview LP + deposit page — tokens resolved
(or explicitly skipped), CDN images backfilled (or local), no agent having re-opened the HTML after the
builder. Kam confirms parity with the proven reference. This is the workflow's validation instance (D-12).
</success_criteria>

<output>
After completion, create `.planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-06-SUMMARY.md`
</output>
