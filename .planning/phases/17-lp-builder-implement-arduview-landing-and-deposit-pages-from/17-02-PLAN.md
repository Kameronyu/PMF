---
phase: 17-lp-builder-implement-arduview-landing-and-deposit-pages-from
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - runs/arduview/site/PAGE-SPEC.md
  - runs/arduview/site/integration.json
autonomous: true
requirements: [LPB-02, LPB-05]
must_haves:
  truths:
    - "A per-instance content contract (PAGE-SPEC) exists for Arduview with an ordered section→component map, a closed copy-slot table (every visible string), an asset-map (filename→alt), and a tokens-used list"
    - "Every copy string in PAGE-SPEC traces to runs/arduview/COPY-DRAFT.md or the UI-SPEC Copywriting Contract — the builder never opens COPY-DRAFT"
    - "The single per-section .hl highlight is marked explicitly in PAGE-SPEC so the builder never CHOOSES emphasis (feeds HARD-1 / HARD-7)"
    - "A manual-paste integration.json config stub exists with the two closed-vocab keys, holding placeholders the operator fills (no secrets committed)"
  artifacts:
    - path: "runs/arduview/site/PAGE-SPEC.md"
      provides: "Arduview content contract (section map + copy slots + asset map + tokens-used)"
      contains: "## Section map"
      min_lines: 60
    - path: "runs/arduview/site/integration.json"
      provides: "Manual-paste provisioning config (Klaviyo + Shopify strings)"
      contains: "KLAVIYO_FORM_ACTION"
  key_links:
    - from: "runs/arduview/site/PAGE-SPEC.md tokens-used list"
      to: "17-INTERFACES.md closed token vocab"
      via: "subset relation (⊆)"
      pattern: "KLAVIYO_FORM_ACTION|SHOPIFY_CHECKOUT"
---

<objective>
Author the Arduview **PAGE-SPEC** (per-instance content contract, D-09/LPB-02) and the manual-paste
**integration.json** provisioning config stub (D-07/LPB-05). PAGE-SPEC is the copy+asset contract the
builder reads INSTEAD of COPY-DRAFT — extraction is a plan-time job, not a builder job (BUILD-FEEDBACK §0).

Purpose: Freeze the Arduview instance's content surface so the builder renders only what the brief gives it
(HARD-7 restraint) and so the single per-section highlight is pre-marked (the builder never chooses emphasis).
The integration.json stub decouples the build from live provisioning — the pipeline builds and tests with
zero live accounts (D-04); the operator pastes two strings later to go live.
Output: `runs/arduview/site/PAGE-SPEC.md`, `runs/arduview/site/integration.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-INTERFACES.md
@.planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-UI-SPEC.md
@runs/arduview/COPY-DRAFT.md

<interfaces>
<!-- PAGE-SPEC schema (RESEARCH L311-340). The builder reads THIS, never COPY-DRAFT. -->
PAGE-SPEC sections:
- ## Pages: index.html (LP — email capture) · deposit.html (offer/checkout)
- ## Section map (ordered): | order | section_id | component (UI-SPEC class) | surface | assets (filename) |
- ## Copy (closed set — every visible string, HARD-7): | slot_id | section | text |
- ## Asset map (filename → alt; alt is NON-visible, unconstrained): | filename | alt |
- ## Tokens used (⊆ UI-SPEC token table): {{KLAVIYO_FORM_ACTION}} (email forms) · {{SHOPIFY_CHECKOUT}} (deposit data-checkout)

Highlight marking (RESEARCH Open Q5 / PATTERNS L218): mark the single per-section .hl inline, e.g.
`The pocket game console you can {{hl:see right through}}.` — so the builder never chooses emphasis.

Stable asset filenames (UI-SPEC L220, reference index.html/deposit.html):
assets/img/transparency.jpg, maker.jpg, gameplay.jpg, scale.jpg, deposit.jpg, kickstarter.png,
payment-icons.jpg, hero-poster.jpg · assets/video/hero.mp4
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the Arduview PAGE-SPEC content contract from COPY-DRAFT + UI-SPEC</name>
  <read_first>
    - .planning/phases/17-.../17-INTERFACES.md (the PAGE-SPEC stage block — Reads/Writes/Invariants you must satisfy; the closed token vocab)
    - .planning/phases/17-.../17-UI-SPEC.md (Component Inventory L183-207 for the section→class map; Copywriting Contract L224-243 for locked CTA/fine-print/value/secure strings; HARD-4/HARD-5 fold+order; HARD-1 highlight rule)
    - runs/arduview/COPY-DRAFT.md (the Arduview copy source — extract visible strings; the builder will NEVER read this file)
    - runs/arduview/site/index.html + runs/arduview/site/deposit.html (the proven reference output — read to confirm which sections exist, which classes carry which copy, and the stable asset filenames; e.g. index.html L26-256 sections, deposit.html L20-85 offer/reassure)
  </read_first>
  <files>runs/arduview/site/PAGE-SPEC.md</files>
  <action>
    Create `runs/arduview/site/PAGE-SPEC.md` following the schema in `<interfaces>` above. Header line:
    "PAGE-SPEC — arduview (content contract; builder reads THIS, never COPY-DRAFT)".

    Build it by reading COPY-DRAFT + the reference index.html/deposit.html + the UI-SPEC Copywriting Contract,
    then transcribing into the four tables:

    1. `## Pages` — `index.html` (LP — email capture) and `deposit.html` (offer/checkout).

    2. `## Section map` (ordered) — one row per section in the reference index.html and deposit.html, mapping
       section_id → the locked UI-SPEC component class → surface (dark/light/paper) → asset filenames. Source
       the order + classes from the reference: index.html hero (.hero, dark, hero.mp4 + hero-poster.jpg),
       the .split bands (.section--dark / .section--light with transparency.jpg, maker.jpg, gameplay.jpg,
       scale.jpg), the founder band, the final CTA; deposit.html .deposit-hero → .offer (deposit.jpg) +
       .reassure (kickstarter.png, payment-icons.jpg). HARD-4 fold budget: mark which sections are above-fold
       per page (LP: hero title+CTA+video; deposit: image+title+CTA+51%-value).

    3. `## Copy` (closed set) — every VISIBLE string the builder may render, one row per slot_id. Include the
       locked UI-SPEC Copywriting Contract values VERBATIM:
         - LP hero CTA `get early access →`, mid CTA `lock in early access →`, final CTA `get early access →`
         - Deposit CTA `reserve for $5 →`
         - LP CTA fine print `Founders get Arduview in August 2026. Everyone else waits until 2027.`
         - Email success `✓ locked in. founders ship aug 2026.` / `✓ done. watch your inbox.`
         - Email placeholder `your email`
         - Deposit value (above fold, once) `Your $5 deposit comes straight off the $146 founder price. Fully refundable until it ships.`
         - Deposit secure note `🔒 secure checkout · no account needed · card details never touch this page`
         - Risk-reversal (each ONCE, HARD-6): founder price $146 locked · ships first Aug 2026 · 30-day money-back
       Plus all headline/body copy transcribed from COPY-DRAFT + the reference HTML (e.g. index.html L77
       "Most gadgets hide the good part. This one shows it off.", L117, L152). Mark the SINGLE per-section
       `.hl` highlight inline using `{{hl:...}}` (RESEARCH Open Q5) — e.g. hero headline
       `The pocket game console you can {{hl:see right through}}.` and the deposit value `{{hl:$5 deposit}}`.
       Exactly ONE highlight per section (HARD-1 / accent <5%).

    4. `## Asset map` — filename → alt text (alt is NON-visible, unconstrained — write descriptive alt). Cover
       all stable filenames listed in `<interfaces>`.

    5. `## Tokens used` — list `{{KLAVIYO_FORM_ACTION}}` (email forms) and `{{SHOPIFY_CHECKOUT}}` (deposit
       data-checkout). This list MUST be a subset of the INTERFACES closed token vocab.

    Restraint guard (HARD-7): include NO invented figcaptions, eyebrows, badge labels, or helper microcopy in
    the Copy table — only strings that exist in COPY-DRAFT / the reference / the UI-SPEC contract.
  </action>
  <verify>
    <automated>test -f runs/arduview/site/PAGE-SPEC.md && grep -q "## Section map" runs/arduview/site/PAGE-SPEC.md && grep -q "## Copy" runs/arduview/site/PAGE-SPEC.md && grep -q "## Asset map" runs/arduview/site/PAGE-SPEC.md && grep -q "## Tokens used" runs/arduview/site/PAGE-SPEC.md && grep -q "{{hl:" runs/arduview/site/PAGE-SPEC.md && grep -q "reserve for \$5" runs/arduview/site/PAGE-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - `runs/arduview/site/PAGE-SPEC.md` exists, ≥60 lines.
    - Has all four tables: `## Section map`, `## Copy`, `## Asset map`, `## Tokens used`.
    - At least one `{{hl:...}}` highlight marked (grep `{{hl:`); the hero highlight present.
    - Contains the locked CTA strings (grep `get early access`, `reserve for $5`) and the deposit value + secure note.
    - `## Tokens used` lists only tokens from the closed vocab (grep finds `KLAVIYO_FORM_ACTION` and `SHOPIFY_CHECKOUT`; no other `{{[A-Z_]+}}` declared as a token).
    - Asset map covers `transparency.jpg`, `hero.mp4`, `hero-poster.jpg`, `deposit.jpg` (grep each).
  </acceptance_criteria>
  <done>
    The Arduview content contract exists, every visible string traces to a source (no invented copy), the
    single per-section highlights are pre-marked, and the tokens-used list is a subset of the closed vocab.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the manual-paste integration.json provisioning config stub</name>
  <read_first>
    - .planning/phases/17-.../17-INTERFACES.md (the integration.json frozen shape + the closed token keys)
    - .planning/phases/17-.../17-RESEARCH.md (D-07 manual-paste posture L19; Security Domain — integration.json is trusted operator input, non-secret public strings L458)
    - tools/asset-upload.js lines 178-190 (the url-map.json type-guard-on-load analog — same trusted-config posture)
  </read_first>
  <files>runs/arduview/site/integration.json</files>
  <action>
    Create `runs/arduview/site/integration.json` as the manual-paste provisioning stub. Frozen shape (from
    INTERFACES): a plain object with exactly the two closed-vocab keys:
    ```json
    {
      "KLAVIYO_FORM_ACTION": "PASTE_KLAVIYO_HOSTED_FORM_ACTION_URL_HERE",
      "SHOPIFY_CHECKOUT": "PASTE_SHOPIFY_5DOLLAR_CART_PERMALINK_HERE"
    }
    ```
    These are PLACEHOLDER strings, not secrets — the operator replaces them at provisioning time (the Klaviyo
    hosted-form action URL and the Shopify $5-product cart permalink, e.g.
    `https://<store>.myshopify.com/cart/<variant>:1`). NON-secret public endpoint strings by design
    (D-04/D-07). Do NOT commit any real credential. The placeholders are intentionally non-URL so a stray
    pre-deploy `{{...}}`/placeholder check can flag an un-provisioned config.

    Add a sibling `runs/arduview/site/integration.README.md` (or a top-of-file comment is not valid JSON, so
    use a separate README) documenting: what each string is, where the operator gets it (Klaviyo dashboard →
    form embed → action URL; Shopify → $5 product → cart permalink), and that `lp-hydrate.js` reads this file
    to fill the two HTML tokens. Note the Klaviyo embed-mode open question (RESEARCH Open Q3): v1 assumes a
    hosted-form-action POST; if the operator's chosen mode needs a fetch, that is a separate change.
  </action>
  <verify>
    <automated>test -f runs/arduview/site/integration.json && node -e "const c=require('./runs/arduview/site/integration.json'); const k=Object.keys(c); if(!(k.length===2 && 'KLAVIYO_FORM_ACTION' in c && 'SHOPIFY_CHECKOUT' in c)) {console.error('bad shape',k); process.exit(1)} console.log('integration.json shape ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `runs/arduview/site/integration.json` exists and is valid JSON parseable by `node -e require(...)`.
    - It is a plain object with EXACTLY the two keys `KLAVIYO_FORM_ACTION` and `SHOPIFY_CHECKOUT` (the node check above exits 0).
    - Values are obvious placeholders, not real URLs/credentials (grep `PASTE_` matches both).
    - A companion `integration.README.md` exists explaining provisioning + the Klaviyo embed-mode note.
  </acceptance_criteria>
  <done>
    The provisioning config stub exists with the frozen 2-key shape, placeholder values (no secrets), and an
    operator README. lp-hydrate.js (Plan 03) can read it.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → integration.json | The operator hand-pastes two endpoint strings; lp-hydrate.js later substitutes them into static HTML. Trusted operator input (same posture as url-map.json). |
| COPY-DRAFT → PAGE-SPEC | Plan-time content extraction; operator-authored static copy, not user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02-01 | Information disclosure | integration.json committed to git | mitigate | Commit only PLACEHOLDER strings (`PASTE_..._HERE`), never real credentials. The two strings are non-secret public endpoints by design (D-04/D-07); the README states this explicitly. No secret enters git. |
| T-17-02-02 | Tampering/XSS | PAGE-SPEC copy → rendered HTML | accept | Copy is operator-authored static text transcribed at plan time (not user input); it renders as static HTML text, no dynamic interpolation. The builder escapes it as ordinary copy (Plan 05 invariant). Low risk; documented. |
</threat_model>

<verification>
- PAGE-SPEC has all four tables, ≥60 lines, highlights pre-marked, tokens-used ⊆ closed vocab.
- integration.json is valid JSON with exactly the two closed-vocab keys and placeholder values.
</verification>

<success_criteria>
The Arduview content contract and provisioning config stub exist. The builder can render the instance from
PAGE-SPEC alone (never opening COPY-DRAFT), and lp-hydrate.js can fill tokens from integration.json once the
operator pastes the two strings.
</success_criteria>

<output>
After completion, create `.planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-02-SUMMARY.md`
</output>
