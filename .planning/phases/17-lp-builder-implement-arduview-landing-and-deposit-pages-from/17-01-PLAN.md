---
phase: 17-lp-builder-implement-arduview-landing-and-deposit-pages-from
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-INTERFACES.md
autonomous: true
requirements: [LPB-01]
must_haves:
  truths:
    - "Every pipeline stage (UI-SPEC, PAGE-SPEC, builder, ui-check, hydrate, asset-upload, deploy) has a frozen Reads/Writes/Invariants/MUST-NOT block a downstream stage can code against without reading the producer's internals"
    - "The token vocabulary is closed and identical to the UI-SPEC token table ({{KLAVIYO_FORM_ACTION}}, {{SHOPIFY_CHECKOUT}}) — no stage may invent a token"
    - "The integration.json and url-map.json JSON seams are frozen as {<payload>, _provenance?} plain-object shapes"
    - "It is stated, unambiguously, where each token lands: {{KLAVIYO_FORM_ACTION}} → email <form action>; {{SHOPIFY_CHECKOUT}} → deposit <a data-checkout href> (replaces the # placeholder); script.js stays token-free"
  artifacts:
    - path: ".planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-INTERFACES.md"
      provides: "Frozen inter-stage I/O contract (D-01) — the law every downstream stage reads"
      contains: "Stage: HTML BUILDER"
      min_lines: 80
  key_links:
    - from: "17-INTERFACES.md HTML BUILDER Writes block"
      to: "lp-ui-check.js / lp-hydrate.js Reads blocks"
      via: "schema-name match (tokenized index.html + deposit.html)"
      pattern: "index\\.html.*deposit\\.html"
---

<objective>
Produce `17-INTERFACES.md` — the FIRST deliverable of Phase 17 (D-01). It freezes the input/output
schema of EVERY stage in the LP-builder pipeline so build order can never force one stage to re-edit
another's output. Every downstream plan (builder, hydrate, ui-checker, deploy) codes against THIS
document's schema, not against the neighbor's internals.

Purpose: This is the "manifest contract" move the asset bricks already proved (Phase 16, 16-05). It is
the single thing that makes the whole no-rework pipeline possible: a stage reads its upstream neighbor's
"Writes" block by schema name only.
Output: `.planning/phases/17-.../17-INTERFACES.md` (committed contract doc).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-CONTEXT.md
@.planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-RESEARCH.md
@.planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-PATTERNS.md
@.planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-UI-SPEC.md

<interfaces>
<!-- The 16-05 manifest-contract precedent (the proven shape for D-01). Mirror this exactly. -->
From .planning/phases/16-.../16-05-PLAN.md `<interfaces>` block:
- S3 JSON store shape: one wrapper object, `JSON.stringify(obj, null, 2)`, underscore-meta keys:
  `images.json = { records: [...], _provenance, gap_list: [...] }`
- Pattern-1 stage-block template (RESEARCH L191-201):
```markdown
### Stage: HTML BUILDER
**Reads:** UI-SPEC.md (Component Inventory class names, HARD-1..7, token table) · PAGE-SPEC.md (copy + asset map + section map)
**Writes:** {space}/site/index.html, deposit.html — tokenized HTML
**Output invariants:**
  - every {{TOKEN}} ∈ the UI-SPEC token table (no invented tokens)
  - every <img src> = assets/img/<name>.jpg per PAGE-SPEC asset map (no live URLs)
  - links <link rel=stylesheet href=styles.css> (never inlines/authors CSS)
  - every visible string ∈ PAGE-SPEC copy (HARD-7)
**MUST NOT:** author CSS · invent chrome · emit live URLs · embed Arduview/crowdfunding semantics
```
UI-SPEC token table (17-UI-SPEC.md L214-218) — the CLOSED token vocab:
| {{KLAVIYO_FORM_ACTION}} | hydrate (script) | post-build |
| {{SHOPIFY_CHECKOUT}}    | hydrate (script) — replaces data-checkout href # | post-build |
| src="assets/img/<name>.jpg" | asset-upload.js backfills from url-map.json | post-build |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the frozen stage-by-stage INTERFACES contract</name>
  <read_first>
    - .planning/phases/17-.../17-CONTEXT.md (D-01..D-13 — the spec; D-01 makes this the first deliverable; D-02 lists the stage spine)
    - .planning/phases/17-.../17-RESEARCH.md (Pattern 1 manifest contract L185-203; the stage-block template L191-201; Pitfall 5 — script.js token reconciliation L268-271)
    - .planning/phases/17-.../17-PATTERNS.md (S2/S3/S4/S5 idioms; the `17-INTERFACES.md` assignment block L194-210)
    - .planning/phases/17-.../17-UI-SPEC.md (Tokenization table L210-220 — the CLOSED token vocab; Component Inventory L183-207)
    - .planning/phases/16-.../16-05-PLAN.md lines 69-85 (the `<interfaces>` block — the proven precedent shape)
  </read_first>
  <files>.planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-INTERFACES.md</files>
  <action>
    Create `17-INTERFACES.md`. It is a markdown contract doc (NOT code). Structure:

    1. Header: title, "Frozen inter-stage contract (D-01)", a one-line statement that downstream stages
       read each other's blocks by schema NAME ONLY, never the producer's internals.

    2. A "Shared JSON shape (S3)" section freezing every JSON seam as a plain object, pretty-printed
       `JSON.stringify(_, null, 2)`, underscore-prefixed meta keys. Freeze exactly two leaf JSONs:
       - `integration.json` = `{ "KLAVIYO_FORM_ACTION": string, "SHOPIFY_CHECKOUT": string }`
         (plain object, manual-paste, trusted operator input — no _provenance required, it is hand-authored).
       - `url-map.json` = `{ "<filename>": "<cdn_url>" }` (consumed by the existing asset-upload.js — do NOT
         redefine asset-upload's behavior, just reference its contract).

    3. A "Closed token vocabulary" section listing EXACTLY the two tokens, verbatim, and stating no stage
       may invent a token outside this set:
         - `{{KLAVIYO_FORM_ACTION}}` — lands in the email `<form action="...">` attribute on index.html
           (the reference form has NO action today — RESEARCH Pitfall 5; the builder MUST emit it).
         - `{{SHOPIFY_CHECKOUT}}` — replaces the `#` placeholder in the deposit `<a class="btn ..." href="#"
           data-checkout>` on deposit.html.
       State the script.js decision explicitly (RESEARCH Pitfall 5 / Open Q4 / PATTERNS Shared Pattern E):
       **script.js stays token-free** — forms POST natively via `<form action>`, the deposit link navigates
       via the `data-checkout` href; `lp-hydrate.js` therefore touches ONLY index.html + deposit.html, never
       script.js. (Note as the v1 posture; flag that if the operator's Klaviyo embed mode requires a fetch,
       script.js joins the hydrate pass — but v1 assumes hosted-form-action POST.)

    4. One Reads/Writes/Output-invariants/MUST-NOT block PER STAGE, in the D-02 spine order:
       a. **UI-SPEC** (visual contract — EXISTS at 17-UI-SPEC.md): Writes = class names + HARD-1..7 + token
          table. Note D-10: it SUPERSEDES STYLE-LOCK + BUILD-FEEDBACK §1; those are never re-read.
       b. **PAGE-SPEC** (content contract): Reads = COPY-DRAFT (plan-time extraction only, NOT by the builder).
          Writes = `runs/<space>/site/PAGE-SPEC.md` (section map + copy-slot table + asset map + tokens-used
          list ⊆ token table). Invariant: every tokens-used entry ∈ the closed vocab.
       c. **HTML BUILDER** (the ONE judgment agent): use the stage-block template above VERBATIM. Reads =
          UI-SPEC + PAGE-SPEC. Writes = `runs/<space>/site/index.html` + `deposit.html` tokenized. Invariants:
          every {{TOKEN}} ∈ closed vocab; every <img src> = `assets/img/<name>.jpg` per PAGE-SPEC asset map
          (no live URLs); links `styles.css` (never authors CSS); every visible string ∈ PAGE-SPEC copy
          (HARD-7). MUST-NOT: author CSS · invent chrome · emit live URLs · embed Arduview/crowdfunding
          semantics.
       d. **lp-ui-check.js** (deterministic gate): Reads = the builder's tokenized index.html + deposit.html
          + PAGE-SPEC copy set + the known locked styles.css rules. Writes = exit code (0 PASS / 1 FAIL) +
          `_ui-check-log.txt` sidecar. Invariant: enforces HARD-1..7; a FAIL loops back to the builder ONLY
          (the sole re-writer).
       e. **lp-hydrate.js** (deterministic): Reads = integration.json + index.html + deposit.html. Writes =
          the two HTML files in place (tokens filled) + `_hydrate-log.txt`. Invariant: fills only the closed
          vocab; NEVER fabricates a missing value (warn + leave token — the asset-upload T-16-05-02 rule).
       f. **asset-upload.js** (EXISTS — Phase 16 brick 8, D-08): Reads = url-map.json + the HTML/manifests.
          Writes = `assets/img/<name>.jpg` → `cdn.shopify.com/...` backfill. Do NOT redefine; reference its
          contract and state the builder leaves stable filenames it backfills.
       g. **deploy** (parameterized surge_drive.py): Reads = `--site` dir + `--domain`. Writes = a live
          surge.sh URL. Invariant: PTY credential-feed unchanged (proven); throwaway host creds stay throwaway.

    5. A one-line "no loop-back except checker FAIL → builder" rule (the load-bearing property — RESEARCH
       data-flow trace L156).

    For each downstream stage, the "Reads" line MUST name the upstream "Writes" artifact by schema name so a
    grep can confirm the seam matches.
  </action>
  <verify>
    <automated>test -f .planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-INTERFACES.md && grep -q "Stage: HTML BUILDER" .planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-INTERFACES.md && grep -q "KLAVIYO_FORM_ACTION" .planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-INTERFACES.md && grep -q "SHOPIFY_CHECKOUT" .planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-INTERFACES.md && grep -qi "token-free" .planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-INTERFACES.md</automated>
  </verify>
  <acceptance_criteria>
    - `17-INTERFACES.md` exists and is ≥80 lines.
    - Contains a `### Stage:` block for each of: UI-SPEC, PAGE-SPEC, HTML BUILDER, lp-ui-check, lp-hydrate, asset-upload, deploy (grep `-c '### Stage:'` ≥ 7).
    - Contains the closed token list with exactly `{{KLAVIYO_FORM_ACTION}}` and `{{SHOPIFY_CHECKOUT}}`; no third `{{...}}` token defined.
    - States `{{KLAVIYO_FORM_ACTION}}` lands in `<form action>` and `{{SHOPIFY_CHECKOUT}}` replaces the `data-checkout href #` (grep `data-checkout`).
    - States script.js is token-free / not hydrated (grep `-i "token-free"` or `script.js` line stating hydrate touches only the two HTML files).
    - Freezes `integration.json` as `{ "KLAVIYO_FORM_ACTION": ..., "SHOPIFY_CHECKOUT": ... }`.
  </acceptance_criteria>
  <done>
    The INTERFACES contract exists, freezes all 7 stage seams + the 2 JSON leaves + the closed 2-token vocab,
    and pins each token's exact DOM landing site. Every downstream plan can code against it without reading
    another stage's source.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none at this stage) | This task produces a markdown contract doc only — no code, no input handling, no runtime surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-01 | Tampering | The closed token vocabulary | mitigate | Freeze the vocab to exactly 2 tokens in writing so downstream stages cannot silently introduce an unescaped/injected token surface. The doc IS the control: it bans invented tokens, which is what later checker/hydrate stages enforce. |
| T-17-01-02 | Information disclosure | Provisioning strings (Klaviyo/Shopify) | accept | INTERFACES defines `integration.json` as non-secret public endpoint strings (a form action + a hosted checkout URL — D-04/D-07). No secret is specified to live in git. Documented, not a runtime threat at this stage. |
</threat_model>

<verification>
- `17-INTERFACES.md` exists, ≥80 lines, ≥7 `### Stage:` blocks.
- Closed 2-token vocab present; no invented third token.
- Each token's DOM landing site pinned; script.js declared token-free.
</verification>

<success_criteria>
The frozen inter-stage contract exists and every Phase-17 downstream stage can be built against its schema
blocks alone. The no-rework property (one writer; no loop-back except checker→builder) is stated as the
contract's load-bearing rule.
</success_criteria>

<output>
After completion, create `.planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-01-SUMMARY.md`
</output>
