---
phase: 17-lp-builder-implement-arduview-landing-and-deposit-pages-from
plan: 05
type: execute
wave: 2
depends_on: [01]
files_modified:
  - .claude/skills/lp-builder/SKILL.md
autonomous: true
requirements: [LPB-03]
must_haves:
  truths:
    - "A .claude/skills/lp-builder/SKILL.md exists defining the ONE judgment agent that writes HTML — pure (UI-SPEC + PAGE-SPEC → tokenized markup), emits tokens not live URLs, links styles.css and never re-authors CSS"
    - "The skill prompt contains ZERO Arduview/crowdfunding/deposit/founder semantics as builder logic — those are PAGE-SPEC content, not engine knowledge (product-agnostic test passes)"
    - "The skill encodes the closed token vocab, the additive-restraint rule (HARD-7), and the no-black-on-black placement rule (HARD-1) as builder behavior constraints"
    - "An ENFORCEMENT MAP states exactly ONE step is JUDGMENT (compose markup); hydrate/asset-upload/check/deploy are DETERMINISTIC — the PMF agent-design law"
  artifacts:
    - path: ".claude/skills/lp-builder/SKILL.md"
      provides: "The sole HTML-writing judgment agent (pure, tokenized, links locked CSS)"
      contains: "the ONLY agent that writes HTML"
      min_lines: 80
  key_links:
    - from: ".claude/skills/lp-builder/SKILL.md"
      to: "17-UI-SPEC.md + runs/<space>/site/PAGE-SPEC.md"
      via: "declared Reads (class names + HARD-1..7 + token table; copy + asset map + section map)"
      pattern: "UI-SPEC.*PAGE-SPEC"
---

<objective>
Author `.claude/skills/lp-builder/SKILL.md` — the ONE judgment agent in this pipeline (LPB-03). It is the
ONLY thing that writes HTML. It is pure: `{UI-SPEC + PAGE-SPEC} → tokenized index.html + deposit.html`,
linking the locked `styles.css` (never re-authoring it), emitting tokens (not live URLs) and stable asset
filenames. Modeled exactly on `.claude/skills/asset-classify/SKILL.md` (the repo's other one-judgment-agent
orchestrator skill).

Purpose: "The builder is the only thing that writes HTML, and it writes tokens not values" is the load-bearing
rule that resolves every re-edit worry (D-03/D-04). Keeping the builder product-agnostic (no Arduview
semantics) is what makes the pipeline reusable across brands.
Output: `.claude/skills/lp-builder/SKILL.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-INTERFACES.md
@.planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-UI-SPEC.md
@.planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-PATTERNS.md

<interfaces>
<!-- Skill structure modeled on .claude/skills/asset-classify/SKILL.md (the exact analog). -->
Frontmatter: `---\nname: lp-builder\ndescription: >-\n  ...\n---` then a "> SCHEMA NOT DUPLICATED HERE"
pointer to 17-INTERFACES.md + 17-UI-SPEC.md, then `# Title` + `## What this is`.
Chain-position ASCII block (asset-classify L40-52 shape):
  UI-SPEC.md (visual) + PAGE-SPEC.md (content) + integration token NAMES
    → /lp-builder  ← THE ONLY AGENT THAT WRITES HTML
      → lp-ui-check.js (deterministic gate, HARD-1..7) — FAIL loops back here, the sole re-writer
        → lp-hydrate.js (deterministic — {{TOKENS}})
          → asset-upload.js (deterministic — assets/img/<name>.jpg → CDN)
            → surge deploy (PTY)
ENFORCEMENT MAP table (asset-classify L56-72): Step | Kind | Mechanism | What enforces it —
  EXACTLY ONE row JUDGMENT (compose markup); hydrate/asset-upload/lp-ui-check/deploy = DETERMINISTIC.

Closed token vocab (INTERFACES / UI-SPEC L214-218): {{KLAVIYO_FORM_ACTION}} (email <form action>),
  {{SHOPIFY_CHECKOUT}} (deposit data-checkout href, replaces #). Stable filenames: assets/img/<name>.jpg.
Builder reads class NAMES only (UI-SPEC Component Inventory L183-207); never reads styles.css body (D-03).
HARD-7 restraint (UI-SPEC L172-177): every visible word ∈ PAGE-SPEC; non-visible (alt/aria/data/class) free.
HARD-1 placement (UI-SPEC L138-144): never place a .hl where the dark-surface cascade collapses to ink.
<head> font-load + stylesheet link (reference index.html L9-13 — verbatim, never re-authored).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the lp-builder SKILL.md (pure, product-agnostic, tokenized HTML writer)</name>
  <read_first>
    - .planning/phases/17-.../17-INTERFACES.md (HTML BUILDER stage block — Reads/Writes/Output-invariants/MUST-NOT; the closed token vocab; the no-loop-back-except-checker rule)
    - .planning/phases/17-.../17-UI-SPEC.md (Component Inventory L183-207 — the class names the builder composes against; HARD-1..7 L134-179; Tokenization L210-220; Color L108-128; the <head> font-load block)
    - .planning/phases/17-.../17-PATTERNS.md (the lp-builder assignment block L140-160 — frontmatter/chain/enforcement-map/load-bearing-truths shape; reference index.html/deposit.html conventions L228-239)
    - .claude/skills/asset-classify/SKILL.md (the EXACT structural analog — copy the frontmatter + "What this is" + chain-position + ENFORCEMENT MAP + "load-bearing truths" shape)
    - runs/arduview/site/index.html + deposit.html (the proven output to MATCH for the Arduview instance — read the section markup conventions: .hero/.split/.cta__form/.frame--dark classes, the <head> font-load L9-13, the email form L40-43, the deposit .offer L24-42 — but treat these as the instance's target, NOT as builder logic)
  </read_first>
  <files>.claude/skills/lp-builder/SKILL.md</files>
  <action>
    Create `.claude/skills/lp-builder/SKILL.md` mirroring `asset-classify/SKILL.md` structure.

    1. **Frontmatter**: `name: lp-builder`; `description: >-` stating it is THE SOLE HTML WRITER, PURE
       (copy + visual-system + assets → tokenized markup), emits TOKENS NOT LIVE URLs, links the locked
       styles.css and NEVER re-authors CSS, is PRODUCT-AGNOSTIC (knows nothing of Arduview/crowdfunding).

    2. **Schema pointer**: a `> SCHEMA NOT DUPLICATED HERE` line pointing to `17-INTERFACES.md` (the seam
       contract) + `17-UI-SPEC.md` (class names + HARD-1..7 + token table). The builder reads those, not this.

    3. **`# LP Builder` + `## What this is`**: state the one-line rule — "the builder is the only thing that
       writes HTML, and it writes tokens not values." It composes index.html + deposit.html for ANY instance
       from that instance's UI-SPEC (visual) + PAGE-SPEC (content).

    4. **Chain position** (ASCII, from `<interfaces>`): UI-SPEC + PAGE-SPEC + token NAMES → /lp-builder (the
       only HTML writer) → lp-ui-check.js (FAIL loops back here only) → lp-hydrate.js → asset-upload.js →
       surge deploy.

    5. **ENFORCEMENT MAP** table: rows for compose-markup (JUDGMENT — the only one), lp-ui-check
       (DETERMINISTIC gate), lp-hydrate (DETERMINISTIC), asset-upload (DETERMINISTIC), deploy (DETERMINISTIC).
       This encodes the CLAUDE.md agent-design law — exactly one judgment row.

    6. **Load-bearing truths (plain fact)**:
       - The builder reads UI-SPEC class NAMES + HARD-1..7, NEVER the styles.css body (D-03 / RESEARCH
         Pattern 3). It links `<link rel="stylesheet" href="styles.css">` and emits the verbatim <head>
         font-load block (reference index.html L9-13).
       - It renders ONLY PAGE-SPEC visible copy (HARD-7 additive restraint): NO invented figcaptions,
         eyebrows, kickers, badge labels, helper microcopy. Non-visible strings (alt/aria/data/class/file)
         are unconstrained. The single per-section highlight comes pre-marked in PAGE-SPEC (`{{hl:...}}`) —
         the builder never CHOOSES emphasis.
       - It emits ONLY the closed token vocab — `{{KLAVIYO_FORM_ACTION}}` in the email `<form action="...">`
         (the reference form has no action today — the builder MUST add it) and `{{SHOPIFY_CHECKOUT}}` as the
         deposit `<a ... href="{{SHOPIFY_CHECKOUT}}" data-checkout>` (replacing `#`) — plus stable
         `assets/img/<name>.jpg` filenames (asset-upload backfills CDN). NEVER a live URL.
       - HARD-1 placement: never place a `.hl` in a dark surface where the cascade collapses to ink; rely on
         the locked cyan-on-dark branch. (The lp-ui-check.js gate enforces this; the builder must not trip it.)
       - HARD-4/HARD-5 fold + order are stated per page in UI-SPEC; the builder follows them, never guesses.

    7. **Product-agnostic guard (anti-pattern callout)**: an explicit section stating the word "deposit",
       "$5", "crowdfunding", "founder" must NEVER appear as builder LOGIC — only as PAGE-SPEC content the
       builder lays out. Validation prompt: "would this builder work for a SaaS LP with different sections?"
       If a line of the prompt says "deposit"/"founder" as engine behavior, it is a bug. (The reference
       index.html/deposit.html are the proven OUTPUT to match for the Arduview INSTANCE, not the builder spec.)

    8. **Read budget** (BUILD-FEEDBACK §0): UI-SPEC + PAGE-SPEC (each once) + ~20 lines of the block being
       edited. Nothing else — never COPY-DRAFT, never styles.css body, never STYLE-LOCK/BUILD-FEEDBACK
       (superseded by UI-SPEC, D-10).

    Keep the SKILL.md product-agnostic: where an example is needed, frame it as "for the {space} instance"
    not "for Arduview" hardcoded into the rules.
  </action>
  <verify>
    <automated>test -f .claude/skills/lp-builder/SKILL.md && grep -q "name: lp-builder" .claude/skills/lp-builder/SKILL.md && grep -qi "only agent that writes html\|sole html writer\|only thing that writes html" .claude/skills/lp-builder/SKILL.md && grep -q "KLAVIYO_FORM_ACTION" .claude/skills/lp-builder/SKILL.md && grep -q "SHOPIFY_CHECKOUT" .claude/skills/lp-builder/SKILL.md && grep -qi "ENFORCEMENT MAP" .claude/skills/lp-builder/SKILL.md && grep -q "PAGE-SPEC" .claude/skills/lp-builder/SKILL.md && grep -q "styles.css" .claude/skills/lp-builder/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/skills/lp-builder/SKILL.md` exists, ≥80 lines, with valid `name: lp-builder` frontmatter.
    - States it is the sole HTML writer (grep one of: "only agent that writes html" / "sole html writer" / "only thing that writes html", case-insensitive).
    - Encodes the closed token vocab (grep `KLAVIYO_FORM_ACTION` AND `SHOPIFY_CHECKOUT`) and stable `assets/img/<name>.jpg` filenames; states it emits tokens NOT live URLs.
    - Has an ENFORCEMENT MAP with exactly one JUDGMENT row (grep `ENFORCEMENT MAP`; the compose-markup row is the only JUDGMENT).
    - References reading UI-SPEC + PAGE-SPEC and linking `styles.css` without re-authoring (grep `styles.css`, `PAGE-SPEC`, `UI-SPEC`).
    - Product-agnostic: contains the explicit anti-pattern guard that "deposit/founder/crowdfunding" must not be builder logic (grep `product-agnostic` or the guard sentence). The rules sections do not hardcode Arduview semantics as engine behavior.
  </acceptance_criteria>
  <done>
    The one judgment agent exists: pure, product-agnostic, the sole HTML writer, emitting the closed token
    vocab + stable filenames, linking the locked CSS, obeying HARD-1/HARD-7, modeled on asset-classify/SKILL.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PAGE-SPEC copy → builder-emitted HTML | The builder transcribes operator-authored copy into static HTML. |
| token names → emitted markup | The builder emits `{{TOKEN}}` placeholders, never live URLs (no secret enters the builder). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05-01 | Tampering / XSS | Copy-brief content rendered as markup in emitted HTML | mitigate | The builder treats PAGE-SPEC copy as STATIC TEXT content, not markup — it does not interpolate copy into attribute/script contexts unescaped, and emits only the two closed tokens in their fixed DOM slots (form action / data-checkout href). The skill states copy is rendered as visible text; the lp-ui-check gate + Kam review backstop. No dynamic/user input is rendered (copy is operator-authored, static). |
| T-17-05-02 | Information disclosure | Builder emitting a live URL or secret | mitigate | The builder is PURE (D-04) — it has no endpoints/secrets in its prompt and emits only `{{TOKEN}}` placeholders + stable filenames. The INTERFACES invariant "no live URLs" is restated in the skill; lp-ui-check / hydrate enforce the seam. |
</threat_model>

<verification>
- SKILL.md exists ≥80 lines, declares sole-HTML-writer + pure + product-agnostic, encodes closed token vocab + stable filenames, has a one-JUDGMENT ENFORCEMENT MAP, links styles.css without re-authoring, guards against Arduview semantics as logic.
</verification>

<success_criteria>
The one judgment agent is fully specified and could regenerate the Arduview instance HTML from UI-SPEC +
PAGE-SPEC alone, while remaining reusable for a different brand's LP with different sections.
</success_criteria>

<output>
After completion, create `.planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-05-SUMMARY.md`
</output>
