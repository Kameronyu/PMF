---
phase: 17-lp-builder-implement-arduview-landing-and-deposit-pages-from
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - tools/lp-hydrate.js
  - runs/arduview/_tooling/surge_drive.py
autonomous: true
requirements: [LPB-04, LPB-07]
must_haves:
  truths:
    - "A deterministic lp-hydrate.js fills {{KLAVIYO_FORM_ACTION}} and {{SHOPIFY_CHECKOUT}} in index.html + deposit.html from integration.json — and NEVER fabricates a missing value (warn + leave token)"
    - "Running hydrate twice is idempotent and leaves zero stray {{...}} when config is complete; leaves the exact token when a config key is absent"
    - "surge_drive.py is parameterized by argv/env (--site/--domain) so it deploys any instance, not just the hardcoded Arduview path — PTY credential-feed mechanism unchanged"
    - "No new third-party dependency is added (plain fs/path string-replace, mirroring asset-upload.js)"
  artifacts:
    - path: "tools/lp-hydrate.js"
      provides: "Deterministic post-build token substitution from integration.json"
      contains: "KLAVIYO_FORM_ACTION"
    - path: "runs/arduview/_tooling/surge_drive.py"
      provides: "Parameterized PTY surge deploy driver"
      contains: "sys.argv"
  key_links:
    - from: "tools/lp-hydrate.js"
      to: "runs/<site>/index.html + deposit.html"
      via: "string-replace of {{TOKEN}} from integration.json"
      pattern: "split"
---

<objective>
Build the two DETERMINISTIC scripts in the post-build substitution pass: `tools/lp-hydrate.js` (fills the
two integration tokens, LPB-04) and the parameterized deploy driver (`surge_drive.py` taking --site/--domain,
LPB-07). Neither is an agent — per the PMF agent-design law, hydrate and deploy are scripts (deterministic
jobs). They mirror existing, proven analogs verbatim.

Purpose: Fold Klaviyo + Shopify integration into a single deterministic post-build pass so no second agent
ever re-opens the HTML (D-06). Parameterizing the deploy driver makes the pipeline instance-agnostic (any
`runs/<space>/site`, not just Arduview).
Output: `tools/lp-hydrate.js`, edited `runs/arduview/_tooling/surge_drive.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-INTERFACES.md
@.planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-PATTERNS.md

<interfaces>
<!-- Copy these idioms VERBATIM from tools/asset-upload.js (the exact string-backfill analog). -->
S5 CLI parse + --help + required-arg guard (asset-upload.js L35-94):
  args -> opts reducer; if (opts.help || args.length===0) print usage + exit 0;
  if (!opts.site) { console.error('ERROR: --site=<dir> is required'); process.exit(1); }
S2 path sanitizer (asset-upload.js L99-110): toLowerCase, strip [^a-z0-9._-], collapse '..'.
Config load + type-guard (asset-upload.js L178-190): JSON.parse readFileSync; reject non-object/array.
Core substitution (asset-upload.js L210-227 string-replace, applied to {{TOKEN}}):
  TOKENS = ['KLAVIYO_FORM_ACTION','SHOPIFY_CHECKOUT'];  // closed vocab — INTERFACES / UI-SPEC L214-218
  for file of ['index.html','deposit.html']: read; for t of TOKENS:
    if cfg[t]==null -> [WARN] leave token, warnings++, continue   // NEVER fabricate (T-16-05-02)
    else html = html.split('{{'+t+'}}').join(cfg[t]); if changed -> [FILL], ok++
  writeFileSync back.
S4 sidecar log + summary + partial-exit (asset-upload.js L340-355):
  write <site>/_hydrate-log.txt; print one-line summary; exit 1 only if ok===0 AND warnings>0.
Deploy parameterization (surge_drive.py L8-9 -> argv/env; PATTERNS L187-188):
  SITE   = sys.argv[1] if len(sys.argv)>1 else os.environ.get('LP_SITE',   '/home/kyu3/PMF/runs/arduview/site')
  DOMAIN = sys.argv[2] if len(sys.argv)>2 else os.environ.get('LP_DOMAIN', 'arduview-see-through.surge.sh')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build tools/lp-hydrate.js (deterministic token substitution)</name>
  <read_first>
    - .planning/phases/17-.../17-INTERFACES.md (lp-hydrate stage block — Reads integration.json + the two HTML files; Writes them in place + _hydrate-log.txt; the closed token vocab; the never-fabricate rule)
    - .planning/phases/17-.../17-PATTERNS.md (the lp-hydrate.js assignment block L98-136 — exact line citations into asset-upload.js)
    - tools/asset-upload.js lines 29-110 (S5 CLI scaffold + S2 sanitizer), 178-190 (config load + type-guard), 210-227 (string-replace core), 340-355 (S4 sidecar log + partial-exit) — the exact analog to mirror
  </read_first>
  <files>tools/lp-hydrate.js</files>
  <action>
    Create `tools/lp-hydrate.js`. Zero third-party deps — `require('fs')`, `require('path')` only (RESEARCH
    A1: this is a 2-token find-replace, NOT a templating engine). Copy the idioms in `<interfaces>` above
    VERBATIM from asset-upload.js:

    - CLI (S5): accept `--site=<dir>` (REQUIRED) and `--config=<path>` (default `<site>/integration.json`).
      `--help`/no-args prints usage and exits 0. Missing `--site` → stderr + exit 1.
    - Sanitizer (S2): if a `--space` is ever passed, sanitize it; for `--site` (a full path) validate it
      exists and is a dir.
    - Config load + type-guard (asset-upload L178-190): load `integration.json`; reject non-object/array
      with a clear error + exit 1.
    - Closed token vocab: `const TOKENS = ['KLAVIYO_FORM_ACTION', 'SHOPIFY_CHECKOUT'];` — hardcode this; it
      is the frozen INTERFACES vocab. Do NOT read the token list from anywhere dynamic; do NOT accept extra
      tokens.
    - Core substitution: for each of `index.html`, `deposit.html` in `--site`: if file missing → `[SKIP]`
      log + continue. Else read; per token `.split('{{'+t+'}}').join(value)`. If `cfg[t] == null` → push
      `[WARN] <token> absent — token left unfilled`, increment warnings, CONTINUE — NEVER fabricate a URL
      (asset-upload T-16-05-02: "never construct a missing value"). Write the file back.
    - Optional pre-deploy guard: after substitution, scan each file for a stray `/\{\{[A-Z_]+\}\}/`; log
      `[STRAY] <file> <token>` to the sidecar (token-leak-to-prod warning). Do NOT hard-fail — a deliberately
      un-provisioned config must still build.
    - S4 sidecar: write `<site>/_hydrate-log.txt`, print a one-line summary
      (`lp-hydrate: <ok> tokens filled, <warnings> unfilled, site=<SITE>`), partial-success exit: exit 1
      ONLY if `ok === 0 && warnings > 0` (work was expected but config keys missing). Do NOT exit 1 on the
      idempotent second run (where ok===0 because tokens already filled and warnings===0).
  </action>
  <verify>
    <automated>node tools/lp-hydrate.js --help >/dev/null 2>&1 && mkdir -p /tmp/lphtest && printf '<form action="{{KLAVIYO_FORM_ACTION}}"></form>' > /tmp/lphtest/index.html && printf '<a href="{{SHOPIFY_CHECKOUT}}" data-checkout></a>' > /tmp/lphtest/deposit.html && printf '{"KLAVIYO_FORM_ACTION":"https://k.example/f","SHOPIFY_CHECKOUT":"https://s.example/cart/1:1"}' > /tmp/lphtest/integration.json && node tools/lp-hydrate.js --site=/tmp/lphtest --config=/tmp/lphtest/integration.json && ! grep -rq '{{' /tmp/lphtest/index.html /tmp/lphtest/deposit.html && grep -q 'k.example' /tmp/lphtest/index.html && echo "HYDRATE OK"</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/lp-hydrate.js --help` exits 0.
    - On the /tmp fixture, after running, `grep -c '{{' /tmp/lphtest/index.html` and `deposit.html` both return 0; both files contain the substituted URLs (grep `k.example`, `s.example`).
    - With a config missing a key (`{"KLAVIYO_FORM_ACTION":"https://k/f"}`), the run leaves `{{SHOPIFY_CHECKOUT}}` intact and logs a `[WARN]` (never fabricates).
    - File requires only `fs`/`path` (no other `require(`).
    - A second consecutive run on the filled site exits 0 (idempotent — not a false-fail).
  </acceptance_criteria>
  <done>
    lp-hydrate.js deterministically fills the two closed-vocab tokens, never fabricates a missing value, is
    idempotent, leaves a sidecar log, and uses zero third-party deps.
  </done>
</task>

<task type="auto">
  <name>Task 2: Parameterize surge_drive.py by argv/env (instance-agnostic deploy)</name>
  <read_first>
    - .planning/phases/17-.../17-INTERFACES.md (deploy stage block — Reads --site + --domain; PTY credential-feed unchanged; throwaway creds stay throwaway)
    - .planning/phases/17-.../17-PATTERNS.md (deploy assignment block L180-190 — keep the PTY mechanism verbatim; ONLY change SITE/DOMAIN to argv/env)
    - runs/arduview/_tooling/surge_drive.py (the full current file — the proven PTY driver; you edit ONLY lines 8-9 + add one comment)
  </read_first>
  <files>runs/arduview/_tooling/surge_drive.py</files>
  <action>
    Edit `runs/arduview/_tooling/surge_drive.py`. Replace ONLY the hardcoded constants at lines 8-9 with
    (PATTERNS L187-188):
    ```python
    SITE   = sys.argv[1] if len(sys.argv) > 1 else os.environ.get('LP_SITE',   '/home/kyu3/PMF/runs/arduview/site')
    DOMAIN = sys.argv[2] if len(sys.argv) > 2 else os.environ.get('LP_DOMAIN', 'arduview-see-through.surge.sh')
    ```
    `sys` and `os` are already imported (line 6). KEEP VERBATIM — do not touch: the
    `argv = ['npx','--yes','surge','.',DOMAIN]` line, the `pty.fork()`/`os.execvp` child block, the
    `select`/read/feed loop, the `b'email:' in buf.lower()` trigger, the 220s timeout. These are the proven
    mechanism (piping stdin fails surge's prompt — memory `reference_surge_static_deploy`).

    SECURITY: the hardcoded EMAIL/PW (lines 10-11) are a THROWAWAY host account. Leave them for v1 but add a
    one-line comment that they are throwaway and must NOT be replaced with real/shared credentials in git.
    Do NOT introduce a real credential.

    Backward compatibility: the Arduview path/domain are preserved as the env fallback, so a bare
    `python3 surge_drive.py` still deploys Arduview exactly as before.
  </action>
  <verify>
    <automated>grep -q "sys.argv" runs/arduview/_tooling/surge_drive.py && grep -q "LP_SITE" runs/arduview/_tooling/surge_drive.py && grep -q "LP_DOMAIN" runs/arduview/_tooling/surge_drive.py && grep -q "pty.fork" runs/arduview/_tooling/surge_drive.py && grep -q "email:" runs/arduview/_tooling/surge_drive.py && python3 -c "import ast; ast.parse(open('runs/arduview/_tooling/surge_drive.py').read()); print('surge_drive.py parses OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `surge_drive.py` references `sys.argv`, `LP_SITE`, `LP_DOMAIN` (grep each).
    - The PTY mechanism is intact: `pty.fork` and the `email:` credential-feed trigger still present (grep each).
    - File still parses as valid Python (`python3 -c "ast.parse(...)"` exits 0).
    - No real/shared credential added (the throwaway account comment present; EMAIL/PW unchanged from prior).
  </acceptance_criteria>
  <done>
    surge_drive.py is instance-agnostic via argv/env with the Arduview default preserved, the proven PTY
    credential-feed unchanged, and throwaway creds flagged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → write/read path | `--site`/`--space` segments are used to build file paths in lp-hydrate.js. |
| integration.json → emitted HTML | URLs from operator config are substituted into static HTML by hydrate. |
| surge host creds | Plaintext throwaway account in surge_drive.py, used to deploy a public site. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-01 | Tampering (path traversal) | lp-hydrate.js `--space`/`--site` argv | mitigate | Apply the S2 `sanitizePathSegment()` to any `--space`; validate `--site` is an existing directory before read/write. Verbatim control from asset-upload.js L99-110 (T-16-05-01). |
| T-17-03-02 | Information disclosure | Unfilled `{{TOKEN}}` shipped to prod | mitigate | hydrate logs `[STRAY] <token>` on any remaining `{{...}}` after substitution; NEVER fabricates a URL (warn + leave). The Plan-06 pre-deploy check greps for stray tokens before shipping. |
| T-17-03-03 | Spoofing/Tampering | Malicious URL pasted into integration.json → injected into HTML | accept | integration.json is trusted operator input (same posture as url-map.json — 16-05 T-16-05-03 "accept"); the live-deploy + Kam review is the backstop. Optional well-formed-https check is a nice-to-have, not required. |
| T-17-03-04 | Information disclosure | Plaintext surge creds in surge_drive.py | accept | Throwaway host account, documented; flagged in-comment not to be replaced with real creds. Acceptable for v1 (RESEARCH Runtime State). |
</threat_model>

<verification>
- lp-hydrate.js: --help exits 0; fills both tokens on the /tmp fixture leaving zero stray `{{`; never fabricates a missing key; idempotent second run exits 0; fs/path only.
- surge_drive.py: parameterized via argv/env, PTY mechanism intact, parses as valid Python, throwaway creds flagged.
</verification>

<success_criteria>
Both deterministic post-build scripts exist and pass their smoke checks. The integration pass is a single
deterministic substitution (no second agent touches HTML), and deploy is instance-agnostic.
</success_criteria>

<output>
After completion, create `.planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-03-SUMMARY.md`
</output>
