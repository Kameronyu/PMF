---
phase: 17-lp-builder-implement-arduview-landing-and-deposit-pages-from
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - tools/lp-ui-check.js
autonomous: true
requirements: [LPB-06]
must_haves:
  truths:
    - "A deterministic tools/lp-ui-check.js gate parses the builder's emitted HTML + the known locked styles.css rules and enforces HARD-1, HARD-2, HARD-3, HARD-7 (and a static HARD-5 DOM-order check) by exit code (0 PASS / 1 FAIL)"
    - "The checker FAILS the reference build when a black-on-black .hl is injected into a dark surface (HARD-1 — the bug that recurred twice)"
    - "The checker PASSES the clean reference index.html + deposit.html"
    - "HARD-4 'hero video ≥50% of hero area' is the one render-dependent check — it is either implemented via Playwright at the known absolute path OR explicitly stubbed/flagged, not silently skipped"
  artifacts:
    - path: "tools/lp-ui-check.js"
      provides: "Deterministic HARD-1..7 gate; exit code is the gate"
      contains: "HARD-1"
  key_links:
    - from: "tools/lp-ui-check.js"
      to: "runs/<site>/index.html + deposit.html"
      via: "static HTML parse + known locked CSS rule table"
      pattern: "hl"
---

<objective>
Build `tools/lp-ui-check.js` — the DETERMINISTIC gate (LPB-06) that enforces the UI-SPEC HARD constraints
on the builder's output. Its exit code IS the gate: exit 1 on any HARD violation loops the build back to the
builder (the ONLY re-writer). Per the PMF agent-design law, this is a script (a deterministic check), not an
agent.

Purpose: HARD-1 (no black-on-black accent) recurred TWICE in the manual build — a machine check is the
durable fix. The checker makes the visual contract enforceable instead of advisory.
Output: `tools/lp-ui-check.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-INTERFACES.md
@.planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-UI-SPEC.md
@.planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-PATTERNS.md

<interfaces>
<!-- The HARD rules to enforce (UI-SPEC L134-179) + the locked CSS facts the checker needs. -->
HARD-1 (highest priority, recurred twice): any `.hl` inside a dark surface
  (.hero, .section--dark, .deposit-hero, or a container with bg --black #0a0a0b / --black-2 #131316)
  MUST resolve to cyan glow (var(--glow) #2be5ff), never inherit --ink. Checker: for every highlighted
  run, computed text color ≠ computed background color.
HARD-2 (CTA legibility): every primary .btn = accent bg (#2be5ff), #000 text, font-family var(--mono)
  IBM Plex Mono — NEVER Doto. Checker: each primary CTA has accent bg + black text + non-Doto font.
HARD-3 (17px body min): body { font-size: 17px }; no primary reading text < 17px except the listed
  fine-print classes (.cta__fine 13px, .nav__meta 12px, .paywall/.offer__secure 12px, .chip 13px).
HARD-5 (element order per breakpoint): static DOM-order check (LP desktop: headline+CTA then video;
  deposit desktop: copy block order tag→title→price-row→value→cta→secure).
HARD-7 (restraint): no rendered <figcaption> text; no visible string absent from PAGE-SPEC copy.
HARD-4 (RENDER- dependent, the one that may need Playwright): hero video ≥50% of hero area.

Locked CSS facts the static checker relies on (from styles.css — read names, not re-author):
  --black #0a0a0b · --black-2 #131316 · --glow #2be5ff · --ink (dark) · --mono IBM Plex Mono.
  Dark-surface containers: .hero, .section--dark, .deposit-hero, .founder(.section--dark-2), .footer.
  .frame__tag { display:none } (figcaptions CSS-hidden — HARD-7 already enforced by CSS).
Playwright (if HARD-4 render needed): require the ABSOLUTE path '/home/kyu3/node_modules/playwright'
  (it is NOT a project dep — RESEARCH Env Availability L401).

S5/S2/S4 idioms (asset-upload.js): same CLI scaffold + sanitizer + sidecar-log as lp-hydrate.js.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build tools/lp-ui-check.js — static HARD-1/2/3/5/7 checks + the failing-fixture proof</name>
  <read_first>
    - .planning/phases/17-.../17-INTERFACES.md (lp-ui-check stage block — Reads tokenized HTML + PAGE-SPEC copy set + known locked CSS rules; Writes exit code + _ui-check-log.txt; FAIL loops to builder only)
    - .planning/phases/17-.../17-UI-SPEC.md (HARD-1..7 verbatim L134-179; Component Inventory class names L183-207; Color values L108-128 — --black/--black-2/--glow; Tokenization L210-220)
    - .planning/phases/17-.../17-PATTERNS.md (the lp-ui-check assignment block L164-176; S5/S2/S4 idioms L39-92)
    - tools/asset-upload.js lines 29-110, 340-355 (S5 CLI + S2 sanitizer + S4 sidecar/exit scaffold to reuse)
    - tools/hooks/validate-asset-record.js (the repo's deterministic-reject precedent — the exit-code-is-the-gate model)
    - runs/arduview/site/index.html + deposit.html (the clean reference the checker must PASS) and runs/arduview/site/styles.css (read the .hl / .btn / dark-surface rules to ground the static checks — never re-author)
  </read_first>
  <files>tools/lp-ui-check.js</files>
  <action>
    Create `tools/lp-ui-check.js`. Zero third-party deps for the static checks (regex/string parse of the
    emitted HTML against the known locked CSS rule table embedded as constants). Reuse the S5 CLI + S2
    sanitizer + S4 sidecar/exit scaffold from asset-upload.js. CLI: `--site=<dir>` (REQUIRED),
    `--page-spec=<path>` (default `<site>/PAGE-SPEC.md`, optional for HARD-7 string-membership check),
    `--render` (opt-in flag to attempt the HARD-4 Playwright render check). `--help`/no-args exits 0.

    Embed the locked-CSS rule facts as constants (from `<interfaces>`): DARK_SURFACE_CLASSES =
    ['hero','section--dark','deposit-hero','founder','footer']; DARK_BG_HEX = ['#0a0a0b','#131316'];
    GLOW = '#2be5ff'; MONO_FAMILY = 'IBM Plex Mono'; DOTO = 'Doto'.

    Implement these STATIC checks (parse each of index.html, deposit.html); each violation pushes a
    `[FAIL HARD-N] <detail>` log line and sets a failure flag:

    - **HARD-1 (highest priority):** find every element with class containing `hl` (the highlight). Walk its
      ancestor chain (simple tag-nesting scan or class-on-section heuristic). If it sits inside any
      DARK_SURFACE_CLASS container, assert the `.hl` resolves to the cyan branch — i.e. the emitted markup
      must NOT carry an inline dark color, and (since the locked CSS encodes both branches) the checker
      asserts the highlight is placed where the dark-surface cascade applies (no inline `style="color:#0..."`
      forcing ink, no `.hl` variant class that collapses to ink on dark). Concretely: FAIL if a `.hl` in a
      dark container has an inline `style` setting a near-black color, OR carries a light-surface-only
      highlight class. PASS otherwise. (This is the no-black-on-black gate.)
    - **HARD-2:** for every primary CTA (`.btn`/`.btn--lg` that is a CTA, e.g. `.hero__cta`, `.offer__cta`,
      `data-checkout` links, `.cta__form .btn`): FAIL if it carries an inline font-family of Doto, or a class
      known to apply Doto. PASS when it relies on the locked `.btn` rule (accent bg, #000, var(--mono)). Since
      the locked CSS already sets `.btn` correctly, the check is: no CTA overrides font-family to Doto and no
      CTA strips the `.btn` class.
    - **HARD-3:** assert no inline `style="font-size:<n>px"` under 17 on a primary reading element; allow the
      listed fine-print classes. (body 17px is locked in CSS; the check guards against builder-inserted inline
      small sizes.)
    - **HARD-5 (static DOM-order):** assert the deposit `.offer__copy` children appear in the order
      tag → title → price-row → value → cta → secure (HARD-5 desktop). Assert the LP hero has headline + CTA
      before the video element in source order. FAIL on out-of-order.
    - **HARD-7:** FAIL if any rendered `<figcaption>` contains visible text (the locked CSS hides
      `.frame__tag`, but a builder could emit a non-hidden caption). If `--page-spec` is given, optionally
      warn (not fail) on a visible string not found in the PAGE-SPEC copy set (string-membership is fuzzy;
      keep it a warning to avoid false fails).

    Implement **HARD-4** behind `--render`: if `--render` is passed, `require('/home/kyu3/node_modules/playwright')`
    (absolute path — it is NOT a project dep), render the page, measure the hero video bounding box vs the
    hero area, FAIL if video < 50%. If Playwright is unavailable or `--render` is NOT passed, log
    `[SKIP HARD-4] render check not run (pass --render with Playwright at /home/kyu3/node_modules/playwright)`
    — do NOT silently pretend it passed, and do NOT fail the gate on the static path for a render-only rule.

    S4 sidecar `_ui-check-log.txt` + one-line summary (`lp-ui-check: <fails> failures, <n> checks, site=<SITE>`).
    Exit code: **exit 1 if any FAIL, else exit 0.** The exit code is the gate.
  </action>
  <verify>
    <automated>node tools/lp-ui-check.js --help >/dev/null 2>&1 && node tools/lp-ui-check.js --site=runs/arduview/site; CLEAN=$?; cp runs/arduview/site/index.html /tmp/idx.bak; node -e "let h=require('fs').readFileSync('runs/arduview/site/index.html','utf8'); h=h.replace(/class=\"hl\"/,'class=\"hl\" style=\"color:#0a0a0b\"'); require('fs').writeFileSync('runs/arduview/site/index.html',h)"; node tools/lp-ui-check.js --site=runs/arduview/site; DIRTY=$?; cp /tmp/idx.bak runs/arduview/site/index.html; echo "clean_exit=$CLEAN dirty_exit=$DIRTY"; test "$CLEAN" -eq 0 -a "$DIRTY" -eq 1 && echo "CHECKER GATE OK"</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/lp-ui-check.js --help` exits 0.
    - The checker exits 0 on the clean reference `runs/arduview/site` (CLEAN==0).
    - After injecting an inline `style="color:#0a0a0b"` (black) onto a `.hl` in a dark surface, the checker exits 1 (DIRTY==1) — the HARD-1 black-on-black gate fires. The verify restores the file.
    - The checker references HARD-1, HARD-2, HARD-3, HARD-5, HARD-7 in its log/code (grep each tag in tools/lp-ui-check.js).
    - HARD-4 is not silently skipped: code contains either a Playwright `require('/home/kyu3/node_modules/playwright')` path or an explicit `[SKIP HARD-4]` log (grep `HARD-4`).
    - Static checks add no third-party require (only `fs`/`path`, plus the optional absolute-path Playwright behind `--render`).
  </acceptance_criteria>
  <done>
    The deterministic gate exists, PASSES the clean reference, FAILS an injected black-on-black `.hl`, enforces
    HARD-1/2/3/5/7 statically, and handles HARD-4 explicitly (render-behind-flag or documented skip).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → read path | `--site`/`--page-spec` argv build file read paths. |
| emitted HTML → checker | The checker reads builder output (trusted in-repo artifact) as data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04-01 | Tampering (path traversal) | lp-ui-check.js `--site`/`--page-spec` argv | mitigate | Apply S2 `sanitizePathSegment()` to any path segment; validate `--site` is an existing dir before read. Verbatim from asset-upload.js L99-110. |
| T-17-04-02 | Denial of Service / false-pass | A render-only rule (HARD-4) silently skipped, shipping a non-compliant page | mitigate | The checker NEVER pretends HARD-4 passed when not rendered — it logs `[SKIP HARD-4]` so the gap is visible; the Plan-06 phase gate surfaces it. The static rules (HARD-1/2/3/5/7) always run and gate. |
</threat_model>

<verification>
- Checker passes clean reference (exit 0), fails injected black-on-black `.hl` (exit 1), enforces HARD-1/2/3/5/7 statically, handles HARD-4 explicitly, fs/path-only for static path.
</verification>

<success_criteria>
The HARD-constraint gate is deterministic and machine-enforced. The black-on-black bug that recurred twice is
now caught by a failing-fixture-verified check; the exit code gates the build before deploy.
</success_criteria>

<output>
After completion, create `.planning/phases/17-lp-builder-implement-arduview-landing-and-deposit-pages-from/17-04-SUMMARY.md`
</output>
