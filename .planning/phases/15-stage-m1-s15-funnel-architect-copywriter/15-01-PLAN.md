---
phase: 15-stage-m1-s15-funnel-architect-copywriter
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - prompts/funnel-deep-pass.md
  - runs/_fixture/funnels/sample.json
  - tools/hooks/validate-analyzer.js
autonomous: true
requirements:
  - FUNNEL-01
  - FUNNEL-02

must_haves:
  truths:
    - "belief_kind field is declared in the 6b schema with closed enum crowdfunding-specific | general-dr"
    - "All 4 fixture belief records carry a belief_kind value"
    - "validate-analyzer.js rejects a record with missing or off-enum belief_kind (exit 2)"
    - "validate-analyzer.js accepts the updated fixture without error (exit 0)"
  artifacts:
    - path: "prompts/funnel-deep-pass.md"
      provides: "belief_kind field + BELIEF_KIND_ENUM added to 6b schema block"
      contains: "BELIEF_KIND_ENUM"
    - path: "runs/_fixture/funnels/sample.json"
      provides: "All 4 belief records carry belief_kind"
      contains: "belief_kind"
    - path: "tools/hooks/validate-analyzer.js"
      provides: "Rule 2b enforcement of belief_kind closed enum"
      contains: "BELIEF_KIND_ENUM"
  key_links:
    - from: "tools/hooks/validate-analyzer.js"
      to: "runs/_fixture/funnels/sample.json"
      via: "node tools/hooks/validate-analyzer.js runs/_fixture/funnels/sample.json"
      pattern: "exit 0"
---

<objective>
Extend the Section Analyzer schema (D1) so `belief_kind` (crowdfunding-specific | general-DR) is a first-class per-belief field. The Funnel Architect reads this field; it must exist in the store before the Architect can classify beliefs for source routing. Three files change: the schema doc, the fixture, and the validator.

Purpose: Unblocks Wave 1 (Funnel Architect skill) by making the contract explicit upstream. The Architect's INPUT spec requires belief_kind per record; without it the skill would silently design without knowing which beliefs require crowdfunding-only fill.
Output: Updated schema, fixture, and validator — all consistent with each other.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-CONTEXT.md
@.planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-RESEARCH.md

<interfaces>
<!-- From 15-RESEARCH.md RQ4 — exact edit anatomy verified against on-disk files -->

Edit 1 — prompts/funnel-deep-pass.md §6b:
Add field after belief_confidence:
  "belief_kind": "'crowdfunding-specific' | 'general-dr' — crowdfunding-specific = beliefs that only appear in crowdfunding funnels (e.g. it-will-ship, trust-the-brand-or-founder as founder-credibility); general-dr = beliefs that appear in any DR funnel"
Add to closed enums block:
  BELIEF_KIND_ENUM: crowdfunding-specific | general-dr

Edit 2 — runs/_fixture/funnels/sample.json (4 belief records, per D1 in RESEARCH.md):
  - record at position 1 (mechanism-is-the-reason / mechanism-explanation): "belief_kind": "general-dr"
  - record at position 3 (trust-the-brand-or-founder / founder-credibility):  "belief_kind": "crowdfunding-specific"
  - record at position 4 (it-will-ship / social-proof):                        "belief_kind": "crowdfunding-specific"
  - record at position 5 (act-now / scarcity):                                 "belief_kind": "general-dr"
NOTE: update sample.json FIRST, validator rule SECOND (pitfall 2 in RESEARCH.md).

Edit 3 — tools/hooks/validate-analyzer.js:
  Add constant:   const BELIEF_KIND_ENUM = new Set(['crowdfunding-specific', 'general-dr']);
  Add Rule 2b in the belief_records loop (after the existing Rule 2, before Rule 3):
    if (rec.belief_kind === undefined || rec.belief_kind === null) {
      violations.push(`REJECT: ${rLabel} missing "belief_kind" — must be 'crowdfunding-specific' | 'general-dr'`);
    } else if (!BELIEF_KIND_ENUM.has(rec.belief_kind)) {
      violations.push(`REJECT: ${rLabel} belief_kind="${rec.belief_kind}" is off-enum — must be 'crowdfunding-specific' | 'general-dr'`);
    }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add belief_kind to fixture (sample.json) and schema doc (funnel-deep-pass.md)</name>
  <files>runs/_fixture/funnels/sample.json, prompts/funnel-deep-pass.md</files>
  <read_first>
    - runs/_fixture/funnels/sample.json — full file; locate the 4 belief records and their current fields
    - prompts/funnel-deep-pass.md — locate §6b belief-instance schema block and the closed enums block
    - .planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-RESEARCH.md §RQ4 — exact field values and positions
  </read_first>
  <action>
    Step 1 (fixture first — per pitfall 2): Open runs/_fixture/funnels/sample.json. Locate the belief_records array (4 records for funnel gameshell-kickstarter). Add "belief_kind" to each record using the exact values from D1/RESEARCH.md RQ4:
      - mechanism-is-the-reason record: "belief_kind": "general-dr"
      - trust-the-brand-or-founder record: "belief_kind": "crowdfunding-specific"
      - it-will-ship record: "belief_kind": "crowdfunding-specific"
      - act-now record: "belief_kind": "general-dr"
    Insert belief_kind immediately after the belief_confidence field in each record (JSON field order matches the schema declaration order for readability).

    Step 2 (schema doc): Open prompts/funnel-deep-pass.md. Find the §6b belief-instance schema JSON block. Add after the belief_confidence field:
      "belief_kind": "'crowdfunding-specific' | 'general-dr' — which category of belief this is; crowdfunding-specific = beliefs that only appear in crowdfunding funnels (e.g. it-will-ship, trust-the-brand-or-founder as founder-credibility); general-dr = beliefs that appear in any DR funnel"
    Then find the closed enums block (BELIEF_ANCHOR_SET, MOVE_ENUM, etc.) and add:
      BELIEF_KIND_ENUM:       crowdfunding-specific | general-dr
    Add it immediately after BELIEF_ANCHOR_SET for logical grouping.

    Do NOT modify validate-analyzer.js in this task (that is Task 2, after the fixture is confirmed valid under the current rules).
  </action>
  <verify>
    node -e "const d=JSON.parse(require('fs').readFileSync('runs/_fixture/funnels/sample.json','utf8')); d.belief_records.forEach((r,i)=>console.log(i, r.belief_id, r.belief_kind))"
    Expected output: all 4 records print a belief_kind value (general-dr or crowdfunding-specific, matching the D1 spec).

    grep "BELIEF_KIND_ENUM" prompts/funnel-deep-pass.md
    Expected: line containing "BELIEF_KIND_ENUM: crowdfunding-specific | general-dr"

    grep "belief_kind" prompts/funnel-deep-pass.md
    Expected: line containing the belief_kind field definition in the §6b schema block.
  </verify>
  <acceptance_criteria>
    - runs/_fixture/funnels/sample.json: all 4 belief records contain "belief_kind" with the correct per-D1 value; JSON parses without error
    - prompts/funnel-deep-pass.md: contains "BELIEF_KIND_ENUM" and "belief_kind" in the §6b section
    - No other fields were modified in either file
  </acceptance_criteria>
  <done>fixture has belief_kind on all 4 records with correct values; schema doc declares BELIEF_KIND_ENUM</done>
</task>

<task type="auto">
  <name>Task 2: Add Rule 2b to validate-analyzer.js; verify fixture passes, bad input rejects</name>
  <files>tools/hooks/validate-analyzer.js</files>
  <read_first>
    - tools/hooks/validate-analyzer.js — full file; locate the belief_records loop, identify where Rule 2 ends and Rule 3 begins; identify where to add the BELIEF_KIND_ENUM constant (near the top with other enum constants like BELIEF_ANCHOR_SET, MOVE_ENUM)
    - runs/_fixture/funnels/sample.json — confirm belief_kind is present (Task 1 must be done first)
    - .planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-RESEARCH.md §RQ4 — exact Rule 2b code
  </read_first>
  <action>
    1. Open tools/hooks/validate-analyzer.js. Find where BELIEF_ANCHOR_SET and MOVE_ENUM constants are declared (near the top of the file). Add after them:
       const BELIEF_KIND_ENUM = new Set(['crowdfunding-specific', 'general-dr']);

    2. Find the belief_records loop. Locate Rule 2 (belief_confidence validation) and the start of Rule 3 (execution_type or proof_tier validation). Insert Rule 2b between them:
       // --- Rule 2b: belief_kind closed vocab (D1 per Phase 15) ---
       if (rec.belief_kind === undefined || rec.belief_kind === null) {
         violations.push(`REJECT: ${rLabel} missing "belief_kind" — must be 'crowdfunding-specific' | 'general-dr'`);
       } else if (!BELIEF_KIND_ENUM.has(rec.belief_kind)) {
         violations.push(`REJECT: ${rLabel} belief_kind="${rec.belief_kind}" is off-enum — must be 'crowdfunding-specific' | 'general-dr'`);
       }
    (Use whatever rLabel variable the loop already uses for the record identifier in other rules.)

    3. Do NOT change any other validation rule, exit code behavior, or error prefix pattern.
  </action>
  <verify>
    # Fixture must pass (exit 0):
    node tools/hooks/validate-analyzer.js runs/_fixture/funnels/sample.json
    echo "exit: $?"
    Expected: exit 0, no output to stderr.

    # Missing belief_kind must be rejected (exit 2):
    node -e "
      const data = JSON.parse(require('fs').readFileSync('runs/_fixture/funnels/sample.json','utf8'));
      delete data.belief_records[0].belief_kind;
      require('fs').writeFileSync('/tmp/test-no-belief-kind.json', JSON.stringify(data));
    "
    node tools/hooks/validate-analyzer.js /tmp/test-no-belief-kind.json
    echo "exit: $?"
    Expected: exit 2, stderr contains 'missing "belief_kind"'.

    # Off-enum belief_kind must be rejected (exit 2):
    node -e "
      const data = JSON.parse(require('fs').readFileSync('runs/_fixture/funnels/sample.json','utf8'));
      data.belief_records[0].belief_kind = 'invalid-value';
      require('fs').writeFileSync('/tmp/test-bad-belief-kind.json', JSON.stringify(data));
    "
    node tools/hooks/validate-analyzer.js /tmp/test-bad-belief-kind.json
    echo "exit: $?"
    Expected: exit 2, stderr contains 'off-enum'.
  </verify>
  <acceptance_criteria>
    - node tools/hooks/validate-analyzer.js runs/_fixture/funnels/sample.json exits 0 (fixture passes clean)
    - node tools/hooks/validate-analyzer.js /tmp/test-no-belief-kind.json exits 2 with "missing" in stderr
    - node tools/hooks/validate-analyzer.js /tmp/test-bad-belief-kind.json exits 2 with "off-enum" in stderr
    - grep "BELIEF_KIND_ENUM" tools/hooks/validate-analyzer.js returns a line with the Set declaration
    - No other validator rules modified
  </acceptance_criteria>
  <done>validate-analyzer.js enforces belief_kind as a closed enum; fixture passes; invalid records are rejected</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema doc → validator | Enum values declared in schema must match the Set in the validator exactly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-1 | Tampering | validate-analyzer.js Rule 2b | mitigate | BELIEF_KIND_ENUM hardcoded as a const Set; not read from any file or argument |
| T-15-01-2 | Tampering | sample.json belief_kind values | accept | Fixture is synthetic test data; no secrets, no PII; wrong values fail the validator on next run |
</threat_model>

<verification>
Full round-trip: node tools/hooks/validate-analyzer.js runs/_fixture/funnels/sample.json exits 0.
Schema and validator are consistent: grep confirms BELIEF_KIND_ENUM in both prompts/funnel-deep-pass.md and tools/hooks/validate-analyzer.js.
</verification>

<success_criteria>
- belief_kind is a declared field in the §6b schema in funnel-deep-pass.md with BELIEF_KIND_ENUM
- All 4 fixture belief records carry belief_kind with the correct D1 values
- validate-analyzer.js Rule 2b rejects missing or off-enum belief_kind (exit 2) and accepts the updated fixture (exit 0)
</success_criteria>

<output>
After completion, create .planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-01-SUMMARY.md
</output>
