---
phase: 15-stage-m1-s15-funnel-architect-copywriter
plan: "05"
type: execute
wave: 3
depends_on:
  - "15-01"
  - "15-02"
  - "15-03"
  - "15-04"
files_modified: []
autonomous: false
requirements:
  - FUNNEL-01
  - FUNNEL-02

must_haves:
  truths:
    - "Architect DR bundle exists, non-empty, and contains exactly 5 DR files confirmed by grep"
    - "Copywriter DR bundle exists, non-empty, and contains exactly 3 craft files; Hormozi absent"
    - "Source-type filter works: --source-type=crowdfunding returns records from fixture; --source-type=dtc returns zero"
    - "Claim tally runs against fixture and produces _tally.json with low_n_warning: true and 14 whitespace entries"
    - "belief_kind round-trips: fixture passes validate-analyzer.js (exit 0); record missing belief_kind fails (exit 2)"
    - "read_first sections in both SKILL.md files declare the bundle path and regenerate command"
    - "Full chain test passes: tally → index query with source filter → both bundles non-empty → validator exit 0"
    - "Operator has verified SKILL.md read_first wiring is declared correctly for both skills"
  artifacts:
    - path: ".claude/skills/funnel-architect/_dr-context.generated.md"
      provides: "Generated architect DR bundle — confirmed 5 files present"
      contains: "=== DR FILE:"
    - path: ".claude/skills/copywriter/_dr-context.generated.md"
      provides: "Generated copywriter DR bundle — confirmed 3 files present"
      contains: "=== DR FILE:"
    - path: "runs/_fixture/funnels/_tally.json"
      provides: "Claim tally for fixture space — confirmed low_n_warning: true"
      contains: "low_n_warning"
    - path: "runs/_fixture/funnels/_index.json"
      provides: "Fixture index with source_type fields on every record"
      contains: "source_type"
  key_links:
    - from: ".claude/skills/funnel-architect/SKILL.md"
      to: ".claude/skills/funnel-architect/_dr-context.generated.md"
      via: "read_first instructs the Architect agent to Read the bundle as first action"
      pattern: "_dr-context.generated.md"
    - from: ".claude/skills/copywriter/SKILL.md"
      to: ".claude/skills/copywriter/_dr-context.generated.md"
      via: "read_first instructs the Copywriter agent to Read the bundle as first action"
      pattern: "_dr-context.generated.md"
    - from: "tools/funnel-rag-query.js"
      to: "runs/_fixture/funnels/_index.json"
      via: "--source-type prefilter on the rebuilt fixture index"
      pattern: "source_type=crowdfunding"
---

<objective>
End-to-end operator verification of all injection points and the full chain against the fixture. This plan contains no new file creation — it runs the full suite of verification commands from 15-RESEARCH.md RQ7 and presents results for operator confirmation. Any failures found here are fixed before the phase is marked complete.

Purpose: The OPERATOR VERIFICATION MANDATE in both specs exists because Claude has historically built skills without verifying injection wiring. This wave is the mandated proof. "Build passes" is not sufficient — each injection point must be proven to carry the correct bytes at run time.
Output: A Wave 3 verification report (in the SUMMARY) documenting pass/fail for each of the 5 verification checks, plus operator sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-CONTEXT.md
@.planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-RESEARCH.md
@.planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-SPEC-funnel-architect.md
@.planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-SPEC-copywriter.md

<prior_summaries>
@.planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-01-SUMMARY.md
@.planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-02-SUMMARY.md
@.planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-03-SUMMARY.md
@.planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-04-SUMMARY.md
</prior_summaries>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all 5 verification checks from the OPERATOR VERIFICATION MANDATE</name>
  <files></files>
  <read_first>
    - .planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-RESEARCH.md §RQ7 — the authoritative list of verification commands (checks A through E); run every command verbatim
    - .claude/skills/funnel-architect/SKILL.md — confirm read_first section content
    - .claude/skills/copywriter/SKILL.md — confirm read_first section content
    - .planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-01-SUMMARY.md through 15-04-SUMMARY.md — check if any prior task flagged a gap that needs resolution before running verification
  </read_first>
  <action>
    Run each verification check from 15-RESEARCH.md RQ7. For each check, record: command, actual output, and PASS/FAIL verdict.

    CHECK A — Architect DR bundle lands in context:
      node tools/hooks/inject-funnel-architect-dr.js
      stat .claude/skills/funnel-architect/_dr-context.generated.md
      grep -c "=== DR FILE:" .claude/skills/funnel-architect/_dr-context.generated.md
      grep "=== DR FILE:" .claude/skills/funnel-architect/_dr-context.generated.md
      PASS if: stat shows a non-empty file; grep -c returns 5; all 5 expected filenames present.

    CHECK B — Copywriter DR bundle lands in context:
      node tools/hooks/inject-copywriter-dr.js
      stat .claude/skills/copywriter/_dr-context.generated.md
      grep -c "=== DR FILE:" .claude/skills/copywriter/_dr-context.generated.md
      grep "=== DR FILE:" .claude/skills/copywriter/_dr-context.generated.md
      grep -i "hormozi" .claude/skills/copywriter/_dr-context.generated.md
      PASS if: stat shows a non-empty file; grep -c returns 3; all 3 craft filenames present; hormozi grep returns 0 matches.

    CHECK C — Source-routed RAG filters correctly:
      node tools/funnel-vectorize.js --space=_fixture
      node -e "const i=require('./runs/_fixture/funnels/_index.json'); console.log(i.records[0].source_type, i.records[0].routing_flag)"
      node tools/funnel-rag-query.js --space=_fixture --query="ship trust" --source-type=crowdfunding --top=10 2>&1 | head -10
      node tools/funnel-rag-query.js --space=_fixture --query="ship trust" --source-type=dtc --top=10 2>&1 | head -5
      PASS if: first node -e prints "crowdfunding both"; crowdfunding query returns records (no "eliminated all"); dtc query returns "[warn] prefilter eliminated all records".

    CHECK D — Claim-tally output is injected:
      node tools/funnel-claim-tally.js --space=_fixture --json 2>&1
      node tools/funnel-claim-tally.js --space=_fixture
      cat runs/_fixture/funnels/_tally.json | node -e "const d=require('fs').readFileSync('/dev/stdin','utf8'); const j=JSON.parse(d); console.log('funnels:', j._meta.total_funnels, '| low_n:', j._meta.low_n_warning, '| whitespace:', j.whitespace.length)"
      PASS if: _tally.json written; low_n: true; whitespace: 14; exit 0.

    CHECK E — belief_kind round-trips:
      node tools/hooks/validate-analyzer.js runs/_fixture/funnels/sample.json; echo "exit: $?"
      node -e "const data=JSON.parse(require('fs').readFileSync('runs/_fixture/funnels/sample.json','utf8')); delete data.belief_records[0].belief_kind; require('fs').writeFileSync('/tmp/test-no-belief-kind.json', JSON.stringify(data));"
      node tools/hooks/validate-analyzer.js /tmp/test-no-belief-kind.json; echo "exit: $?"
      PASS if: first validator exits 0; second validator exits 2 with "missing" in stderr.

    CHECK F — read_first wiring (script-side, not runtime):
      grep "read_first" .claude/skills/funnel-architect/SKILL.md
      grep "_dr-context.generated.md" .claude/skills/funnel-architect/SKILL.md
      grep "inject-funnel-architect-dr.js" .claude/skills/funnel-architect/SKILL.md
      grep "read_first" .claude/skills/copywriter/SKILL.md
      grep "_dr-context.generated.md" .claude/skills/copywriter/SKILL.md
      grep "inject-copywriter-dr.js" .claude/skills/copywriter/SKILL.md
      grep "funnel-rag-query.js" .claude/skills/copywriter/SKILL.md
      PASS if: all greps return matches.

    If ANY check fails: diagnose and fix immediately (do not defer). Document the fix in the verification report. Re-run the failed check after fixing.

    At the end, compile a verification report table (embed in the SUMMARY):
    | Check | Cmd | Expected | Actual | PASS/FAIL |
    |-------|-----|----------|--------|-----------|
    | A — Architect bundle | grep -c "=== DR FILE:" architect-bundle | 5 | <actual> | <verdict> |
    | B — Copywriter bundle | grep -c "=== DR FILE:" copywriter-bundle | 3 | <actual> | <verdict> |
    | B — Hormozi absent | grep -i hormozi copywriter-bundle | 0 | <actual> | <verdict> |
    | C — Source routing | --source-type=crowdfunding returns records | records returned | <actual> | <verdict> |
    | C — DTC filter | --source-type=dtc returns 0 | "eliminated all" | <actual> | <verdict> |
    | D — Claim tally | whitespace.length | 14 | <actual> | <verdict> |
    | D — low_n_warning | _meta.low_n_warning | true | <actual> | <verdict> |
    | E — Validator pass | exit code on fixture | 0 | <actual> | <verdict> |
    | E — Validator reject | exit code on no-belief-kind | 2 | <actual> | <verdict> |
    | F — Architect read_first | grep in SKILL.md | match | <actual> | <verdict> |
    | F — Copywriter read_first | grep in SKILL.md | match | <actual> | <verdict> |
    | F — RAG wiring in copywriter | grep funnel-rag-query.js | match | <actual> | <verdict> |
  </action>
  <verify>
    All 12 checks in the verification report table show PASS.
    No check was bypassed or deferred without operator approval.
  </verify>
  <acceptance_criteria>
    - All 12 verification checks pass (PASS in each row of the report table)
    - Any check that initially failed was fixed and re-verified (fix documented in the report)
    - Verification report is committed as part of the SUMMARY
  </acceptance_criteria>
  <done>all automated verification checks pass; verification report table complete</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All 5 plans of Phase 15 are complete:
    - Wave 0 (Plans 01-02): belief_kind added to schema + validator; RAG source routing extended; fixture rebuilt; claim-tally script built.
    - Wave 1 (Plan 03): Funnel Architect skill with DR bundle (5 files) and SKILL.md with read_first.
    - Wave 2 (Plan 04): Copywriter skill with DR bundle (3 craft files, Hormozi absent) and SKILL.md with read_first.
    - Wave 3 (Plan 05): All 12 automated verification checks passed (see report in Task 1 output).

    The operator verification mandate from both specs requires you to confirm the read_first wiring is correctly declared before the phase is marked complete.
  </what-built>
  <how-to-verify>
    1. Open .claude/skills/funnel-architect/SKILL.md. Confirm:
       - The "## read_first" section appears before the procedure sections.
       - It declares .claude/skills/funnel-architect/_dr-context.generated.md as mandatory.
       - It includes the warning "Do NOT proceed without it."
       - It includes the regenerate command: node tools/hooks/inject-funnel-architect-dr.js
       - The "SUPPORTING KNOWLEDGE" section does NOT say "auto-injected" (replaced with read_first reference).

    2. Open .claude/skills/copywriter/SKILL.md. Confirm:
       - The "## read_first" section appears before the procedure sections.
       - It declares .claude/skills/copywriter/_dr-context.generated.md as mandatory.
       - It includes the warning "Do NOT proceed without it."
       - It includes the regenerate command: node tools/hooks/inject-copywriter-dr.js
       - It includes the per-section RAG orchestration contract (funnel-rag-query.js --source-type=<gate>).
       - The "SUPPORTING KNOWLEDGE" section does NOT say "auto-injected."

    3. Run the full chain check from the terminal:
       cd /home/kyu3/PMF
       node tools/funnel-claim-tally.js --space=_fixture && \
       node tools/hooks/validate-analyzer.js runs/_fixture/funnels/sample.json && \
       node tools/funnel-rag-query.js --space=_fixture --query="open hardware" --belief=mechanism-is-the-reason --source-type=crowdfunding --top=3 2>&1 | head -15 && \
       wc -c .claude/skills/funnel-architect/_dr-context.generated.md .claude/skills/copywriter/_dr-context.generated.md
       Expected: tally writes _tally.json; validator exits 0; RAG returns results with crowdfunding filter active; both bundle files are non-empty.
  </how-to-verify>
  <resume-signal>Type "approved" if all three checks pass, or describe any issue found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification scripts → filesystem | All verification reads are from the project tree; no external network calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05-1 | Tampering | /tmp test files for validator check | accept | Temp files are written and read by the same verification script; no external input; deleted after verification |
</threat_model>

<verification>
All 12 automated checks in the verification report table show PASS.
Operator human-verify checkpoint approved.
</verification>

<success_criteria>
- All 5 verification checks (A-E) from 15-RESEARCH.md RQ7 pass
- Architect bundle: 5 DR files confirmed by grep
- Copywriter bundle: 3 craft files confirmed; Hormozi absent
- Source-routing: crowdfunding filter returns records; dtc filter returns zero
- Claim tally: _tally.json with low_n_warning: true and 14 whitespace entries
- belief_kind: fixture passes validator; record missing belief_kind fails validator
- Both SKILL.md files confirmed by operator to have correct read_first wiring
- Phase 15 may be marked COMPLETE in ROADMAP.md and STATE.md after operator approval
</success_criteria>

<output>
After completion, create .planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-05-SUMMARY.md

The SUMMARY must include:
- The verification report table (all 12 checks with PASS/FAIL and actual output)
- Any fixes applied during verification
- Operator checkpoint outcome ("approved" text from the human-verify)
- Phase 15 completion status
</output>
