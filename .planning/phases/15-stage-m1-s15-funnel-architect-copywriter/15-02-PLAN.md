---
phase: 15-stage-m1-s15-funnel-architect-copywriter
plan: "02"
type: execute
wave: 0
depends_on:
  - "15-01"
files_modified:
  - tools/funnel-vectorize.js
  - tools/funnel-rag-query.js
  - runs/_fixture/funnels/_index.json
  - tools/funnel-claim-tally.js
autonomous: true
requirements:
  - FUNNEL-01
  - FUNNEL-02

must_haves:
  truths:
    - "funnel-vectorize.js carries source_type and routing_flag into each indexed unit"
    - "funnel-rag-query.js --source-type prefilter reduces the result set by source_type"
    - "Rebuilt fixture _index.json records contain source_type and routing_flag fields"
    - "funnel-claim-tally.js runs against _fixture space and produces correct _tally.json"
    - "claim-tally low_n_warning is true for a 1-funnel store"
  artifacts:
    - path: "tools/funnel-vectorize.js"
      provides: "source_type + routing_flag carried into unit push"
      contains: "source_type"
    - path: "tools/funnel-rag-query.js"
      provides: "--source-type prefilter branch"
      contains: "source-type"
    - path: "tools/funnel-claim-tally.js"
      provides: "moves[]-based claim tally script per 15-CLAIM-TALLY-IMPL-SPEC.md"
      contains: "low_n_warning"
    - path: "runs/_fixture/funnels/_index.json"
      provides: "Rebuilt fixture index with source_type and routing_flag on each record"
      contains: "source_type"
  key_links:
    - from: "tools/funnel-rag-query.js"
      to: "runs/_fixture/funnels/_index.json"
      via: "node tools/funnel-rag-query.js --space=_fixture --query='ship trust' --source-type=crowdfunding"
      pattern: "source_type=crowdfunding"
    - from: "tools/funnel-claim-tally.js"
      to: "runs/_fixture/funnels/_tally.json"
      via: "node tools/funnel-claim-tally.js --space=_fixture"
      pattern: "low_n_warning"
---

<objective>
Two parallel substrate tasks: (A) extend the RAG pipeline to carry and filter by source_type (D2/D3), and (B) build the claim-tally script (D4). Both are deterministic scripts that unblock Wave 1 (Architect) and Wave 2 (Copywriter). Wave 1 needs the tally in its context; Wave 2 needs source-routed retrieval.

Purpose: The Architect's copy brief specifies a source gate per section; without the --source-type filter the Copywriter cannot scope retrieval. The claim tally is an Architect input for Step 6 (dead-ground/whitespace analysis).
Output: Extended vectorize + query scripts, a rebuilt fixture _index.json, and tools/funnel-claim-tally.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-CONTEXT.md
@.planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-RESEARCH.md
@.planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-CLAIM-TALLY-IMPL-SPEC.md

<interfaces>
<!-- From 15-RESEARCH.md RQ3 — exact edit sites verified against on-disk code -->

funnel-vectorize.js loadUnits() — add two lines to the units.push({}) block:
  source_type:   funnel.source_type  ?? null,   // 'dtc' | 'crowdfunding'
  routing_flag:  funnel.routing_flag ?? null,   // 'structure_only' | 'messaging_full' | 'both'
(The push block is at lines 107-128 of the current file; add after validation_strength.)

funnel-rag-query.js prefilter block (currently lines 79-81) — add after the existing two prefilter lines:
  if (opts['source-type'])  records = records.filter(r => norm(r.source_type)  === norm(opts['source-type']));
  if (opts['routing-flag']) records = records.filter(r => norm(r.routing_flag) === norm(opts['routing-flag']));
Also update the --help text and the "prefilter:" emit line in the output block.

claim-tally primary key: individual moves[] tag.
claim-tally output path: runs/<space>/funnels/_tally.json.
Pattern reference: tools/aggregate-mechanisms-in-play.js (same arg parsing, fail(), distinct(), stderr prefix [claim-tally]).
Fixture _index.json rebuild: node tools/funnel-vectorize.js --space=_fixture
  - If VOYAGE_API_KEY is set in the environment: rebuild with Voyage (preferred, keeps dim=1024).
  - If not set: the stub backend runs (dim=512). Document which backend was used in a comment in the rebuilt _index.json's _meta block is fine — the script adds _meta automatically.
  - Do not try to force Voyage if the key is absent; the stub is valid for prefilter verification.

Fixture source_type/routing_flag values (from sample.json funnel-level fields):
  source_type: "crowdfunding", routing_flag: "both"
  After rebuild, all 4 belief-record units in _index.json should carry source_type: "crowdfunding".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend funnel-vectorize.js and funnel-rag-query.js; rebuild fixture index</name>
  <files>tools/funnel-vectorize.js, tools/funnel-rag-query.js, runs/_fixture/funnels/_index.json</files>
  <read_first>
    - tools/funnel-vectorize.js — full file; locate loadUnits() and the units.push({}) block; identify where validation_strength is and where source_type should follow
    - tools/funnel-rag-query.js — full file; locate the structured prefilter block (lines ~79-81), the --help text, and the "prefilter:" output emit line
    - runs/_fixture/funnels/sample.json — confirm source_type and routing_flag are present at funnel level (they are: "crowdfunding" / "both")
    - runs/_fixture/funnels/_index.json — read current structure to understand what fields each unit carries and confirm source_type is absent
  </read_first>
  <action>
    Edit 1 — funnel-vectorize.js:
    Open tools/funnel-vectorize.js. Find the loadUnits() function and the units.push({...}) block. After the line that assigns validation_strength (or whichever is last before the per-belief-record fields begin), add:
      source_type:         funnel.source_type  ?? null,
      routing_flag:        funnel.routing_flag ?? null,
    Preserve all other fields exactly. Do not reorder or remove any existing field.

    Edit 2 — funnel-rag-query.js:
    Open tools/funnel-rag-query.js. Find the structured prefilter block containing the existing lines:
      if (opts.belief)        records = records.filter(r => norm(r.belief_id)  === norm(opts.belief));
      if (opts['proof-tier']) records = records.filter(r => norm(r.proof_tier) === norm(opts['proof-tier']));
    Add immediately after:
      if (opts['source-type'])  records = records.filter(r => norm(r.source_type)  === norm(opts['source-type']));
      if (opts['routing-flag']) records = records.filter(r => norm(r.routing_flag) === norm(opts['routing-flag']));
    Find the --help usage text block and add --source-type and --routing-flag to the flags list (same style as --belief and --proof-tier entries).
    Find the "prefilter:" emit line in the output block (the one that prints which filters were active) and add source-type and routing-flag to it.

    Rebuild the fixture index:
    Run: echo "VOYAGE_API_KEY=$VOYAGE_API_KEY" (check if key is set)
    Then: node tools/funnel-vectorize.js --space=_fixture
    This regenerates runs/_fixture/funnels/_index.json. The file will now carry source_type and routing_flag on each unit.
  </action>
  <verify>
    # Confirm source_type is in the rebuilt index:
    node -e "const i=require('./runs/_fixture/funnels/_index.json'); console.log('source_type:', i.records[0].source_type, '| routing_flag:', i.records[0].routing_flag)"
    Expected: source_type: crowdfunding | routing_flag: both

    # Filter test — all 4 fixture records are crowdfunding → expect 4 returned:
    node tools/funnel-rag-query.js --space=_fixture --query="ship trust" --source-type=crowdfunding --top=10 2>&1 | head -8
    Expected: output includes "source_type=crowdfunding" in prefilter line and returns result blocks (or "[warn] prefilter eliminated all records" must NOT appear).

    # Filter test — dtc source → 0 results since fixture is all crowdfunding:
    node tools/funnel-rag-query.js --space=_fixture --query="ship trust" --source-type=dtc --top=10 2>&1 | head -5
    Expected: "[warn] prefilter eliminated all records" appears (correct behavior — no dtc records in fixture).

    grep "source-type" tools/funnel-rag-query.js | head -5
    Expected: at least two lines — one in the prefilter block, one in the --help text.
  </verify>
  <acceptance_criteria>
    - node -e ... i.records[0].source_type prints "crowdfunding" (not null or undefined)
    - funnel-rag-query.js --source-type=crowdfunding returns records (no "eliminated all" warning)
    - funnel-rag-query.js --source-type=dtc returns "eliminated all" warning (filter works)
    - grep "source-type" tools/funnel-rag-query.js shows lines in both prefilter and help text
    - No existing prefilter behavior broken: --belief and --proof-tier still work unchanged
  </acceptance_criteria>
  <done>source_type and routing_flag carried through vectorize into the index; --source-type filter works in the query; fixture index rebuilt</done>
</task>

<task type="auto">
  <name>Task 2: Build tools/funnel-claim-tally.js per 15-CLAIM-TALLY-IMPL-SPEC.md</name>
  <files>tools/funnel-claim-tally.js</files>
  <read_first>
    - .planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-CLAIM-TALLY-IMPL-SPEC.md — full file; this is the implementation authority; implement every section
    - tools/aggregate-mechanisms-in-play.js — full file; mirror the arg parsing, fail(), distinct(), stderr log prefix, exit codes, and sort patterns exactly
    - tools/funnel-rag-query.js — locate sanitizePathSegment function and copy it verbatim (T-03-13 parity for --space sanitization)
    - runs/_fixture/funnels/sample.json — confirm belief_records structure and moves[] field location
  </read_first>
  <action>
    Create tools/funnel-claim-tally.js implementing the full specification from 15-CLAIM-TALLY-IMPL-SPEC.md §3:

    Header comment: mirror aggregate-mechanisms-in-play.js style. State the purpose, input/output contract, and that it is a deterministic script (not an agent).

    CLI arg parsing: exact opts one-liner from the spec (same as aggregate-mechanisms-in-play.js).

    Path sanitization: copy sanitizePathSegment from funnel-rag-query.js verbatim (same [a-z0-9._-] regex). Apply to --space.

    fail() helper: same pattern as aggregate-mechanisms-in-play.js — console.error then process.exit(2).

    distinct() helper: same [...new Set(arr)] pattern.

    Core algorithm — implement all 9 steps from the spec §3c exactly:
      1. RESOLVE PATHS (storeDir, outPath, threshold; validate threshold >= 1)
      2. ENUMERATE FUNNEL FILES (filter *.json, exclude _ prefix)
      3. PARSE FUNNEL RECORDS (require funnel_id + belief_records; call fail() on parse error)
      4. BUILD THE MOVE→FUNNELS INDEX (Map; count DISTINCT funnels per tag; skip empty/non-string tags)
      5. BUILD BELIEF-ID ANNOTATION MAP (parallel Map: move_tag → Set of belief_ids)
      6. CLASSIFY (dead_ground vs whitespace by threshold)
      7. SORT EACH LIST (dead_ground: desc by funnel_count then move asc; whitespace: asc by funnel_count then move asc)
      8. COMPUTE METADATA (_meta object with all fields from spec §3c step 8)
      9. WRITE OUTPUT (write to --out/default path if specified; emit to stdout if --json; log summary to stderr with [claim-tally] prefix; log low_n_warning if applicable)

    Edge cases — implement all from spec §3e:
      - Empty store: write empty tally, exit 0 (valid state, not an error)
      - Duplicate funnel_id across files: call fail()
      - --threshold=0 or negative: call fail()
      - Non-integer threshold: parseInt || 5 (fallback to 5)
      - Missing belief_records: treat as [], log warn to stderr with funnel_id
      - Missing moves: treat as []

    Output JSON shape: match spec §3d exactly (including _meta field names and order).

    Stderr log format: "[claim-tally] N funnels · M move keys · D dead ground / W whitespace"
    Low-N stderr: "[claim-tally] WARN: store has N funnels < threshold T — dead-ground classification is unreliable; see _meta.low_n_warning."

    Do NOT call any external service, embed text, or read any file outside the funnel store.
  </action>
  <verify>
    # Test A — default threshold, single-funnel fixture:
    node tools/funnel-claim-tally.js --space=_fixture --json 2>&1
    Expected (stderr): "[claim-tally] WARN: store has 1 funnels < threshold 5"
    Expected (parsed stdout JSON): _meta.total_funnels === 1, _meta.low_n_warning === true, dead_ground.length === 0, whitespace.length === 14
    node tools/funnel-claim-tally.js --space=_fixture --json 2>/dev/null | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); console.log('funnels:', d._meta.total_funnels, '| low_n:', d._meta.low_n_warning, '| whitespace:', d.whitespace.length, '| dead:', d.dead_ground.length)"
    Expected: funnels: 1 | low_n: true | whitespace: 14 | dead: 0

    # Test B — threshold=1 forces all moves to dead_ground:
    node tools/funnel-claim-tally.js --space=_fixture --threshold=1 --json 2>/dev/null | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); console.log('dead:', d.dead_ground.length, '| white:', d.whitespace.length, '| low_n:', d._meta.low_n_warning)"
    Expected: dead: 14 | white: 0 | low_n: false

    # Test C — write to file:
    node tools/funnel-claim-tally.js --space=_fixture
    node -e "require('fs').accessSync('runs/_fixture/funnels/_tally.json'); console.log('file exists')"
    Expected: "file exists"

    # Test D — missing space:
    node tools/funnel-claim-tally.js --space=nonexistent; echo "exit: $?"
    Expected: exit 2, stderr contains "does not exist"

    # Test E — empty store:
    mkdir -p /tmp/tally-test/funnels && node tools/funnel-claim-tally.js --space=tally-test --out=/tmp/tally-test/funnels/_tally.json --json 2>/dev/null | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); console.log('funnels:', d._meta.total_funnels, '| low_n:', d._meta.low_n_warning)"
    Expected: funnels: 0 | low_n: true
  </verify>
  <acceptance_criteria>
    - Test A: whitespace.length === 14, dead_ground.length === 0, low_n_warning === true, exit 0
    - Test B: dead_ground.length === 14, whitespace.length === 0, low_n_warning === false, exit 0
    - Test C: runs/_fixture/funnels/_tally.json created, exit 0
    - Test D: exit 2, "does not exist" in stderr
    - Test E: total_funnels === 0, low_n_warning === true, exit 0
    - grep "low_n_warning" tools/funnel-claim-tally.js returns at least one line
    - grep "claim-tally" tools/funnel-claim-tally.js shows the [claim-tally] stderr prefix
  </acceptance_criteria>
  <done>funnel-claim-tally.js passes all 5 spec verification tests; _tally.json is written to runs/_fixture/funnels/ on default invocation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| --space CLI arg → filesystem path | Space slug used to build the store directory path |
| funnel JSON files → claim tally | Funnel store files read as trusted data (no external input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-1 | Tampering | funnel-claim-tally.js --space arg | mitigate | sanitizePathSegment from funnel-rag-query.js applied to --space; same [a-z0-9._-] guard (T-03-13 parity) |
| T-15-02-2 | Denial of Service | funnel-claim-tally.js duplicate funnel_id | mitigate | Duplicate funnel_id across files → fail() exit 2 (per spec §3e) |
| T-15-02-3 | Information Disclosure | funnel-vectorize.js source_type leak | accept | source_type is funnel-level metadata, not a secret; already present in the funnel store files |
</threat_model>

<verification>
node tools/funnel-claim-tally.js --space=_fixture --json 2>&1 produces low_n_warning: true, 14 whitespace entries, exit 0.
node tools/funnel-rag-query.js --space=_fixture --query="test" --source-type=crowdfunding --top=4 returns records without "eliminated all" warning.
node -e "require('./runs/_fixture/funnels/_index.json').records.forEach(r=>console.log(r.source_type))" prints "crowdfunding" for each of the 4 records.
</verification>

<success_criteria>
- funnel-vectorize.js carries source_type and routing_flag into each unit; fixture _index.json rebuilt with these fields present
- funnel-rag-query.js --source-type filter works: crowdfunding returns 4 records, dtc returns 0 from fixture
- funnel-claim-tally.js passes all 5 spec tests (A-E) including the low_n_warning fixture behavior
- _tally.json written to runs/_fixture/funnels/ on default invocation
</success_criteria>

<output>
After completion, create .planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-02-SUMMARY.md
</output>
