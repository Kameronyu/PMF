---
phase: 15-stage-m1-s15-funnel-architect-copywriter
plan: "03"
type: execute
wave: 1
depends_on:
  - "15-01"
  - "15-02"
files_modified:
  - tools/hooks/inject-funnel-architect-dr.js
  - .claude/skills/funnel-architect/SKILL.md
  - .claude/skills/funnel-architect/_dr-context.generated.md
autonomous: true
requirements:
  - FUNNEL-01

must_haves:
  truths:
    - "inject-funnel-architect-dr.js generates a bundle at .claude/skills/funnel-architect/_dr-context.generated.md"
    - "The architect bundle contains exactly 5 DR files (funnel-architecture, persuasion, differentiator-framework, consumer-psychology, offer-construction)"
    - "SKILL.md declares read_first with the bundle path and the regenerate command"
    - "SKILL.md embeds the full Funnel Architect spec behavior (not a pointer to the spec file)"
    - "SKILL.md declares a SELF-AUDIT checklist matching the spec's named failure modes"
  artifacts:
    - path: "tools/hooks/inject-funnel-architect-dr.js"
      provides: "DR bundler for architect skill, mirrors inject-market-selection-dr.js"
      contains: "funnel-architect"
    - path: ".claude/skills/funnel-architect/SKILL.md"
      provides: "Funnel Architect judgment skill with read_first + behavior + self-audit"
      contains: "read_first"
    - path: ".claude/skills/funnel-architect/_dr-context.generated.md"
      provides: "Concatenated DR bundle (generated file)"
      contains: "=== DR FILE:"
  key_links:
    - from: "tools/hooks/inject-funnel-architect-dr.js"
      to: ".claude/skills/funnel-architect/_dr-context.generated.md"
      via: "node tools/hooks/inject-funnel-architect-dr.js"
      pattern: "=== DR FILE:"
    - from: ".claude/skills/funnel-architect/SKILL.md"
      to: ".claude/skills/funnel-architect/_dr-context.generated.md"
      via: "read_first section instructs the agent to Read the bundle"
      pattern: "_dr-context.generated.md"
---

<objective>
Build the Funnel Architect judgment skill: the DR bundler script (D5), the generated bundle, and the SKILL.md that declares the read_first mandatory pre-read and embeds the full operator spec behavior. The skill is conversational — propose→push→revise — and is deliberately KB-heavy so the operator can interrogate its reasoning.

Purpose: FUNNEL-01. Without deterministic DR injection, the Architect runs with zero grounding (the Arduview zero-grounding incident). The bundler + read_first pattern is the proven fix.
Output: inject-funnel-architect-dr.js, .claude/skills/funnel-architect/SKILL.md, and .claude/skills/funnel-architect/_dr-context.generated.md (generated by running the bundler).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-CONTEXT.md
@.planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-RESEARCH.md
@.planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-SPEC-funnel-architect.md

<interfaces>
<!-- From 15-RESEARCH.md RQ1 and the code examples section — exact bundler anatomy -->

Pattern file to mirror exactly: tools/hooks/inject-market-selection-dr.js
  - Mirror this file's structure: ALLOWLIST const, DEFAULT_OUT const, isUnderDrDir(), load loop, zero-files guard (exit 2), banner, write.
  - Do NOT mirror inject-dr.js (that one lacks the zero-files guard; market-selection-dr.js is the correct template).

Architect DR ALLOWLIST (5 files, Claude's discretion within spec categories):
  { file: 'funnel-architecture--carl-weische.md', use: 'V-shape awareness model, 4 funnel types, pre-sale 4-part, 8-step advertorial, hybrid sales-page architecture — structural vocabulary' },
  { file: 'persuasion--carl-weische.md',          use: 'Cold-offer persuasion elements (social proof, authority, certainty/risk-reversal, scarcity, urgency, exclusivity) + objection→element mapping' },
  { file: 'differentiator-framework__2_.md',      use: '4 levers (Market/UM/Angle/Offer), claim-typing, believability tiers, market-vs-angle test — keeps angle singular' },
  { file: 'consumer-psychology--carl-weische.md', use: 'Awareness-stage + sophistication-stage tables; Mass Desire core drivers — awareness-calibration reference' },
  { file: 'offer-construction--carl-weische.md',  use: 'Irresistible-offer / value-equation, anchor structures, crowdfunding offer mechanics (deposit, tiered pledge, early-bird, founder pricing)' },

Architect bundle DEFAULT_OUT:
  path.resolve(__dirname, '..', '..', '.claude', 'skills', 'funnel-architect', '_dr-context.generated.md')

DR_DIR: path.join(os.homedir(), 'knowledge', 'dr-marketing')

read_first pattern (from market-selection/SKILL.md + 15-RESEARCH.md RQ6):
  ## read_first (load before running — do not proceed without these)
  - `runs/<space>/funnels/*.json` — the funnel store (all analyzed competitor funnels for this space)
  - `runs/<space>/funnels/_tally.json` — the claim tally (run `node tools/funnel-claim-tally.js --space=<space>` if missing or stale)
  - `.claude/skills/funnel-architect/_dr-context.generated.md` — all 5 DR files bundled.
    ONE Read loads them all. If missing or stale, regenerate with
    `node tools/hooks/inject-funnel-architect-dr.js` then Read it.
    **Do NOT proceed without it** — the 2026-06 Arduview run executed the market-selection
    gate with ZERO DR grounding because this step was silently skipped.

  Bundled DR files (sources under ~/knowledge/dr-marketing/):
  - funnel-architecture--carl-weische.md — V-shape awareness model, 4 funnel types, structural vocabulary
  - persuasion--carl-weische.md — cold-offer persuasion elements + objection mapping
  - differentiator-framework__2_.md — 4 levers, claim-typing, believability tiers, market-vs-angle test
  - consumer-psychology--carl-weische.md — awareness + sophistication stage tables; Mass Desire drivers
  - offer-construction--carl-weische.md — irresistible-offer / value-equation, crowdfunding offer mechanics

SKILL.md YAML frontmatter:
  name: funnel-architect
  description: >-
    Judgment skill that reads the deep-analysis funnel store (belief_records[] + funnel fields),
    a claim tally, and operator run-context, then produces a congruent crowdfunding funnel DESIGN
    + COPY BRIEF (belief chain in order, install spec per belief, single governing angle,
    awareness-calibrated structure, offer/urgency, dead-ground/whitespace). Conversational —
    propose, operator pushes, revise. Run after the funnel store is built for a space.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build inject-funnel-architect-dr.js and generate the architect DR bundle</name>
  <files>tools/hooks/inject-funnel-architect-dr.js, .claude/skills/funnel-architect/_dr-context.generated.md</files>
  <read_first>
    - tools/hooks/inject-market-selection-dr.js — full file; this is the exact pattern to mirror; copy its structure verbatim then replace ALLOWLIST, DEFAULT_OUT, and header comment
    - .planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-RESEARCH.md §RQ1 — bundler anatomy and the "Code Examples" section for the exact architect allowlist
    - .planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-CONTEXT.md §D5 — confirms the allowlist and output path
  </read_first>
  <action>
    Create tools/hooks/inject-funnel-architect-dr.js by mirroring inject-market-selection-dr.js exactly. Replace:
      1. Header comment: explain this is the Funnel Architect DR bundler, not the market-selection one; describe what the 5 files provide; include the same security note (hardcoded allowlist, path-traversal guard).
      2. ALLOWLIST constant: use the 5 architect files exactly as listed in the interfaces block above.
      3. DEFAULT_OUT: path.resolve(__dirname, '..', '..', '.claude', 'skills', 'funnel-architect', '_dr-context.generated.md')
      4. Banner text: update to say "This is the Funnel Architect DR knowledge bundle" instead of the market-selection text.
      5. Everything else (isUnderDrDir, load loop, zero-files guard, write logic, exit codes) is identical to inject-market-selection-dr.js — do not change it.

    Create the .claude/skills/funnel-architect/ directory if it does not exist.

    After creating the script, run it to generate the bundle:
      node tools/hooks/inject-funnel-architect-dr.js
    If the script exits non-zero, read the stderr, fix the issue, and re-run before proceeding.
  </action>
  <verify>
    # Script exists and is runnable:
    node tools/hooks/inject-funnel-architect-dr.js; echo "exit: $?"
    Expected: exit 0.

    # Bundle file exists and is non-empty:
    wc -c .claude/skills/funnel-architect/_dr-context.generated.md
    Expected: size > 0 bytes.

    # All 5 DR files are present in the bundle:
    grep "=== DR FILE:" .claude/skills/funnel-architect/_dr-context.generated.md
    Expected: exactly 5 lines, each starting with "=== DR FILE:", containing:
      funnel-architecture--carl-weische.md
      persuasion--carl-weische.md
      differentiator-framework__2_.md
      consumer-psychology--carl-weische.md
      offer-construction--carl-weische.md

    grep -c "=== DR FILE:" .claude/skills/funnel-architect/_dr-context.generated.md
    Expected: 5
  </verify>
  <acceptance_criteria>
    - tools/hooks/inject-funnel-architect-dr.js exists and exits 0
    - .claude/skills/funnel-architect/_dr-context.generated.md exists with size > 0
    - grep -c "=== DR FILE:" returns 5
    - All 5 expected DR filenames appear in the bundle header lines
    - Script contains BELIEF in its header that hooks do NOT fire in subagents (carries the lesson forward)
  </acceptance_criteria>
  <done>architect DR bundler script created; bundle generated with 5 DR files confirmed present</done>
</task>

<task type="auto">
  <name>Task 2: Build .claude/skills/funnel-architect/SKILL.md with read_first and full spec behavior</name>
  <files>.claude/skills/funnel-architect/SKILL.md</files>
  <read_first>
    - .claude/skills/market-selection/SKILL.md — full file; mirror the YAML frontmatter structure, the ## read_first section format, the SELF-AUDIT checklist pattern
    - .planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-SPEC-funnel-architect.md — full file; this is the behavior authority; inline ALL sections verbatim as the skill body (do not summarize or shorten)
    - .planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-RESEARCH.md §RQ6 — SKILL.md structural rules and the exact read_first text
  </read_first>
  <action>
    Create .claude/skills/funnel-architect/SKILL.md with the following structure (from 15-RESEARCH.md RQ6 structural rules):

    1. YAML frontmatter (required — Claude uses this for skill routing):
       name: funnel-architect
       description: >- (the multi-line description from the interfaces block above)

    2. Derivation note: "> Derived from 15-SPEC-funnel-architect.md — edit the spec, regenerate this."

    3. ## read_first (MUST be the second substantive section, before the procedure):
       Include the full read_first block from the interfaces section:
         - runs/<space>/funnels/*.json with description
         - runs/<space>/funnels/_tally.json with the run command if missing
         - .claude/skills/funnel-architect/_dr-context.generated.md with the "Do NOT proceed without it" warning and regenerate command
         Then the "Bundled DR files" list naming all 5 files with their rationale.

    4. Horizontal rule separator.

    5. ALL sections from 15-SPEC-funnel-architect.md inlined verbatim (no summarizing, no shortening):
       - # Funnel Architect Skill (intro paragraph)
       - ## ROLE
       - ## THE CORE STANCE
       - ## WHY STRUCTURE FOLLOWS AWARENESS, NOT "CROWDFUNDING-NESS"
       - ## THE CONGRUENCY LAW
       - ## THE THREE-LAYER AUTHORITY MODEL
       - ## INPUTS
       - ## SUPPORTING KNOWLEDGE (update the "auto-injected" line to say the bundle is read via read_first — this is the one place you MUST override the spec's "auto-injected" language, per D5)
       - ## HOW YOU DESIGN
       - ## NAMED FAILURE MODES
       - ## OUTPUT
       - ## EPISTEMIC DISCIPLINE
       - ## OPERATOR VERIFICATION MANDATE

    6. ## SELF-AUDIT (run before presenting a design — based on the spec's NAMED FAILURE MODES):
       - [ ] Congruency check: every fill is my transformation, all threaded by one angle. No off-transformation messaging anywhere.
       - [ ] Proven moves only: every install backed by a validated funnel source. Nothing invented.
       - [ ] Structure follows awareness: funnel type selected by backer awareness (V-shape), not by generic crowdfunding position.
       - [ ] Awareness calibration: backer entry level explicitly identified; structure matches it.
       - [ ] Currencies not merged: belief ORDER references trust Currency B (crowdfunding); messaging fill references Currency A + in-transformation B.
       - [ ] Validation weighting: no single vivid funnel dominates the design unless it has the highest validation_strength.
       - [ ] Blocked ports respected: no install uses a claim or asset the operator flagged as unavailable.
       - [ ] DR bundle read: _dr-context.generated.md was the first Read action before any analysis. If not, stop and read it now.
       - [ ] Claim tally consulted: _tally.json was read before Step 6 (dead-ground/whitespace mapping). If not, read or regenerate it.
       - [ ] Low-N acknowledged: if _tally.json._meta.low_n_warning is true, all dead-ground conclusions are flagged as provisional.
  </action>
  <verify>
    # Frontmatter is present and valid:
    grep "^name: funnel-architect" .claude/skills/funnel-architect/SKILL.md
    Expected: matches.

    # read_first section is present:
    grep "## read_first" .claude/skills/funnel-architect/SKILL.md
    Expected: matches.

    # Bundle path declared in read_first:
    grep "_dr-context.generated.md" .claude/skills/funnel-architect/SKILL.md
    Expected: matches (path to the bundle file).

    # Regenerate command is present:
    grep "inject-funnel-architect-dr.js" .claude/skills/funnel-architect/SKILL.md
    Expected: matches.

    # SELF-AUDIT section is present:
    grep "## SELF-AUDIT" .claude/skills/funnel-architect/SKILL.md
    Expected: matches.

    # OPERATOR VERIFICATION MANDATE inlined from spec:
    grep "OPERATOR VERIFICATION MANDATE" .claude/skills/funnel-architect/SKILL.md
    Expected: matches.

    # The "auto-injected" language from the spec is NOT present verbatim (it must be replaced with the read_first mechanism):
    grep "auto-injected" .claude/skills/funnel-architect/SKILL.md
    Expected: zero matches (or if it appears in the OPERATOR VERIFICATION MANDATE quote context, that is acceptable — but the SUPPORTING KNOWLEDGE section must NOT claim auto-injection).
  </verify>
  <acceptance_criteria>
    - grep "^name: funnel-architect" returns a match (YAML frontmatter)
    - grep "## read_first" returns a match
    - grep "_dr-context.generated.md" returns at least 2 matches (read_first body + bundled files list)
    - grep "inject-funnel-architect-dr.js" returns a match (regenerate command)
    - grep "## SELF-AUDIT" returns a match with at least 8 checklist items
    - grep "OPERATOR VERIFICATION MANDATE" returns a match (spec section inlined)
    - grep "CONGRUENCY LAW" returns a match (core spec behavior present)
    - The SUPPORTING KNOWLEDGE section says the bundle is read via read_first, not "auto-injected"
  </acceptance_criteria>
  <done>SKILL.md exists with YAML frontmatter, read_first mandatory pre-read section, full spec behavior inlined verbatim, and SELF-AUDIT checklist; auto-injected language replaced with the deterministic read_first mechanism</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ~/knowledge/dr-marketing/ → bundle | DR files are operator-controlled, read by a hardcoded allowlist |
| SKILL.md → agent context | SKILL.md is read by the Architect agent as its governing prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-1 | Tampering | inject-funnel-architect-dr.js ALLOWLIST | mitigate | Filenames hardcoded as const (never from argv); each resolved path checked via isUnderDrDir() before read — identical guard to inject-market-selection-dr.js |
| T-15-03-2 | Information Disclosure | _dr-context.generated.md | accept | Contains only operator's own DR knowledge base; no secrets; already on local filesystem |
| T-15-03-3 | Spoofing | SKILL.md read_first | mitigate | Bundle path is a relative project path; the agent reads it via the tool interface which uses the project root — no traversal possible |
</threat_model>

<verification>
node tools/hooks/inject-funnel-architect-dr.js exits 0 and produces a non-empty bundle.
grep -c "=== DR FILE:" .claude/skills/funnel-architect/_dr-context.generated.md returns 5.
grep "## read_first" .claude/skills/funnel-architect/SKILL.md returns a match.
grep "inject-funnel-architect-dr.js" .claude/skills/funnel-architect/SKILL.md confirms the regenerate command is present.
</verification>

<success_criteria>
- inject-funnel-architect-dr.js exists, exits 0, and produces a bundle with all 5 DR files (grep -c "=== DR FILE:" returns 5)
- SKILL.md exists with read_first section declaring the bundle path, the "Do NOT proceed without it" warning, and the regenerate command
- SKILL.md has inlined behavior from 15-SPEC-funnel-architect.md (CONGRUENCY LAW, THREE-LAYER AUTHORITY MODEL, HOW YOU DESIGN, NAMED FAILURE MODES all present)
- SKILL.md has SELF-AUDIT checklist with at least 8 items
- "auto-injected" language is absent from the SUPPORTING KNOWLEDGE section (replaced with read_first)
</success_criteria>

<output>
After completion, create .planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-03-SUMMARY.md
</output>
