---
phase: 15-stage-m1-s15-funnel-architect-copywriter
plan: "04"
type: execute
wave: 2
depends_on:
  - "15-01"
  - "15-02"
files_modified:
  - tools/hooks/inject-copywriter-dr.js
  - .claude/skills/copywriter/SKILL.md
  - .claude/skills/copywriter/_dr-context.generated.md
autonomous: true
requirements:
  - FUNNEL-02

must_haves:
  truths:
    - "inject-copywriter-dr.js generates a bundle at .claude/skills/copywriter/_dr-context.generated.md"
    - "The copywriter bundle contains exactly 3 craft files (spencer-origins, carl-weische, mark-builds-brands) and NOT Hormozi"
    - "SKILL.md declares read_first with the bundle path and the regenerate command"
    - "SKILL.md embeds the full Copywriter spec behavior including the TWO RULES and LOCKED FORMAT RULES"
    - "SKILL.md declares a SELF-AUDIT checklist including the locked format checks"
  artifacts:
    - path: "tools/hooks/inject-copywriter-dr.js"
      provides: "DR bundler for copywriter skill, mirrors inject-market-selection-dr.js"
      contains: "copywriter"
    - path: ".claude/skills/copywriter/SKILL.md"
      provides: "Copywriter judgment skill with read_first + behavior + self-audit"
      contains: "read_first"
    - path: ".claude/skills/copywriter/_dr-context.generated.md"
      provides: "Concatenated craft DR bundle (generated file)"
      contains: "=== DR FILE:"
  key_links:
    - from: "tools/hooks/inject-copywriter-dr.js"
      to: ".claude/skills/copywriter/_dr-context.generated.md"
      via: "node tools/hooks/inject-copywriter-dr.js"
      pattern: "=== DR FILE:"
    - from: ".claude/skills/copywriter/SKILL.md"
      to: ".claude/skills/copywriter/_dr-context.generated.md"
      via: "read_first section instructs the agent to Read the bundle"
      pattern: "_dr-context.generated.md"
---

<objective>
Build the Copywriter judgment skill: the DR craft bundler script (D5), the generated bundle, and the SKILL.md that declares read_first and embeds the full operator spec behavior. The copywriter is a constrained writer — not a strategist — that takes the Architect's copy brief and per-section RAG'd verbatim and writes finished prose in the locked format.

Purpose: FUNNEL-02. Same zero-grounding risk as the Architect: the spec says "auto-injected" but hooks do not fire in subagents. The bundler + read_first pattern is the proven fix.
Output: inject-copywriter-dr.js, .claude/skills/copywriter/SKILL.md, and .claude/skills/copywriter/_dr-context.generated.md (generated by running the bundler).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-CONTEXT.md
@.planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-RESEARCH.md
@.planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-SPEC-copywriter.md
@.planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-COPYWRITE-WIRING-REFERENCE.md

<interfaces>
<!-- From 15-RESEARCH.md DR Allowlist section + 15-SPEC-copywriter.md SUPPORTING KNOWLEDGE -->

Pattern file to mirror exactly: tools/hooks/inject-market-selection-dr.js
  Same structure: ALLOWLIST const, DEFAULT_OUT const, isUnderDrDir(), load loop, zero-files guard (exit 2), banner, write.

Copywriter DR ALLOWLIST (3 files, exactly as specified in 15-SPEC-copywriter.md — Hormozi EXCLUDED per spec):
  { file: 'copywriting--spencer-origins.md',   use: 'Word/line-level craft: power-word substitution, show-dont-tell, steal-frameworks-not-words, Grade-6 readability — primary file for HOW a line is written' },
  { file: 'copywriting--carl-weische.md',      use: 'Section/paragraph construction and flow: Hook-Pain-Bridge-Outcome, feature-to-benefit, sensory language, advertorial frameworks, deliberate-element flow model, readability/dont-dilute — primary file for HOW a section is built' },
  { file: 'copywriting--mark-builds-brands.md', use: 'Body-copy structure (eight-element sales page, advertorial body structure, PIG punch-in-the-gut) and slippery-slide copy-chiefing pass — secondary file for body-copy structure and final flow check' },

Copywriter bundle DEFAULT_OUT:
  path.resolve(__dirname, '..', '..', '.claude', 'skills', 'copywriter', '_dr-context.generated.md')

DR_DIR: path.join(os.homedir(), 'knowledge', 'dr-marketing')

SKILL.md YAML frontmatter:
  name: copywriter
  description: >-
    Judgment skill that turns the Funnel Architect's copy brief into finished prose, section by section.
    The Architect has already designed the funnel — angle, belief per section, install spec, source routing.
    This skill writes the words. Run after receiving the Architect's copy brief for a space.
    Constrained copywriter, not a strategist: no re-deciding angle, belief order, or install spec.

read_first for copywriter SKILL.md:
  ## read_first (load before running — do not proceed without these)
  - The Architect's copy brief for this space (provided by the operator after running the funnel-architect skill)
  - `.claude/skills/copywriter/_dr-context.generated.md` — all 3 craft DR files bundled.
    ONE Read loads them all. If missing or stale, regenerate with
    `node tools/hooks/inject-copywriter-dr.js` then Read it.
    **Do NOT proceed without it** — hooks do not fire in subagents; there is no auto-injection
    mechanism in this repo (same lesson as the 2026-06 Arduview zero-grounding incident).

  Bundled DR files (craft layer — sources under ~/knowledge/dr-marketing/):
  - copywriting--spencer-origins.md — word/line craft: power words, show-dont-tell, steal-frameworks-not-words, Grade-6 readability
  - copywriting--carl-weische.md — section/paragraph construction: Hook-Pain-Bridge-Outcome, feature-to-benefit, sensory language, flow model
  - copywriting--mark-builds-brands.md — body-copy structure and slippery-slide chiefing pass

  Per-section RAG injection (orchestrator step — NOT auto-injected):
  For each section in the Architect's copy brief, the orchestrator runs:
    node tools/funnel-rag-query.js --space=<space> \
         --query="<section.angle_intent>" \
         --belief=<section.belief_id> \
         --source-type=<section.source_gate> \
         --top=8
  and provides the returned attributed block as context for that section. You do NOT run retrieval.
  If a section has no RAG block, say so and write conservatively, flagging the gap.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build inject-copywriter-dr.js and generate the copywriter DR bundle</name>
  <files>tools/hooks/inject-copywriter-dr.js, .claude/skills/copywriter/_dr-context.generated.md</files>
  <read_first>
    - tools/hooks/inject-market-selection-dr.js — full file; this is the exact pattern to mirror; copy its structure verbatim then replace ALLOWLIST, DEFAULT_OUT, and header comment
    - .planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-RESEARCH.md §"DR Allowlist Recommendations" — confirms the 3-file copywriter allowlist with rationale; Hormozi exclusion is explicit
    - .planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-SPEC-copywriter.md §SUPPORTING KNOWLEDGE — the spec explicitly names the 3 files and excludes Hormozi
  </read_first>
  <action>
    Create tools/hooks/inject-copywriter-dr.js by mirroring inject-market-selection-dr.js exactly. Replace:
      1. Header comment: explain this is the Copywriter DR bundler (not the market-selection one); describe the 3 craft files and what each provides; include the security note; explicitly state "Hormozi is excluded per operator spec."
      2. ALLOWLIST constant: use the 3 copywriter files exactly as listed in the interfaces block above.
      3. DEFAULT_OUT: path.resolve(__dirname, '..', '..', '.claude', 'skills', 'copywriter', '_dr-context.generated.md')
      4. Banner text: update to say "This is the Copywriter craft knowledge bundle" with a note that this is word/line and section/paragraph craft, not strategy — the brief governs strategy.
      5. Everything else (isUnderDrDir, load loop, zero-files guard, write logic, exit codes) is identical to inject-market-selection-dr.js — do not change it.

    Create the .claude/skills/copywriter/ directory if it does not exist.

    After creating the script, run it to generate the bundle:
      node tools/hooks/inject-copywriter-dr.js
    If the script exits non-zero, read the stderr, fix the issue, and re-run before proceeding.
  </action>
  <verify>
    # Script exists and is runnable:
    node tools/hooks/inject-copywriter-dr.js; echo "exit: $?"
    Expected: exit 0.

    # Bundle file exists and is non-empty:
    wc -c .claude/skills/copywriter/_dr-context.generated.md
    Expected: size > 0 bytes.

    # All 3 craft DR files are present in the bundle:
    grep "=== DR FILE:" .claude/skills/copywriter/_dr-context.generated.md
    Expected: exactly 3 lines containing:
      copywriting--spencer-origins.md
      copywriting--carl-weische.md
      copywriting--mark-builds-brands.md

    grep -c "=== DR FILE:" .claude/skills/copywriter/_dr-context.generated.md
    Expected: 3

    # Hormozi must NOT be in the bundle:
    grep -i "hormozi" .claude/skills/copywriter/_dr-context.generated.md
    Expected: zero matches.
  </verify>
  <acceptance_criteria>
    - tools/hooks/inject-copywriter-dr.js exists and exits 0
    - .claude/skills/copywriter/_dr-context.generated.md exists with size > 0
    - grep -c "=== DR FILE:" returns 3
    - All 3 expected craft filenames appear in the bundle header lines
    - grep -i "hormozi" returns 0 matches (excluded per spec)
    - Script header comment explicitly states Hormozi is excluded
  </acceptance_criteria>
  <done>copywriter DR bundler script created; bundle generated with exactly 3 craft DR files confirmed present; Hormozi absent</done>
</task>

<task type="auto">
  <name>Task 2: Build .claude/skills/copywriter/SKILL.md with read_first and full spec behavior</name>
  <files>.claude/skills/copywriter/SKILL.md</files>
  <read_first>
    - .claude/skills/market-selection/SKILL.md — full file; mirror the YAML frontmatter structure, read_first format, SELF-AUDIT pattern
    - .planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-SPEC-copywriter.md — full file; this is the behavior authority; inline ALL sections verbatim
    - .planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-COPYWRITE-WIRING-REFERENCE.md — read the per-section RAG orchestration contract to include in the read_first section
    - .planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-RESEARCH.md §RQ6 — structural rules for SKILL.md
  </read_first>
  <action>
    Create .claude/skills/copywriter/SKILL.md with the following structure:

    1. YAML frontmatter:
       name: copywriter
       description: >- (the multi-line description from the interfaces block)

    2. Derivation note: "> Derived from 15-SPEC-copywriter.md — edit the spec, regenerate this."

    3. ## read_first (MUST be the second substantive section, before the procedure):
       Include the full read_first block from the interfaces section:
         - The Architect's copy brief
         - .claude/skills/copywriter/_dr-context.generated.md with the "Do NOT proceed" warning and regenerate command
         - The bundled files list
         - The per-section RAG injection instructions (from COPYWRITE-WIRING-REFERENCE.md contract)

    4. Horizontal rule separator.

    5. ALL sections from 15-SPEC-copywriter.md inlined verbatim:
       - # Copywriter Skill (intro paragraph)
       - ## ROLE
       - ## INPUTS (note: "RAG'd verbatim copy, scoped per section" — the orchestrator provides this; the agent does NOT run retrieval)
       - ## THE TWO RULES (this is the governing constraint block — verbatim)
       - ## LOCKED FORMAT RULES (verbatim — no em dashes, no "it's not X it's Y", etc.)
       - ## HOW TO USE THE RAG vs YOUR KNOWLEDGE
       - ## SUPPORTING KNOWLEDGE (update the "auto-injected" line to point to the read_first bundle — replace "auto-injected / pasted alongside" with "read via read_first before proceeding")
       - ## OUTPUT
       - ## OPERATOR VERIFICATION MANDATE

    6. ## SELF-AUDIT (run before submitting copy — based on the TWO RULES and LOCKED FORMAT RULES):
       - [ ] Brief angle honored: I did not change the angle, belief assignment, or install spec. The brief's angle beats the RAG's angle.
       - [ ] Blocked ports respected: no section claims or implies an asset the brief marked as unavailable.
       - [ ] No verbatim lift: I rebuilt every line in my words using the RAG's structure, not its words. (Steal frameworks, not words.)
       - [ ] No em dashes in any line.
       - [ ] No "it's not X, it's Y" negation-flip pattern in any line.
       - [ ] No section announcement scaffolding (no "Now let's talk about...", no labels on sections beyond the bold section name).
       - [ ] No flowery or grandiose language. Clear beats clever.
       - [ ] Section-formatted copy: bold section name, same font size, no indents, no horizontal lines, minimal parentheses, minimal punctuation.
       - [ ] Gaps surfaced: any section where I couldn't write a congruent line without a blocked claim, or where RAG was thin, is flagged in the post-copy flag list.
       - [ ] DR bundle read: _dr-context.generated.md was the first Read action before writing. If not, stop and read it now.
  </action>
  <verify>
    # Frontmatter:
    grep "^name: copywriter" .claude/skills/copywriter/SKILL.md
    Expected: matches.

    # read_first section:
    grep "## read_first" .claude/skills/copywriter/SKILL.md
    Expected: matches.

    # Bundle path in read_first:
    grep "_dr-context.generated.md" .claude/skills/copywriter/SKILL.md
    Expected: matches.

    # Regenerate command:
    grep "inject-copywriter-dr.js" .claude/skills/copywriter/SKILL.md
    Expected: matches.

    # TWO RULES section (core behavior):
    grep "THE TWO RULES" .claude/skills/copywriter/SKILL.md
    Expected: matches.

    # LOCKED FORMAT RULES:
    grep "LOCKED FORMAT RULES" .claude/skills/copywriter/SKILL.md
    Expected: matches.

    # No em dash rule specifically:
    grep "em dash" .claude/skills/copywriter/SKILL.md
    Expected: matches.

    # SELF-AUDIT:
    grep "## SELF-AUDIT" .claude/skills/copywriter/SKILL.md
    Expected: matches.

    # OPERATOR VERIFICATION MANDATE:
    grep "OPERATOR VERIFICATION MANDATE" .claude/skills/copywriter/SKILL.md
    Expected: matches.

    # RAG orchestration wiring:
    grep "funnel-rag-query.js" .claude/skills/copywriter/SKILL.md
    Expected: matches (the per-section retrieval command).

    # source-type flag documented:
    grep "source-type" .claude/skills/copywriter/SKILL.md
    Expected: matches (from the per-section RAG injection instructions).
  </verify>
  <acceptance_criteria>
    - grep "^name: copywriter" returns a match (YAML frontmatter)
    - grep "## read_first" returns a match
    - grep "_dr-context.generated.md" returns at least 2 matches
    - grep "inject-copywriter-dr.js" returns a match (regenerate command)
    - grep "THE TWO RULES" returns a match (core constraint block inlined)
    - grep "LOCKED FORMAT RULES" returns a match
    - grep "## SELF-AUDIT" returns a match with at least 9 checklist items
    - grep "OPERATOR VERIFICATION MANDATE" returns a match
    - grep "funnel-rag-query.js" returns a match (RAG wiring documented)
    - grep "source-type" returns a match (source routing flag documented)
    - The SUPPORTING KNOWLEDGE section does NOT claim "auto-injected" (replaced with read_first)
  </acceptance_criteria>
  <done>copywriter SKILL.md exists with YAML frontmatter, read_first with bundle + per-section RAG orchestration contract, full spec behavior inlined verbatim including TWO RULES and LOCKED FORMAT RULES, and SELF-AUDIT checklist; auto-injected language replaced</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ~/knowledge/dr-marketing/ → bundle | Craft DR files read by hardcoded allowlist |
| SKILL.md → copywriter agent context | SKILL.md is the copywriter's governing prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-1 | Tampering | inject-copywriter-dr.js ALLOWLIST | mitigate | Filenames hardcoded as const; each resolved path verified via isUnderDrDir(); Hormozi exclusion is structurally enforced by its absence from the ALLOWLIST |
| T-15-04-2 | Information Disclosure | _dr-context.generated.md | accept | Operator's own craft knowledge; no secrets; already on local filesystem |
| T-15-04-3 | Elevation of Privilege | Copywriter re-decides strategy | mitigate | TWO RULES are inlined verbatim in SKILL.md; SELF-AUDIT checklist item 1 checks for angle changes; LOCKED FORMAT RULES prevent structural departures |
</threat_model>

<verification>
node tools/hooks/inject-copywriter-dr.js exits 0 and produces a non-empty bundle.
grep -c "=== DR FILE:" .claude/skills/copywriter/_dr-context.generated.md returns 3.
grep -i "hormozi" .claude/skills/copywriter/_dr-context.generated.md returns 0 matches.
grep "## read_first" .claude/skills/copywriter/SKILL.md returns a match.
grep "funnel-rag-query.js" .claude/skills/copywriter/SKILL.md returns a match (RAG wiring documented).
</verification>

<success_criteria>
- inject-copywriter-dr.js exists, exits 0, and produces a bundle with exactly 3 craft DR files (grep -c "=== DR FILE:" returns 3)
- Hormozi is absent from the bundle (grep -i "hormozi" returns 0)
- SKILL.md exists with read_first section, TWO RULES, LOCKED FORMAT RULES, per-section RAG orchestration contract, and SELF-AUDIT checklist
- "auto-injected" language is absent from the SUPPORTING KNOWLEDGE section (replaced with read_first)
</success_criteria>

<output>
After completion, create .planning/phases/15-stage-m1-s15-funnel-architect-copywriter/15-04-SUMMARY.md
</output>
