---
phase: 16-asset-classifier-image-and-video-bricks
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .venv
  - tools/asset/section-table.json
  - tools/asset/section-list.default.json
  - tools/asset/README.md
  - runs/arduview/asset-classify/CLAIM-LIST.json
autonomous: true
requirements: [ASSET-09]
must_haves:
  truths:
    - ".venv exists and `.venv/bin/python -c \"import imagehash, imageio_ffmpeg\"` exits 0"
    - "tools/asset/section-table.json is require()-able and routes all 8 Arduview claims"
    - "runs/arduview/asset-classify/CLAIM-LIST.json lists 8 claims with 2 flagged load_bearing"
    - "the .venv/bin/python spawn convention is documented for Node bricks"
  artifacts:
    - path: ".venv/bin/python"
      provides: "isolated python env with imagehash + imageio-ffmpeg (PEP 668 venv)"
    - path: "tools/asset/section-table.json"
      provides: "claim -> eligible LP sections routing table (brick 5 reads this)"
      contains: "see_through_display_on"
    - path: "tools/asset/section-list.default.json"
      provides: "default LP section list + video-only slots"
      contains: "hero_loop"
    - path: "runs/arduview/asset-classify/CLAIM-LIST.json"
      provides: "per-run claim list (per-launch input, swappable per product)"
      contains: "transparency_internals_visible"
    - path: "tools/asset/README.md"
      provides: "the .venv/bin/python spawn convention + brick map"
  key_links:
    - from: "Node bricks (asset-fetch.js etc.)"
      to: ".venv/bin/python tools/asset/<brick>.py"
      via: "spawnSync with explicit venv interpreter path"
      pattern: "\\.venv/bin/python"
---

<objective>
Wave-0 setup: create the project `.venv` with the two missing pixel-brick dependencies, and author the three data artifacts (section-table, section-list defaults, the Arduview claim list) by extracting them verbatim from the spec. Document the Node->Python spawn convention so every later brick is reproducible.

Purpose: Every brick in this phase depends on this. PEP 668 blocks bare `pip` (the box is `EXTERNALLY-MANAGED`), so the pixel bricks (probe.py, probe_video.py, sample_montage.py) cannot run until `.venv` exists with `imagehash` + `imageio-ffmpeg`. The routing table and claim list are read by the map/rank brick and the vision agents respectively — they are config, not code, and live as editable artifacts (the whole point of keeping section-assignment out of the agent's head).

Output: A working `.venv`, three JSON data artifacts, and a README documenting the spawn convention.

ASSIGNED REQUIREMENT IDS FOR PHASE 16 (recorded here for coverage audit — every ID appears in at least one plan):
- ASSET-01 fetch local+drive modes (plan 02)
- ASSET-02 probe/downscale/dedupe image (plan 02)
- ASSET-03 relevance-bucket + role-classify grounded record (plan 04)
- ASSET-04 video probe+sample+montage (plan 03)
- ASSET-05 comprehend-video record (plan 04)
- ASSET-06 map+rank+section-table+gap_list (plan 05)
- ASSET-07 emit manifest launch format (plan 05)
- ASSET-08 upload+url-backfill (plan 05)
- ASSET-09 orchestrator skill fan-out + validator + setup (plans 01, 04)
- ASSET-10 UAT on local Arduview set (plan 06)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-asset-classifier-image-and-video-bricks/16-RESEARCH.md
@.planning/phases/16-asset-classifier-image-and-video-bricks/16-PATTERNS.md
@prompts/_specs/image-classifier-brick.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .venv and install pixel-brick deps</name>
  <files>.venv (created), .gitignore</files>
  <read_first>
    - .planning/phases/16-asset-classifier-image-and-video-bricks/16-RESEARCH.md (§Installation, §Environment Availability, Pitfall 3 PEP 668)
    - .planning/phases/16-asset-classifier-image-and-video-bricks/16-PATTERNS.md (§Wave-0 setup)
  </read_first>
  <action>
Ask the operator before running installs if network/pip access is uncertain (CLAUDE.md: ask before side-effecting setup), then run from the repo root `/home/kyu3/PMF`:

```bash
python3 -m venv .venv
.venv/bin/python -m pip install --upgrade pip
.venv/bin/python -m pip install imagehash imageio-ffmpeg
```

DO NOT use bare `pip install` — the box is `EXTERNALLY-MANAGED` (PEP 668 verified present at /usr/lib/python3.12/EXTERNALLY-MANAGED); bare pip errors `externally-managed-environment`. Pillow (12.2.0) is already on the box and is pulled into the venv as a satisfied dep; `imagehash` pulls numpy+scipy; `imageio-ffmpeg` ships a bundled static ffmpeg binary (29.5 MB manylinux2014 wheel, no system ffmpeg needed).

Add `.venv/` to `.gitignore` if not already ignored (do not commit the venv).

Verify the bundled ffmpeg binary resolves:
```bash
.venv/bin/python -c "from imageio_ffmpeg import get_ffmpeg_exe; print(get_ffmpeg_exe())"
```
This must print a path to a bundled `ffmpeg` executable.
  </action>
  <verify>
    <automated>cd /home/kyu3/PMF && .venv/bin/python -c "import imagehash, imageio_ffmpeg; from imageio_ffmpeg import get_ffmpeg_exe; import os; assert os.path.exists(get_ffmpeg_exe())" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python` exists
    - `.venv/bin/python -c "import imagehash, imageio_ffmpeg"` exits 0
    - `get_ffmpeg_exe()` returns an existing file path
    - `.venv/` is git-ignored (grep `.venv` in `.gitignore` returns a match)
  </acceptance_criteria>
  <done>The venv exists with imagehash + imageio-ffmpeg importable and the bundled ffmpeg binary present; .venv is not tracked by git.</done>
</task>

<task type="auto">
  <name>Task 2: Author the three data artifacts (section-table, section-list, claim-list)</name>
  <files>tools/asset/section-table.json, tools/asset/section-list.default.json, runs/arduview/asset-classify/CLAIM-LIST.json</files>
  <read_first>
    - prompts/_specs/image-classifier-brick.md (§Controlled vocabularies → claim list (spec lines 141-149) + Section table (spec lines 155-166))
    - .planning/phases/16-asset-classifier-image-and-video-bricks/16-RESEARCH.md (Open item 2, 3, 4 — the JSON shapes are written there verbatim)
    - .planning/phases/16-asset-classifier-image-and-video-bricks/16-PATTERNS.md (§section-table.json, §section-list.default.json, §CLAIM-LIST.json — paste-ready)
  </read_first>
  <action>
Create `tools/asset/section-table.json` VERBATIM (claim -> eligible LP sections; brick 5 require()s this; the `requires.display_state` conditional is load-bearing — the lit-through-glass shot routes to hero ONLY when display_state==on_legible):

```json
{
  "see_through_display_on": { "primary": ["hero"], "also": ["feature_transparency"], "requires": { "display_state": "on_legible" } },
  "transparency_internals_visible": { "primary": ["feature_transparency"], "also": ["hero", "specs"] },
  "pocket_scale": { "primary": ["feature_scale"], "also": ["lifestyle", "hero"] },
  "retro_gaming": { "primary": ["feature_games"], "also": ["lifestyle", "social_proof"] },
  "collectible_object": { "primary": ["lifestyle", "final_cta"], "also": ["hero"] },
  "hackable_maker": { "primary": ["feature_maker"], "also": ["specs"] },
  "build_material_quality": { "primary": ["feature_transparency"], "also": ["specs"] },
  "whats_in_box": { "primary": ["offer"], "also": ["final_cta"] }
}
```

Create `tools/asset/section-list.default.json` — the default LP section list (the section table's RHS section names) plus the two video-only slots. This is the product-agnostic default brick 5 falls back to when no `--section-list` is provided (M1-S3 funnel container not built yet):

```json
{
  "sections": ["hero", "feature_transparency", "feature_scale", "feature_games", "feature_maker", "lifestyle", "specs", "social_proof", "offer", "final_cta"],
  "video_slots": ["hero_loop", "feature_demo"],
  "_note": "Default crowdfunding/DTC section list. brick 5 (asset-map-rank.js) takes --section-list=<path> to override when M1-S3 deep-analysis produces a funnel structure."
}
```

Create `runs/arduview/asset-classify/CLAIM-LIST.json` — the per-run claim list (per-launch input, swappable per product, like launch/inkleaf-launch/LAUNCH-INPUTS.md). Each claim = `{ id, gloss, load_bearing }`. The 8 Arduview claims from spec §Controlled vocabularies, with the 2 load-bearing claims flagged (these get the `display_state` special-case in brick-4 step 3):

```json
{
  "product": "arduview",
  "_note": "Per-run claim list. The classifier machinery is product-agnostic; this file is the per-product swap. The fan-out subagent receives this EMBEDDED in its spawn prompt (small, trusted operator input — unlike the image, which it Reads).",
  "claims": [
    { "id": "transparency_internals_visible", "gloss": "see the PCB/board through shell+display", "load_bearing": true },
    { "id": "see_through_display_on", "gloss": "game pixels lit on the transparent pane", "load_bearing": true },
    { "id": "pocket_scale", "gloss": "size in hand / next to a common object (~50g, 85x43x13mm)", "load_bearing": false },
    { "id": "retro_gaming", "gloss": "classic D-pad + A/B, pixel game on screen", "load_bearing": false },
    { "id": "collectible_object", "gloss": "desirable object to own/display", "load_bearing": false },
    { "id": "hackable_maker", "gloss": "USB-C data / Arduino-flashable / open framing", "load_bearing": false },
    { "id": "build_material_quality", "gloss": "resin finish, clean edges, premium feel", "load_bearing": false },
    { "id": "whats_in_box", "gloss": "unit + cable + packaging (offer completeness)", "load_bearing": false }
  ]
}
```

All three are committed to git (data artifacts, not the venv). Use `JSON.parse(fs.readFileSync(...))` to validate each is well-formed JSON after writing.
  </action>
  <verify>
    <automated>cd /home/kyu3/PMF && node -e "const t=require('./tools/asset/section-table.json'); const l=require('./tools/asset/section-list.default.json'); const c=require('./runs/arduview/asset-classify/CLAIM-LIST.json'); if(!t.see_through_display_on.requires||t.see_through_display_on.requires.display_state!=='on_legible') throw 'table missing requires'; if(!l.video_slots.includes('hero_loop')) throw 'no hero_loop'; const lb=c.claims.filter(x=>x.load_bearing).map(x=>x.id); if(lb.length!==2||!lb.includes('see_through_display_on')||!lb.includes('transparency_internals_visible')) throw 'load_bearing wrong'; console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `tools/asset/section-table.json` contains `see_through_display_on` with `requires.display_state` == `on_legible`
    - `tools/asset/section-table.json` has all 8 claim keys
    - `tools/asset/section-list.default.json` contains `hero_loop` in `video_slots`
    - `runs/arduview/asset-classify/CLAIM-LIST.json` has 8 claims, exactly 2 with `load_bearing: true` (`see_through_display_on` + `transparency_internals_visible`)
    - all three files are valid JSON (the node `require` above does not throw)
  </acceptance_criteria>
  <done>The three data artifacts exist, are valid JSON, and carry the exact spec values (8-claim table with the hero display_state conditional, 8-claim list with 2 load-bearing).</done>
</task>

<task type="auto">
  <name>Task 3: Document the .venv/bin/python spawn convention</name>
  <files>tools/asset/README.md</files>
  <read_first>
    - .planning/phases/16-asset-classifier-image-and-video-bricks/16-PATTERNS.md (§S5 — Spawn a sibling script; §Wave-0 setup)
    - tools/funnel-analyzer-context.js (lines 167-182 — the spawn-a-sibling-script pattern to mirror)
  </read_first>
  <action>
Create `tools/asset/README.md` documenting the brick map and the load-bearing Node->Python spawn convention so every later Node brick spawns the python pixel bricks the same way. Include this exact convention block (the executor of plans 02/03/04 copies it):

```markdown
# tools/asset — Asset Classifier bricks (Phase 16 / Stage M1-S16)

## Node <-> Python split (load-bearing convention)
Pixel work is Python (PIL on this box); orchestration/emit/upload is Node. Node bricks spawn the
python pixel bricks via the EXPLICIT venv interpreter path so spawn works without an active shell:

    const { spawnSync } = require('child_process');
    const path = require('path');
    const VENV_PY = path.resolve(process.cwd(), '.venv', 'bin', 'python');
    const run = spawnSync(VENV_PY,
      [path.resolve(__dirname, 'asset', 'probe.py'), rawPath, workPath],
      { encoding: 'utf8', maxBuffer: 256 * 1024 * 1024 });
    if (run.error || run.status !== 0 || !run.stdout) {
      console.error('ERROR: probe.py failed — refusing to fabricate technical{}');
      if (run.stderr) console.error(run.stderr.trim());
      process.exit(2);
    }
    const technical = JSON.parse(run.stdout);   // python bricks print JSON to stdout

Never call bare `python3` (PEP 668 venv must be used). Never use shell-string interpolation of
filenames into the spawn — pass argv arrays only (command-injection guard, T-16-02).

## Brick map
| # | Brick | Lang | File |
|---|-------|------|------|
| 1 | fetch | Node | tools/asset-fetch.js |
| 2 | probe (+downscale +phash) | Python | tools/asset/probe.py |
| 2v | probe-video | Python | tools/asset/probe_video.py |
| 3v | sample + montage | Python | tools/asset/sample_montage.py |
| 3 | relevance-bucket | agent | .claude/skills/asset-classify/SKILL.md |
| 4 | role-classify | agent | .claude/skills/asset-classify/SKILL.md |
| 4v | comprehend-video | agent | .claude/skills/asset-classify/SKILL.md |
| validate | validate-asset-record | Node hook | tools/hooks/validate-asset-record.js |
| 5 | map + rank | Node | tools/asset-map-rank.js |
| 6 | pick gate | human | — |
| 7 | emit manifest | Node | tools/asset-emit.js |
| 8 | upload + url-backfill | Node | tools/asset-upload.js |

## Setup
    python3 -m venv .venv
    .venv/bin/python -m pip install imagehash imageio-ffmpeg   # Pillow already present
```
  </action>
  <verify>
    <automated>cd /home/kyu3/PMF && grep -q "\.venv/bin/python" tools/asset/README.md && grep -q "argv arrays only" tools/asset/README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `tools/asset/README.md` exists and contains `.venv/bin/python`
    - it documents the argv-array (no shell-string) spawn rule
    - it carries the brick map table (all 8 bricks + video bricks + validator)
  </acceptance_criteria>
  <done>The spawn convention + brick map is documented; later plans copy the convention block verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> pip install | network fetch of imagehash/imageio-ffmpeg wheels into the venv |
| data artifacts -> downstream bricks | section-table/claim-list are trusted operator config, read by scripts + embedded in agent prompts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-S1-01 | Tampering | pip wheel supply chain | accept | installing two well-known PyPI packages into an isolated venv; no lockfile pinning in-phase (local research tool, low value target) |
| T-16-S1-02 | Information disclosure | .venv committed to git | mitigate | add `.venv/` to `.gitignore`; never commit the env (Task 1) |
| T-16-S1-03 | Tampering | section-table.json edited to route a weak claim to hero | accept | the table is trusted operator config by design (the whole point is auditable, editable routing); the human pick gate (brick 6) is the backstop |
</threat_model>

<verification>
- `.venv/bin/python -c "import imagehash, imageio_ffmpeg"` exits 0
- all three data artifacts are valid JSON with the spec values
- README documents the spawn convention
</verification>

<success_criteria>
Wave-0 setup complete: pixel-brick deps installed in an isolated venv, the routing table + section list + claim list authored from the spec, the Node->Python spawn convention documented. Everything downstream can now run.
</success_criteria>

<output>
After completion, create `.planning/phases/16-asset-classifier-image-and-video-bricks/16-01-SUMMARY.md`
</output>
