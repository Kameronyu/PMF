---
phase: 16-asset-classifier-image-and-video-bricks
plan: 04
type: execute
wave: 2
depends_on: [16-01, 16-02, 16-03]
files_modified:
  - tools/hooks/validate-asset-record.js
  - .claude/skills/asset-classify/SKILL.md
autonomous: true
requirements: [ASSET-03, ASSET-05, ASSET-09]
must_haves:
  truths:
    - "validate-asset-record.js rejects any demonstrates[].claim not in the run's claim list (closed vocab)"
    - "validate-asset-record.js rejects off-enum shot_type / disqualifiers / strength"
    - "validate-asset-record.js rejects a demonstrates[] entry missing evidence (grounding gate)"
    - "the SKILL.md orchestrator assembles per-asset context (fact sheet + claim list + vocab) + a Read path, never image bytes"
    - "the SKILL.md fans out one subagent per image (brick 4) and per video (brick 4v), each blind to the others"
  artifacts:
    - path: "tools/hooks/validate-asset-record.js"
      provides: "closed-vocab + grounding reject (orchestrator-run on each returned record)"
      contains: "DISQUALIFIER_ENUM"
    - path: ".claude/skills/asset-classify/SKILL.md"
      provides: "the asset-classify orchestrator — deterministic chain + fan-out + 3 agent prompts verbatim"
      contains: "relevance-bucket"
  key_links:
    - from: "orchestrator assemble step"
      to: "subagent spawn prompt"
      via: "embeds trusted text + a Read path (work_path / contact sheets), NOT image bytes"
      pattern: "Read"
    - from: "returned record"
      to: "validate-asset-record.js"
      via: "orchestrator runs the validator before persist (hooks don't fire in subagents)"
      pattern: "validate-asset-record"
---

<objective>
Build the judgment core: the `validate-asset-record.js` deterministic gate (closed-vocab + grounding reject) and the `.claude/skills/asset-classify/SKILL.md` orchestrator — a structural twin of `funnel-deep-pass/SKILL.md` that runs the deterministic chain, fans out one vision subagent per asset, validates each returned record, and persists. The three agent prompts (relevance-bucket, role-classify, comprehend-video) are folded into the SKILL verbatim.

Purpose: This is where vision lives. Bricks 3 (relevance-bucket, cheap) and 4/4v (role-classify / comprehend-video, quality) are the only judgment agents in the phase. The orchestrator's load-bearing move (from the proof + RESEARCH) is that it assembles trusted text (fact sheet + claim list + vocab) AND hands the agent a local file path it Reads (the downscaled `work_path` for images, the ordered contact sheets for videos) — image bytes NEVER enter context. The validator enforces the spec's grounding rule deterministically: it can't see pixels, but it CAN reject any claim-tag missing `evidence`, and reject off-vocab claims/shot_types/disqualifiers. Hooks don't fire in subagents, so the orchestrator runs the validator on each returned record before persist.

Output: `tools/hooks/validate-asset-record.js` + `.claude/skills/asset-classify/SKILL.md` (with the 3 prompts).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-asset-classifier-image-and-video-bricks/16-RESEARCH.md
@.planning/phases/16-asset-classifier-image-and-video-bricks/16-PATTERNS.md
@prompts/_specs/image-classifier-brick.md
@runs/arduview/_asset-classify-proof.md
@.claude/skills/funnel-deep-pass/SKILL.md
@tools/hooks/validate-analyzer.js
@runs/arduview/asset-classify/CLAIM-LIST.json
@tools/asset/section-list.default.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tools/hooks/validate-asset-record.js — closed-vocab + grounding reject</name>
  <files>tools/hooks/validate-asset-record.js</files>
  <read_first>
    - tools/hooks/validate-asset-record.js (the file being created)
    - tools/hooks/validate-analyzer.js (the structural twin — header/usage/exit contract lines 1-44; read+parse lines 46-67; closed-enum-as-Set lines 69-122; violations loop lines 264-388)
    - .planning/phases/16-asset-classifier-image-and-video-bricks/16-PATTERNS.md (validate-asset-record.js section — the exact idioms + the 4 asset-specific reject rules)
    - prompts/_specs/image-classifier-brick.md (record schema spec lines 58-83; shot_type vocab line 138; disqualifier enum line 76; video record lines 245-274)
    - .planning/phases/16-asset-classifier-image-and-video-bricks/16-RESEARCH.md (Technical unknown — Parallel vision fan-out → Validate on return, lines 214-219; Pitfall 4 grounding)
  </read_first>
  <behavior>
    - `node validate-asset-record.js <record.json> --claim-list=<CLAIM-LIST.json>` exits 0 (silent) on a valid record, exits 2 + prints REJECT lines to stderr on any violation.
    - REJECT if any `demonstrates[].claim` is not in the claim list's claim ids (closed vocab).
    - REJECT if `shot_type` not in SHOT_TYPE_ENUM.
    - REJECT if any `disqualifiers[]` value not in DISQUALIFIER_ENUM.
    - REJECT if any `demonstrates[].strength` not in STRENGTH_ENUM.
    - REJECT if any `demonstrates[]` entry is missing `strength` OR `evidence` (grounding gate — image records).
    - (video) REJECT if any `demonstrates[].at` (timestamp) missing OR `best_use` not in {hero_loop, feature_demo, full_explainer}.
    - A record using the proof's img-01 values (4 valid claims, each with evidence) passes; a record tagging an off-list claim or a claim with no evidence is rejected.
  </behavior>
  <action>
Create `tools/hooks/validate-asset-record.js` mirroring `validate-analyzer.js` exactly in form. Copy the header/usage/`Exit 0 = pass (silent). Exit 2 + stderr = REJECT.` contract, the `process.argv[2]` path arg + read+parse-with-REJECT, the closed-enum-as-Set idiom, and the violations-accumulate-then-dump loop.

Enums (paste verbatim from the spec):
```javascript
const SHOT_TYPE_ENUM = new Set(['hero','detail_macro','in_hand_scale','lifestyle_context',
  'screen_on_ui','turntable_frame','packaging_unboxing','group_collection','diagram_annotated']);
const DISQUALIFIER_ENUM = new Set(['text_overlay','watermark','low_res','wrong_product','competitor_ad','sensitive']);
const STRENGTH_ENUM = new Set(['strong','partial','incidental']);
const BEST_USE_ENUM = new Set(['hero_loop','feature_demo','full_explainer']);
const DISPLAY_STATE_ENUM = new Set(['off','on_glow','on_legible']);
```

Load the run's claim list from `--claim-list=<path>` (default `runs/arduview/asset-classify/CLAIM-LIST.json`); build a Set of `claims[].id`. The input record may be a single object OR `{records:[...]}` — handle both (iterate records).

Per-record reject rules (accumulate into `violations`, never throw):
1. **claim ∈ claim list:** for each `demonstrates[]` entry, if `entry.claim` not in the claim-id Set → REJECT (off-list).
2. **shot_type ∈ SHOT_TYPE_ENUM** (images only — videos have no shot_type).
3. **disqualifiers[] ⊆ DISQUALIFIER_ENUM.**
4. **strength ∈ STRENGTH_ENUM** for each demonstrates entry.
5. **grounding:** each `demonstrates[]` entry must have a non-empty `evidence` (image) OR `motion_value` (video) AND a `strength`. Missing → REJECT (the script can't see pixels but enforces that the agent cited SOMETHING).
6. **display_state** (if present) ∈ DISPLAY_STATE_ENUM.
7. **video detection:** if the record has `segments` or `best_use` or `eligible_slots`, treat as a video record: require each `demonstrates[].at` (timestamp) present, `best_use` ∈ BEST_USE_ENUM, and `loop_safe`/`needs_audio` booleans present.

Dump all violations to stderr and `process.exit(2)` if any; else `process.exit(0)` silently. Include a `--help` block.

This is the FORM of a PostToolUse hook; the INVOCATION is an orchestrator step (the SKILL runs it on each returned record — hooks don't fire in subagents).
  </action>
  <verify>
    <automated>cd /home/kyu3/PMF && cat > /tmp/rec-good.json <<'EOF'
{"id":"arduview-img-01","relevance":"product_shot","shot_type":"in_hand_scale","display_state":"on_legible","demonstrates":[{"claim":"transparency_internals_visible","strength":"strong","evidence":"chip + ribbon + traces read through the shell"},{"claim":"see_through_display_on","strength":"strong","evidence":"lit cyan game legible on the pane"}],"disqualifiers":[]}
EOF
cat > /tmp/rec-bad.json <<'EOF'
{"id":"x","shot_type":"in_hand_scale","display_state":"on_legible","demonstrates":[{"claim":"made_up_claim","strength":"strong","evidence":"foo"},{"claim":"retro_gaming","strength":"strong"}],"disqualifiers":["busy_background"]}
EOF
node tools/hooks/validate-asset-record.js /tmp/rec-good.json --claim-list=runs/arduview/asset-classify/CLAIM-LIST.json && echo "GOOD-PASS" && ( node tools/hooks/validate-asset-record.js /tmp/rec-bad.json --claim-list=runs/arduview/asset-classify/CLAIM-LIST.json; test $? -eq 2 && echo "BAD-REJECTED" )</automated>
  </verify>
  <acceptance_criteria>
    - `tools/hooks/validate-asset-record.js` contains `DISQUALIFIER_ENUM` and `SHOT_TYPE_ENUM`
    - valid record (good fixture above) exits 0 silently
    - bad record (off-list claim `made_up_claim`, missing evidence on `retro_gaming`, off-enum disqualifier `busy_background`) exits 2 with REJECT lines
    - claim-list Set is loaded from `--claim-list=` path
    - video records (segments/best_use present) get the timestamp + best_use checks
  </acceptance_criteria>
  <done>The validator enforces closed vocab + grounding deterministically, mirroring validate-analyzer.js; passes the proof's img-01 record, rejects off-vocab/un-grounded records.</done>
</task>

<task type="auto">
  <name>Task 2: .claude/skills/asset-classify/SKILL.md — orchestrator + 3 agent prompts</name>
  <files>.claude/skills/asset-classify/SKILL.md</files>
  <read_first>
    - .claude/skills/asset-classify/SKILL.md (the file being created)
    - .claude/skills/funnel-deep-pass/SKILL.md (the structural twin — frontmatter 1-13; ENFORCEMENT MAP 48-63; PRECONDITION CHECK 82-97; DETERMINISTIC CHAIN 100-127; PER-FUNNEL LOOP 131-179; AGENT PROMPTS 183-475; HAND-OFF 479-483)
    - .planning/phases/16-asset-classifier-image-and-video-bricks/16-PATTERNS.md (.claude/skills/asset-classify/SKILL.md section — the asset ENFORCEMENT MAP table + which prompts to write; asset-analyzer-context.js section — the Read-path assemble spine)
    - prompts/_specs/image-classifier-brick.md (brick-4 method lines 94-133 + record schema 58-83; brick-4v method 222-241 + video record 245-274; the grounding rule 29-33)
    - tools/asset/README.md (the deterministic chain bricks + spawn convention)
  </read_first>
  <action>
Create `.claude/skills/asset-classify/SKILL.md`, structured exactly like `funnel-deep-pass/SKILL.md`, swapping the domain. Sections:

**Frontmatter** (`name: asset-classify`, `description:` summarizing: deterministic fetch→probe→[video: probe-video→sample-montage] chain → per-asset vision fan-out (relevance-bucket + role-classify / comprehend-video) → validate-on-return → emit manifest. Runs standalone against a local asset set via `--local`).

**ENFORCEMENT MAP** table (deterministic vs judgment ledger) — use the asset version from PATTERNS.md:
| Step | Kind | Mechanism | What enforces it |
covering: asset-fetch.js / probe.py / probe_video.py / sample_montage.py (DETERMINISTIC scripts); relevance-bucket + role-classify/comprehend-video (JUDGMENT agents); validate-asset-record.js (DETERMINISTIC hook, orchestrator-run); asset-map-rank.js / asset-emit.js / asset-upload.js (DETERMINISTIC scripts).

Plus the "load-bearing truths" plain-fact block: (1) the agents **Read a local work_path / contact-sheet path** — image bytes NEVER in context (the proof's move); (2) validate-asset-record REJECTS off-vocab / un-grounded records deterministically; (3) only relevance-bucket + role-classify/comprehend-video are judgment — everything else is script/hook.

**PRECONDITION CHECK** (copy the `test -f ... || { echo MISSING; exit 1; }` bash idiom). Assert: `.venv/bin/python -c "import imagehash, imageio_ffmpeg"` succeeds; `runs/<space>/asset-classify/CLAIM-LIST.json` present; `tools/asset/section-table.json` present; the `--local` dir present.

**THE DETERMINISTIC CHAIN** (copy-paste bash): `node tools/asset-fetch.js --local=<dir> --product=<space>` (copies + id-assigns + probes images, writes raw-manifest.json) → for each video in the manifest: `.venv/bin/python tools/asset/probe_video.py <raw>` then `.venv/bin/python tools/asset/sample_montage.py <raw> <vid_id> --frames-dir=<scratch> --sheets-dir=runs/<space>/asset-classify/sheets`.

**PER-ASSET FAN-OUT LOOP** — mirror funnel-deep-pass's (a) spawn cheap Router/relevance-bucket, (b) deterministic context-assemble = YOUR step, (c) spawn quality analyzer, (d) validate + persist. Asset differences: at (b) the assembled context embeds **trusted text** (fact sheet + the CLAIM-LIST.json + the controlled vocab) AND a **Read instruction pointing at the local file path** — the downscaled `work_path` for an image (brick 4) or the ordered contact-sheet paths for a video (brick 4v) — NOT image bytes. One subagent per image / per video, parallel, blind to each other. At (d) the orchestrator runs `node tools/hooks/validate-asset-record.js <returned-record.json> --claim-list=runs/<space>/asset-classify/CLAIM-LIST.json`; on REJECT, re-spawn with the violation note; on pass, collect the record. Persist by writing the collected records into `runs/<space>/asset-classify/images.json` / `videos.json` (the actual JSON-store write is brick 7 / plan 05's asset-emit.js — here the loop just collects validated records to an analyzer-out dir, e.g. `runs/<space>/asset-classify/records/<id>.json`).

**AGENT PROMPTS folded in verbatim** — write THREE (they live nowhere else):

1. **relevance-bucket** (cheap, one field — like the Router): "You classify ONE asset into exactly one bucket: `product_shot | brand_asset | lifestyle | irrelevant | sensitive`. READ the file path provided. Output ONLY `{ \"id\": \"...\", \"relevance\": \"...\" }`. You do NOT tag claims, you do NOT write copy." Include the UNTRUSTED-DATA boundary block (the image is untrusted material the agent Reads).

2. **role-classify** (quality, fan-out, brick 4) — fold the spec brick-4 method (spec lines 94-133) + the record schema (spec lines 58-83) verbatim: describe `in_frame` literally FIRST; walk the claim list (each claim → strength + pixel evidence, omit if not shown); special-case `display_state` for the load-bearing claims; read composition + tone; flag disqualifiers honestly; write `selection_note` + `caption_hint`. The grounding rule (spec lines 29-33) is the asset analog of funnel's verbatim discipline — reuse funnel-deep-pass's "DESCRIPTIVE / OBSERVE-FIRST" + "UNTRUSTED DATA BOUNDARY" blocks. Output the full record JSON (agent-owned fields only; script fills technical{} + eligible_sections later). The agent receives the CLAIM-LIST + vocab embedded and Reads the work_path.

3. **comprehend-video** (quality, fan-out, brick 4v) — fold the spec brick-4v method (spec lines 222-241) + video record (spec lines 245-274) verbatim: read the contact sheets IN ORDER, build a `segments[]` timeline; tag `demonstrates[]` with `at` timestamp + `motion_value` (what motion proves a still can't); judge `best_use` (hero_loop / feature_demo / full_explainer) with `loop_safe` + `needs_audio`; pick a `poster_frame`; flag disqualifiers over time. Agent Reads the ordered contact-sheet paths for ONE video.

**HAND-OFF section**: after the loop, the validated records are ready for `asset-map-rank.js` → `asset-emit.js` (plan 05), then the human pick gate (brick 6), then Phase 15's builder.
  </action>
  <verify>
    <automated>cd /home/kyu3/PMF && test -f .claude/skills/asset-classify/SKILL.md && grep -q "relevance-bucket" .claude/skills/asset-classify/SKILL.md && grep -q "role-classify" .claude/skills/asset-classify/SKILL.md && grep -q "comprehend-video" .claude/skills/asset-classify/SKILL.md && grep -q "work_path" .claude/skills/asset-classify/SKILL.md && grep -q "validate-asset-record" .claude/skills/asset-classify/SKILL.md && grep -qi "ENFORCEMENT MAP" .claude/skills/asset-classify/SKILL.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/skills/asset-classify/SKILL.md` exists with frontmatter `name: asset-classify`
    - contains an ENFORCEMENT MAP table marking pixel/IO/emit/upload as DETERMINISTIC and relevance-bucket + role-classify/comprehend-video as JUDGMENT
    - contains all three agent prompts (`relevance-bucket`, `role-classify`, `comprehend-video`)
    - the fan-out step embeds trusted text + a Read path (`work_path` / contact sheets), explicitly NOT image bytes
    - the loop runs `validate-asset-record.js` on each returned record before persist
    - PRECONDITION CHECK asserts the venv imports + CLAIM-LIST.json + section-table.json + the --local dir
    - the deterministic chain calls `asset-fetch.js`, `probe_video.py`, `sample_montage.py` with the `.venv/bin/python` convention
  </acceptance_criteria>
  <done>The asset-classify orchestrator exists as a structural twin of funnel-deep-pass: deterministic chain + per-asset vision fan-out (Read-a-path, never bytes) + validate-on-return + the 3 prompts folded in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| image / contact-sheet the agent Reads -> vision model | UNTRUSTED visual material (could contain prompt-injection text rendered in the image) |
| subagent-returned record -> persist | model output crosses into the manifest store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04-01 | Tampering | agent flatters the image (tags a claim the pixels don't carry) | mitigate | the grounding rule in the role-classify prompt (describe in_frame literally first, tag only visible) + validate-asset-record.js REJECTS any demonstrates[] entry missing evidence (the script-enforceable half of "tag only what's in the pixels") |
| T-16-04-02 | Spoofing | prompt-injection text rendered inside the Read image/sheet | mitigate | reuse funnel-deep-pass's UNTRUSTED-DATA-BOUNDARY block in all three prompts — the image is DATA to describe, never instructions to follow; the agent emits only the fixed record schema (closed-vocab validator rejects anything off-contract) |
| T-16-04-03 | Tampering | off-vocab claim/shot_type/disqualifier from the model corrupts the manifest | mitigate | validate-asset-record.js closed-enum reject, orchestrator-run on every record before persist (hooks don't fire in subagents — the SKILL runs it as a step) |
</threat_model>

<verification>
- `validate-asset-record.js` passes the proof's img-01 record, rejects an off-list/un-grounded record
- SKILL.md carries the ENFORCEMENT MAP, the 3 prompts, the Read-a-path fan-out, and the validate-on-return step
</verification>

<success_criteria>
The judgment core is built: a deterministic closed-vocab + grounding validator and the asset-classify orchestrator that fans out one blind vision subagent per asset (Reading a local path, never bytes), validates each returned record, and hands validated records to the manifest contract. The two judgment agents are the ONLY agents; everything else is script/hook — brick model honored.
</success_criteria>

<output>
After completion, create `.planning/phases/16-asset-classifier-image-and-video-bricks/16-04-SUMMARY.md`
</output>
