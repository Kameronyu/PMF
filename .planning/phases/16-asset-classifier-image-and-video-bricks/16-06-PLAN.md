---
phase: 16-asset-classifier-image-and-video-bricks
plan: 06
type: execute
wave: 4
depends_on: [16-02, 16-03, 16-04, 16-05]
files_modified:
  - runs/arduview/asset-classify/PICKS.md
autonomous: false
requirements: [ASSET-10]
must_haves:
  truths:
    - "the full image string runs on the 5 local JPGs and replays the proof: img-04 keeps, img-05 -> dup_of, img-02 strawberry-bg -> disqualifier, no-clean-hero -> gap_list"
    - "the full video string runs on the 3 local MP4s: sheets timestamped, records carry segments[] + motion_value + best_use + poster_frame"
    - "the manifest images.json/videos.json is queryable by claim+strong+clean and IMAGES.md/VIDEOS.md mirror launch format"
    - "the human reads the manifest and makes the per-section picks (brick 6 gate)"
  artifacts:
    - path: "runs/arduview/asset-classify/images.json"
      provides: "the queryable image manifest (UAT output)"
      contains: "records"
    - path: "runs/arduview/asset-classify/videos.json"
      provides: "the queryable video manifest (UAT output)"
      contains: "best_use"
    - path: "runs/arduview/asset-classify/PICKS.md"
      provides: "the human pick-gate decisions (brick 6 — usable by Phase 15)"
  key_links:
    - from: "the manifest"
      to: "the human pick gate"
      via: "operator reads IMAGES.md/VIDEOS.md + gap_list, records per-section picks"
      pattern: "PICKS"
---

<objective>
End-to-end UAT on the local Arduview asset set + the human pick gate (brick 6). This is the phase acceptance, anchored to Success Criteria 3 (a manifest the builder can query) and 5 (the human pick gate). The image string has a known-good expected output (the proof's records + manifest table) — replay it as the acceptance. The video string is the net-new track — its first full run through the orchestrator IS the UAT.

Purpose: Prove the whole 8-brick string works on real files standalone (no Drive), and close the brick-6 human gate. SC1 (right executor per brick), SC2 (grounded claim-tagged record, fan-out), and SC4 (5fps sheets + timeline) are exercised by running the orchestrator; SC3 + SC5 are the gates this plan verifies explicitly.

Output: the populated manifest (`images.json`/`videos.json`/`IMAGES.md`/`VIDEOS.md`) + `PICKS.md` (the operator's per-section picks).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-asset-classifier-image-and-video-bricks/16-RESEARCH.md
@prompts/_specs/image-classifier-brick.md
@runs/arduview/_asset-classify-proof.md
@.claude/skills/asset-classify/SKILL.md
@runs/arduview/asset-classify/CLAIM-LIST.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run the full string on the Arduview set + verify the manifest (SC3)</name>
  <files>runs/arduview/asset-classify/ (manifest outputs)</files>
  <read_first>
    - .claude/skills/asset-classify/SKILL.md (the orchestrator to run — deterministic chain + fan-out)
    - runs/arduview/_asset-classify-proof.md (the known-good expected output — img-01/04/05/02/03 records + manifest table)
    - .planning/phases/16-asset-classifier-image-and-video-bricks/16-RESEARCH.md (Validation Architecture — SC3/SC5 UAT checks, lines 397-413)
    - prompts/_specs/image-classifier-brick.md (I/O contract — what the builder queries, lines 170-184)
  </read_first>
  <action>
Run the asset-classify orchestrator (`.claude/skills/asset-classify/SKILL.md`) end-to-end on the local Arduview set at `/mnt/c/Users/kyu3/Downloads/arduview visuals/` with `--space=arduview`:

1. Deterministic chain: `node tools/asset-fetch.js --local="/mnt/c/Users/kyu3/Downloads/arduview visuals/" --product=arduview` → probe.py per image (in fetch) → per video: `probe_video.py` + `sample_montage.py` → contact sheets in `runs/arduview/asset-classify/sheets/`.
2. Run `probe.py --dedupe` across the image records to fill `dup_of` (img-04/img-05).
3. Per-asset fan-out: relevance-bucket then role-classify (one subagent per image, Reading `assets/work/<id>.jpg`) / comprehend-video (one subagent per video, Reading the ordered sheets). Validate each returned record with `validate-asset-record.js`.
4. `node tools/asset-map-rank.js --space=arduview` → `node tools/asset-emit.js --space=arduview`.

Then VERIFY the manifest replays the proof (the image-string known-good acceptance):
- `images.json` records exist for the 5 JPGs with grounded `demonstrates[]` (claim+strength+evidence).
- **img-04 keeps, img-05 → `dup_of: arduview-img-04`** (the perceptual-hash dedupe case).
- **img-02 (strawberry-fabric bg) → non-empty `disqualifiers`** (busy/offbrand background gating hero use).
- **`hero` in the gap_list** — no clean-studio hero (every Arduview shot has a busy/novelty background).
- Query check: filter `images.json` for `claim == transparency_internals_visible && strength == strong && disqualifiers == []` returns the right clean shot(s).
- `videos.json` records carry `segments[]` + `motion_value` + `best_use` + `poster_frame`; `IMAGES.md`/`VIDEOS.md` mirror launch format.
  </action>
  <what-built>
The full 8-brick asset-classify string run on the real Arduview set (5 JPGs + 3 MP4s) standalone — fetch → probe → vision fan-out → validate → map+rank → emit. Output: images.json/videos.json + IMAGES.md/VIDEOS.md + gap_list.
  </what-built>
  <how-to-verify>
1. Open `runs/arduview/asset-classify/images.json` — confirm 5 image records, each with grounded `demonstrates[]` (claim + strength + pixel evidence).
2. Confirm the proof replay:
   - img-05 has `dup_of` pointing at img-04 (dedupe caught it)
   - img-02 has a non-empty `disqualifiers` (strawberry background)
   - `gap_list` contains `hero` (no clean-studio hero)
3. Open `runs/arduview/asset-classify/IMAGES.md` — confirm `## <section>` tables in launch format + a `## Gaps` section.
4. Open `runs/arduview/asset-classify/videos.json` — confirm each video has `segments[]`, `best_use`, `poster_frame`.
5. Spot-check one `videos.json` record against its contact sheets — does the timeline match what the sheets show?
6. Confirm the claim-query works: a builder filtering by claim+strong+clean gets the right asset.
  </how-to-verify>
  <verify>
    <automated>cd /home/kyu3/PMF && node -e "const im=require('./runs/arduview/asset-classify/images.json'); const byId=Object.fromEntries(im.records.map(r=>[r.id,r])); const dup=im.records.find(r=>r.dup_of); if(!dup) throw 'no dedupe (expected img-05 dup_of img-04)'; if(!im.gap_list.some(g=>(g.section||g)==='hero'||/hero/.test(JSON.stringify(g)))) throw 'hero not in gap_list'; const disq=im.records.find(r=>Array.isArray(r.disqualifiers)&&r.disqualifiers.length>0); if(!disq) throw 'no disqualifier record (expected img-02 strawberry bg)'; const clean=im.records.filter(r=>(r.disqualifiers||[]).length===0 && !r.dup_of && (r.demonstrates||[]).some(d=>d.claim==='transparency_internals_visible'&&d.strength==='strong')); if(!clean.length) throw 'claim-query returned nothing'; console.log('OK: dedupe+disqualifier+gap+query all present')"</automated>
  </verify>
  <acceptance_criteria>
    - `runs/arduview/asset-classify/images.json` has 5 image records with grounded `demonstrates[]`
    - img-05 carries `dup_of` (dedupe), img-02 carries non-empty `disqualifiers`, `hero` is in `gap_list`
    - the claim+strong+clean query returns the correct clean transparency shot
    - `videos.json` records carry `segments[]` + `best_use` + `poster_frame`
    - `IMAGES.md`/`VIDEOS.md` mirror launch format with a Gaps section
    - operator confirms (checkpoint) the manifest replays the proof and the video records match the sheets
  </acceptance_criteria>
  <resume-signal>Type "approved" if the manifest replays the proof (dedupe, disqualifier, gap) and the video records match the contact sheets, or describe the discrepancy.</resume-signal>
  <done>The full string runs on the Arduview set; the manifest is queryable by claim, replays the proof's dedupe/disqualifier/gap, and carries the video timelines (SC3 met, operator-verified).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Human pick gate (brick 6) — operator picks per section (SC5)</name>
  <files>runs/arduview/asset-classify/PICKS.md</files>
  <read_first>
    - runs/arduview/asset-classify/IMAGES.md (the manifest the operator reads to pick)
    - runs/arduview/asset-classify/VIDEOS.md (the video manifest + hero_loop candidates)
    - runs/arduview/_asset-classify-proof.md (the proof's "Manifest the builder would consume" picks — the expected ballpark)
    - prompts/_specs/image-classifier-brick.md (brick 6 — "we discuss what pics go in it", line 48)
  </read_first>
  <action>
This is the one human judgment in the string (brick 6 — the pick gate). The operator reads the ranked manifest + gap_list and makes the per-section picks. Claude PRESENTS the ranked candidates per section (from `ranked.json` / `IMAGES.md` / `VIDEOS.md`) and the gap_list, then RECORDS the operator's decisions — Claude does NOT make the picks (it's the human gate).

Present, per section:
- the ranked candidate(s) with their claim+strength + selection_note + any disqualifiers
- the gap_list (sections needing a reshoot/source — e.g. no clean-studio hero)
- for video: the hero_loop candidate(s) with loop_safe + needs_audio

Record the operator's picks to `runs/arduview/asset-classify/PICKS.md` (a small decision log: `## <section>` → chosen asset id + rationale + any "reshoot needed" notes). The picks must be usable by Phase 15's builder (claim-keyed, with the gap notes telling the operator what to source before the LP can be built).

Ask the operator before committing anything (CLAUDE.md). The picks are the operator's call; Claude only assembles the options and writes down the decision.
  </action>
  <what-built>
The ranked manifest + gap_list presented to the operator; the per-section picks recorded to PICKS.md. This closes the brick-6 human gate — the only human judgment in the string.
  </what-built>
  <how-to-verify>
1. Claude presents each section's ranked candidates + the gap_list (hero reshoot flag).
2. Operator decides the pick per section (e.g. hero: img-01; feature_transparency: img-04; screen-legible callout: img-02 cropped; hero_loop: the transparency+screen video).
3. Operator confirms the picks are recorded correctly in `runs/arduview/asset-classify/PICKS.md`.
4. Operator confirms the picks + gap notes are usable by Phase 15 (claim-keyed, reshoot flags clear).
  </how-to-verify>
  <verify>
    <automated>cd /home/kyu3/PMF && test -f runs/arduview/asset-classify/PICKS.md && grep -q "## " runs/arduview/asset-classify/PICKS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Claude presented ranked candidates + gap_list per section (did NOT auto-pick)
    - `runs/arduview/asset-classify/PICKS.md` exists with per-section operator decisions + rationale + reshoot notes
    - the picks are claim-keyed and usable by Phase 15
    - operator confirms the gate is closed
  </acceptance_criteria>
  <resume-signal>Type "approved" once the per-section picks are recorded in PICKS.md and you confirm they're usable by Phase 15.</resume-signal>
  <done>The human pick gate is closed: the operator made the per-section picks against the ranked manifest + gap_list, recorded to PICKS.md, usable by Phase 15 (SC5 met).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator local asset set -> the full string | the UAT runs the whole pipeline on operator media (covered by per-brick threat models in plans 02-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06-01 | Tampering | manifest claims an asset proves a claim it doesn't (mis-pick on a load-bearing claim) | mitigate | the grounding validator (plan 04) already rejected un-grounded records; the human pick gate (this plan) is the final backstop — the operator eyeballs each pick against the claim before it ships to Phase 15 |
| T-16-06-02 | Repudiation | no record of why a section was left empty (gap) | mitigate | gap_list + PICKS.md reshoot notes create an auditable record of what was deferred and why |
</threat_model>

<verification>
- the full string runs on the Arduview set; manifest replays the proof (dedupe/disqualifier/gap) — SC3
- the operator makes the per-section picks recorded to PICKS.md — SC5
</verification>

<success_criteria>
Phase 16 acceptance: the 8-brick string runs standalone on the real Arduview set (5 JPGs + 3 MP4s), produces a manifest the builder can query by claim+strength with dedupe/disqualifiers/gap_list, the video bricks deliver 5fps timestamped sheets + segment timelines, and the human closes the pick gate with per-section picks usable by Phase 15.
</success_criteria>

<output>
After completion, create `.planning/phases/16-asset-classifier-image-and-video-bricks/16-06-SUMMARY.md`
</output>
