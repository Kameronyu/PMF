---
phase: 19-cleanup-health-nuke-e-ink-quarantine-eink-tablets-quarantine
plan: 03
type: execute
wave: 2
depends_on: ["19-02"]
files_modified:
  - _quarantine/
  - launch/
autonomous: true
requirements: []
must_haves:
  truths:
    - "All e-ink-era artifacts (_quarantine/runs/eink-tablets, _quarantine/archive, launch/inkleaf-*, launch/README.md) are gone from the working tree"
    - "Deletions are committed (git history is the archive — D-01); no _archive/ dir is created"
  artifacts: []
  key_links:
    - from: "git history"
      to: "deleted e-ink artifacts"
      via: "git rm (tracked) — recoverable from history, not the working tree"
      pattern: ""
---

<objective>
Hard-delete the e-ink-era artifacts (D-01, D-02): the `_quarantine/` quarantine dirs and the entire `launch/` tree. Git history is the archive — NO `_archive/` directory is created (D-01 overrides the POST-RUN-HARDENING-PLAN "archive" wording).

Purpose: InkLeaf is retired; `_quarantine/runs/eink-tablets/` (660 tracked files / 160M) and `_quarantine/archive/` are quarantined dead weight, and all of `launch/` is InkLeaf/e-ink-era with zero active hard deps (reference-checked clear in `doc-index-phases-launch.md`). The only blocker — the `image-classifier-brick.md` cite — was severed in 19-02 (depends_on).

Output: `_quarantine/` and `launch/` removed from the working tree, committed. Atomic commits per group (D's discretion) so each is independently revertable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-cleanup-health-nuke-e-ink-quarantine-eink-tablets-quarantine/19-CONTEXT.md

# Authoritative delete scope + reference-check verdict (read before deleting):
@.planning/intel/INDEX.md
@.planning/intel/doc-index-phases-launch.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hard-delete _quarantine/ (eink-tablets + archive) — git rm</name>
  <read_first>
    - .planning/intel/INDEX.md (NUKE list — confirms _quarantine/runs/eink-tablets and _quarantine/archive are reference-checked clear)
    - .planning/intel/doc-index-phases-launch.md JOB 2 (verdict: SAFE TO NUKE, zero runtime references from any active script/agent)
  </read_first>
  <files>_quarantine/</files>
  <action>
Confirm precondition then delete. `_quarantine/` is 660 tracked files / ~160M → use `git rm` (D-02, tracked → git rm).

1. Sanity: `git ls-files _quarantine/ | wc -l` should report ~660. `ls -d _quarantine/runs/eink-tablets _quarantine/archive` should both exist.
2. Confirm nothing active loads from `_quarantine/`: `grep -rl "_quarantine/" --include=*.js --include=*.cjs --include=*.md prompts/ tools/ .claude/skills/ runs/arduview/_tooling/ 2>/dev/null` — expected: only tombstone MENTIONS in .planning/* docs (ROADMAP/STATE/PROJECT/audit), never a code load path. If any active SCRIPT path loads `_quarantine/`, STOP and flag (it would contradict the reference-check). Doc mentions are fine — they are not deps.
3. Delete: `git rm -r _quarantine/`
4. Verify removed: `test ! -e _quarantine` (or `! git ls-files _quarantine/ | grep -q .`).
5. Commit this group atomically: `git commit -m "chore(19): nuke _quarantine e-ink artifacts (eink-tablets + archive) per D-01"` — ASK the operator before committing per project CLAUDE.md (ask-before-commit), or stage and report for operator confirmation.
  </action>
  <verify>
    <automated>test ! -e _quarantine && test "$(git ls-files _quarantine/ | wc -l)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e _quarantine` — directory gone from working tree
    - `git ls-files _quarantine/ | wc -l` returns 0 — no tracked files remain
    - No active script load-path referenced `_quarantine/` (grep over prompts/ tools/ .claude/skills/ runs/arduview/_tooling/ returned only doc-mention rows, not code loads) — verified pre-delete
  </acceptance_criteria>
  <done>_quarantine/ removed and staged/committed; no tracked files remain; reference-check confirmed no active dep.</done>
</task>

<task type="auto">
  <name>Task 2: Hard-delete launch/ (inkleaf-landing + inkleaf-launch incl. _deep-pass + README.md)</name>
  <read_first>
    - .planning/intel/doc-index-phases-launch.md JOB 1b (all launch/ = EINK, no durable content not captured elsewhere)
    - prompts/_specs/image-classifier-brick.md (CONFIRM 19-02 landed: `grep "launch/inkleaf" prompts/_specs/image-classifier-brick.md` MUST return nothing before deleting)
  </read_first>
  <files>launch/</files>
  <action>
Precondition gate (D-13): `! grep -rq "launch/inkleaf" prompts/_specs/image-classifier-brick.md` — the cite-fix (19-02) MUST be in place. If the grep still finds the path, STOP — 19-02 did not land; do not delete.

`launch/` = 36 tracked files + untracked `launch/inkleaf-launch/_deep-pass/`. Mixed tracked/untracked → handle both (D-02):

1. Tracked: `git rm -r launch/` removes the 36 tracked files (inkleaf-landing/, inkleaf-launch/ tracked content, README.md).
2. Untracked leftovers: the untracked `_deep-pass/` (and any other untracked files under launch/) survive `git rm` → remove with plain `rm`: `rm -rf launch/` (clears any untracked remainder + the now-empty dir).
3. Verify: `test ! -e launch`.
4. Atomic commit: `git commit -m "chore(19): nuke launch/ inkleaf e-ink era (landing + launch + README) per D-01/D-13"` — ASK operator before committing.
  </action>
  <verify>
    <automated>! grep -rq "launch/inkleaf" prompts/_specs/image-classifier-brick.md && test ! -e launch && test "$(git ls-files launch/ | wc -l)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - Cite-fix precondition met: `! grep -rq "launch/inkleaf" prompts/_specs/image-classifier-brick.md` exits 0
    - `test ! -e launch` — entire tree (tracked + untracked _deep-pass) gone
    - `git ls-files launch/ | wc -l` returns 0
    - No `_archive/` dir created anywhere (D-01): `test ! -e _archive`
  </acceptance_criteria>
  <done>launch/ fully removed (tracked via git rm, untracked _deep-pass via rm); no _archive/ created; staged/committed after operator OK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Destructive cleanup. No network/auth/input surface — stated explicitly. Dominant threat = irreversible deletion of a load-bearing file, or deleting before a dependent cite is severed.

| Boundary | Description |
|----------|-------------|
| working tree → git history | Tracked deletions recoverable from history (D-01 intent); untracked deletions (`_deep-pass/`) are NOT recoverable — extra caution |
| spec → launch/ | The 19-02 cite-fix must precede this delete (enforced by depends_on + a grep gate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | I (irreversible loss) | Deleting launch/ while a live dep still references it | mitigate | depends_on 19-02 + a hard grep precondition gate on `launch/inkleaf` in the spec before the delete runs. |
| T-19-08 | I (unrecoverable untracked loss) | Untracked `_deep-pass/` rm is permanent | accept | Reference-check (doc-index JOB 2) confirms all launch/ is EINK with no durable content; operator chose hard-delete (D-01). Accepted with rationale. |
| T-19-09 | T (glob over-match) | `rm -rf launch/` or `git rm -r` over-reaching | mitigate | Paths are exact top-level dirs (`_quarantine/`, `launch/`), not globs; `git status`/`git ls-files` reviewed before commit; atomic per-group commits keep each revertable. |
| T-19-10 | E (privilege) | n/a | accept | No privilege boundary; local file ops only. |
</threat_model>

<verification>
- `_quarantine/` and `launch/` absent from working tree; `git ls-files` shows zero remaining tracked files under either.
- No `_archive/` directory exists.
- `git status` reviewed; only the intended deletions staged before each atomic commit.
- Operator approved commits (project ask-before-commit rule).
</verification>

<success_criteria>
Every e-ink-era artifact named in the NUKE list is gone from the working tree and the deletions are committed atomically per group. Git history is the only archive (D-01).
</success_criteria>

<output>
After completion, create `.planning/phases/19-cleanup-health-nuke-e-ink-quarantine-eink-tablets-quarantine/19-03-SUMMARY.md`
</output>
