---
phase: 19-cleanup-health-nuke-e-ink-quarantine-eink-tablets-quarantine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - prompts/_specs/image-classifier-brick.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "image-classifier-brick.md no longer cites launch/inkleaf-launch/IMAGES.md, so deleting launch/ in 19-03 leaves no dangling reference"
    - "The spec still conveys the IMAGES.md manifest format (section -> role -> file -> CDN slot) without depending on the deleted file"
  artifacts:
    - path: "prompts/_specs/image-classifier-brick.md"
      provides: "Active image-classifier spec with the launch/ cite inlined as a self-contained format snippet"
      contains: "section -> role -> file"
  key_links:
    - from: "prompts/_specs/image-classifier-brick.md"
      to: "self-contained manifest format example"
      via: "inlined snippet replacing the launch/inkleaf-launch/IMAGES.md citation"
      pattern: "IMAGES.md"
---

<objective>
Fix the one cite in `prompts/_specs/image-classifier-brick.md` that references `launch/inkleaf-launch/IMAGES.md` (D-13). This is the only hard touch-point blocking the `launch/` delete in plan 19-03 — it MUST land first.

Purpose: the spec cites InkLeaf's `IMAGES.md` as a *format example* (illustrative, not a data dependency). Once `launch/` is hard-deleted the cite would dangle. Per D-13 + Claude's discretion: inline a minimal format snippet so the spec is self-contained.

Output: edited `prompts/_specs/image-classifier-brick.md` with the stale path replaced by an inline format example. The spec stays a MUST-KEEP active doc — only the cite changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-cleanup-health-nuke-e-ink-quarantine-eink-tablets-quarantine/19-CONTEXT.md

# The spec to edit (the cite is on line ~11 and again referenced in the brick-4 record discussion):
@prompts/_specs/image-classifier-brick.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inline the IMAGES.md format snippet, remove the launch/ path cite</name>
  <read_first>
    - prompts/_specs/image-classifier-brick.md (FULL — locate every occurrence of `launch/inkleaf-launch/IMAGES.md`; there is the prose cite near line 11 "The format target is the worked `launch/inkleaf-launch/IMAGES.md`" and the InkLeaf hand-cataloguing references)
    - runs/arduview/asset-classify/IMAGES.md (a kept, live example of the exact manifest format — use it to author an accurate inline snippet)
  </read_first>
  <files>prompts/_specs/image-classifier-brick.md</files>
  <action>
Two surgical edits, no scope creep (this is a MUST-KEEP spec — change only the cite):

1. Replace the prose at line ~11 that reads `The format target is the worked `launch/inkleaf-launch/IMAGES.md` (section -> role -> file), but the **classification is richer than InkLeaf's hand-cataloguing**...` so it NO LONGER names the `launch/` path. Replace with an inline self-contained format target. Concretely, change it to describe the manifest shape directly and reference the live in-repo example instead of the deleted InkLeaf one. Example replacement text:

   `The output format is a section -> role -> file -> CDN-slot manifest (the **classification is richer than a hand-catalogue** so the builder can retrieve images by the *claim they demonstrate*, not just the slot). Minimal manifest shape:`

   immediately followed by a fenced markdown snippet so the spec carries its own format example (drawn from runs/arduview/asset-classify/IMAGES.md):

   ```markdown
   ## hero
   - role: primary proof | file: arduview-0007.png | claim: transparency_internals_visible | cdn: <slot>

   ## feature_transparency
   - role: internals macro | file: arduview-0012.png | claim: transparency_internals_visible | cdn: <slot>
   ```

   (Adjust the example rows to match the real shape in runs/arduview/asset-classify/IMAGES.md — keep it 2-4 rows, illustrative.)

2. Scan the rest of the file for any other literal `launch/inkleaf` path string (e.g. the "InkLeaf 'best for screen-content callout' move" mention near brick-4 method is a stylistic reference to a *move*, not a path — leave prose that doesn't name a `launch/` path). The ONLY hard requirement: zero occurrences of the literal substring `launch/inkleaf` remain in the file. Generic "InkLeaf" brand mentions in prose that don't reference the deleted directory may stay, but prefer to neutralize them to "a hand-catalogue" where trivial — do not rewrite the spec's substance.

Do NOT touch any other section (the brick string, classification record, vocabularies, video extension all stay verbatim).
  </action>
  <verify>
    <automated>! grep -q "launch/inkleaf" prompts/_specs/image-classifier-brick.md && grep -q "section -> role -> file" prompts/_specs/image-classifier-brick.md && test -f prompts/_specs/image-classifier-brick.md</automated>
  </verify>
  <acceptance_criteria>
    - Stale path gone: `grep -c "launch/inkleaf" prompts/_specs/image-classifier-brick.md` returns 0 (command `! grep -q "launch/inkleaf" prompts/_specs/image-classifier-brick.md` exits 0)
    - Format still conveyed: `grep -q "section -> role -> file" prompts/_specs/image-classifier-brick.md`
    - File intact and substantial: `test "$(wc -l < prompts/_specs/image-classifier-brick.md)" -ge 280` (spec was ~298 lines; edits are small)
    - `launch/` NOT yet deleted (that is 19-03): `test -d launch`
  </acceptance_criteria>
  <done>The literal substring `launch/inkleaf` no longer appears in the spec; the manifest format is inlined; the spec is otherwise unchanged; launch/ still present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Documentation edit only. No new network/auth/input surface — stated explicitly. Threat class = breaking an active MUST-KEEP spec or leaving a dangling reference.

| Boundary | Description |
|----------|-------------|
| spec ↔ launch/ directory | The spec referenced a file about to be deleted; the boundary is the cite that must be severed cleanly before deletion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | I (dangling ref / info loss) | launch/ deleted while spec still cites it | mitigate | This plan severs the cite and inlines the format; 19-03 (launch delete) depends_on this plan, enforced by wave ordering. Grep gate proves zero `launch/inkleaf` substrings remain. |
| T-19-05 | T (over-edit of MUST-KEEP spec) | Accidentally rewriting spec substance | mitigate | Edits scoped to the cite line + a stray-path scan; line-count floor (≥280) blocks a gutted spec; acceptance keeps `section -> role -> file` present. |
| T-19-06 | D | n/a runtime | accept | Static spec, no execution surface. |
</threat_model>

<verification>
- `grep "launch/inkleaf" prompts/_specs/image-classifier-brick.md` returns nothing.
- Spec retains its format description and full length.
- `launch/` still on disk (deleted in 19-03).
</verification>

<success_criteria>
The image-classifier spec is self-contained on the manifest format and carries no reference into the soon-to-be-deleted `launch/` tree, unblocking the launch delete in 19-03.
</success_criteria>

<output>
After completion, create `.planning/phases/19-cleanup-health-nuke-e-ink-quarantine-eink-tablets-quarantine/19-02-SUMMARY.md`
</output>
