---
phase: 19-cleanup-health-nuke-e-ink-quarantine-eink-tablets-quarantine
plan: 04
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - .planning/BUILD-STATE.md
  - map/data_inventory.md
  - MARKETING-LENS.md
  - drive.cjs
  - .claude/skills/market-selection/mechanisms-in-play-stopgap.md
  - runs/arduview/STRATEGY-DISCUSS-HANDOFF.md
  - runs/arduview/RERUN-BRIEF.md
  - runs/arduview/_asset-classify-proof.md
  - runs/arduview/brand-refs/
  - runs/arduview/funnels-context/
  - runs/arduview/asset-classify/sheets/
  - ads/
  - .planning/phases/01-stage-m1-s1-light-pass/01-01-SUMMARY.pre-revision.md
  - .planning/phases/01-stage-m1-s1-light-pass/01-02-SUMMARY.pre-revision.md
  - .planning/phases/01-stage-m1-s1-light-pass/01-04-SUMMARY.pre-revision.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every SLOP doc named in the delete lists is gone (BUILD-STATE.md, mechanisms-in-play-stopgap.md, STRATEGY-DISCUSS-HANDOFF.md, RERUN-BRIEF.md, brand-refs/*, funnels-context/*, MARKETING-LENS.md, _asset-classify-proof.md, 3 pre-revision snapshots)"
    - "map/data_inventory.md is deleted ONLY after the 19-01 keeper exists and verifies"
    - "Junk binaries are gone (asset-classify/sheets ~23M, ads/*_adv.png, dup root drive.cjs)"
    - "The 2 PROMOTE items (15-DEBUG-funnel-architect.md, 01-DEBUG-RUN-NOTES.md) are NOT deleted (D-14)"
  artifacts: []
  key_links:
    - from: "map/data-model-notes.md (keeper from 19-01)"
      to: "map/data_inventory.md deletion gate"
      via: "distill-before-delete precondition (D-06): keeper must exist before source is removed"
      pattern: "data-model-notes"
---

<objective>
Hard-delete the non-e-ink SLOP docs and junk binaries (D-01, D-02, D-11), and — gated on the 19-01 keeper — delete `map/data_inventory.md` and `.planning/BUILD-STATE.md` (D-06). Tracked → `git rm`; untracked → `rm`.

Purpose: clear superseded/ephemeral docs and scratch binaries the delete lists flag, after their durable content has been preserved (keeper from 19-01). The PROMOTE items are explicitly OUT of scope (D-14) — they must survive untouched.

Output: all SLOP/junk targets removed, committed atomically per group. The 2 PROMOTE items remain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-cleanup-health-nuke-e-ink-quarantine-eink-tablets-quarantine/19-CONTEXT.md

# Authoritative delete scope (read before deleting — these list the exact SLOP rows):
@.planning/intel/INDEX.md
@.planning/intel/doc-index-core.md
@.planning/intel/doc-index-runs.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete data_inventory.md + BUILD-STATE.md (GATED on 19-01 keeper) — D-06</name>
  <read_first>
    - map/data-model-notes.md (the 19-01 keeper — MUST exist and contain the 5 decision subsections before this delete runs)
    - .planning/intel/INDEX.md (DISTILL-THEN-ARCHIVE row for data_inventory.md; SLOP row for BUILD-STATE.md)
  </read_first>
  <files>map/data_inventory.md, .planning/BUILD-STATE.md</files>
  <action>
HARD GATE (D-06): verify the keeper landed before removing either source:
`test -f map/data-model-notes.md && grep -q "hypothesis-record schema" map/data-model-notes.md && grep -q "author_id-join-as-heaviest" map/data-model-notes.md`
If this fails, STOP — 19-01 did not complete; do not delete the sources.

Both are tracked → `git rm` (D-02):
1. `git rm map/data_inventory.md`
2. `git rm .planning/BUILD-STATE.md`
3. Verify: `test ! -e map/data_inventory.md && test ! -e .planning/BUILD-STATE.md`
4. Atomic commit (ASK operator first): `git commit -m "chore(19): distill+delete data_inventory.md and BUILD-STATE.md; keeper at map/data-model-notes.md (D-03/D-04/D-06)"`
  </action>
  <verify>
    <automated>test -f map/data-model-notes.md && grep -q "hypothesis-record schema" map/data-model-notes.md && test ! -e map/data_inventory.md && test ! -e .planning/BUILD-STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - Keeper precondition: `test -f map/data-model-notes.md && grep -q "hypothesis-record schema" map/data-model-notes.md` exits 0 (verified BEFORE delete)
    - `test ! -e map/data_inventory.md` — source removed
    - `test ! -e .planning/BUILD-STATE.md` — superseded build-state removed
    - Both untracked from git: `git ls-files map/data_inventory.md .planning/BUILD-STATE.md | wc -l` returns 0
  </acceptance_criteria>
  <done>data_inventory.md and BUILD-STATE.md deleted only after the keeper verified; staged/committed after operator OK.</done>
</task>

<task type="auto">
  <name>Task 2: Delete SLOP docs (tracked → git rm; untracked → rm)</name>
  <read_first>
    - .planning/intel/doc-index-core.md (SLOP rows: MARKETING-LENS.md, mechanisms-in-play-stopgap.md)
    - .planning/intel/doc-index-runs.md (SLOP rows: STRATEGY-DISCUSS-HANDOFF.md, RERUN-BRIEF.md, brand-refs/*, funnels-context/*, _asset-classify-proof.md)
    - .planning/intel/doc-index-phases-launch.md (the 3 .pre-revision.md snapshots are SLOP; 01-DEBUG-RUN-NOTES.md is PROMOTE — DO NOT DELETE)
  </read_first>
  <files>MARKETING-LENS.md, .claude/skills/market-selection/mechanisms-in-play-stopgap.md, runs/arduview/STRATEGY-DISCUSS-HANDOFF.md, runs/arduview/RERUN-BRIEF.md, runs/arduview/_asset-classify-proof.md, runs/arduview/brand-refs/, runs/arduview/funnels-context/, .planning/phases/01-stage-m1-s1-light-pass/01-01-SUMMARY.pre-revision.md, .planning/phases/01-stage-m1-s1-light-pass/01-02-SUMMARY.pre-revision.md, .planning/phases/01-stage-m1-s1-light-pass/01-04-SUMMARY.pre-revision.md</files>
  <action>
Per-file, check tracked status then delete (D-02). Known statuses from this session's filesystem scan: `MARKETING-LENS.md` = TRACKED; the rest below are tracked GSD/run artifacts → confirm with `git ls-files --error-unmatch <path>` and use `git rm` for tracked, `rm` for untracked.

Tracked → `git rm` (these are committed docs):
- `git rm MARKETING-LENS.md` (root redirect stub — content lives in marketing-lens/)
- `git rm .claude/skills/market-selection/mechanisms-in-play-stopgap.md` (RETIRED "do NOT run" stopgap)
- `git rm runs/arduview/STRATEGY-DISCUSS-HANDOFF.md` (ephemeral session brief, work complete)
- `git rm runs/arduview/RERUN-BRIEF.md` (one-shot handoff, re-run done)
- `git rm runs/arduview/_asset-classify-proof.md` (superseded by full asset-classify pass)
- `git rm -r runs/arduview/brand-refs/` (nothing.md, playdate.md, teenage-engineering.md — reconciled into site/STYLE-LOCK.md)
- `git rm -r runs/arduview/funnels-context/` (4 machine-gen analyzer input blobs — outputs live in funnel store)
- `git rm .planning/phases/01-stage-m1-s1-light-pass/01-01-SUMMARY.pre-revision.md .planning/phases/01-stage-m1-s1-light-pass/01-02-SUMMARY.pre-revision.md .planning/phases/01-stage-m1-s1-light-pass/01-04-SUMMARY.pre-revision.md` (vestigial pre-edit snapshots)

For any path that `git ls-files --error-unmatch` reports as NOT tracked, use plain `rm`/`rm -r` instead of `git rm`.

DO NOT DELETE (D-14 PROMOTE, out of scope — guard against glob over-reach):
- `.planning/phases/01-stage-m1-s1-light-pass/01-DEBUG-RUN-NOTES.md` (PROMOTE — keep)
- `runs/arduview/15-DEBUG-funnel-architect.md` (PROMOTE — keep)

After deletes, review `git status --short` to confirm ONLY the intended SLOP paths are staged/removed and the two PROMOTE files are untouched. Atomic commit (ASK operator first): `git commit -m "chore(19): remove slop docs (stubs, ephemeral handoffs, reconciled brand-refs, machine-gen context, pre-revision snapshots) per D-01"`.
  </action>
  <verify>
    <automated>test ! -e MARKETING-LENS.md && test ! -e .claude/skills/market-selection/mechanisms-in-play-stopgap.md && test ! -e runs/arduview/STRATEGY-DISCUSS-HANDOFF.md && test ! -e runs/arduview/RERUN-BRIEF.md && test ! -e runs/arduview/_asset-classify-proof.md && test ! -e runs/arduview/brand-refs && test ! -e runs/arduview/funnels-context && test ! -e .planning/phases/01-stage-m1-s1-light-pass/01-01-SUMMARY.pre-revision.md && test -f .planning/phases/01-stage-m1-s1-light-pass/01-DEBUG-RUN-NOTES.md && test -f runs/arduview/15-DEBUG-funnel-architect.md</automated>
  </verify>
  <acceptance_criteria>
    - All SLOP paths gone: each `test ! -e <path>` passes for MARKETING-LENS.md, mechanisms-in-play-stopgap.md, STRATEGY-DISCUSS-HANDOFF.md, RERUN-BRIEF.md, _asset-classify-proof.md, brand-refs/, funnels-context/, and all 3 .pre-revision.md
    - PROMOTE survivors intact (D-14): `test -f .planning/phases/01-stage-m1-s1-light-pass/01-DEBUG-RUN-NOTES.md` AND `test -f runs/arduview/15-DEBUG-funnel-architect.md`
    - `git status --short` shows only intended removals (reviewed pre-commit)
  </acceptance_criteria>
  <done>All SLOP docs removed (tracked via git rm, untracked via rm); both PROMOTE files untouched; staged/committed after operator OK.</done>
</task>

<task type="auto">
  <name>Task 3: Delete junk binaries (asset-classify/sheets ~23M, ads/*_adv.png, dup root drive.cjs)</name>
  <read_first>
    - .planning/intel/INDEX.md (junk binaries: asset-classify/sheets ~23M, dup root drive.cjs)
    - 19-CONTEXT.md <domain> (junk binaries list: asset-classify/sheets, ads/*_adv.png, dup root drive.cjs)
  </read_first>
  <files>runs/arduview/asset-classify/sheets/, ads/, drive.cjs</files>
  <action>
Check tracked status per target, then delete (D-02). Known from this session's scan: `runs/arduview/asset-classify/sheets` has 3 TRACKED files (+ ~23M untracked media); `ads/*_adv.png` (anbernic/divoom/flipper-zero/nothing-phone) are TRACKED; root `drive.cjs` is UNTRACKED (the kept copy is `runs/arduview/_tooling/drive.cjs`).

1. Sheets (montage contact sheets, ~23M regenerable): `git rm -r runs/arduview/asset-classify/sheets/` for tracked content, then `rm -rf runs/arduview/asset-classify/sheets/` to clear untracked media + empty dir. Verify `test ! -e runs/arduview/asset-classify/sheets`.
2. Ad screenshots (tracked binaries): `git rm ads/anbernic_adv.png ads/divoom_adv.png ads/flipper-zero_adv.png ads/nothing-phone_adv.png` (or `git rm ads/*_adv.png`). These are the ad-library PNG dumps; the `.gitignore` already has an `ads/*_adv.png` rule (line 5) so future copies stay ignored — this removes the committed ones. Leave any non-`_adv.png` files in `ads/` untouched unless the dir is now empty.
3. Dup root drive.cjs (UNTRACKED — confirm the kept copy survives): `test -f runs/arduview/_tooling/drive.cjs` MUST pass first, then `rm drive.cjs`. Do NOT touch `runs/arduview/_tooling/drive.cjs`.
4. Review `git status --short`; atomic commit (ASK operator first): `git commit -m "chore(19): remove junk binaries (contact sheets ~23M, ad screenshots, dup root drive.cjs) per D-01/D-11"`.

GUARD: `_caption_*` scratch and `_*-log.txt` are handled in plan 19-05 (gitignore group) — do NOT delete them here, to keep group boundaries clean.
  </action>
  <verify>
    <automated>test ! -e runs/arduview/asset-classify/sheets && test ! -e drive.cjs && test -f runs/arduview/_tooling/drive.cjs && test "$(ls ads/*_adv.png 2>/dev/null | wc -l)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e runs/arduview/asset-classify/sheets` — sheets dir + media gone
    - `ls ads/*_adv.png 2>/dev/null | wc -l` returns 0 — ad screenshots removed
    - `test ! -e drive.cjs` (root) AND `test -f runs/arduview/_tooling/drive.cjs` — dup removed, canonical copy preserved
    - `git ls-files runs/arduview/asset-classify/sheets ads/*_adv.png | wc -l` returns 0
  </acceptance_criteria>
  <done>Junk binaries removed (tracked via git rm, untracked via rm); canonical drive.cjs in _tooling preserved; staged/committed after operator OK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Destructive cleanup. No network/auth/input surface — stated explicitly. Threats: deleting a source before its keeper exists, glob over-match deleting a MUST-KEEP/PROMOTE file, deleting the canonical drive.cjs by mistake.

| Boundary | Description |
|----------|-------------|
| working tree → git history | Tracked deletes recoverable; untracked deletes (sheets media, root drive.cjs) permanent |
| keeper → data_inventory.md delete | D-06 gate: keeper must exist + verify before source removal |
| SLOP glob → PROMOTE/MUST-KEEP files | brand-refs/, funnels-context/, .pre-revision.md, ads/*_adv.png must not catch 01-DEBUG-RUN-NOTES.md, 15-DEBUG-funnel-architect.md, or non-junk ads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-11 | I (loss before preserve) | data_inventory.md/BUILD-STATE.md deleted before keeper exists | mitigate | depends_on 19-01 + hard grep gate on the keeper's required sections before Task 1 deletes. |
| T-19-12 | I (wrong-file deletion) | A glob (`ads/*_adv.png`, `git rm -r brand-refs/`) over-matches a keeper | mitigate | Exact path lists, not wildcards over keeper dirs; explicit `test -f` assertions that the 2 PROMOTE files survive; `git status --short` review before each commit. |
| T-19-13 | I (canonical tool loss) | Deleting `runs/arduview/_tooling/drive.cjs` instead of the root dup | mitigate | `test -f runs/arduview/_tooling/drive.cjs` precondition before `rm drive.cjs`; only the root path is removed. |
| T-19-14 | T (untracked media unrecoverable) | sheets media rm is permanent | accept | Contact sheets are regenerable emitter output (intel INDEX); operator chose hard-delete (D-01). |
</threat_model>

<verification>
- All SLOP docs + junk binaries absent; `git ls-files` shows zero remaining for each target.
- Keeper verified present before data_inventory.md/BUILD-STATE.md removed.
- Both PROMOTE files (01-DEBUG-RUN-NOTES.md, 15-DEBUG-funnel-architect.md) still present.
- Canonical `runs/arduview/_tooling/drive.cjs` intact.
- `git status` reviewed before each atomic commit; operator approved.
</verification>

<success_criteria>
Every SLOP doc and junk binary in the delete lists is gone; data_inventory.md/BUILD-STATE.md removed only after the keeper verified; the 2 PROMOTE items survive; commits are atomic per group.
</success_criteria>

<output>
After completion, create `.planning/phases/19-cleanup-health-nuke-e-ink-quarantine-eink-tablets-quarantine/19-04-SUMMARY.md`
</output>
