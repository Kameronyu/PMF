---
phase: 19-cleanup-health-nuke-e-ink-quarantine-eink-tablets-quarantine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - map/data-model-notes.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "The VOC/data-model design context that BUILD-STATE.md and data_inventory.md were the only home for survives the phase in a single keeper doc"
    - "A future VOC-build planner (Track B, M1-S4+) can read the 5 unresolved cross-cutting decisions and the non-VOC capability IO schemas without the deleted source"
  artifacts:
    - path: "map/data-model-notes.md"
      provides: "Distilled keeper: non-VOC capability IO schemas + 5 unresolved cross-cutting decisions + Open Questions #1-5 with resolution status"
      min_lines: 80
      contains: "hypothesis-record schema"
  key_links:
    - from: "map/data-model-notes.md"
      to: "future VOC-build / substrate decision"
      via: "design-context handoff (replaces map/data_inventory.md + BUILD-STATE.md as the home for these notes)"
      pattern: "augment.*not.*overwrite"
---

<objective>
Write the keeper doc `map/data-model-notes.md` that distills the load-bearing design content out of `map/data_inventory.md` (and the cross-cutting decisions that `.planning/BUILD-STATE.md` points to `data_inventory.md` for) BEFORE either source is deleted later in the phase (D-03, D-04, D-06).

Purpose: `map/data_inventory.md` and `BUILD-STATE.md` are both hard-deleted this phase. `data_inventory.md` is the ONLY home for: (a) the non-VOC capability IO schemas, (b) the 5 still-unresolved cross-cutting design decisions BUILD-STATE.md cites it for, and (c) the Open Questions list. Deleting blind loses durable VOC-build context. This plan writes the keeper FIRST so nothing load-bearing leaves the working tree (D-06 ordering gate).

Output: `map/data-model-notes.md` (new keeper). NO deletions in this plan — the source files stay intact until plan 19-04 verifies the keeper exists and removes them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-cleanup-health-nuke-e-ink-quarantine-eink-tablets-quarantine/19-CONTEXT.md

# Distill SOURCE (read in full before writing the keeper — it is deleted in 19-04):
@map/data_inventory.md

# The 5 unresolved decisions BUILD-STATE.md cites data_inventory.md for (read these lines; file deleted in 19-04):
@.planning/BUILD-STATE.md

# Already-resolved VOC granularity question (so the keeper marks Q1 RESOLVED, not re-opened):
@handoff-step3-voc-build.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the keeper map/data-model-notes.md from data_inventory.md + BUILD-STATE.md</name>
  <read_first>
    - map/data_inventory.md (FULL — this is the distill source; lines 56-258 non-VOC IO schemas, lines 690-759 cross-cutting, lines 40-44 Open Questions, lines 263-411 VOC-chain schemas which are OPTIONAL per D-05)
    - .planning/BUILD-STATE.md lines 61, 137, 149, 150, 206-213 (the rows pointing back to data_inventory.md for the 5 unresolved decisions)
    - handoff-step3-voc-build.md (around lines 119-124 — resolves the voc_record_id granularity / Open Question #1)
    - .planning/REQUIREMENTS.md VOC-02 (line ~12 — per-quote unit-of-record; confirms Q1 resolved)
  </read_first>
  <files>map/data-model-notes.md</files>
  <action>
Create `map/data-model-notes.md`. It REPLACES `map/data_inventory.md` + `BUILD-STATE.md` as the home for the durable design context. Do NOT copy the whole 770-line source — distill. No "v1"/"placeholder"/"future" hedging; write the real content. Use exactly these section headers (acceptance criteria greps them):

```
# Data-Model Notes (keeper)

> Distilled 2026-06-04 from `map/data_inventory.md` (hard-deleted, see git history) and the
> cross-cutting rows of `.planning/BUILD-STATE.md` (also hard-deleted). This is the surviving
> home for the non-VOC capability IO schemas, the unresolved cross-cutting design decisions,
> and the open questions. Substrate choice + VOC-chain field-level schemas are re-specified at
> Track B M1-S4 (codebook keystone).

## Implicit entities + source-metadata constraint
[Distill data_inventory.md lines 11-32: the ID list (author_id, brand_id, niche_id, transformation_id,
market_id, subniche_id, voc_record_id, quote_id, theme_id, claim_id, mechanism_id, investigation_id)
and the locked source-metadata constraint (platform/venue/author_id/url/timestamp/engagement on every
VOC record + downstream artifact). Keep the one-author-id-locked note.]

## Non-VOC capability IO schemas
[Distill data_inventory.md lines 56-258 — the per-brand extractor, market aggregator, ad creative +
visual extractor, offer/bundle extractor, channel analysis, trend/temporal signal, mechanism research,
product candidate discovery, transformation-from-product expander, transformation-from-niche expander.
For each: Input | Output fields | Source-metadata flag | Read-by. Preserve the field lists faithfully —
these are the only surviving record-shape definitions.]

## VOC-chain IO schemas (optional reference)
[D-05: OPTIONAL. Include a COMPACT reference of the VOC scraper/cleaner/classifier/quote-extractor/
clusterer/frequency-synthesizer/copy-bank schemas (data_inventory.md lines 263-411) IF it fits without
bloating the keeper past ~250 lines. The handoff covers them architecturally and M1-S4 re-specs at field
level, so a one-line-per-op pointer is acceptable here rather than full field dumps. Planner's call:
include a condensed table. Do NOT drop the source-metadata-preservation chain note.]

## The 5 unresolved cross-cutting decisions
[Distill data_inventory.md lines 690-759 PLUS the BUILD-STATE.md rows that cite them. Each as a named
subsection — these are the exact 5 the keeper MUST absorb (D-04):]
### hypothesis-record schema
[Open: explicit Human step (Step 0->1), `hypothesis_record` = {market_id, selected_at, rationale_text,
accumulated_gap_record_ref}. Schema described, never implemented (BUILD-STATE row line 61/149). Blocked on
substrate.]
### augment-not-overwrite multi-pass write lean
[Locked lean: re-run augments, never overwrites. Implies depth_pass + extracted_at on records; gap/
hypothesis records timestamped + chained. This is the same principle the phase-19 no-overwrite-v1 rule
generalizes to run outputs.]
### awareness-level inference gap
[Classifier emits awareness_proxies; no dedicated awareness-stage inference Op. Needed at Step 4. UNDER
(BUILD-STATE row line 150).]
### author_id-join-as-heaviest constraint
[author_id joins across classified VOC records = the heaviest join in the system (PMBD clusterer 5+ rule,
single-individual co-occurrence, cross-niche-possible). Drives the substrate decision more than any other
query. Source-metadata chain is non-negotiable end-to-end.]
### substrate framing (~25 record types)
[~25 joinable record types on a small ID set; append-mostly + augment-on-rerun; sparse classifier output
(N/A valid everywhere); single-machine scale (SQLite/duckdb plausible, postgres if multi-process).
Substrate CHOICE itself stays deferred — capture the framing, not a decision.]

## Open Questions #1-5 (with resolution status)
1. **VOC unit-of-record granularity** — RESOLVED: per-quote, keyed author_id+source+char-offsets
   (`handoff-step3-voc-build.md` §3-4 ~lines 119-124; `REQUIREMENTS.md` VOC-02). The clusterer 5+ rule
   operates at the per-quote level.
2. **Investigation scope** — LEAN STATED: one global map (stack patterns across spaces); no
   investigation_id on records; cross-investigation queries first-class. Cost noted, not blocking.
3. **Competitive classifier as distinct Op** — OPEN/PARKED (bundle vs split raw-extractor + classifier).
4. **Multi-pass writes** — LEAN STATED (augment not overwrite) — see decision subsection above.
5. **N/A in classifier output** — RESOLVED-AS-CONSTRAINT: N/A valid for any universal-schema field →
   sparse storage; favors document store / nullable-heavy SQL.
```

Fill each bracketed block with the actual distilled content from the sources (not the bracket text). Target 100-250 lines. The keeper must stand alone — a reader who never saw `data_inventory.md` must be able to plan the VOC substrate from it.
  </action>
  <verify>
    <automated>test -f map/data-model-notes.md && grep -q "hypothesis-record schema" map/data-model-notes.md && grep -q "augment-not-overwrite" map/data-model-notes.md && grep -q "author_id-join-as-heaviest" map/data-model-notes.md && grep -q "substrate framing" map/data-model-notes.md && grep -qi "awareness-level inference" map/data-model-notes.md && grep -q "Open Questions" map/data-model-notes.md && grep -qi "per-quote" map/data-model-notes.md && test "$(wc -l < map/data-model-notes.md)" -ge 80</automated>
  </verify>
  <acceptance_criteria>
    - `test -f map/data-model-notes.md` — keeper exists
    - All 5 decision subsections present: `grep -c "hypothesis-record schema" map/data-model-notes.md` ≥ 1, plus `augment-not-overwrite`, `awareness-level inference`, `author_id-join-as-heaviest`, `substrate framing` each grep-match
    - Open Question #1 marked resolved: `grep -i "RESOLVED" map/data-model-notes.md | grep -i "per-quote"` matches
    - Non-VOC schemas present: `grep -qi "per-brand extractor" map/data-model-notes.md` AND `grep -qi "market aggregator" map/data-model-notes.md`
    - Length ≥ 80 lines: `test "$(wc -l < map/data-model-notes.md)" -ge 80`
    - Sources STILL EXIST (not deleted in this plan): `test -f map/data_inventory.md && test -f .planning/BUILD-STATE.md`
  </acceptance_criteria>
  <done>map/data-model-notes.md exists, contains all 5 named decision subsections + non-VOC schemas + Open Questions with resolution status, ≥80 lines; source files untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a documentation-distillation plan. No new network, auth, or external-input surface is introduced — note that explicitly. The dominant threat class is **irreversible context loss**, not external attack.

| Boundary | Description |
|----------|-------------|
| working tree → git history | Source docs are deleted later in the phase; once removed, the keeper is the only working-tree copy of this design context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | I (Info loss) | Deleting data_inventory.md / BUILD-STATE.md before content is preserved | mitigate | This plan writes + verifies the keeper FIRST; deletion lives in 19-04 which depends_on this plan. The verify grep gate proves the 5 decisions + schemas landed before any source is removed. |
| T-19-02 | T (Tampering/lossy distill) | Keeper omits a load-bearing schema or silently simplifies a decision | mitigate | Acceptance grep checks each of the 5 named subsections + non-VOC schema names; min-line floor blocks a stub keeper. Reviewer compares against git-history source if a gap is suspected. |
| T-19-03 | D (DoS) | n/a — no runtime surface | accept | Static markdown, no execution path, no attacker. |
</threat_model>

<verification>
- `map/data-model-notes.md` exists and passes all acceptance greps.
- `map/data_inventory.md` and `.planning/BUILD-STATE.md` still present (deletion deferred to 19-04).
- No other files modified (`git status --short` shows only the new keeper).
</verification>

<success_criteria>
The keeper doc captures every piece of design context that the two deleted sources were the sole home for: non-VOC capability IO schemas, the 5 unresolved cross-cutting decisions, and Open Questions #1-5 with resolution status. A VOC-build planner can work from it alone.
</success_criteria>

<output>
After completion, create `.planning/phases/19-cleanup-health-nuke-e-ink-quarantine-eink-tablets-quarantine/19-01-SUMMARY.md`
</output>
