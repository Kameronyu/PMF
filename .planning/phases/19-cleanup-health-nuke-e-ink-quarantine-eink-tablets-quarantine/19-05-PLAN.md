---
phase: 19-cleanup-health-nuke-e-ink-quarantine-eink-tablets-quarantine
plan: 05
type: execute
wave: 3
depends_on: ["19-03", "19-04"]
files_modified:
  - .gitignore
  - CLAUDE.md
  - runs/arduview/_mechanisms-in-play.agent.json
  - runs/arduview/funnels/_index.json
  - corpus/_clean-log.txt
  - assets/_asset-fetch-log.txt
  - runs/arduview/funnels/_funnel-store-log.txt
  - runs/arduview/funnels-scored/_funnel-score-log.txt
  - runs/arduview/funnels-clean/_funnel-clean-log.txt
  - runs/arduview/asset-classify/_asset-map-rank-log.txt
  - runs/arduview/asset-classify/_asset-emit-log.txt
  - runs/arduview/asset-classify/_caption_gradient.png
  - runs/arduview/asset-classify/_caption_gradient.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "New gitignore patterns (_index.json, _*-log.txt, _*.agent.json, _caption_*) keep regenerable scratch out of the tree going forward"
    - "The tracked test fixture runs/_fixture/funnels/_index.json SURVIVES the new ignore pattern (negation works)"
    - "Junk matches (logs, caption scratch, tracked _mechanisms-in-play.agent.json, tracked/untracked _index.json) are removed now"
    - "The no-overwrite-v1 versioning convention is documented in project CLAUDE.md"
    - "gsd-health on .planning/ runs clean (or repairable issues are addressed) as the final verification step"
  artifacts:
    - path: ".gitignore"
      provides: "New ignore patterns with a fixture-preserving negation"
      contains: "!runs/_fixture"
    - path: "CLAUDE.md"
      provides: "no-overwrite-v1 versioning rule (convention)"
      contains: "no-overwrite"
  key_links:
    - from: ".gitignore _index.json pattern"
      to: "runs/_fixture/funnels/_index.json"
      via: "negation preserves the tracked fixture"
      pattern: "!runs/_fixture"
---

<objective>
Final cleanup + health pass: (1) add the new gitignore patterns with a fixture-preserving negation (D-10, D-12), (2) remove the junk matches now (D-11, D-12), (3) document the no-overwrite-v1 versioning convention in project CLAUDE.md (D-07/08/09), and (4) run `gsd-health` on `.planning/` as the final verification step (D-15).

Purpose: stop regenerable scratch from re-entering the tree, throw out the junk already there (without dropping the tracked test fixture), adopt the versioning convention, and confirm `.planning/` integrity after the deletions in 19-03/19-04.

Output: updated `.gitignore`, junk removed, no-overwrite-v1 rule in CLAUDE.md, `gsd-health` clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-cleanup-health-nuke-e-ink-quarantine-eink-tablets-quarantine/19-CONTEXT.md

# The gitignore to extend (already category-grouped — append in the same style):
@.gitignore
# The project CLAUDE.md (the no-overwrite-v1 rule target — PMF-specific rule → project file):
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend .gitignore with new patterns + fixture negation; verify fixture survives (D-10, D-12)</name>
  <read_first>
    - .gitignore (current 5-rule category-grouped file)
    - 19-CONTEXT.md D-10/D-11/D-12 (patterns + the fixture nuance)
  </read_first>
  <files>.gitignore</files>
  <action>
Append a new category-grouped block to `.gitignore` (match the existing comment-grouped style). Add, on their own lines, exactly these four ignore patterns under a comment like `# Regenerable emitter side-outputs (D-10) — scratch, never committed`:
  - `_index.json`
  - `_*-log.txt`
  - `_*.agent.json`
  - `_caption_*`

Then add a SECOND comment block `# Preserve tracked test fixtures from the patterns above (D-12)` with TWO negation lines, in this order (parent dir un-exclude first, then the recursive content un-exclude — gitignore requires the parent not be excluded for a deeper negation to take):
  - `!runs/_fixture/`
  - `!runs/_fixture/**`

VERIFY the negation BEFORE relying on it:
  - `git check-ignore runs/_fixture/funnels/_index.json` MUST exit NON-zero (fixture NOT ignored).
  - `git check-ignore runs/arduview/funnels/_index.json` SHOULD exit 0 (a real run copy IS ignored).
If `git check-ignore` reports the fixture as ignored, the negation order/glob is wrong — fix and re-test until the fixture is preserved.
  </action>
  <verify>
    <automated>grep -q "_index.json" .gitignore && grep -q "agent.json" .gitignore && grep -q "_caption" .gitignore && grep -q "log.txt" .gitignore && grep -q "!runs/_fixture" .gitignore && ! git check-ignore runs/_fixture/funnels/_index.json && git check-ignore runs/arduview/funnels/_index.json</automated>
  </verify>
  <acceptance_criteria>
    - All four patterns present: grep matches `_index.json`, a `*-log.txt` pattern, a `*.agent.json` pattern, a `_caption` pattern in .gitignore
    - Negation present: `grep -q "!runs/_fixture" .gitignore`
    - Fixture preserved: `git check-ignore runs/_fixture/funnels/_index.json` exits NON-zero (command `! git check-ignore runs/_fixture/funnels/_index.json` exits 0)
    - Real run copy ignored: `git check-ignore runs/arduview/funnels/_index.json` exits 0
  </acceptance_criteria>
  <done>.gitignore has the four new patterns + the fixture negation; `git check-ignore` confirms the fixture is NOT ignored while a real run _index.json IS.</done>
</task>

<task type="auto">
  <name>Task 2: Remove junk matches now — logs, caption scratch, tracked agent.json, _index.json copies (D-11, D-12)</name>
  <read_first>
    - 19-CONTEXT.md D-11 (junk → throw out now) + D-12 (_index.json: git rm --cached the run copies; preserve fixture)
    - .planning/intel/INDEX.md scratch row (_*-log.txt, _*.agent.json, _caption_*)
  </read_first>
  <files>runs/arduview/_mechanisms-in-play.agent.json, corpus/_clean-log.txt, assets/_asset-fetch-log.txt, runs/arduview/funnels/_funnel-store-log.txt, runs/arduview/funnels-scored/_funnel-score-log.txt, runs/arduview/funnels-clean/_funnel-clean-log.txt, runs/arduview/asset-classify/_asset-map-rank-log.txt, runs/arduview/asset-classify/_asset-emit-log.txt, runs/arduview/asset-classify/_caption_gradient.png, runs/arduview/asset-classify/_caption_gradient.py, runs/arduview/funnels/_index.json</files>
  <action>
Statuses from this session's scan: the 7 `_*-log.txt` files and the 2 `_caption_gradient.*` files are UNTRACKED → `rm`. `runs/arduview/_mechanisms-in-play.agent.json` is TRACKED (parked-decision #3, retired) → `git rm`. `runs/arduview/funnels/_index.json` is UNTRACKED; `runs/_fixture/funnels/_index.json` is TRACKED (the fixture — KEEP).

1. Untracked logs (D-11): `rm corpus/_clean-log.txt assets/_asset-fetch-log.txt runs/arduview/funnels/_funnel-store-log.txt runs/arduview/funnels-scored/_funnel-score-log.txt runs/arduview/funnels-clean/_funnel-clean-log.txt runs/arduview/asset-classify/_asset-map-rank-log.txt runs/arduview/asset-classify/_asset-emit-log.txt` (re-confirm each is untracked with `git ls-files --error-unmatch`; if any is tracked, use `git rm` instead).
2. Caption scratch (untracked, D-11): `rm runs/arduview/asset-classify/_caption_gradient.png runs/arduview/asset-classify/_caption_gradient.py`.
3. Tracked agent.json (D-11): `git rm runs/arduview/_mechanisms-in-play.agent.json`.
4. Untracked run _index.json (D-12): `rm runs/arduview/funnels/_index.json`. (It is untracked, so plain rm; if any `_index.json` under runs/ is TRACKED and is NOT the fixture, use `git rm --cached <path>` to stop tracking while leaving the file — per D-12 — then the new ignore keeps it out. The fixture `runs/_fixture/funnels/_index.json` is explicitly NOT touched.)
5. GUARD: `test -f runs/_fixture/funnels/_index.json` MUST still pass after this task (fixture preserved).
6. Review `git status --short`; atomic commit (ASK operator first): `git commit -m "chore(19): remove junk scratch (logs, caption scratch, retired agent.json, run _index.json); gitignore going forward (D-10/D-11/D-12)"`.
  </action>
  <verify>
    <automated>test ! -e corpus/_clean-log.txt && test ! -e runs/arduview/asset-classify/_caption_gradient.png && test ! -e runs/arduview/asset-classify/_caption_gradient.py && test ! -e runs/arduview/_mechanisms-in-play.agent.json && test ! -e runs/arduview/funnels/_index.json && test -f runs/_fixture/funnels/_index.json</automated>
  </verify>
  <acceptance_criteria>
    - All 7 `_*-log.txt` removed: `find . -name "_*-log.txt" -not -path "*/.git/*" | wc -l` returns 0
    - Caption scratch gone: `test ! -e runs/arduview/asset-classify/_caption_gradient.png && test ! -e runs/arduview/asset-classify/_caption_gradient.py`
    - Retired agent.json gone + untracked: `test ! -e runs/arduview/_mechanisms-in-play.agent.json` AND `git ls-files runs/arduview/_mechanisms-in-play.agent.json | wc -l` returns 0
    - Run _index.json gone: `test ! -e runs/arduview/funnels/_index.json`
    - FIXTURE PRESERVED (D-12): `test -f runs/_fixture/funnels/_index.json` AND `git ls-files runs/_fixture/funnels/_index.json | wc -l` returns 1
  </acceptance_criteria>
  <done>All junk scratch removed (untracked via rm, tracked agent.json via git rm); the tracked test fixture survives; staged/committed after operator OK.</done>
</task>

<task type="auto">
  <name>Task 3: Document the no-overwrite-v1 versioning rule in project CLAUDE.md (D-07/08/09)</name>
  <read_first>
    - CLAUDE.md (PMF project instructions — the rule target; PMF-specific → project file, not dotfiles, per planner default)
    - 19-CONTEXT.md D-07/D-08/D-09 (rule text, enforcement = convention only, scope)
  </read_first>
  <files>CLAUDE.md</files>
  <action>
TARGET-FILE AMBIGUITY (D-08): the rule could live in the dotfiles `~/CLAUDE.md` or the project `PMF/CLAUDE.md`. DEFAULT to the PROJECT file `CLAUDE.md` (this file, `/home/kyu3/PMF/CLAUDE.md`) — the rule is PMF-specific (governs `runs/<space>/…` outputs + emitted bricks). Do NOT edit the dotfiles `~/CLAUDE.md`. If the operator prefers the dotfiles file, they will redirect — and per project rule, REMIND them to push the dotfiles change. Note this choice in the SUMMARY.

Add a new `## Versioning` section to the project `CLAUDE.md` with the no-overwrite-v1 rule. Write it as a documented convention (NOT a hook — enforcement is deferred per D-08). Cover the three points:
  - Rule (D-07): a committed run output (a file/dir under `runs/<space>/…`) or an emitted brick is never mutated in place on a re-run. A re-run writes a NEW versioned location (e.g. a `v2/` subdir or a `-v2` suffix); v1 stays intact for provenance/diffing.
  - Scope (D-09): governs committed run outputs + emitted bricks. Does NOT govern logs/scratch (gitignored) or in-flight uncommitted work.
  - Enforcement (D-08): convention only; a guard hook/script is explicitly DEFERRED.
The text MUST contain the literal token `no-overwrite` so it is greppable.
  </action>
  <verify>
    <automated>grep -q "no-overwrite" CLAUDE.md && grep -qi "versioned" CLAUDE.md && grep -qi "DEFERRED\|convention" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - Rule documented: `grep -q "no-overwrite" CLAUDE.md`
    - Versioned-location concept present: `grep -qi "versioned\|v2" CLAUDE.md`
    - Enforcement-deferred / convention-only noted: `grep -qi "convention\|deferred" CLAUDE.md`
    - Edited the PROJECT CLAUDE.md (not dotfiles): the working-tree `CLAUDE.md` diff shows the new `## Versioning` section
  </acceptance_criteria>
  <done>Project CLAUDE.md has a Versioning section documenting the no-overwrite-v1 convention (rule + scope + deferred enforcement); dotfiles file untouched (or operator-redirected with a push reminder noted in SUMMARY).</done>
</task>

<task type="auto">
  <name>Task 4: Run gsd-health on .planning/ as the final verification step (D-15)</name>
  <read_first>
    - .planning/STATE.md (current machine state — gsd-health checks consistency against it)
    - .planning/ROADMAP.md (Phase 19 entry — health checks plan/phase linkage)
  </read_first>
  <files>.planning/</files>
  <action>
This is the FINAL task of the phase (D-15). Invoke the gsd-health workflow on `.planning/` to validate integrity after all deletions:
`/gsd-health` (or run the health workflow at `$HOME/.claude/get-shit-done/workflows/health.md` end-to-end).

Health checks for: missing files, invalid configs, inconsistent STATE, orphaned plans. Because this phase deleted `.planning/BUILD-STATE.md` and 3 `.pre-revision.md` snapshots, expect health to confirm those removals didn't break linkage (BUILD-STATE.md was an orphan/SLOP, not a GSD-required artifact; pre-revision snapshots are vestigial).

Address any REPAIRABLE issues it surfaces (D-15): e.g. a stale STATE pointer, a dangling plan reference, a ROADMAP plan-list mismatch. Use `/gsd-health --repair` for auto-repairable items. For non-repairable findings, record them in the SUMMARY for operator follow-up — do NOT invent fixes outside the cleanup scope.

Re-run gsd-health after repairs to confirm clean (or known-accepted) state.
  </action>
  <verify>
    <automated>node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" verify phase-completeness 19 2>&1 | head -5; ls .planning/STATE.md .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - gsd-health ran on `.planning/` and its report is captured in the SUMMARY
    - All issues are either repaired or explicitly recorded as accepted/operator-follow-up (no silent skips)
    - Core GSD artifacts intact post-cleanup: `test -f .planning/STATE.md && test -f .planning/ROADMAP.md && test -f .planning/REQUIREMENTS.md && test -f .planning/PROJECT.md`
    - No deleted file (BUILD-STATE.md, pre-revision snapshots) is reported as a broken REQUIRED dependency by health (they were SLOP/orphans)
  </acceptance_criteria>
  <done>gsd-health run on .planning/; repairable issues fixed; remaining findings recorded; core artifacts confirmed intact; phase health is clean or known-accepted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Mixed config-edit + destructive cleanup + health check. No network/auth/input surface — stated explicitly. Threats: the new ignore pattern dropping the tracked fixture; deleting a still-needed scratch file; health-repair making an unscoped change.

| Boundary | Description |
|----------|-------------|
| gitignore pattern → tracked fixture | `_index.json` ignore would drop `runs/_fixture/funnels/_index.json` without the negation |
| working tree → git history | Untracked junk (logs, caption scratch, run _index.json) rm is permanent |
| gsd-health --repair → .planning/ | Auto-repair could touch state beyond cleanup scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-15 | I (fixture loss) | New `_index.json` ignore drops the tracked test fixture | mitigate | `!runs/_fixture/` + `!runs/_fixture/**` negation, VERIFIED with `git check-ignore` (must exit non-zero for the fixture) before commit; Task 2 asserts `test -f` on the fixture after deletes. |
| T-19-16 | I (unrecoverable scratch) | rm of untracked logs/caption/index is permanent | accept | These are regenerable emitter side-outputs (intel INDEX, code_context); operator: "if they're junk just throw them out" (D-11). |
| T-19-17 | T (unscoped repair) | gsd-health --repair alters STATE/ROADMAP beyond intent | mitigate | Review repair diffs before committing; non-repairable findings recorded for operator, not auto-fixed; core-artifact existence asserted. |
| T-19-18 | T (wrong CLAUDE.md) | Rule written to dotfiles instead of project file | mitigate | Default to project `CLAUDE.md`; acceptance checks the working-tree project file; dotfiles edit only on operator redirect with a push reminder. |
</threat_model>

<verification>
- `.gitignore` has the four patterns + negation; `git check-ignore` confirms the fixture is preserved and a real run _index.json is ignored.
- All junk scratch removed; tracked fixture intact.
- Project CLAUDE.md documents the no-overwrite-v1 convention.
- gsd-health run on `.planning/`; issues repaired or recorded; core artifacts intact.
- `git status` reviewed; operator approved commits.
</verification>

<success_criteria>
The repo ignores regenerable scratch going forward without dropping the tracked fixture, the junk is gone, the no-overwrite-v1 convention is documented, and `.planning/` passes gsd-health (clean or known-accepted) as the phase's final verification.
</success_criteria>

<output>
After completion, create `.planning/phases/19-cleanup-health-nuke-e-ink-quarantine-eink-tablets-quarantine/19-05-SUMMARY.md`
</output>
