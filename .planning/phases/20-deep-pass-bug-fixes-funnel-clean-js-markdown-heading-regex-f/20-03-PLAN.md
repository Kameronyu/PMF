---
phase: 20-deep-pass-bug-fixes-funnel-clean-js-markdown-heading-regex-f
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - runs/arduview/funnels/_index.json   # regenerable gitignored scratch — rebuilt, not hand-edited
autonomous: true
requirements: []   # phase_req_ids is null — coverage = CONTEXT decision D-11.
must_haves:
  truths:
    - "The arduview RAG index is rebuilt from the current funnel-vectorize.js so retrieval units carry source_type and routing_flag"
    - "The --source-type / --routing-flag RAG prefilters become operative (they no longer filter against null for every record)"
    - "The rebuild produces regenerable gitignored scratch, not a mutated committed artifact (no-overwrite-v1 respected)"
  artifacts:
    - path: "runs/arduview/funnels/_index.json"
      provides: "RAG index with source_type + routing_flag populated on each unit"
      contains: "routing_flag"
  key_links:
    - from: "tools/funnel-vectorize.js loadUnits"
      to: "runs/arduview/funnels/_index.json"
      via: "node tools/funnel-vectorize.js --space=arduview"
      pattern: "routing_flag"
---

<objective>
Rebuild the stale arduview RAG index so the source_type / routing_flag prefilters in funnel-rag-query.js become operative. The on-disk _index.json was built by an older funnel-vectorize.js that did not stamp these fields; the current code (loadUnits, ~lines 107-129) does. Rebuild only — no code change.

Purpose: The copywriter RAG prefilters currently filter against null for every record (audit/08 Bug 6). A rebuild with the current code restores them. The regenerated index is gitignored regenerable scratch (Phase 19), so this respects no-overwrite-v1 — we are not mutating a committed artifact.

Output: runs/arduview/funnels/_index.json regenerated with source_type + routing_flag on each retrieval unit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Authoritative user decision — D-11 (rebuild only, no code change)
@.planning/phases/20-deep-pass-bug-fixes-funnel-clean-js-markdown-heading-regex-f/20-CONTEXT.md
# Scope source of truth — audit/08 Bug 6 (stale index)
@.planning/audit/08-deep-pass-bugs.md

# Rebuild command target (NO code change — read to confirm the command + that loadUnits stamps the fields)
@tools/funnel-vectorize.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the arduview RAG index (D-11)</name>
  <files>runs/arduview/funnels/_index.json</files>
  <read_first>tools/funnel-vectorize.js (confirm the --space flag, the output path runs/<space>/funnels/_index.json, and that loadUnits ~107-129 assigns source_type + routing_flag); 20-CONTEXT.md D-11; audit/08-deep-pass-bugs.md Bug 6; /home/kyu3/PMF/CLAUDE.md no-overwrite-v1 (index is gitignored scratch, regenerable — rebuild is allowed)</read_first>
  <action>
Run the rebuild command exactly:
   node tools/funnel-vectorize.js --space=arduview

This is a REBUILD ONLY — do NOT edit tools/funnel-vectorize.js or any other code. The current loadUnits() already stamps source_type and routing_flag from each funnel file onto every retrieval unit; the stale on-disk index simply predates that code.

Notes / contingencies:
- If the command needs an embedding API key or network (Voyage RAG) and it is unavailable, do NOT fabricate an index. Report the blocker clearly and STOP — the index stays as-is and the rebuild is deferred to when the key/network is present. (Fail loud, never stub — project principle.)
- If runs/arduview/funnels/ has no funnel records to index, report that the source store is empty and STOP — do not invent units.
- The regenerated _index.json is gitignored regenerable scratch (Phase 19 added the gitignore). It is NOT a committed artifact, so rebuilding it does not violate no-overwrite-v1. Do NOT git-add the index.
- Confirm post-rebuild that the new index carries the two fields on at least one unit (verify check below).
  </action>
  <verify>
    <automated>cd /home/kyu3/PMF && test -f runs/arduview/funnels/_index.json && node -e 'const fs=require("fs");const j=JSON.parse(fs.readFileSync("runs/arduview/funnels/_index.json","utf8"));const units=Array.isArray(j)?j:(j.units||j.records||j.index||[]);const arr=Array.isArray(units)?units:Object.values(units);const hit=arr.some(u=>u&&(("routing_flag" in u)||("source_type" in u)));if(hit){console.log("PASS index carries source_type/routing_flag");}else{console.error("FAIL no unit carries source_type/routing_flag — check funnel-vectorize.js output shape");process.exit(1);}' </automated>
  </verify>
  <done>runs/arduview/funnels/_index.json exists and at least one retrieval unit carries source_type and/or routing_flag; no code was changed; the index was not git-added (remains gitignored scratch). If the rebuild was blocked (missing key / empty store), that blocker is reported in the SUMMARY and the index is left untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan runs an existing deterministic script against an existing local store and regenerates a gitignored index. No code change, no new input parsing, no external-facing surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | (no new surface) | funnel-vectorize.js rebuild | accept | Running an already-shipped, verified deterministic script against the local arduview store. No code change, no new attack surface, no auth/network beyond what funnel-vectorize.js already uses. If an embedding API key is involved, it is the same key the pipeline already uses — no new secret handling introduced. |

No external-facing attack surface; minimal by design, stated explicitly rather than inventing threats.
</threat_model>

<verification>
- D-11: runs/arduview/funnels/_index.json regenerated; at least one unit carries source_type / routing_flag (Task 1 automated check).
- No code changed: git diff shows tools/funnel-vectorize.js (and all other tools/) untouched.
- Index not committed: git status shows _index.json as ignored/untracked (gitignored scratch per Phase 19) — not staged.
</verification>

<success_criteria>
- The rebuild ran (or the blocker was reported and the index left intact).
- The fresh index carries the prefilter fields; the --source-type / --routing-flag RAG prefilters are operative.
- no-overwrite-v1 respected — regenerable gitignored scratch, no committed artifact mutated, no code edited.
</success_criteria>

<output>
After completion, create `.planning/phases/20-deep-pass-bug-fixes-funnel-clean-js-markdown-heading-regex-f/20-03-SUMMARY.md`
</output>
