---
phase: 20-deep-pass-bug-fixes-funnel-clean-js-markdown-heading-regex-f
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/funnel-deep-pass/SKILL.md
  - .claude/skills/market-selection/SKILL.md
  - .claude/skills/funnel-architect/SKILL.md
  - prompts/step1-light-pass.md
  - prompts/funnel-deep-pass.md
  - tools/aggregate-mechanisms-in-play.js
autonomous: true
requirements: []   # phase_req_ids is null — coverage = CONTEXT decisions D-04, D-05, D-07, D-09, D-10.
must_haves:
  truths:
    - "The funnel-deep-pass orchestrator excludes (with a clear message) any brand whose corpus/<brand>/raw/home.html AND corpus/<brand>/clean/home.md are both absent — never stubs placeholder data"
    - "A no-ads DTC brand (no Currency A, no crowdfunding_stats) is documented as EXCLUDE-with-message, not stub-a-fake-funnel; the operator hand-feed path is the only way a no-ads LP enters analysis"
    - "Every pointer to space-map.json across all SKILLs/scripts/docs resolves to runs/<space>/space-map.json — zero remaining repo-root references, INCLUDING the path.join(ROOT, 'space-map.json') default in tools/aggregate-mechanisms-in-play.js"
    - "The stale 'Section Analyzer MUST Read that bundle' line in prompts/funnel-deep-pass.md no longer instructs a Read that contradicts the deterministic-embed design"
    - "belief_kind and source_routing no longer appear in the funnel-architect SKILL.md INPUTS spec as fields the Section Analyzer emits"
  artifacts:
    - path: ".claude/skills/funnel-deep-pass/SKILL.md"
      provides: "Corpus-absent + no-ads-DTC exclude guard in the precondition path"
      contains: "EXCLUDE"
    - path: ".claude/skills/market-selection/SKILL.md"
      provides: "space-map.json pointer consistent on runs/<space>/"
      contains: "runs/<space>/space-map.json"
    - path: "prompts/step1-light-pass.md"
      provides: "Classifier output write-path pinned to runs/<space>/space-map.json"
      contains: "runs/<space>/space-map.json"
    - path: "tools/aggregate-mechanisms-in-play.js"
      provides: "space-map default migrated off repo root to the canonical run-relative path"
      contains: "runs"
    - path: "prompts/funnel-deep-pass.md"
      provides: "Stale Read line scrubbed to match deterministic-embed design"
    - path: ".claude/skills/funnel-architect/SKILL.md"
      provides: "INPUTS spec free of belief_kind / source_routing ghost fields"
  key_links:
    - from: ".claude/skills/funnel-deep-pass/SKILL.md PRECONDITION CHECK"
      to: "exclude-with-message on absent corpus / no-currency"
      via: "early-exit guard added to precondition block"
      pattern: "EXCLUDE"
    - from: "prompts/step1-light-pass.md classifier output"
      to: "runs/<space>/space-map.json"
      via: "write-path declaration"
      pattern: "runs"
    - from: "tools/aggregate-mechanisms-in-play.js --space-map default"
      to: "runs/<space>/space-map.json (no repo-root path.join(ROOT, 'space-map.json'))"
      via: "default-path migration mirroring the sidecar default"
      pattern: "runs"
---

<objective>
Make the funnel deep-pass SKILL/doc set internally consistent and fail-loud-by-exclusion: add the corpus-absent + no-ads-DTC exclude guards (D-04, D-05), unify every space-map.json pointer on the canonical runs/<space>/ path (D-07), scrub the one stale Read line that contradicts the deterministic-embed design (D-09), and remove the belief_kind/source_routing ghost fields from the architect's INPUTS spec (D-10).

Purpose: A fresh run must (a) exclude brands with no corpus / no validation currency with a clear message instead of stubbing fabricated funnels, and (b) not be misled by docs OR a script default that disagree with reality (wrong space-map path, stale Read instruction, fields the analyzer never emits). Doc/SKILL edits plus ONE script default-path migration (D-07) — no code logic change beyond the default path, no schema freeze.

Output: funnel-deep-pass SKILL precondition excludes absent-corpus / no-currency brands with a message; all space-map.json references — docs AND the aggregate-mechanisms-in-play.js read/write default — resolve to runs/<space>/space-map.json with zero repo-root references; the stale Read line is gone; the architect INPUTS spec matches reality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Authoritative user decisions — read FIRST (D-04, D-05, D-07, D-09, D-10 are locked)
@.planning/phases/20-deep-pass-bug-fixes-funnel-clean-js-markdown-heading-regex-f/20-CONTEXT.md
# Scope source of truth (bug triage + fix designs; Bug 2/5 + §1 corpus/ads/precondition)
@.planning/audit/08-deep-pass-bugs.md
# The original friction points (analysis artifact — consume, do not keep in sync)
@agents/funnel-deep-pass-run-notes.md

# Files to edit (read the named regions before editing)
@.claude/skills/funnel-deep-pass/SKILL.md
@.claude/skills/market-selection/SKILL.md
@.claude/skills/funnel-architect/SKILL.md
@prompts/step1-light-pass.md
@prompts/funnel-deep-pass.md
@tools/aggregate-mechanisms-in-play.js
# Reference pattern for D-06 hand-feed (DO NOT MODIFY — read only)
@tools/funnel-assemble.js

<interfaces>
<!-- Exact on-disk locations verified during planning. Line numbers approximate; grep to confirm before editing. -->

CORPUS / NO-ADS GUARD (D-04, D-05) — funnel-deep-pass/SKILL.md:
- PRECONDITION CHECK block at lines ~82-96 (bash: tests space-map.json + assembled funnels + DR bundle).
- THE DETERMINISTIC CHAIN at lines ~100-127 (assemble -> clean -> score).
- run-notes precedent: a no-ads brand produces bound_ads:[] and (after Plan 20-01) funnel-score.js now FAILS on no-currency. The SKILL must tell the orchestrator to EXCLUDE such a brand UPSTREAM with a message, not feed it to the scorer.

SPACE-MAP PATH (D-07) — TWO repo-root references exist on disk; D-07 eliminates BOTH:
- (1) DOC reference: .claude/skills/market-selection/SKILL.md lines ~95-102 — "Arduview run — PATH MISMATCH (load-bearing)" block stating files live at repo root: "space-map.json -> /home/kyu3/PMF/space-map.json" (+ brands.json, ads/, corpus/ siblings). THIS is the prose D-07 eliminates.
- (2) SCRIPT default: tools/aggregate-mechanisms-in-play.js line ~35 — `const SPACE_MAP = path.resolve(opts['space-map'] || path.join(ROOT, 'space-map.json'));`. This script READS and RE-WRITES space-map.json, defaulting its path to the repo root — exactly the inconsistency D-07 exists to eliminate. Its usage doc (~line 21) advertises `[--space-map=PATH] (default space-map.json)`. NOTE: the SIDECAR default on line ~34 was ALREADY migrated to the run-relative form `path.join(ROOT, 'runs', 'arduview', '_mechanisms-in-play.agent.json')` — mirror that migration for the space-map default.
- market-selection/SKILL.md line ~40 already says runs/<space>/ (correct) — keep consistent.
- prompts/step1-light-pass.md: classifier output chain line ~20 ("-> space-map.json") references a BARE filename with no directory — pin it to runs/<space>/space-map.json (the "make the classifier write there" WRITE-PATH half of D-07).
- prompts/step1-light-pass.md line ~120 ("### `space-map.json` — Space Classifier output (reads all `dump.json`)") is a DESCRIPTIVE SCHEMA HEADER, not a write-path. It is OUT OF SCOPE for D-07 — do NOT rewrite it to a directory path. Task 2 only pins WRITE/OUTPUT references; descriptive schema headers are left as-is.
- funnel-deep-pass/SKILL.md precondition already checks runs/<space>/space-map.json (line ~86) — correct, the canonical target. Leave it.
- tools/hooks/route.js + validate-classifier.js match on BASENAME only (path-agnostic) — NO change needed.

STALE READ LINE (D-09) — prompts/funnel-deep-pass.md line ~172:
- "...The Section Analyzer MUST `Read` that bundle as its first step (see the analyzer prompt below); the orchestrator must regenerate it if the DR sources changed."
- Contradicts the deterministic-embed design (SKILL.md truth #1: the Section Analyzer NEVER Reads files; the orchestrator EMBEDS those bytes). Scrub the MUST-Read instruction.

GHOST FIELDS (D-10) — funnel-architect/SKILL.md INPUTS spec (the "you receive" list):
- Line ~118: "...belief_id (+ belief_confidence for overflow), belief_kind (crowdfunding-specific | general-DR)"
- Line ~121: "source_routing (which funnel type the fill should come from)"
- These document fields the Section Analyzer never emits (audit/07). Remove them from the INPUTS list so the doc matches reality. SAFE-NOW doc-correction half ONLY — do NOT touch the prompts/funnel-deep-pass.md SCHEMA (line ~84 belief_kind field, ~146 BELIEF_KIND_ENUM): adding/removing those is the CONTRACT-GATED schema work, deferred to Track C / Phase 21.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add corpus-absent + no-ads-DTC exclude guard to funnel-deep-pass SKILL precondition (D-04, D-05, D-06)</name>
  <files>.claude/skills/funnel-deep-pass/SKILL.md</files>
  <read_first>.claude/skills/funnel-deep-pass/SKILL.md lines 82-127 (PRECONDITION CHECK + DETERMINISTIC CHAIN); agents/funnel-deep-pass-run-notes.md sections 1 (corpus req + ads req) and 2 (no-ads fallback); 20-CONTEXT.md D-04/D-05/D-06; /home/kyu3/CLAUDE.md + /home/kyu3/PMF/CLAUDE.md "exclude, do not stub" principle; tools/funnel-assemble.js lines 474-553 (hand-feed precedent, read-only)</read_first>
  <action>
Add a per-brand EXCLUDE guard to the PRECONDITION CHECK block (~82-96) and/or the DETERMINISTIC CHAIN intro, implementing the project "exclude, do not stub" principle. Whether the two checks share one code path or are two checks is Claude's discretion — both produce "exclude with message".

1. CORPUS-ABSENT GUARD (D-04): Before assembling a brand, if BOTH corpus/<brand>/raw/home.html AND corpus/<brand>/clean/home.md are absent, EXCLUDE that brand with a clear message. Provide a copy-paste bash snippet matching the existing precondition bash-block style, e.g.:
   echo "EXCLUDE <brand>: no corpus (raw/home.html and clean/home.md both absent) — cannot assemble a funnel. Skipping; not stubbing."
   NEVER fabricate placeholder corpus data.

2. NO-ADS DTC GUARD (D-05): Document that a DTC (non-crowdfunding) brand with no ads has NO funnel and NO validation currency — no ad->LP funnel to reconstruct (no Currency A) and no crowdfunding_stats (run-notes section 3 forbids adding them to DTC brands -> no Currency B). Decision: EXCLUDE with a clear message; do NOT stub a fake funnel package (stubbing manufactures ungrounded belief records). Note this is exactly why the Plan 20-01 funnel-score.js no-currency FAIL is a guardrail of last resort — the SKILL excludes such brands UPSTREAM so the scorer is never handed them.

3. HAND-FEED ESCAPE HATCH (D-06): Add a one-line note that IF a no-ads brand's landing page is genuinely worth analyzing, the operator HAND-FEEDS the LP as a deliberate act (mirroring the existing crowdfunding zero-ad hand-feed path in tools/funnel-assemble.js ~474-553) — the pipeline NEVER auto-invents a funnel. Reference funnel-assemble.js as the precedent; do NOT modify funnel-assemble.js.

Keep edits within the existing SKILL structure and bash-block style. No new agents (guard text + bash = deterministic, per project CLAUDE.md). Do NOT change the schema-pointer section or the agent prompts.
  </action>
  <verify>
    <automated>cd /home/kyu3/PMF && grep -q "EXCLUDE" .claude/skills/funnel-deep-pass/SKILL.md && grep -qi "not stub\|never.*stub\|do not stub\|not stubbing" .claude/skills/funnel-deep-pass/SKILL.md && grep -q "raw/home.html" .claude/skills/funnel-deep-pass/SKILL.md && grep -q "clean/home.md" .claude/skills/funnel-deep-pass/SKILL.md && grep -qi "hand-feed\|hand feed\|funnel-assemble" .claude/skills/funnel-deep-pass/SKILL.md && echo PASS || { echo FAIL; exit 1; }</automated>
  </verify>
  <done>The SKILL precondition excludes (with a clear message) brands with no corpus and no-ads DTC brands, references the corpus file paths and the no-stub rule, and documents the operator hand-feed escape hatch pointing at funnel-assemble.js. No new agents.</done>
</task>

<task type="auto">
  <name>Task 2: Unify all space-map.json pointers on runs/<space>/ — docs AND the aggregate-mechanisms-in-play.js read/write default (D-07)</name>
  <files>.claude/skills/market-selection/SKILL.md, prompts/step1-light-pass.md, tools/aggregate-mechanisms-in-play.js</files>
  <read_first>.claude/skills/market-selection/SKILL.md lines 40 and 95-102 (the repo-root PATH MISMATCH block); prompts/step1-light-pass.md lines 14-28 (output chain) and ~120 (descriptive schema header — OUT OF SCOPE, do not rewrite); tools/aggregate-mechanisms-in-play.js lines 19-22 (usage doc) and 33-35 (ROOT / SIDECAR / SPACE_MAP defaults); 20-CONTEXT.md D-07 (binding constraint = internal consistency, zero repo-root references)</read_first>
  <action>
Canonical location is runs/<space>/space-map.json (aligns with the project runs/<space>/ output convention). Make every pointer internally consistent — ZERO remaining references that RESOLVE to the repo-root space-map.json, in EITHER the literal `/home/kyu3/PMF/space-map.json` form OR the script form `path.join(ROOT, 'space-map.json')`.

1. market-selection/SKILL.md (~95-102): the "Arduview run — PATH MISMATCH (load-bearing)" block currently documents the four data file sets at repo root (space-map.json -> /home/kyu3/PMF/space-map.json, plus brands.json, ads/, corpus/). Rewrite so space-map.json points at runs/<space>/space-map.json. For the other three files, point them at their runs/<space>/-aligned homes consistent with how the rest of the SKILL and step1 reference them — leave NONE asserting a repo-root /home/kyu3/PMF/ path. The block's purpose flips from "documenting a one-run mismatch" to "the canonical run-relative locations"; reword the heading accordingly (drop "PATH MISMATCH"). Keep line ~40's existing runs/<space>/ phrasing.

2. prompts/step1-light-pass.md: pin the Space Classifier output WRITE-PATH to the canonical location. In the output chain (line ~20: "-> space-map.json"), make explicit the classifier writes runs/<space>/space-map.json. This is the "make the classifier write there" half of D-07.
   SCOPE GUARD: line ~120 ("### `space-map.json` — Space Classifier output (reads all `dump.json`)") is a DESCRIPTIVE SCHEMA HEADER documenting the output's JSON shape, NOT a write-path. Leave it untouched — do not prepend a directory. Only WRITE/OUTPUT references get pinned.

3. tools/aggregate-mechanisms-in-play.js — migrate the space-map DEFAULT path off the repo root (this is the BLOCKER the checker found). This script reads AND re-writes space-map.json; its default currently resolves to the repo root.
   BEFORE (line ~35):
     const SPACE_MAP = path.resolve(opts['space-map'] || path.join(ROOT, 'space-map.json'));
   AFTER — mirror exactly how the SIDECAR default on line ~34 was already migrated to the run-relative form:
     const SPACE_MAP = path.resolve(opts['space-map'] || path.join(ROOT, 'runs', 'arduview', 'space-map.json'));
   (Use the same `runs/arduview/` base as the sidecar default on line 34 so the two defaults are consistent; the operator overrides with --space-map=PATH per run.)
   Then update the usage-doc comment to match.
   BEFORE (line ~21):
     //   [--space-map=PATH] (default space-map.json)
   AFTER:
     //   [--space-map=PATH] (default runs/arduview/space-map.json)
   Do NOT change any other logic in this script (the merge/count/serialize behavior, the sidecar default, ownability math) — ONLY the SPACE_MAP default and its usage-doc line.

4. Do NOT touch tools/hooks/route.js or validate-classifier.js — basename-match, path-agnostic.

After editing, the acceptance grep MUST show zero references that resolve to a repo-root space-map.json — in BOTH the literal-path form and the path.join(ROOT, 'space-map.json') form — anywhere under .claude/, prompts/, tools/.
  </action>
  <verify>
    <automated>cd /home/kyu3/PMF && ROOTLIT=$(grep -rn "/PMF/space-map.json" .claude/ prompts/ tools/ 2>/dev/null | wc -l); PJOIN=$(grep -rnE "path\.join\(ROOT,[[:space:]]*['\"]space-map\.json['\"]\)|ROOT,[[:space:]]*['\"]space-map\.json['\"]" .claude/ prompts/ tools/ 2>/dev/null | wc -l); BARE=$(grep -rnE "default[[:space:]]+space-map\.json" tools/ 2>/dev/null | wc -l); SCRIPTRUN=$(grep -nE "runs', 'arduview', 'space-map\.json'|runs/arduview/space-map\.json" tools/aggregate-mechanisms-in-play.js 2>/dev/null | wc -l); CANON=$(grep -rl "runs/<space>/space-map.json" .claude/skills/market-selection/SKILL.md prompts/step1-light-pass.md 2>/dev/null | wc -l); if [ "$ROOTLIT" -eq 0 ] && [ "$PJOIN" -eq 0 ] && [ "$BARE" -eq 0 ] && [ "$SCRIPTRUN" -ge 1 ] && [ "$CANON" -ge 2 ]; then echo "PASS repo-root-literal=0 path-join-root=0 bare-default=0 script-run-relative=$SCRIPTRUN canonical-docs=$CANON"; else echo "FAIL root_lit=$ROOTLIT path_join=$PJOIN bare=$BARE script_run=$SCRIPTRUN canon=$CANON"; exit 1; fi</automated>
  </verify>
  <done>Zero references resolve to a repo-root space-map.json anywhere under .claude/, prompts/, tools/ — neither the literal /PMF/space-map.json form NOR the path.join(ROOT, 'space-map.json') script form, NOR a bare "default space-map.json" usage-doc line. tools/aggregate-mechanisms-in-play.js read/write default and its usage doc now resolve to the run-relative runs/arduview/space-map.json. market-selection SKILL and step1-light-pass both reference runs/<space>/space-map.json as canonical. The descriptive schema header at step1-light-pass.md line ~120 is left untouched (out of scope — not a write-path).</done>
</task>

<task type="auto">
  <name>Task 3: Scrub stale Read line (D-09) + remove ghost fields from architect INPUTS (D-10)</name>
  <files>prompts/funnel-deep-pass.md, .claude/skills/funnel-architect/SKILL.md</files>
  <read_first>prompts/funnel-deep-pass.md lines 168-173 (the inject-dr.js / stale Read line); .claude/skills/funnel-architect/SKILL.md lines 108-122 (the INPUTS "you receive" list); 20-CONTEXT.md D-08/D-09/D-10; .claude/skills/funnel-deep-pass/SKILL.md lines 64-78 (the deterministic-embed truths the scrub must align with)</read_first>
  <action>
TWO doc-correction edits — no code, no schema change.

D-09 — scrub the stale Read line (prompts/funnel-deep-pass.md ~172):
The current text reads "...the concatenated six-file bundle with attribution headers. The Section Analyzer MUST `Read` that bundle as its first step (see the analyzer prompt below); the orchestrator must regenerate it if the DR sources changed."
Rewrite so it no longer instructs the Section Analyzer to Read the bundle. The correct design (funnel-deep-pass/SKILL.md truth #1): the orchestrator EMBEDS the bundle bytes into the Section Analyzer's spawn prompt; the analyzer NEVER Reads it. Replace the "MUST Read that bundle as its first step" clause with the embed framing (e.g. "...the orchestrator EMBEDS this bundle into the Section Analyzer's spawn prompt — the analyzer never Reads it; the orchestrator regenerates the bundle if the DR sources changed."). This file's orchestration was relocated to SKILL.md (commit 8e45fa6); this is residual cleanup so the stale file cannot mislead. Do NOT add new behavior — just align the line.

D-10 — remove ghost fields from the architect INPUTS spec (funnel-architect/SKILL.md ~118, ~121):
- Line ~118: remove the "belief_kind (crowdfunding-specific | general-DR)" clause from the belief-instance INPUTS bullet (keep position, belief_id, belief_confidence).
- Line ~121: remove the whole "source_routing (which funnel type the fill should come from)" bullet.
These document fields the Section Analyzer never emits (audit/07). Removing them makes the architect's "what you receive" list match reality.
SCOPE GUARD: do NOT touch prompts/funnel-deep-pass.md SCHEMA belief_kind field (~84) or BELIEF_KIND_ENUM (~146) — modifying the producer-side schema is the CONTRACT-GATED half deferred to Track C / Phase 21. This task removes the consumer-side ghost-field DOC references only.
  </action>
  <verify>
    <automated>cd /home/kyu3/PMF && ! grep -qi "MUST .Read. that bundle\|MUST \`Read\` that bundle" prompts/funnel-deep-pass.md && ! grep -q "belief_kind" .claude/skills/funnel-architect/SKILL.md && ! grep -q "source_routing" .claude/skills/funnel-architect/SKILL.md && grep -q "belief_kind" prompts/funnel-deep-pass.md && echo "PASS: stale Read gone, architect ghost fields gone, schema belief_kind preserved" || { echo FAIL; exit 1; }</automated>
  </verify>
  <done>The stale "MUST Read that bundle" instruction is replaced with the embed framing; belief_kind and source_routing are gone from the funnel-architect INPUTS spec; the prompts/funnel-deep-pass.md SCHEMA belief_kind field is untouched (CONTRACT-GATED, out of scope).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits SKILL.md / prompt markdown docs plus one script DEFAULT-PATH constant — no new executable logic, no auth, no network, no untrusted input parsing changed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | (no new surface) | SKILL/doc edits + one script default-path constant | accept | Tasks 1 and 3 are documentation/orchestration-text edits. Task 2 additionally changes ONE constant (the SPACE_MAP default in aggregate-mechanisms-in-play.js) from a repo-root path to a run-relative path — no new logic, no input parsing change; the script already accepts --space-map override. The corpus/no-ads guard is bash the operator runs in the existing precondition flow (already a trusted boundary). No new attack surface, no auth, no external input. The net security effect is positive: the exclude guard prevents fabricated/stubbed data from entering the belief store, and the path-default fix removes a footgun where a re-write could land at an unexpected repo-root location. |

This phase edits internal pipeline SKILLs and prompts (plus one script default-path constant) with no external-facing attack surface. Threat surface is minimal by design; stated explicitly rather than inventing threats.
</threat_model>

<verification>
- D-04/D-05/D-06: funnel-deep-pass SKILL contains EXCLUDE guard text, no-stub rule, corpus file paths, and hand-feed escape hatch (Task 1 grep).
- D-07: zero references resolve to a repo-root space-map.json across .claude/, prompts/, tools/ — neither the /PMF/space-map.json literal NOR the path.join(ROOT, 'space-map.json') script form NOR a bare "default space-map.json" usage line; tools/aggregate-mechanisms-in-play.js read/write default + usage doc now resolve to runs/arduview/space-map.json; runs/<space>/space-map.json present in market-selection SKILL + step1; step1 line ~120 descriptive schema header left untouched (Task 2 grep).
- D-09: no "MUST Read that bundle" instruction remains in prompts/funnel-deep-pass.md; embed framing present (Task 3 grep).
- D-10: belief_kind and source_routing absent from funnel-architect SKILL INPUTS; prompts/funnel-deep-pass.md SCHEMA belief_kind preserved (Task 3 grep).
- Scope guard: tools/funnel-assemble.js, route.js, validate-classifier.js unchanged (git diff shows them untouched); aggregate-mechanisms-in-play.js diff is ONLY the SPACE_MAP default + usage-doc line; no schema-freeze edits leaked.
</verification>

<success_criteria>
- All three tasks' automated grep checks PASS.
- The deep-pass SKILL excludes (not stubs) absent-corpus and no-ads DTC brands and documents the hand-feed path.
- space-map.json is internally consistent on runs/<space>/ with zero repo-root references — including the aggregate-mechanisms-in-play.js read/write default.
- Stale Read line scrubbed; architect ghost fields removed; producer-side schema untouched (CONTRACT-GATED work stays deferred).
</success_criteria>

<output>
After completion, create `.planning/phases/20-deep-pass-bug-fixes-funnel-clean-js-markdown-heading-regex-f/20-02-SUMMARY.md`
</output>
</content>
</invoke>
