---
phase: 20-deep-pass-bug-fixes-funnel-clean-js-markdown-heading-regex-f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/funnel-clean.js
  - tools/funnel-score.js
autonomous: true
requirements: []   # phase_req_ids is null — no REQUIREMENTS.md IDs map to this hardening pass. Coverage = CONTEXT decisions D-01, D-02.
must_haves:
  truths:
    - "A funnel_package whose landing_page_body is markdown (## Heading) emits [SECTION] markers, not a flat unmarked body"
    - "funnel-clean.js still emits [SECTION] markers correctly for HTML-body input (no regression)"
    - "funnel-score.js exits non-zero when a funnel package has neither bound_ads (Currency A) nor valid crowdfunding_stats (Currency B)"
    - "funnel-score.js still scores normally for a package WITH a valid currency (no regression on the unknown→stamp behavior for a present-but-empty lane)"
  artifacts:
    - path: "tools/funnel-clean.js"
      provides: "Section-marking that handles both HTML heading tags AND markdown ATX headings"
      contains: "markdown"
    - path: "tools/funnel-score.js"
      provides: "Fail-loud guard rejecting packages with no validation currency"
      contains: "process.exit"
  key_links:
    - from: "tools/funnel-clean.js stripToText"
      to: "[SECTION] marker emission"
      via: "markdown ATX heading regex pass added before HTML tag-strip"
      pattern: "\\^#\\{1,6\\}|ATX|markdown"
    - from: "tools/funnel-score.js scoreFunnelPackage / CLI boundary"
      to: "non-zero exit"
      via: "no-currency guard (no bound_ads AND no valid crowdfunding_stats)"
      pattern: "process\\.exit\\(1\\)"
---

<objective>
Apply the two `tools/*.js` logic fixes from the funnel deep-pass audit: D-01 (funnel-clean.js markdown-heading section markers) and D-02 (funnel-score.js minimal fail-fast on no-currency packages).

Purpose: A fresh real run must not silently drop section markers on a markdown-body fallback, and must not silently score a package that has no legitimate validation currency. Both are fail-LOUD bug fixes — the operator's explicit ask is "don't let a run silently shit the bed."

Output: `tools/funnel-clean.js` emits `[SECTION]` markers for both HTML and markdown bodies; `tools/funnel-score.js` exits non-zero when a package carries neither Currency A (bound_ads) nor Currency B (valid crowdfunding_stats).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Authoritative user decisions — read FIRST
@.planning/phases/20-deep-pass-bug-fixes-funnel-clean-js-markdown-heading-regex-f/20-CONTEXT.md
# Scope source of truth (bug triage + fix designs)
@.planning/audit/08-deep-pass-bugs.md

# Code to fix (read these regions before editing)
@tools/funnel-clean.js
@tools/funnel-score.js

<interfaces>
<!-- Extracted from codebase. Use directly — no exploration needed. -->

tools/funnel-clean.js — section marking lives in stripToText(html):
- SECTION_MARKER constant (line ~82): `const SECTION_MARKER = '\n\n[SECTION]\n\n';`
- HTML heading marker (line ~159): `s = s.replace(/<h[1-4][^>]{0,200}>/gi, SECTION_MARKER + '$&');`
- HTML block marker (line ~163): `s = s.replace(/<(?:section|article|main|aside)[^>]{0,200}>/gi, SECTION_MARKER + '$&');`
- Tag-strip (line ~166): `s = s.replace(/<[^>]{0,500}>/g, ' ');` — runs AFTER the marker passes.
- Dedup pass (line ~187): `s = s.replace(/(\[SECTION\]\s*){2,}/g, '[SECTION]\n\n');`
- IMPORTANT: markdown ATX headings (`## Heading`) are plain text, NOT tags — they survive tag-strip. A markdown-marker pass must run on lines, anchored to start-of-line `^#{1,6}\s`.

tools/funnel-score.js — scoring + validation lane stamping:
- `scoreCurrencyA(pkg.bound_ads)` returns null when bound_ads is absent/empty.
- `scoreCurrencyB(crowdfunding_stats)` returns null when crowdfunding_stats is null/absent (line ~225-226).
- `scoreFunnelPackage(pkg)` (line ~254): pushes 'unknown' to validation_lane when BOTH scoreA and scoreB are null (line ~271-276) — THIS is the silent path D-02 must make fail loud.
- CLI: single-file mode (line ~344-374) and batch mode (line ~294-342). Single-file mode already `process.exit(1)` on read/parse error (line ~371-372). Batch mode is resilient (one bad funnel never aborts) — see D-02 nuance below.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add markdown ATX heading section-marking to funnel-clean.js (D-01)</name>
  <files>tools/funnel-clean.js</files>
  <read_first>tools/funnel-clean.js lines 141-190 (stripToText), 78-95 (SECTION_MARKER + header comment), .planning/audit/08-deep-pass-bugs.md Bug 3/§3 row, agents/funnel-deep-pass-run-notes.md §3 (Option B markdown caveat)</read_first>
  <behavior>
    - Test A (markdown body): a landing_page_body containing markdown ATX headings on their own lines (e.g. "## Why it works\n", "### Proof\n") → cleaned_body contains at least one "[SECTION]" marker, one per heading.
    - Test B (HTML body, no regression): a landing_page_body with HTML `<h2>...</h2>` and `<section>` tags → still emits "[SECTION]" markers as before.
    - Test C (mixed / dedup): adjacent markers from a markdown heading immediately following a block boundary collapse via the existing dedup pass — no run of 2+ consecutive [SECTION] markers in output.
    - Test D (not-a-heading): a line that merely contains '#' mid-text (e.g. "issue #47 shipped") does NOT get a [SECTION] marker — only start-of-line `#{1,6}` followed by whitespace counts as an ATX heading.
  </behavior>
  <action>
In `stripToText(html)`, add a markdown ATX heading marker pass. Place it BEFORE the final tag-strip (line ~166) and after the HTML block-marker pass (line ~163), so both HTML and markdown paths feed the same dedup at line ~187.

Use a line-anchored, bounded regex (ReDoS-safe per T-03-07 — no nested unbounded quantifiers): match start-of-line `#{1,6}` followed by at least one space/tab, e.g. `/^(#{1,6})[ \t]+/gm`, and prefix the matched line with SECTION_MARKER:
  `s = s.replace(/^#{1,6}[ \t]+/gm, SECTION_MARKER + '$&');`

Rationale to encode in a code comment: markdown ATX headings are plain text (not tags), so they survive HTML tag-stripping; the `clean/home.md` fallback path (run-notes §3 Option B) otherwise produces a flat body with zero [SECTION] markers. This makes BOTH input paths (raw HTML and clean markdown) emit section markers — per D-01. Do NOT touch the HTML heading/block regexes, the entity decode, or the dedup pass logic. The existing dedup at line ~187 already collapses adjacent markers — verify it still fires for markdown-adjacent cases (it operates on the literal `[SECTION]` string, so it does).

Update the header doc comment block (the "Inserts structural section markers on heading/block boundaries" line ~9 and/or the in-function comment near line 156-158) to note markdown ATX headings are also marked.

Do NOT change the CLI, output schema, file paths, or review-block handling. This is a single additive regex pass plus comments.
  </action>
  <verify>
    <automated>cd /home/kyu3/PMF && node -e '
const cp=require("child_process");const fs=require("fs");const os=require("os");const path=require("path");
const d=fs.mkdtempSync(path.join(os.tmpdir(),"fc-"));
const mdPkg={funnel_id:"md-test",competitor:"x",landing_page_url:"http://x",landing_page_body:"Intro line\n## Why it works\nbody copy here that is long enough\n### Proof\nmore copy here that is also sufficiently long\nissue #47 shipped fine\n"};
fs.writeFileSync(path.join(d,"md-test.json"),JSON.stringify(mdPkg));
cp.execSync(`node tools/funnel-clean.js ${path.join(d,"md-test.json")} --out=${d}`,{stdio:"ignore"});
const out=JSON.parse(fs.readFileSync(path.join(d,"md-test-clean.json"),"utf8"));
const n=(out.cleaned_body.match(/\[SECTION\]/g)||[]).length;
const midHash=/\[SECTION\][^\n]*issue #47/.test(out.cleaned_body);
if(n>=2 && !midHash){console.log("PASS markdown sections="+n);}else{console.error("FAIL sections="+n+" cleaned="+JSON.stringify(out.cleaned_body));process.exit(1);}
'</automated>
  </verify>
  <done>A markdown landing_page_body emits ≥2 [SECTION] markers (one per ATX heading); a mid-line "#" does not produce a marker; HTML-body marking is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make funnel-score.js fail loud on no-currency packages (D-02)</name>
  <files>tools/funnel-score.js</files>
  <read_first>tools/funnel-score.js lines 218-284 (scoreCurrencyB, scoreFunnelPackage), 286-375 (CLI single + batch modes), .planning/audit/08-deep-pass-bugs.md Bug §5/§2 rows + D-02/D-03 in 20-CONTEXT.md (MINIMAL only — no field-name schema validator)</read_first>
  <behavior>
    - Test A (no currency → fail): a package with `bound_ads: []` (or absent) AND no `crowdfunding_stats` → single-file run exits NON-ZERO with a clear message naming the missing currency. NO `*-scored.json` claiming `validation_lane: ['unknown']` is silently written as the success path.
    - Test B (Currency B present → pass): a package with valid `crowdfunding_stats` (e.g. amount_raised set) and no ads → exits 0 and writes a scored file with validation_lane ['B'].
    - Test C (Currency A present → pass): a package with non-empty `bound_ads` → exits 0, validation_lane includes 'A'.
    - Test D (batch resilience): in batch mode, a no-currency package is reported as an error line and does NOT abort the batch (one bad funnel never aborts — fetch.js pattern), but the batch process exits non-zero at the end if ANY funnel had no currency, so a run cannot pass silently.
  </behavior>
  <action>
Implement MINIMAL fail-fast per D-02. The bar: error (non-zero exit) when a funnel package has NEITHER `bound_ads` (Currency A) NOR valid `crowdfunding_stats` (Currency B) — no validation currency at all means nothing legitimate to score. Do NOT build the full field-name schema validator (D-03 — that is Track C / Phase 21; do not enforce `amount_raised` vs `amount_raised_usd` here).

Add a `hasValidationCurrency(pkg)` helper near scoreFunnelPackage: returns true if `scoreCurrencyA(pkg.bound_ads) !== null || scoreCurrencyB(pkg.crowdfunding_stats) !== null`. (Reuse the existing scoring functions so the definition of "valid" stays single-sourced — a package with `crowdfunding_stats: {}` where all four fields are null still produces a non-null scoreB object today; minimal scope keeps that as-is, currency = the object exists. Treat presence of a non-null `crowdfunding_stats` object as Currency B, and a non-empty `bound_ads` array as Currency A.)

Wire the guard at the CLI boundary, NOT inside scoreFunnelPackage (leave the 'unknown' stamp logic intact so any downstream caller importing the function is unaffected — but the CLI must refuse to emit a scored file for a no-currency package):

- Single-file mode (line ~344-374): before calling scoreFunnelPackage / writing output, check `hasValidationCurrency(pkg)`. If false: `console.error('[funnel-score] FAIL: <funnel_id> has no validation currency — neither bound_ads (Currency A) nor crowdfunding_stats (Currency B). Nothing legitimate to score. (No-ads DTC brands must be EXCLUDED upstream, not scored — see funnel-deep-pass SKILL corpus/no-ads guard.)'); process.exit(1);`

- Batch mode (line ~294-342): inside the per-funnel try, if `!hasValidationCurrency(pkg)`, push a clear `[NO-CURRENCY] <file>: ...` line, do NOT write a scored file for it, and set a `let hadNoCurrency = true;` flag (declare it before the loop). Keep processing the rest (resilient batch). After the loop + sidecar log write, if `hadNoCurrency` is true, `console.error('[funnel-score] FAIL: one or more packages had no validation currency — see _funnel-score-log.txt'); process.exit(1);` so the run cannot pass silently.

Use clear, specific error wording (exact message is Claude's discretion per CONTEXT). Do NOT add new dependencies, change the scored-output schema for valid packages, or alter the two-currency / never-normalize invariant (D-09 of Phase 03).
  </action>
  <verify>
    <automated>cd /home/kyu3/PMF && node -e '
const cp=require("child_process");const fs=require("fs");const os=require("os");const path=require("path");
const d=fs.mkdtempSync(path.join(os.tmpdir(),"fs-"));
function run(p){try{cp.execSync(`node tools/funnel-score.js ${p} --out=${d}`,{stdio:"pipe"});return 0;}catch(e){return e.status||1;}}
// A: no currency -> non-zero, no scored file written
const noCur={funnel_id:"nc",bound_ads:[],landing_page_url:"http://x"};
fs.writeFileSync(path.join(d,"nc.json"),JSON.stringify(noCur));
const rcA=run(path.join(d,"nc.json"));
const wroteNc=fs.existsSync(path.join(d,"nc-scored.json"));
// B: currency B present -> zero, scored file written
const cf={funnel_id:"cf",bound_ads:[],crowdfunding_stats:{amount_raised:120000,backer_count:800,funded_vs_failed:"funded",delivered_vs_not:"delivered"}};
fs.writeFileSync(path.join(d,"cf.json"),JSON.stringify(cf));
const rcB=run(path.join(d,"cf.json"));
const wroteCf=fs.existsSync(path.join(d,"cf-scored.json"));
if(rcA!==0 && !wroteNc && rcB===0 && wroteCf){console.log("PASS no-currency exits "+rcA+", currency-B exits "+rcB);}else{console.error(`FAIL rcA=${rcA} wroteNc=${wroteNc} rcB=${rcB} wroteCf=${wroteCf}`);process.exit(1);}
'</automated>
  </verify>
  <done>A no-currency package causes a non-zero exit with a clear message and no scored output; a package with a valid Currency A or B scores and exits 0. Batch mode stays resilient but exits non-zero if any package lacked currency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| competitor LP copy → funnel-clean.js | Untrusted third-party HTML/markdown enters as `landing_page_body`. This boundary already existed; this plan adds one more regex pass over the same input. |
| funnel_package JSON → funnel-score.js | Operator/pipeline-produced JSON; not externally reachable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | D (DoS / ReDoS) | new markdown-heading regex in funnel-clean.js stripToText | mitigate | Use a line-anchored, bounded regex `/^#{1,6}[ \t]+/gm` — no nested/unbounded quantifiers; input already capped at MAX_BODY_BYTES before any regex (existing T-03-07 control). No new unbounded backtracking introduced. |
| T-20-02 | T (Tampering / injection) | markdown heading content preserved verbatim | accept | funnel-clean only MARKS and preserves; it never executes content. The downstream Section Analyzer prompt-injection boundary (existing UNTRUSTED DATA boundary in SKILL.md) is unchanged. No new surface. |
| T-20-03 | (no new surface) | funnel-score.js guard | accept | Pure internal validation logic over pipeline-produced JSON; no auth, no network, no external input beyond what the pipeline already handles. The change makes behavior MORE conservative (fails loud). No new attack surface. |

This plan edits internal pipeline scripts with no external-facing attack surface, no auth, and no untrusted network input beyond what the existing pipeline already handles. Threat surface is minimal by design; the only net-new code is one bounded regex and one validation guard, both covered above.
</threat_model>

<verification>
- D-01: `node tools/funnel-clean.js` on a markdown-body funnel_package emits ≥2 `[SECTION]` markers; HTML-body marking unchanged; mid-line `#` is not marked (Task 1 automated check).
- D-02: `node tools/funnel-score.js` exits non-zero on a no-currency package (no scored file written); exits 0 and writes a scored file when Currency A or B is present (Task 2 automated check).
- No new npm dependencies introduced (`git diff package.json` empty).
- Scope guard: no field-name schema validation added (D-03 out of scope) — grep the diff to confirm no `amount_raised_usd` normalization or required-field enforcement was added.
</verification>

<success_criteria>
- Both tasks' automated checks PASS.
- `tools/funnel-clean.js` marks markdown ATX headings AND retains HTML marking (no regression).
- `tools/funnel-score.js` fails loud on no-currency packages and still scores valid ones.
- Two-currency / never-normalize invariant preserved; no schema-freeze work leaked in.
</success_criteria>

<output>
After completion, create `.planning/phases/20-deep-pass-bug-fixes-funnel-clean-js-markdown-heading-regex-f/20-01-SUMMARY.md`
</output>
