---
phase: 02-stage-m1-s2-market-selection-gate
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - runs/arduview/market-selection.md
autonomous: false
requirements: [GATE-01]
must_haves:
  truths:
    - "The rewritten skill runs against the real on-disk Arduview data and produces an output artifact"
    - "runs/arduview/market-selection.md exists with a per-cell gate record for all 6 cells"
    - "Every gate verdict cites the count/claim/trend/axis datum behind it"
    - "All 6 cells are carried through and ranked (soft-gate mode), none hard-eliminated"
    - "demand_trend:unknown is surfaced; 'fix the Trends fetch' is the #1 blocker"
    - "mechanisms-in-play is a DERIVED [INFERENCE] read per evaluated cell (shared vs unique, n= per cluster) from mechanism[], not a bare DATA GAP"
    - "comparable_bet_seed brands never appear as candidate cells"
    - "Kam judges the kill/survive verdicts + ranking sound (human UAT)"
  artifacts:
    - path: "runs/arduview/market-selection.md"
      provides: "Ranked PROVISIONAL survivor set + per-cell gate records for Kam's bet pick"
      contains: "Gate 1"
  key_links:
    - from: "runs/arduview/market-selection.md"
      to: "space-map.json combos[]"
      via: "6 per-cell gate records derived from the 6 combos"
      pattern: "maker-identity\\|learn-to-code\\|retro-gaming\\|novelty-object\\|music-creation"
---

<objective>
Exercise the rewritten `.claude/skills/market-selection/SKILL.md` (from Plan 01) against the REAL
on-disk Arduview data and produce `runs/arduview/market-selection.md` — the ranked PROVISIONAL
survivor set with per-cell gate records that Kam reads to make the bet pick (D1).

Purpose: This is the phase's UAT. Success Criterion 4 is human-judgment — "Kam runs candidate NTPs
and judges the kill/survive verdicts + ranking sound." That requires a concrete artifact produced
by the actual skill against the actual data, not a dry-run description. Verification on this project
is UAT, not unit tests (STATE.md decision).

Output: `runs/arduview/market-selection.md` containing 6 per-cell gate records (all 6 combos cells),
evidence-cited verdicts, all flags (durability UNKNOWN, a DERIVED [INFERENCE] mechanisms-in-play read
per cell, community-heat deferred), a ranked provisional survivor list, and "fix the Trends fetch
(Phase 1)" surfaced as the #1 blocker — all in soft-gate provisional mode (D-08/D-09).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.claude/skills/market-selection/SKILL.md
@.planning/phases/02-stage-m1-s2-market-selection-gate/02-CONTEXT.md
@runs/arduview/pre-research-plan.md

<interfaces>
<!-- The real data files the skill consumes. Confirmed on disk. PATH MISMATCH is load-bearing. -->
- space-map.json   → repo ROOT: /home/kyu3/PMF/space-map.json   (combos[] = the 6 cells; per_brand[]; bet_types[])
- brands.json      → repo ROOT: /home/kyu3/PMF/brands.json      (revenue_est, crowdfunding, price_points, channel)
- ads/<brand>.json → repo ROOT: /home/kyu3/PMF/ads/*.json       (ads[].run_length_days; most empty, flipper-zero dense)
- pre-research-plan.md → /home/kyu3/PMF/runs/arduview/pre-research-plan.md  (the 4 operator overrides as prose, all SET)

The 6 cells the gate records must cover (from combos[]):
  1. maker-identity × maker-diy-hobbyists
  2. learn-to-code × maker-diy-hobbyists
  3. learn-to-code × learn-to-code-students
  4. retro-gaming-relive × retro-gamers
  5. novelty-object-own × edc-aesthetic-collectors
  6. music-creation × edc-aesthetic-collectors

Confirmed run facts (the skill must REFLECT these, never paper over):
- demand_trend.shape == "unknown" for all 20 brands → every cell stamped "durability UNKNOWN"
- revenue_est.value_usd_monthly == null for all 20 brands → no revenue signal clears Gate 1
- combos[].anti_fluke.qualifying_creatives == 0 in all 6 cells → no ad cleared 7+ days in candidate cells
- enhanced_claim_count == 0, claims[].type ∈ {direct, enlarged} only → Gate 3 reads Stage 1–2 across the board
- crowdfunding populated only on gameshell (kickstarter, $350,000+, 700%+, funded-shipped) and skeleton-key (unknown)
- mechanisms_in_play[] slot ABSENT from space-map.json → the skill reads mechanisms-in-play-stopgap.md and
  DERIVES the shared-vs-unique read per cell from per-pitch mechanism[] in repo-root corpus/<brand>/dump.json (canonical-layout label runs/<space>/corpus/<brand>/dump.json),
  fenced [INFERENCE] with n= per cluster (live in-geo brands only; comparable_bet_seed + dead/region-only excluded) — NOT a bare DATA GAP
- comparable_bet_seed brands: arduboy, flipper-zero, nothing-phone, skeleton-key → BET-EVIDENCE only, never cells
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the rewritten skill against the real Arduview data and write runs/arduview/market-selection.md</name>
  <files>runs/arduview/market-selection.md</files>
  <read_first>
    - .claude/skills/market-selection/SKILL.md (the rewritten skill — follow its procedure exactly; do NOT improvise gate logic)
    - /home/kyu3/PMF/space-map.json (combos[] — the 6 cells; per_brand[] — bet_type + sophistication cross-check + demand_trend)
    - /home/kyu3/PMF/brands.json (per-brand revenue_est, crowdfunding, price_points, product_observed, channel, ads_flag)
    - /home/kyu3/PMF/ads/anbernic.json, ads/divoom.json, ads/flipper-zero.json, ads/nothing-phone.json (run_length_days; cross-check against the pre-counted combos[].anti_fluke.qualifying_creatives)
    - /home/kyu3/PMF/runs/arduview/pre-research-plan.md (§"Supply-side validation stance" line 62, §"Deferred reads" line 67 — the 4 operator overrides, all SET this run)
    - .claude/skills/market-selection/mechanisms-in-play-stopgap.md (D-05 — the decision-time mechanism derivation the skill runs per cell: cluster per-pitch mechanism[], shared 3+ / unique 1, [INFERENCE], n= per cluster)
    - /home/kyu3/PMF/corpus/<brand>/dump.json (repo-root for the Arduview run; canonical-layout label runs/<space>/corpus/<brand>/dump.json) (creatives[].pitches[].mechanism[] — the source for the derived mechanisms-in-play read; live in-geo brands per cell, exclude comparable_bet_seed + dead/region-only)
    - .planning/phases/02-stage-m1-s2-market-selection-gate/02-CONTEXT.md (D-05, D-07, D-08, D-09, D-10 — what the output must contain and how soft-gate/provisional shape it)
  </read_first>
  <action>
    INVOKE the rewritten market-selection skill as the assessor — read its inlined procedure and
    EXECUTE it against the real on-disk Arduview data. Do not re-derive the gate logic; the SKILL.md
    is the procedure. Honor the path mismatch: read the four data files at repo ROOT and the brief at
    `runs/arduview/pre-research-plan.md`.

    Produce `runs/arduview/market-selection.md` with, in order:

    1. A header noting RUN MODE: soft-gate, PROVISIONAL (D-08/D-09), Gate 4 deferred, and the run's
       data-quality caveats (demand_trend unknown, revenue null).

    2. The #1 BLOCKER surfaced up front: "fix the Trends fetch (Phase 1)" — demand_trend is unknown for
       all 20 brands, so Gate 1 durability cannot clear with confidence (D-09). List the two bare DATA
       GAPs: `demand_trend: unknown` and `revenue_est.value_usd_monthly` (null for all brands — state the
       revenue read as null in each Gate-1 record, e.g. "revenue_est null"). For `mechanisms_in_play`
       (D-05): note the space-map.json output slot is still absent (cross-phase Phase-1 add, BREAK 5), but
       this run DERIVES the shared-vs-unique mechanism read per cell from per-pitch `mechanism[]` per the
       stopgap doc, fenced `[INFERENCE]` — it is NOT carried as a bare DATA GAP.

    3. A per-cell gate record for EACH of the 6 combos cells (D-07 — evaluate every cell; no shortlist).
       For each cell, follow the SKILL.md output-record shape:
       - Cell: `<transformation> × <niche>`.
       - Gate 1 (Demand): per-signal read with the cited datum (revenue null; ad longevity —
         qualifying_creatives = 0 from anti_fluke; crowdfunding status if any brand in the cell has it);
         trend = unknown → "durability UNKNOWN — Gate 1 cannot clear with confidence"; soft-gate → flag +
         ranking penalty, NOT eliminated; demand magnitude for identity cells flagged "requires separate
         community-heat read."
       - Gate 2 (Product): named UM axis read from the cell brands' OPEN `bet_type` + `bet_type_basis`
         (NOT a closed enum); the 2.2 differentiation check uses the DERIVED `[INFERENCE]` mechanisms-in-play
         read (shared vs unique, `n=` per cluster) from `mechanism[]` per the stopgap doc; price band from
         `price_points[]`; product-category/channel; economics = pending operator COGS.
       - Gate 3 (Sophistication): per-cell stage from `claim_count`/`enhanced_claim_count`/`claims[].type`
         (Stage 1–2 expected, enhanced=0); note where S1's per-brand sophistication string disagrees
         (cross-check only); 3.3-S3 → a DERIVED `[INFERENCE]` mechanisms-in-play read (shared vs unique,
         `n=` per cluster) from per-pitch `mechanism[]` per the stopgap doc — NOT a bare DATA GAP — at
         lowered confidence.
       - Gate 4: "PENDING DEEP-RESEARCH STEP — PROVISIONAL."
       - Verdict + 4-sentence prose synthesis.
       Every factual claim cites its datum (count/claim/trend/axis) — no verdict without its datum.
       Every cell record carries an explicit `Gate 1`, `Gate 2`, AND `Gate 3` heading (Gate 4 deferred
       label) — all three gates run per cell, so all three appear ≥6 times across the artifact.
       Fence every AI read with `[INFERENCE]` (stage assignment, intensity proxy, is-live inference).

    4. The ranked PROVISIONAL survivor list — all 6 cells carried through (soft-gate), ranked by relative
       demand + the difficulty/growth tilt, each with a one-line rationale. State which operator overrides
       were applied (all 4 SET this run, per the brief).

    5. STOP at the ranked survivors. Do NOT pick the bet — that is Kam's D1. Do NOT write "we should test X."

    Confirm comparable_bet_seed brands (arduboy, flipper-zero, nothing-phone, skeleton-key) appear ONLY
    as bet-durability evidence and NEVER as candidate cells.
  </action>
  <verify>
    <automated>test -f runs/arduview/market-selection.md && grep -q "maker-identity" runs/arduview/market-selection.md && grep -q "learn-to-code" runs/arduview/market-selection.md && grep -q "retro-gaming-relive" runs/arduview/market-selection.md && grep -q "novelty-object-own" runs/arduview/market-selection.md && grep -q "music-creation" runs/arduview/market-selection.md && grep -qi "durability UNKNOWN" runs/arduview/market-selection.md && grep -qi "mechanisms_in_play" runs/arduview/market-selection.md && grep -q "n=" runs/arduview/market-selection.md && ! grep -q "DATA GAP: mechanisms_in_play" runs/arduview/market-selection.md && grep -qi "revenue.*null\|revenue_est.*null" runs/arduview/market-selection.md && grep -qi "INFERENCE" runs/arduview/market-selection.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `test -f runs/arduview/market-selection.md` succeeds (artifact exists — Success Criterion 4 has something to judge)
    - All 6 cell names present: `grep -q "maker-identity"` AND `grep -q "learn-to-code"` AND `grep -q "retro-gaming-relive"` AND `grep -q "novelty-object-own"` AND `grep -q "music-creation"` all succeed (6 per-cell records, D-07)
    - `grep -ci "Gate 1" runs/arduview/market-selection.md` returns ≥ 6 (a Gate-1 record per cell)
    - `grep -ci "Gate 2" runs/arduview/market-selection.md` returns ≥ 6 (a Gate-2 record per cell — proves every cell got a full 3-gate record)
    - `grep -ci "Gate 3" runs/arduview/market-selection.md` returns ≥ 6 (a Gate-3 record per cell — proves every cell got a full 3-gate record)
    - `grep -qi "durability UNKNOWN" runs/arduview/market-selection.md` succeeds (D-09 provisional stamp present)
    - `grep -qi "fix the Trends fetch" runs/arduview/market-selection.md` succeeds (D-09 #1 blocker surfaced)
    - `grep -qi "mechanisms_in_play\|mechanisms-in-play" runs/arduview/market-selection.md` succeeds AND a `[INFERENCE]` mechanisms-in-play read with `n=` per cluster is present (D-05 — derived shared-vs-unique read per cell from mechanism[], NOT a bare DATA GAP)
    - `grep -qi "revenue.*null\|revenue_est.*null" runs/arduview/market-selection.md` succeeds (D-03 — the revenue-null DATA GAP, one of the two bare DATA GAPs with demand_trend, explicitly surfaced, not papered over)
    - `grep -qi "PENDING DEEP-RESEARCH" runs/arduview/market-selection.md` succeeds (Gate 4 deferred, D-05)
    - `grep -qi "INFERENCE" runs/arduview/market-selection.md` succeeds (AI reads fenced, D-10)
    - `grep -qi "we should test" runs/arduview/market-selection.md` returns NOTHING (A4-not-D1 stop rule, D-10)
  </acceptance_criteria>
  <done>runs/arduview/market-selection.md exists with 6 evidence-cited per-cell gate records (all combos cells, each carrying a full Gate 1/2/3 record), durability-UNKNOWN stamps, the Trends-fetch #1 blocker, the two bare DATA GAPs surfaced (demand_trend unknown, revenue_est null) plus a DERIVED [INFERENCE] mechanisms-in-play read per cell (shared vs unique, n= per cluster, from mechanism[] per the stopgap doc — not a bare DATA GAP), Gate-4-deferred provisional labels, [INFERENCE] fencing, and a ranked all-6-carried-through provisional survivor list. The file stops at ranked survivors — no bet pick.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Kam judges the verdicts + ranking sound (Success Criterion 4 UAT)</name>
  <files>runs/arduview/market-selection.md</files>
  <action>Human-judgment UAT checkpoint: Kam reviews runs/arduview/market-selection.md and judges whether the kill/survive verdicts and the ranking are sound. Present the artifact and the verification steps below; do not auto-resolve.</action>
  <what-built>
    The rewritten market-selection skill was run against the real on-disk Arduview data, producing
    `runs/arduview/market-selection.md`: 6 per-cell gate records, evidence-cited verdicts, soft-gate
    provisional ranking of all 6 cells, with durability-UNKNOWN flags, a DERIVED [INFERENCE] mechanisms-in-play read per cell,
    the Trends-fetch #1 blocker, and Gate-4-deferred labels.
  </what-built>
  <how-to-verify>
    1. Open `runs/arduview/market-selection.md`.
    2. Read the 6 per-cell gate records. For each: does the kill/survive/flag verdict follow from the
       cited datum (count/claim/trend/axis), or is it hand-waved?
    3. Check the ranking: are the 6 cells ordered by a defensible relative-demand + difficulty read,
       given that revenue is null and trend is unknown? Is soft-gate mode honored (all 6 carried, none
       hard-eliminated)?
    4. Confirm the honesty flags: durability UNKNOWN on every cell, the mechanisms-in-play read DERIVED
       and fenced [INFERENCE] with n= per cluster (not a bare DATA GAP, not silently invented), community-heat
       deferred on identity cells, Trends-fetch as the #1 blocker.
    5. Confirm comparable_bet_seed brands (arduboy/flipper-zero/nothing-phone/skeleton-key) are NOT
       treated as candidate cells, only as bet-durability evidence.
    6. Confirm the skill STOPPED at ranked survivors and did NOT pick the bet.
    Judgment call: are the kill/survive verdicts and the ranking SOUND? If not, note which cell's verdict
    or which ranking position is wrong and why — that feeds a Plan-01 revision (the skill logic), not a
    re-run.
  </how-to-verify>
  <resume-signal>Type "approved" if the verdicts + ranking are sound, or describe which verdicts/rankings are wrong (feeds a skill revision).</resume-signal>
</task>

</tasks>

<threat_model>
This plan produces a markdown analysis artifact (`runs/arduview/market-selection.md`) by reading
local JSON the operator produced. No executable, network, auth, or secret surface; no untrusted
input. Threat model: not applicable — local markdown read/write only.
</threat_model>

<verification>
- `test -f runs/arduview/market-selection.md` (artifact produced)
- All 6 cell names present; ≥6 each of Gate 1 / Gate 2 / Gate 3 records (full per-cell 3-gate coverage, D-07)
- `grep -qi "durability UNKNOWN"`, `grep -qi "fix the Trends fetch"`, `grep -qi "mechanisms_in_play\|mechanisms-in-play"`,
  `grep -qi "revenue.*null"`, `grep -qi "PENDING DEEP-RESEARCH"`, `grep -qi "INFERENCE"` all succeed
  (two bare DATA GAPs + a DERIVED [INFERENCE] mechanisms-in-play read + honesty flags + provisional mode)
- `grep -qi "we should test"` returns nothing (A4-not-D1 stop rule)
- Human (Kam): verdicts + ranking judged sound (Success Criterion 4).
</verification>

<success_criteria>
- The rewritten skill runs against real Arduview data and produces runs/arduview/market-selection.md.
- 6 evidence-cited per-cell gate records; per-cell saturation honored, never pooled.
- Soft-gate provisional mode: all 6 cells ranked + carried, demand_trend-unknown surfaced, the two bare
  DATA GAPs flagged not guessed (demand_trend, revenue null) and mechanisms-in-play DERIVED [INFERENCE]
  per cell from mechanism[] (not a bare DATA GAP, not invented), Gate 4 deferred.
- Kam judges the kill/survive verdicts + ranking sound (human UAT — Success Criterion 4).
</success_criteria>

<output>
After completion, create
`.planning/phases/02-stage-m1-s2-market-selection-gate/02-02-SUMMARY.md`.
</output>
