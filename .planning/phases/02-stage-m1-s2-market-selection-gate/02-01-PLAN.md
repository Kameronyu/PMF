---
phase: 02-stage-m1-s2-market-selection-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/market-selection/SKILL.md
autonomous: true
requirements: [GATE-01]
must_haves:
  truths:
    - "SKILL.md reasons Gate 2.2 over the OPEN bet_type the classifier named (and never imports the spec's competitive_axis enum during inlining)"
    - "SKILL.md reads ad longevity off run_length_days, not days_running"
    - "SKILL.md defers Gate 4 (awareness) and labels survivors PROVISIONAL"
    - "SKILL.md derives Gate 3 stage per-CELL off typed claims, not the per-brand sophistication string"
    - "SKILL.md reads mechanisms-in-play-stopgap.md and derives the shared-vs-unique mechanism read from mechanism[], fenced [INFERENCE] — it does NOT emit a bare DATA GAP and skip"
    - "SKILL.md runs in SOFT-GATE mode (kills become flags + ranking penalties this run)"
    - "SKILL.md reads operator overrides as prose from pre-research-plan.md"
    - "SKILL.md documents the repo-root vs runs/<space>/ input path mismatch"
    - "SKILL.md auto-injects the assessor-cut DR-KB set via read_first"
    - "SKILL.md carries the derived-from-spec header"
  artifacts:
    - path: ".claude/skills/market-selection/SKILL.md"
      provides: "Runnable 3-gate (4th deferred) assessor wired to the real S1 contract"
      contains: "bet_type"
  key_links:
    - from: ".claude/skills/market-selection/SKILL.md"
      to: "space-map.json combos[]"
      via: "Gate 1/2/3 iterate the 6 cells"
      pattern: "combos"
    - from: ".claude/skills/market-selection/SKILL.md"
      to: "ads/<brand>.json"
      via: "Gate 1.1 reads run_length_days"
      pattern: "run_length_days"
    - from: ".claude/skills/market-selection/SKILL.md"
      to: "runs/arduview/pre-research-plan.md"
      via: "operator overrides read as prose"
      pattern: "pre-research-plan"
---

<objective>
Rewrite `.claude/skills/market-selection/SKILL.md` in place so the authoritative four-gate
procedure from `prompts/_specs/market-selection-assessor-spec.md` EXECUTES when the skill is
invoked, and so its input contract matches the CURRENT real S1 output on disk (the Arduview run).

The current SKILL.md is STALE: it carries a full Gate-4 awareness procedure, a `days_running`
field name, and a wishlist "❌ add to schema" input-contract table for fields that now EXIST on
disk. This plan replaces all of that. SEPARATELY, the MASTER spec
(`prompts/_specs/market-selection-assessor-spec.md`) still carries the dead `competitive_axis`
closed enum (`function-capability-price | visual-statement | community-openness`) in its Gate 2.2
INPUTS block — the spec's own bottom reconciliation note flags it. D-02's positive work is twofold:
(a) rewrite SKILL.md's Gate 2.2 body to reason over the OPEN `bet_type` / `bet_type_basis` the
classifier named, and (b) ensure that when the spec's procedure is inlined into SKILL.md, the
inlining does NOT import that dead `competitive_axis` enum from the spec. (Current SKILL.md's
Gate 2.2 already uses generic "differentiating axis" prose — the job is to wire it to the real
OPEN bet_type, not to delete an enum that isn't there.)

Purpose: This skill is the DECISION half of Track A (M1-S2 / GATE-01). It reads already-labeled
S1 data and produces kill/survive/rank verdicts over the 6 candidate cells — an A4 (synthesize)
brick feeding a D1 (decide) human. It must be runnable before Plan 02 can exercise it.

Output: A rewritten, runnable `SKILL.md` wired to `space-map.json` / `brands.json` / `ads/*.json`
+ `runs/arduview/pre-research-plan.md`, carrying a derived-from-spec header, running in soft-gate
provisional mode for this first/debug run.

This is a markdown prompt file with NO executable/network/auth surface — see <threat_model>.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-stage-m1-s2-market-selection-gate/02-CONTEXT.md
@.planning/phases/02-stage-m1-s2-market-selection-gate/02-PATTERNS.md
@prompts/_specs/market-selection-assessor-spec.md
@.claude/skills/market-selection/SKILL.md
@prompts/step1-light-pass.md

<interfaces>
<!-- Real on-disk shapes the rewrite must wire to. Confirmed present this run. Do NOT re-derive. -->

space-map.json top-level keys (CONFIRMED — note: NO mechanisms_in_play slot):
['_provenance','_anti_fluke_floor','_comparable_bet_seed_note',
 'transformations','niches','angles','bet_types','combos','per_brand','saturation']

combos[] — the per-CELL gate unit (6 cells; Gate 1/2/3 iterate these):
{
  "transformation": "maker-identity", "niche": "maker-diy-hobbyists",
  "brand_count": 5, "creative_count": 5, "brands": ["thumby","gameshell",...],
  "claim_count": 3, "enhanced_claim_count": 0,
  "claims": [ {"text": "...", "type": "direct"} ],            // types present this run: direct, enlarged ONLY
  "anti_fluke": { "brands_at_scale": 5, "qualifying_creatives": 0 },  // PRE-COUNTED = 0 in all 6 cells
  "_note": "Comparable_bet_seeds (arduboy, flipper-zero) EXCLUDED ..."
}
The 6 cells: maker-identity×maker-diy-hobbyists, learn-to-code×maker-diy-hobbyists,
learn-to-code×learn-to-code-students, retro-gaming-relive×retro-gamers,
novelty-object-own×edc-aesthetic-collectors, music-creation×edc-aesthetic-collectors.

bet_types[] (top-level OPEN canonical list — Gate 2.2 reasons over this, NOT the dead spec enum):
["novel-hardware-as-lead","open-source-hackability-as-lead","hardware-authenticity-as-lead",
 "value-breadth-as-lead","maker-ecosystem-store"]

per_brand[]:
{
  "slug":"arduboy", "comparable_bet_seed": true,            // seeds: arduboy, flipper-zero, nothing-phone, skeleton-key
  "bet_type":"open-source-hackability-as-lead", "bet_type_basis":"Page leads: 'open source ...'",
  "demand_trend": {"shape":"unknown","window":null,"source":null,"basis":null},  // unknown for ALL 20 brands
  "sophistication": "Stage 1 — in maker-identity × maker-diy cell: ..."   // mis-grained STRING — cross-check only
}

brands.json per-brand (revenue null for ALL 20 brands this run):
{
  "brand":"Arduboy","slug":"arduboy","channel":"dtc","ads_flag":"unsure","crowdfunding": null,
  "price_points":["$64.00","$79.00"],
  "revenue_est": {"value_usd_monthly": null, "method": null, "confidence": null, ...},
  "demand_trend": {"shape":"unknown", ...}, "comparable_bet_seed": true,
  "status":"live", "market_presence":"in-geo"
}
crowdfunding populated only on: gameshell ({"platform":"kickstarter","raised":"$350,000+",
"pct_funded":"700%+","status":"funded-shipped"}), skeleton-key (all-unknown).

ads/<brand>.json — Gate 1.1 signal-2 (field is run_length_days, NOT days_running):
{ "slug":"flipper-zero","active_ad_count":"48",
  "ads":[ {"library_id":"...","start_date":"...","end_date":null,"run_length_days":155,"text":"..."} ] }
Most ads files are empty (ads:[], active_ad_count:"0", e.g. anbernic). Only flipper-zero.json is
dense (117 ads) — and flipper is a comparable_bet_seed, EXCLUDED → this is WHY qualifying_creatives=0.

corpus/<brand>/dump.json — D-05 mechanism source (field is creatives[].pitches[].mechanism[]):
For the Arduview run the dumps live at repo ROOT: /home/kyu3/PMF/corpus/<brand>/dump.json
(e.g. corpus/flipper-zero/dump.json, corpus/adafruit/dump.json — all 6 cells' live brands present).
The canonical layout LABEL is runs/<space>/corpus/<brand>/dump.json; the Arduview on-disk path is
repo-root corpus/<brand>/dump.json (same repo-root vs runs/<space>/ mismatch as the four data files).

DR-KB files (assessor-cut set, confirmed on disk under ~/knowledge/dr-marketing/):
consumer-psychology--carl-weische.md, brand-building--spencer-origins.md,
ecommerce--mark-builds-brands.md ($300–500K floor MISCALIBRATED — do NOT apply),
differentiator-framework__2_.md, product-research--spencer-origins.md (3-variable difficulty model).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the skill header, read_first, input-contract scaffolding, DR-KB injection, and the path-mismatch resolution</name>
  <files>.claude/skills/market-selection/SKILL.md</files>
  <read_first>
    - .claude/skills/market-selection/SKILL.md (the file being rewritten — read it whole first)
    - prompts/_specs/market-selection-assessor-spec.md (MASTER procedure + §SUPPORTING KNOWLEDGE lines 19-28 + bottom RECONCILIATION NOTE lines 155-169)
    - .planning/phases/02-stage-m1-s2-market-selection-gate/02-CONTEXT.md (D-01, D-05, D-06, D-09; Claude's Discretion block on injection mechanism + path)
    - .planning/phases/02-stage-m1-s2-market-selection-gate/02-PATTERNS.md (KEEP lines 39-67: frontmatter, A4→D1 role, read_first, layer-discipline, output record, self-audit, DATA GAP; the Reconciled INPUT CONTRACT section lines 99-191; the path-mismatch note lines 100-101)
    - prompts/step1-light-pass.md (step 6 lines 486-490 — the mechanism shared-vs-unique cluster S1 instructs; no output slot yet → D-05)
    - .claude/skills/market-selection/mechanisms-in-play-stopgap.md (the decision-time stopgap: derive shared-vs-unique from per-pitch mechanism[] in the dumps, label [INFERENCE], never emit a bare DATA GAP — D-05)
  </read_first>
  <action>
    Edit `.claude/skills/market-selection/SKILL.md` (do NOT create a new file — rewrite in place).

    1. ADD a derived-from header (D-01) as the first content line after the frontmatter close,
       verbatim:
       `> Derived from prompts/_specs/market-selection-assessor-spec.md — edit the spec, regenerate this.`

    2. EDIT the frontmatter `description` (KEEP the `name: market-selection` + folded-description
       house shape, PATTERNS lines 40-49). The current description says "four ordered kill-gates —
       Demand, Product, Sophistication, Awareness". Change it to describe THREE live gates with
       Gate 4 deferred: "three ordered kill-gates — Demand, Product, Sophistication — with Awareness
       (Gate 4) DEFERRED to the deep-research step; ranks PROVISIONAL survivors for the human bet
       pick." Keep the "Run AFTER prompts/step1-light-pass.md" and the "does NOT pick the market"
       A4-not-D1 lines.

    3. KEEP the Role section (A4→D1 framing, UM-assumed-present) intact — PATTERNS lines 52-57. Do
       not rewrite it.

    4. REWRITE the `## read_first` block (D-06 + DR-KB discretion). KEEP the existing 4 entries
       (definitions.md, workflow.md Step 0, the light-pass output, agents/implementation-notes.md).
       ADD:
       - `runs/<space>/pre-research-plan.md` — the bet brief; the operator overrides live in its
         prose §"Supply-side validation stance" + §"Deferred reads" (read as layer-A prose, never
         schema, never hook-validated — D-06).
       - The assessor-cut DR-KB set as auto-injected SUPPORTING KNOWLEDGE (resolve the discretion:
         use read_first, NOT a hook — no hook infrastructure exists in .claude/skills/, the house
         convention is the read_first block). List the four-file set by absolute path with its role:
         `~/knowledge/dr-marketing/consumer-psychology--carl-weische.md` (sophistication + awareness
         stage tables, Mass Desire core drivers — grounds override #4),
         `~/knowledge/dr-marketing/brand-building--spencer-origins.md` (sophistication progression,
         5 mechanism types, 5+=dead-ground rule),
         `~/knowledge/dr-marketing/ecommerce--mark-builds-brands.md` (anti-fluke thresholds — but its
         $300–500K/mo floor is MISCALIBRATED for this category; do NOT apply it — spec Gate 1 / D-09),
         `~/knowledge/dr-marketing/differentiator-framework__2_.md` (market-vs-angle / me-too test for
         Gate 2.2's real-axis read).
       State explicitly these are SUPPORTING KNOWLEDGE (read to classify terms correctly), NOT the
       procedure — the four-gate framework inlined below is the procedure (spec §SUPPORTING KNOWLEDGE).

    5. RESOLVE THE PATH MISMATCH (PATTERNS lines 100-101 — load-bearing). The skill's `<space>`
       indirection assumes inputs under `runs/<space>/`, but for the current Arduview run the data
       lives at repo ROOT (`/home/kyu3/PMF/space-map.json`, `/brands.json`, `/ads/*.json`,
       `/corpus/<brand>/dump.json`) and only `pre-research-plan.md` is under `runs/arduview/`. Add an
       `## INPUTS` block that documents BOTH: state the canonical layout the skill expects
       (`runs/<space>/space-map.json`, `runs/<space>/brands.json`, `runs/<space>/ads/<brand>.json`,
       `runs/<space>/corpus/<brand>/dump.json`, `runs/<space>/pre-research-plan.md`) AND a literal note
       that for the Arduview debug run the FIVE input file sets — space-map.json, brands.json,
       ads/<brand>.json, AND the corpus dumps `corpus/<brand>/dump.json` — are at repo root while
       `pre-research-plan.md` is at `runs/arduview/pre-research-plan.md` — so an invoker passes the
       actual on-disk paths. Mirror the corpus dumps on the SAME repo-root-vs-`runs/<space>/` footing
       as the four data files: for the Arduview run they live at repo-root `corpus/<brand>/dump.json`,
       with `runs/<space>/corpus/...` kept as the canonical-layout label. Do not leave the read paths
       ambiguous: Plan 02 must run against these exact files.

    6. REPLACE the stale `INPUT DATA CONTRACT` "❌ add to schema" table (current lines 48-86) with a
       RECONCILED INPUT CONTRACT that wires each gate to the REAL shapes (from <interfaces> above +
       PATTERNS lines 99-191). For each gate signal, name the real file + real field path:
       - Gate 1 signal-1 revenue: `brands.json` → `revenue_est.{value_usd_monthly, method, confidence}`
         (null for all 20 brands this run; weight by method/confidence; distrust review_proxy/low).
       - Gate 1 signal-2 ad longevity: `ads/<brand>.json` → `ads[].run_length_days` (7+ = validated;
         NOT days_running) AND the pre-counted `space-map.json` → `combos[].anti_fluke.qualifying_creatives`
         (= 0 in all 6 cells this run; the skill READS this, does not recount). This is also where the
         input-contract table's stale `days_running` longevity label gets replaced by `run_length_days`.
       - Gate 1 signal-3 crowdfunding: `brands.json` → `crowdfunding.{platform, raised, pct_funded, status}`.
       - Gate 1.2 trend: `per_brand[].demand_trend.{shape,window,source,basis}` (shape == "unknown"
         for ALL 20 brands → D-09 provisional handling, wired in Task 2).
       - Gate 2.1c price: `brands.json` → `price_points[]` verbatim.
       - Gate 2 product/channel: `brands.json` → `product_observed`, `channel`.
       - Gate 2.2 bet: `per_brand[].bet_type` + `bet_type_basis` (OPEN; Gate 2.2 body wired to it in Task 2).
       - Gate 3 stage: `combos[].claim_count` / `enhanced_claim_count` / `claims[].type` (per-CELL).

    7. KEEP the LAYER DISCIPLINE port (PATTERNS lines 61, current SKILL.md lines 89-104) intact — its
       e-ink worked examples are still on-domain. Gate 3 dies without it.

    8. KEEP the DATA GAP discipline block (current lines 50-52) and STATE the live triggers this
       run. TWO are bare DATA GAPs: `demand_trend: unknown` (all brands, D-09) and
       `revenue_est.value_usd_monthly: null` (most/all brands, D-03) — surfaced, never guessed.
       The THIRD, `mechanisms_in_play[]`, is NO LONGER a bare DATA GAP (D-05): the space-map.json output
       slot is still absent (a cross-phase Phase-1 add per 01-DEBUG-RUN-NOTES.md BREAK 5, NOT built here),
       but at decision-time the skill READS `.claude/skills/market-selection/mechanisms-in-play-stopgap.md`
       and DERIVES the shared-vs-unique mechanism read on the fly from per-pitch `mechanism[]` in
       `corpus/<brand>/dump.json` → `creatives[].pitches[].mechanism[]` (canonical-layout label
       `runs/<space>/corpus/<brand>/dump.json`; for the Arduview run the dumps are at repo-root
       `corpus/<brand>/dump.json`) (NOT `problem_um_raw`, which is sparse/secondary), fenced `[INFERENCE]`
       with `n=` per cluster and lowered confidence. State explicitly the skill does NOT emit a bare
       `DATA GAP` and skip the mechanism check for this field — it derives + labels per the stopgap
       procedure. (The full Gate 2.2 / 3.3-S3 wiring is Task 2.)

    NOTE on scope: this task owns the input-contract TABLE's `days_running` → `run_length_days`
    replacement (current line 66). The OTHER two `days_running` occurrences live below `## THE GATES`
    (Gate 1.1 body, line 118; output record, line 216) and belong to Task 2 — so the global
    "no days_running anywhere" assertion lives in Task 2's verify, not here.

    Do NOT touch the gate bodies (## THE GATES) or the output/self-audit sections in this task —
    Task 2 owns those. This task owns everything ABOVE `## THE GATES`.
  </action>
  <verify>
    <automated>grep -q "Derived from prompts/_specs/market-selection-assessor-spec.md" .claude/skills/market-selection/SKILL.md && grep -q "pre-research-plan" .claude/skills/market-selection/SKILL.md && grep -q "mechanisms-in-play-stopgap" .claude/skills/market-selection/SKILL.md && ! grep -q "DATA GAP: mechanisms_in_play" .claude/skills/market-selection/SKILL.md && grep -q "differentiator-framework" .claude/skills/market-selection/SKILL.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Derived from prompts/_specs/market-selection-assessor-spec.md" .claude/skills/market-selection/SKILL.md` succeeds (derived-from header present, D-01)
    - `grep -q "pre-research-plan" .claude/skills/market-selection/SKILL.md` succeeds (D-06 override source in read_first)
    - `grep -q "mechanisms-in-play-stopgap" .claude/skills/market-selection/SKILL.md` succeeds AND `! grep -q "DATA GAP: mechanisms_in_play" .claude/skills/market-selection/SKILL.md` succeeds (D-05 — SKILL.md references the stopgap doc and derives the mechanism read fenced [INFERENCE] rather than emitting a bare DATA GAP for this field)
    - `grep -qi "differentiator-framework" .claude/skills/market-selection/SKILL.md` succeeds (DR-KB injection wired, discretion resolved to read_first)
    - `grep -qi "MISCALIBRATED" .claude/skills/market-selection/SKILL.md` succeeds (the $300–500K floor guard present)
    - `grep -qi "repo root\|repo-root" .claude/skills/market-selection/SKILL.md` succeeds (path-mismatch documented)
    - `grep -q "qualifying_creatives" .claude/skills/market-selection/SKILL.md` succeeds (reads the pre-counted anti-fluke field)
    - `grep -q "run_length_days" .claude/skills/market-selection/SKILL.md` succeeds (input-contract table now names the real longevity field — D-03)
  </acceptance_criteria>
  <done>SKILL.md header through the input-contract/layer-discipline sections is rewritten: derived-from header, 3-gate description, expanded read_first with override source + DR-KB read_first injection, documented path mismatch (the four data files AND the corpus dumps at repo-root for the Arduview run), reconciled real-shape input contract (input-contract table longevity field now `run_length_days`), the two bare DATA GAP triggers (demand_trend, revenue) stated and the mechanisms-in-play read pointed to the stopgap doc for [INFERENCE] derivation off repo-root corpus/<brand>/dump.json (no bare DATA GAP). Gate bodies untouched (Task 2 owns the remaining two days_running occurrences + the global removal assertion).</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the four gate bodies, soft-gate/provisional mode, output record, and self-audit</name>
  <files>.claude/skills/market-selection/SKILL.md</files>
  <read_first>
    - .claude/skills/market-selection/SKILL.md (current gate bodies lines 107-249 — the sections being rewritten)
    - prompts/_specs/market-selection-assessor-spec.md (THE DECISION PROCEDURE lines 59-151: Gate 1 lines 65-79, Gate 2.2 line 86, Gate 3 lines 91-104, Gate 4 DEFERRED stub lines 105-106, OPERATOR OVERRIDES lines 116-122, STANDING BIASES lines 126-130, OUTPUT lines 133-145, EPISTEMIC DISCIPLINE lines 147-151; bottom RECONCILIATION NOTE lines 155-169 — the dead competitive_axis enum lives HERE in the spec, do NOT carry it into SKILL.md)
    - .planning/phases/02-stage-m1-s2-market-selection-gate/02-CONTEXT.md (D-02, D-03, D-04, D-05, D-07, D-08, D-09, D-10)
    - .claude/skills/market-selection/mechanisms-in-play-stopgap.md (D-05 — the decision-time derivation procedure Gate 2.2 / 3.3-S3 inline-reference: cluster per-pitch mechanism[], shared 3+ / unique 1, [INFERENCE], n= per cluster)
    - .planning/phases/02-stage-m1-s2-market-selection-gate/02-PATTERNS.md (REPLACE table lines 73-82; the Reconciled contract lines 110-191; Soft-gate mode lines 210-212; [INFERENCE] fencing lines 206-208)
    - space-map.json (confirm combos[] / per_brand[] / bet_types[] real shapes before wiring the gate text)
    - runs/arduview/pre-research-plan.md (§"Supply-side validation stance" line 62, §"Deferred reads" line 67 — the 4 operator overrides as prose, all SET this run, D-06)
  </read_first>
  <action>
    Continue editing `.claude/skills/market-selection/SKILL.md` — the `## THE GATES` block through
    the end (`## SELF-AUDIT`). Inline the spec's decision procedure so it EXECUTES.

    GATE 1 — Demand (rewrite current lines 114-131):
    - Replace the hard `< 2 qualifying brands = KILL` (current line 119/120) with SOFT-GATE MODE
      (D-08): for this first/debug run, a Gate-1 kill becomes a FLAG + a ranking penalty, NOT an
      elimination. Gates still run in fixed order and emit per-gate verdicts, but ALL 6 cells are
      carried through to the ranked output, RANKED by relative demand. State the hard-kill discipline
      RE-ENABLES once revenue_est + demand_trend are trustworthy (cite D-08). Add a one-line rationale:
      hard kills on null/unreliable revenue would nuke every cell or fire arbitrarily.
    - 1.1 SCALE GATE: inline the spec's three OR-signals (spec lines 68-71): revenue
      (`revenue_est.value_usd_monthly`, weighted by `method`/`confidence`; distrust review_proxy/low),
      ad volume+longevity (`ads[].run_length_days` 7+ AND the pre-counted
      `combos[].anti_fluke.qualifying_creatives`), crowdfunding (`raised`/`pct_funded`/`status`). Inline
      the anti-fake-signal rule (a pro-looking site / reviews do NOT clear the gate) and the no-absolute-
      dollar-floor rule — the DR $300–500K floor is MISCALIBRATED, do NOT apply (spec lines 73-74 / D-09).
      Replace the Gate 1.1-body `days_running` reference (current line 118) with `run_length_days`.
    - 1.2 trend SHAPE: inline parabolic-spike=KILL(→soft flag), steady/rising=pass, declining=warning.
      Because `demand_trend.shape == "unknown"` for ALL brands (D-09): stamp every cell
      "durability UNKNOWN — Gate 1 cannot clear with confidence," LOWER confidence, and surface
      "fix the Trends fetch (Phase 1)" as the #1 blocker before any committed bet pick. RUN PROVISIONAL.
    - 1.3 demand magnitude (ranking only): for BELONGING/IDENTITY cells, do NOT fabricate intensity from
      absent community-heat data — FLAG "requires separate community-heat read — not scored here" (spec
      line 77 + override #4).
    - Keep the standing-bias-1 trap line (never reward low competition).

    GATE 2 — Product (rewrite current lines 133-145):
    - 2.2 DIFFERENTIATING AXIS (D-02 — load-bearing): the dead `competitive_axis
      (function-capability-price | visual-statement | community-openness)` closed enum lives in the SPEC's
      INPUTS block / bottom reconciliation note — when inlining the spec procedure here, do NOT carry that
      enum or any enum-worded worked example into SKILL.md. WIRE the 2.2 BODY to reason over the OPEN
      `bet_type` + `bet_type_basis` the classifier named (`per_brand[]`), checked against the cell's
      competing bets. The named axis that passes IS the UM. The skill reasons over whatever bet the
      classifier named (the OPEN canonical bet_types[] list), not a fixed 3-value enum. The Gate 2.2 body
      MUST reference `bet_type_basis` and at least one canonical `bet_types[]` value by name (e.g.
      `open-source-hackability-as-lead`). (This implements the spec's own bottom RECONCILIATION NOTE
      lines 161-162.)
    - For the 2.2 differentiation read, the skill also needs the cell's mechanisms-in-play (shared vs
      unique). Since the `mechanisms_in_play[]` slot is absent (D-05), instruct it to DERIVE that read from
      the stopgap doc (`.claude/skills/market-selection/mechanisms-in-play-stopgap.md`) off per-pitch
      `mechanism[]`, fenced `[INFERENCE]` (same procedure detailed in Gate 3.3-S3 below) — NOT a bare DATA GAP.
    - Keep 2.1 mechanism efficacy, 2.1b product-category spend-transfer, 2.1c price-conditioning (reads
      `price_points[]`), 2.3 believability tier, 2.4 economics (COGS = pending operator data, flag, don't
      pass blind) from the spec lines 83-88.

    GATE 3 — Sophistication (rewrite current lines 147-178):
    - 3.1 stage is PER-CELL (D-04): mechanical rule = stage = highest `claim_type` tier that 2+ LIVE
      in-geo brands deploy in the cell, derived from `combos[].claim_count` / `enhanced_claim_count` /
      `claims[].type`. State that S1's `per_brand[].sophistication` STRING is mis-grained (brand grain, not
      cell grain) and is CROSS-CHECK ONLY — flag disagreement, never the gate input. Note for this run:
      `claims[].type` are only `direct`/`enlarged`, `enhanced_claim_count = 0` in every cell → reads
      Stage 1–2 across the board.
    - 3.3-S3 ("is your mechanism already claimed?"): the `mechanisms_in_play[]` space-map.json slot is
      still ABSENT (D-05), but do NOT emit a bare `DATA GAP` and skip. Instead instruct the skill to READ
      `.claude/skills/market-selection/mechanisms-in-play-stopgap.md` and DERIVE the shared-vs-unique read
      per its procedure: collect per-pitch `mechanism[]` from `corpus/<brand>/dump.json`
      (`creatives[].pitches[].mechanism[]`; canonical-layout label `runs/<space>/corpus/<brand>/dump.json`,
      Arduview on-disk path repo-root `corpus/<brand>/dump.json`) for the cell's LIVE in-geo brands —
      EXCLUDING `comparable_bet_seed` and dead/region-only brands; cluster near-duplicates by meaning;
      count distinct brands per cluster; classify shared (3+ brands → not ownable) vs unique (1 brand →
      ownable); scope per-cell (a cross-cell mechanism is not taken here); use `mechanism[]` as primary,
      `problem_um_raw` only as secondary when non-empty. Fence the derived read `[INFERENCE]`, surface
      `n=` per cluster, lower the cell's confidence. Do NOT write the derived cluster back into
      space-map.json (that is the BREAK-5 S1 fix's job).
    - Keep 3.2 required-move, 3.3 executability, 3.4 defense-quality / 5+-dead-ground, and the RESOLVED
      believability-joint reasoning (spec lines 91-104; the joint is RESOLVED in the spec — inline the
      resolution, drop the old "open joint carry-forward" note at current lines 175-178).

    GATE 4 — Awareness (replace the full procedure at current lines 180-195):
    - Replace with the spec's DEFERRED stub (spec lines 105-106 / D-05): do NOT attempt Gate 4, do not
      infer awareness from thin signals. Label every Gate-1–3 survivor "passed Gates 1–3, Gate 4
      (awareness reachability) PENDING DEEP-RESEARCH STEP — PROVISIONAL."

    OPERATOR OVERRIDES (add a section, mapping spec lines 116-122 onto the run prose — D-06):
    - Instruct the skill to read the 4 overrides as PROSE from `pre-research-plan.md` §"Supply-side
      validation stance" + §"Deferred reads", never schema, never hook-validated. For the Arduview run
      all 4 are SET: (1) one-comparable → PASS to a DRY TEST only, never to MOQ/commitment; (2)
      community-heat → do not auto-kill thin maker-niche spend, flag for the deferred read; (3)
      durability-load-bearing → spiked-and-died = WARNING, weight parabolic-spike harder; (4) demand-type
      → belonging/identity/status is legitimate demand, read intensity off community heat not pain
      severity. If a run supplies no brief override stance → run generic AND FLAG it ran without overrides.

    CANDIDATE CELLS (D-07): instruct evaluating EVERY cell in `combos[]` (all 6 this run) — no pre-curated
    shortlist. `comparable_bet_seed: true` brands (arduboy, flipper-zero, nothing-phone, skeleton-key) stay
    BET-EVIDENCE only, NEVER become candidate cells, excluded from per-cell saturation.

    SURVIVOR RANKING + OUTPUT (rewrite current lines 199-235, D-10): output = ranked PROVISIONAL survivor
    set (Gates 1–3 run; Gate 4 DEFERRED) with per-cell gate records, evidence citations (cite the
    count/claim/trend/axis behind every call — no verdict without its datum), and all flags, written to
    `runs/<space>/market-selection.md`. KEEP the per-pair output-record shape but EDIT: drop the Gate-4
    line from the record, restyle survivor label to "passed Gates 1–3, Gate 4 PENDING DEEP-RESEARCH",
    and replace the output-record's `days_running` reference (current line 216) with `run_length_days`.
    KEEP `[INFERENCE]`-fencing for every AI read (stage, intensity proxy, is-live inference). The skill
    STOPS at ranked survivors + per-cell synthesis; Kam makes the bet pick (D1). Do NOT write "we should
    test X."

    SELF-AUDIT (KEEP current lines 237-249, EDIT bias-3): awareness is now DEFERRED ENTIRELY, not just
    "read separately" (spec standing-bias #3). Keep the gate-order, per-cell-saturation, typed-claims,
    layer-discipline, DATA-GAP-not-guessed, and A4-not-D1 checks.
  </action>
  <verify>
    <automated>! grep -q "days_running" .claude/skills/market-selection/SKILL.md && grep -q "bet_type_basis" .claude/skills/market-selection/SKILL.md && grep -Eq "novel-hardware-as-lead|open-source-hackability-as-lead|hardware-authenticity-as-lead|value-breadth-as-lead|maker-ecosystem-store" .claude/skills/market-selection/SKILL.md && ! grep -Eq "function-capability-price|visual-statement|community-openness" .claude/skills/market-selection/SKILL.md && grep -qi "PROVISIONAL" .claude/skills/market-selection/SKILL.md && grep -qi "PENDING DEEP-RESEARCH" .claude/skills/market-selection/SKILL.md && grep -qi "soft-gate\|ranking penalt" .claude/skills/market-selection/SKILL.md && grep -qi "durability UNKNOWN" .claude/skills/market-selection/SKILL.md && grep -q "comparable_bet_seed" .claude/skills/market-selection/SKILL.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "days_running" .claude/skills/market-selection/SKILL.md` succeeds (ALL 3 stale-field occurrences removed — Task 1's table + Task 2's Gate-1.1 body + output record; global removal assertion lives here because Task 2 owns 2 of the 3 occurrences)
    - `grep -q "bet_type_basis" .claude/skills/market-selection/SKILL.md` succeeds (Gate 2.2 body reasons over the named OPEN bet, D-02)
    - `grep -Eq "novel-hardware-as-lead|open-source-hackability-as-lead|hardware-authenticity-as-lead|value-breadth-as-lead|maker-ecosystem-store" .claude/skills/market-selection/SKILL.md` succeeds (Gate 2.2 names at least one canonical bet_types[] value — proves it wired the OPEN list, not generic prose, D-02)
    - `! grep -Eq "function-capability-price|visual-statement|community-openness" .claude/skills/market-selection/SKILL.md` succeeds (the dead spec enum values were NOT imported during inlining, D-02)
    - `grep -qi "PENDING DEEP-RESEARCH" .claude/skills/market-selection/SKILL.md` succeeds (Gate 4 deferred stub, D-05)
    - `grep -qi "soft-gate\|ranking penalt" .claude/skills/market-selection/SKILL.md` succeeds (D-08 soft-gate mode)
    - `grep -qi "durability UNKNOWN" .claude/skills/market-selection/SKILL.md` succeeds (D-09 provisional stamp)
    - `grep -qi "fix the Trends fetch" .claude/skills/market-selection/SKILL.md` succeeds (D-09 #1 blocker surfaced)
    - `grep -q "claims\[\].type\|claim_count\|enhanced_claim_count" .claude/skills/market-selection/SKILL.md` succeeds (Gate 3 per-cell typed-claim input, D-04)
    - `grep -qi "cross-check only\|mis-grained" .claude/skills/market-selection/SKILL.md` succeeds (per-brand sophistication demoted to cross-check, D-04)
    - `grep -q "comparable_bet_seed" .claude/skills/market-selection/SKILL.md` succeeds (D-07 seed exclusion)
    - `grep -qi "INFERENCE" .claude/skills/market-selection/SKILL.md` succeeds ([INFERENCE] fencing kept, D-10)
    - `grep -qi "all 6\|every cell\|all cells" .claude/skills/market-selection/SKILL.md` succeeds (D-07 evaluate every cell)
  </acceptance_criteria>
  <done>The four gate bodies execute the spec procedure wired to real fields: Gate 1 soft-gate + provisional-on-unknown-trend (Gate-1.1 body + output record longevity refs now run_length_days, no days_running anywhere), Gate 2.2 reasons over the OPEN bet_type/bet_type_basis naming a canonical bet_types[] value (the dead spec enum NOT imported), Gate 3 per-cell typed-claim stage with sophistication demoted to cross-check, Gate 4 deferred. Operator-override prose mapping, all-6-cells + seed-exclusion, ranked-provisional output to runs/<space>/market-selection.md, [INFERENCE] fencing, and the edited self-audit are all present.</done>
</task>

</tasks>

<threat_model>
The deliverable is a prompt-only markdown skill file (`.claude/skills/market-selection/SKILL.md`)
with NO executable, network, or auth surface. It parses no untrusted input beyond the operator's
own JSON research output, holds no secrets, opens no sockets, and runs no code. There is no STRIDE
attack surface to register. Threat model: not applicable — markdown prompt artifact.
</threat_model>

<verification>
- `! grep -q "days_running" SKILL.md` (stale field removed everywhere — Task 2's assertion)
- `! grep -Eq "function-capability-price|visual-statement|community-openness" SKILL.md` (dead spec enum not imported during inlining, D-02)
- `grep -q "bet_type_basis"` + a canonical `bet_types[]` value present (Gate 2.2 wired to the OPEN list)
- `grep -q "run_length_days"`, `grep -q "mechanisms-in-play-stopgap"` + `! grep -q "DATA GAP: mechanisms_in_play"` (mechanism read derived from stopgap, not a bare DATA GAP — D-05),
  `grep -q "Derived from prompts/_specs/market-selection-assessor-spec.md"` (contract reconciled)
- `grep -qi "PENDING DEEP-RESEARCH"`, `grep -qi "soft-gate"`, `grep -qi "durability UNKNOWN"`
  (deferral + soft-gate + provisional modes wired)
- Manual: every one of D-01..D-10 traceable to concrete gate/section text in SKILL.md.
</verification>

<success_criteria>
- SKILL.md inlines the spec's four-gate procedure (Gate 4 deferred) and EXECUTES on invocation.
- Input contract wired to the CURRENT real S1 shapes: bet_type, demand_trend, run_length_days,
  per-cell typed-claim saturation; awareness dropped → Gate 4 deferred; DATA GAPs surfaced never guessed.
- Gate 2.2 reasons over the OPEN bet_type/bet_type_basis (names a canonical bet_types[] value); the dead
  spec competitive_axis enum is NOT imported into SKILL.md during inlining (D-02).
- Soft-gate + provisional mode honored for this first/debug run (D-08/D-09).
- Path mismatch resolved (repo-root data documented; Plan 02 can run against real files).
- DR-KB assessor-cut set auto-injected via read_first; $300–500K floor guarded out.
</success_criteria>

<output>
After completion, create
`.planning/phases/02-stage-m1-s2-market-selection-gate/02-01-SUMMARY.md`.
</output>
