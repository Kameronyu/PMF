---
phase: 06-smoke-run-definition-of-done
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/contracts/smoke-dod.sh
  - engine/bricks/run-controller.js
autonomous: true
requirements: [SMOKE-01, SMOKE-02, SMOKE-03, SMOKE-04, SMOKE-05]

must_haves:
  truths:
    - "`bin/run all --space=<fresh> --smoke` runs all 11 steps in canonical R1 order and exits 0"
    - "Every declared writes[] slot across all 11 manifests is materialized on disk under the fresh run space"
    - "Every preflight is green and every validator_verdict == pass across the whole run"
    - "All 6 gated steps log gate.decision='auto-approved-smoke'; non-gated log decision=null; the 11-receipt chain is unbroken"
    - "The producer->consumer graph over the REAL emitted run has zero orphan outputs and zero dangling inputs"
    - "A `run all` over a pipeline with a producing step DROPPED fails fast with a NAMED completeness refusal BEFORE any spawn"
    - "A `run all` over a pipeline with two steps REORDERED fails fast with the same NAMED refusal"
    - "Two fresh full runs are byte-identical (excluding _receipts/ and *-log.txt)"
    - "smoke-dod.sh is the 7th green acceptance gate; the 6 prior gates stay green; pipeline.yaml, manifests, stub emits, and runs/smoke/ are unchanged"
  artifacts:
    - path: "engine/contracts/smoke-dod.sh"
      provides: "The milestone DoD harness — the 7th acceptance gate asserting SMOKE-01..05 at the run grain"
      min_lines: 180
    - path: "engine/bricks/run-controller.js"
      provides: "A `run all` pipeline-completeness preflight (SMOKE-05 mechanism) in front of the runAll loop"
      contains: "pipeline incomplete/misordered"
  key_links:
    - from: "engine/contracts/smoke-dod.sh"
      to: "engine/bricks/run-controller.js (runAll completeness preflight)"
      via: "--pipeline=<temp dropped/reordered fixture> expecting a NAMED non-zero refusal"
      pattern: "pipeline incomplete/misordered"
    - from: "engine/bricks/run-controller.js runAll"
      to: "loadManifest + producedBy graph"
      via: "strictly-earlier-producer check before the runStep loop"
      pattern: "producedBy"
---

<objective>
Formalize the milestone Definition-of-Done as a 7th acceptance gate and close the last
deferred shell hole (SMOKE-05). Two coupled deliverables:

1. `engine/contracts/smoke-dod.sh` — the DoD harness. Runs the FULL pipeline
   `run all --space=<fresh> --smoke` and asserts the Core Value at the RUN grain
   (SMOKE-01..05), plus determinism. Authored RED-first: its SMOKE-05 negative asserts
   (dropped/reordered producer must fail-fast) fail until the controller preflight exists.

2. A `run all` PIPELINE-COMPLETENESS PREFLIGHT added IN FRONT of `runAll`'s loop in
   `engine/bricks/run-controller.js` (the SMOKE-05 mechanism). Before spawning anything,
   build `producedBy[normalizedPath]=[stepId...]` over the active pipeline list and assert
   every step's `reads[]` has a strictly-earlier producer (or is the pipeline-entry step 00).
   A dropped/reordered producer → NAMED refusal, `process.exit != 0`, before any spawn.

Purpose: prove the shell completes end-to-end on stubs — every artifact produced AND
consumed, deterministic, gates logged, receipts unbroken, zero orphans/dangling — so real
prompts and field schemas become pure drop-in slots. This is the FINAL build phase.

Output: `engine/contracts/smoke-dod.sh` (new gate) + a completeness preflight in the
controller's `runAll` path. No other files change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-smoke-run-definition-of-done/06-CONTEXT.md
@.planning/REQUIREMENTS.md
@engine/contracts/manifest-smoke.sh
@engine/contracts/validate-smoke.sh
@engine/bricks/run-controller.js
@pipeline.yaml

<interfaces>
<!-- Extracted from the codebase — executor uses these directly, no exploration needed. -->

RECEIPT SHAPE (confirmed by probing a real run; one JSON file per step under
runs/<space>/_receipts/):
```json
{
  "spawn_id": "00-bet-compiler-<ts>-<rand>",
  "step": "00-bet-compiler",
  "space": "<space>",
  "inputs_hash": "<64-hex sha256>",
  "inputs":  ["runs/<space>/...", ...],
  "outputs": ["runs/<space>/...", ...],
  "validator_verdicts": [ { "output": "runs/<space>/...", "verdict": "pass" }, ... ],
  "gate":  { "step_gated": true|false, "decision": "auto-approved-smoke"|null },
  "ts": "<iso>"
}
```
- GATED steps {00,01,05,07,08,10}: gate.step_gated===true, gate.decision==="auto-approved-smoke".
- NON-GATED steps {02,03,04,06,09}: gate.step_gated===false, gate.decision===null.
- inputs_hash is a 64-char hex sha256 on every receipt.
- validator_verdicts is non-empty and EVERY entry .verdict==="pass".

CONTROLLER ENTRY POINTS (engine/bricks/run-controller.js):
```js
function parsePipeline(file)      // -> flat ordered [stepId,...] from `- ` lines  (L124-130)
function loadManifest(id, dir)    // -> §5 manifest object {id,reads,writes,...}    (~L80-119)
function runStep(stepId, space, opts)                                              // (L518-537)
function runAll(space, opts) {    // CURRENT — no completeness check (L542-546)
  for (const id of parsePipeline(opts.pipeline)) {
    runStep(sanitizePathSegment(id), space, opts);
  }
}
```
- opts carries: { pipeline, manifestDir, space, smoke, ... } (runOpts()).
- DEFAULT_MANIFEST_DIR = 'engine/manifests'; DEFAULT_PIPELINE = 'pipeline.yaml'.
- manifest reads[]/writes[] use the `runs/{space}/...` slot form; {space} substituted at run.

PRODUCER->CONSUMER GRAPH NORMALIZATION (mirror manifest-smoke.sh MANIFEST-02 EXACTLY):
- norm: replace `{space}` with a fixed token (run grain uses the literal space; manifest
  grain uses "SPACE"). For the controller preflight, normalize manifest slots by
  substituting the active space so {space} differences never cause a false dangle.
- dir-slot match: a write `w` ending in "/" satisfies a read `r` (r !== w && r.startsWith(w)).
- strictly-earlier: producer step position < reader step position (reject self + back-edge).
- pipeline-entry: step 00 has empty reads[] — the ONLY entry with no producer.
- TERMINAL allowlist (writes consumed by nobody — for the smoke-dod orphan walk only):
  queries_run.json & dumps/ (step 01), audit-verdicts.json (step 07),
  review/_review.json & review/ (step 10).

PRIOR GATES (must stay green — the 7th is smoke-dod.sh):
  store-smoke.sh, controller-smoke.sh, h6-all.sh, manifest-smoke.sh, stub-smoke.sh,
  validate-smoke.sh
</interfaces>

<harness_idiom>
Mirror validate-smoke.sh / manifest-smoke.sh exactly:
- `set -u`; `cd "$(dirname "$0")/../.."` (engine/contracts -> repo root); `unset VOYAGE_API_KEY` at top.
- `FAIL=0`; `ok()/bad()` accumulate FAIL=1; inline `node -e` JSON asserts.
- `expect_reject "<label>" "<grep-pattern>" <cmd...>` — non-zero exit AND a named stderr match.
- Disposable `runs/_vfy*` spaces + a TEMP pipeline/manifest dir for the drop/reorder fixtures.
- `rm -rf` every temp artifact after. Final `ALL ASSERTS PASS` / non-zero on any named fail.
- NEVER touch runs/smoke/, pipeline.yaml, or engine/manifests/ — copy to temp dirs / write temp pipeline files.
</harness_idiom>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author smoke-dod.sh RED-first (the 7th DoD gate, SMOKE-01..05 + determinism)</name>
  <files>engine/contracts/smoke-dod.sh</files>
  <action>
Create `engine/contracts/smoke-dod.sh` (chmod +x). Copy the validate-smoke.sh harness skeleton
verbatim: `set -u`, `cd "$(dirname "$0")/../.."`, `unset VOYAGE_API_KEY`, `FAIL=0`, `ok()/bad()`,
and the `expect_reject` helper. Every assert at the RUN grain over a FRESH temp `runs/_vfy*`
space — NEVER `runs/smoke/`. Each assert block carries a comment binding it to its SMOKE-0x id.

Run ONE clean full pipeline into a fresh space `VSP=_vfy_dod` once at the top
(`node engine/bricks/run-controller.js all --space="$VSP" --smoke`), reuse it for SMOKE-01..05.

SMOKE-01 (`run all` completes end-to-end on stub prompts):
  - assert the clean run exited 0.
  - assert exactly 11 receipts in runs/$VSP/_receipts/.
  - assert R1 CANONICAL ORDER: sort the 11 receipt files by their embedded `ts` (or by spawn-id
    timestamp), map each to receipt.step, and assert the resulting step sequence === the 11
    canonical ids in pipeline.yaml order. (Order proven by the receipts, not by re-reading pipeline.yaml.)

SMOKE-02 (preflights green + validators green):
  - read EVERY receipt; assert each validator_verdicts is non-empty and every entry .verdict==="pass".
  - (preflight-green is implied by exit 0 + 11 receipts, since a REFUSE aborts the run — note this in a comment.)

SMOKE-03 (every declared writes[] slot materialized under the run space):
  - load all 11 engine/manifests/*.json, collect every writes[] entry, {space}->$VSP substitute.
    For a file slot assert the file exists; for a dir slot (endsWith "/") assert the directory exists.
    Zero unmaterialized slots.

SMOKE-04 (every operator gate logged + unbroken receipt chain):
  - exactly 11 receipts, one per canonical step, none missing/duplicate.
  - GATED={00,01,05,07,08,10}: gate.step_gated===true AND gate.decision==="auto-approved-smoke".
  - non-gated: gate.step_gated===false AND gate.decision===null.
  - every receipt: inputs_hash is 64-hex; validator_verdicts non-empty & all pass.

SMOKE-05 ORPHAN/DANGLING over the REAL run (positive, run grain):
  - walk the ACTUAL emitted artifacts (receipt.outputs[] / receipt.inputs[] per step, in canonical
    order) and build producedBy[path]=[stepId...] over the REAL run. Then:
      * every step's writes[] materialized on disk (re-uses SMOKE-03 fact);
      * every step's reads[] was present (produced by a strictly-earlier step OR step-00 entry);
      * ZERO dangling inputs (every read has a strictly-earlier producer; dir-parent match via
        endsWith("/") + startsWith, EXACTLY like manifest-smoke.sh);
      * ZERO orphan outputs (every non-terminal write consumed by a strictly-later step; terminals
        bound to their owning step via the TERMINAL allowlist from <interfaces>).

SMOKE-05 NEGATIVE (the new completeness-preflight proof — RED until Task 2 lands):
  - Build a TEMP pipeline file under runs/_vfy_dod_md/ (or a $TMP path) with one PRODUCING step
    DROPPED — drop `02-funnel-analysis` so step 03's read of ad-volume-aggregate.json dangles.
    Point the controller at it: `run all --space=_vfy_dod_drop --smoke --pipeline=<temp>`. Assert
    via expect_reject it FAILS (exit != 0) with the NAMED pattern
    `pipeline incomplete/misordered`. (Do NOT mutate committed pipeline.yaml or engine/manifests/.)
  - Build a 2nd TEMP pipeline with two steps REORDERED — put `03-space-map` BEFORE
    `02-funnel-analysis`. expect_reject same NAMED `pipeline incomplete/misordered` fail.
  - HAPPY-PATH PROOF: write a TEMP pipeline that is the FULL canonical 11 (a copy) and assert
    `run all --pipeline=<that temp>` PASSES the preflight (exit 0). (Proves the guard is not a
    blanket reject.)

DETERMINISM:
  - re-run the full pipeline into a 2nd fresh space `_vfy_dod_det2`. Walk every emitted artifact
    (exclude `_receipts/` and `*-log.txt`), assert byte-identical to $VSP. Same walk idiom as
    validate-smoke.sh DETERMINISM block.

Cleanup: `rm -rf` every `runs/_vfy*` space + every temp pipeline/manifest dir at the end (and
before each fresh run). Final: print `ALL ASSERTS PASS` + exit 0 when FAIL=0, else a named
failure summary + exit 1.

RED expectation: with the controller preflight NOT yet added, the two SMOKE-05 NEGATIVE asserts
will NOT see the `pipeline incomplete/misordered` line (the controller would faithfully run the
shorter/reordered list and likely exit 0 OR die with a generic preflight REFUSE on the dangling
input) — so they FAIL by name now. The happy-path + SMOKE-01..04 + determinism + orphan-walk
asserts should already PASS against the current controller.
  </action>
  <verify>
    <automated>bash engine/contracts/smoke-dod.sh; test $? -ne 0 && echo "RED-OK: harness fails (preflight not yet wired)"</automated>
  </verify>
  <done>
smoke-dod.sh exists, is executable, and runs without bash crashes. SMOKE-01..04, the
SMOKE-05 orphan/dangling run-grain walk, the SMOKE-05 happy-path, and DETERMINISM asserts
PASS; the two SMOKE-05 NEGATIVE asserts (dropped + reordered) FAIL by name (RED) because the
`pipeline incomplete/misordered` refusal does not yet exist. No `runs/_vfy*` spaces or temp
dirs remain after the run. runs/smoke/, pipeline.yaml, engine/manifests/ untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the run-all pipeline-completeness preflight (SMOKE-05 mechanism) — turn the harness GREEN</name>
  <files>engine/bricks/run-controller.js</files>
  <action>
Add a completeness preflight to `runAll(space, opts)` IN FRONT of the existing runStep loop.
This is WIRING — a new ordering guard, NOT a rewrite of any brick core. Do not touch runStep,
loadManifest, parsePipeline, preflight, or any phase function.

In runAll, before the `for` loop:
  1. `const ids = parsePipeline(opts.pipeline).map(sanitizePathSegment);`  (the ACTIVE list)
  2. Load each id's manifest via `loadManifest(id, opts.manifestDir)`.
  3. Build `producedBy[normPath] = [stepId,...]` in ACTIVE-LIST order, where
     `norm = p => p.replace('{space}', space)` (honor {space} substitution exactly).
     Collect each manifest's writes[] (normalized) into producedBy.
  4. Record each step's position by its index in `ids` (NOT by numeric prefix — honor the
     active list order so a REORDERED pipeline is judged on its actual walk order).
  5. For every step's reads[] (normalized), assert a STRICTLY-EARLIER producer exists in the
     active list. Reuse manifest-smoke.sh's matching idiom EXACTLY:
       - direct file producer: producedBy[r] has a step whose position < reader position;
       - dir-parent producer: some write `w` endsWith("/") with r !== w && r.startsWith(w),
         produced by a step whose position < reader position.
     Step 00's empty reads[] = the only pipeline-entry (no producer needed).
  6. A read whose only producer is the SAME step or a LATER step (dropped/reordered producer),
     OR has NO producer at all (and is not step-00's empty entry) → a NAMED refusal:
       `REFUSE [run all] pipeline incomplete/misordered: step <X> reads <path> with no strictly-earlier producer in the active pipeline`
     then `process.exit(1)` (non-zero) — BEFORE any runStep/spawn. Accumulate all bad edges,
     print each, then exit on the first failing pass (named, fail-fast).
  7. No per-step special-casing. The happy full pipeline must pass it unchanged.

Keep the existing `for (const id of ...) runStep(...)` loop exactly as-is AFTER the new guard.
  </action>
  <verify>
    <automated>bash engine/contracts/smoke-dod.sh</automated>
  </verify>
  <done>
`bash engine/contracts/smoke-dod.sh` exits 0 with `ALL ASSERTS PASS` — the two SMOKE-05
NEGATIVE asserts now see the `pipeline incomplete/misordered` named refusal (GREEN). The
happy full pipeline still passes the preflight and the full run still completes (SMOKE-01..04
+ determinism + orphan-walk all green). No per-step special-casing added; runStep/loadManifest
/parsePipeline unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Prove the acceptance invariant — all 7 gates green, committed run + sources unchanged</name>
  <files>engine/contracts/smoke-dod.sh, engine/bricks/run-controller.js</files>
  <action>
Run the full acceptance battery and confirm the invariant, fixing any regression found:
  1. The committed smoke run stays clean:
       `node engine/bricks/run-controller.js all --space=smoke --smoke` exits 0
       (then DISCARD any runs/smoke/ changes — do NOT commit a mutated runs/smoke/; if the run
       wrote new versioned dirs per no-overwrite-v1, leave the committed v1 intact via
       `git checkout -- runs/smoke/ 2>/dev/null` and remove any new untracked v2 dirs it created).
  2. All 6 prior gates stay green:
       store-smoke.sh, controller-smoke.sh, h6-all.sh, manifest-smoke.sh, stub-smoke.sh, validate-smoke.sh.
  3. The 7th gate is green: smoke-dod.sh exits 0.
  4. Confirm pipeline.yaml, engine/manifests/*.json, and the committed stub prompt emits are
     UNCHANGED by diff (`git diff --stat` shows only smoke-dod.sh [new] + run-controller.js).
  5. Confirm no stray `runs/_vfy*` spaces remain (`ls runs/ | grep _vfy` empty).
If any prior gate regressed because of the runAll change, fix the preflight (it must be a pure
front-guard) until all 7 are green.
  </action>
  <verify>
    <automated>for g in store-smoke controller-smoke h6-all manifest-smoke stub-smoke validate-smoke smoke-dod; do bash engine/contracts/$g.sh >/dev/null 2>&1 && echo "PASS $g" || echo "FAIL $g"; done; node engine/bricks/run-controller.js all --space=smoke --smoke >/dev/null 2>&1 && echo "PASS smoke-run-clean" || echo "FAIL smoke-run-clean"; git checkout -- runs/smoke 2>/dev/null; ls runs/ | grep _vfy && echo "FAIL stray-vfy" || echo "PASS no-stray-vfy"</automated>
  </verify>
  <done>
All 7 gates print PASS (store-smoke, controller-smoke, h6-all, manifest-smoke, stub-smoke,
validate-smoke, smoke-dod). `run all --space=smoke --smoke` exits 0 and runs/smoke/ is left
byte-intact (no committed mutation). `git diff --stat` shows ONLY engine/contracts/smoke-dod.sh
(new) + engine/bricks/run-controller.js. pipeline.yaml, engine/manifests/, and stub emits
unchanged. No stray runs/_vfy* spaces remain.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline.yaml / --pipeline file → runAll | A caller-supplied pipeline list (possibly dropped/reordered) crosses into the controller's spawn loop. The completeness preflight is the gate. |
| temp fixture dirs → harness | The harness writes temp pipeline/manifest fixtures it controls; cleanup must not touch committed runs/smoke/ or engine/manifests/. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | runAll over a dropped/reordered pipeline | mitigate | Completeness preflight refuses by name (`pipeline incomplete/misordered`) and exits non-zero BEFORE any spawn — the SMOKE-05 NEGATIVE asserts prove it. |
| T-06-02 | Tampering | harness cleanup mutating committed state | mitigate | All work in disposable runs/_vfy* + temp dirs; `rm -rf` only those; `git checkout -- runs/smoke` guard; diff-stat asserts only the 2 intended files changed. |
| T-06-03 | Denial of Service | a malformed --pipeline causing a hang/crash past the guard | accept | parsePipeline + loadManifest already named-refuse malformed input upstream (CTRL-10/CTRL-11); the new guard runs after parse, before spawn — no new unbounded path introduced. |
| T-06-04 | Information disclosure | VOYAGE_API_KEY live network call breaking byte-stable re-runs | mitigate | `unset VOYAGE_API_KEY` pinned at the top of smoke-dod.sh (matches h5-e2e/validate-smoke idiom). |
</threat_model>

<verification>
- smoke-dod.sh exits 0 with `ALL ASSERTS PASS`.
- The two SMOKE-05 NEGATIVE asserts only pass AFTER Task 2 (RED→GREEN proven).
- All 6 prior gates + the committed `run all --space=smoke --smoke` stay green.
- `git diff --stat` shows ONLY engine/contracts/smoke-dod.sh (new) + engine/bricks/run-controller.js.
- pipeline.yaml, engine/manifests/, runs/smoke/, and stub emits unchanged; no stray runs/_vfy* spaces.
</verification>

<success_criteria>
- SMOKE-01: `run all --space=<fresh> --smoke` exits 0, 11 receipts, canonical R1 order proven via receipts.
- SMOKE-02: every preflight green + every validator_verdict == pass across the run.
- SMOKE-03: every declared writes[] slot (all 11 manifests, {space}-substituted) materialized on disk.
- SMOKE-04: 6 gated steps log gate.decision='auto-approved-smoke', non-gated decision=null; 11-receipt chain unbroken (64-hex inputs_hash, non-empty all-pass verdicts).
- SMOKE-05: zero orphan/dangling over the REAL run; a dropped OR reordered producer fails fast with a NAMED `pipeline incomplete/misordered` refusal before any spawn; the happy full pipeline passes the guard.
- Determinism: two fresh full runs byte-identical (excl. _receipts/ + *-log.txt).
- Invariant: 7 gates green; committed smoke run clean; sources unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/06-smoke-run-definition-of-done/06-01-SUMMARY.md`.

Atomic commit guidance (ask before committing per project CLAUDE.md; do NOT push):
  1. commit the harness RED:   `test(06): smoke-dod DoD harness (SMOKE-01..05 + determinism), RED on completeness preflight`
  2. commit the preflight GREEN: `feat(06): run-all pipeline-completeness preflight (SMOKE-05) — fail fast on dropped/reordered producer`
  3. commit any review fixes separately.
</output>
