---
phase: 02-run-controller-pipeline-spine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline.yaml
  - runs/_fixture/pipeline/pipeline.yaml
  - runs/_fixture/pipeline/manifests/fx-01-emit.json
  - runs/_fixture/pipeline/manifests/fx-02-gate.json
  - runs/_fixture/pipeline/manifests/fx-03-wave.json
  - runs/_fixture/pipeline/manifests/fx-bad-emit.json
  - engine/contracts/controller-smoke.sh
autonomous: true
requirements: [CTRL-02, CTRL-03, CTRL-06, CTRL-08, CTRL-09, CTRL-10, CTRL-11, CTRL-12]
must_haves:
  truths:
    - "The real pipeline.yaml lists the 11 R1 step ids (00-bet-compiler .. 10-adversarial-re-review) in order"
    - "A fixture pipeline.yaml + 1-3 minimal §5-shaped JSON manifests exist under runs/_fixture/pipeline/ (git-whitelisted)"
    - "engine/contracts/controller-smoke.sh exists with one named assert per CTRL-01..12 and is self-cleaning"
    - "Running the harness BEFORE the controller exists fails RED (controller missing), proving the asserts are real"
  artifacts:
    - path: "pipeline.yaml"
      provides: "Real R1 flat step-id list (CTRL-10/CTRL-02 production artifact)"
      contains: "00-bet-compiler"
      min_lines: 12
    - path: "runs/_fixture/pipeline/pipeline.yaml"
      provides: "Fixture flat id list driving CTRL-02/CTRL-10 asserts"
      contains: "fx-01-emit"
    - path: "runs/_fixture/pipeline/manifests/fx-01-emit.json"
      provides: "Minimal §5 manifest (validator:null, gate:false) — the happy-path emit"
      contains: "\"id\""
    - path: "runs/_fixture/pipeline/manifests/fx-02-gate.json"
      provides: "gate:true manifest exercising CTRL-09 block-and-log"
      contains: "\"gate\": true"
    - path: "runs/_fixture/pipeline/manifests/fx-03-wave.json"
      provides: "agents:6 manifest exercising CTRL-06 wave-cap"
      contains: "\"agents\": 6"
    - path: "runs/_fixture/pipeline/manifests/fx-bad-emit.json"
      provides: "Manifest pointing at a basename route.js validates (space-map.json) to prove CTRL-07 exit-2 propagation on a deliberately-bad emit"
      contains: "space-map.json"
    - path: "engine/contracts/controller-smoke.sh"
      provides: "Acceptance harness — one named assert per CTRL id; mirrors store-smoke.sh"
      contains: "CONTROLLER-SMOKE"
      min_lines: 80
  key_links:
    - from: "engine/contracts/controller-smoke.sh"
      to: "engine/bricks/run-controller.js"
      via: "invokes the controller (node engine/bricks/run-controller.js / bin/run) per assert"
      pattern: "run-controller.js|bin/run"
    - from: "engine/contracts/controller-smoke.sh"
      to: "runs/_fixture/pipeline/pipeline.yaml"
      via: "--pipeline=runs/_fixture/pipeline/pipeline.yaml flag"
      pattern: "_fixture/pipeline"
---

<objective>
Author the DATA and the ACCEPTANCE HARNESS for the Phase-2 run-controller, BEFORE the controller itself exists. This is the Nyquist Wave-0 step (mirrors Phase 1's "STORE-02/03 smoke asserts authored RED ahead of Plan 02 bricks"): the harness and fixtures land first and fail RED, so Plan 02's controller has a real, executable target.

Deliverables:
- `pipeline.yaml` (repo root) — the REAL flat R1 step-id list (steps 0..10), the production artifact for CTRL-10/CTRL-02. Reordering is a one-line edit (no controller change).
- A fixture manifest set under `runs/_fixture/pipeline/` (git-WHITELISTED) — a fixture `pipeline.yaml` + 1-3 minimal §5-shaped `.json` manifests that drive the full 7-phase loop on a stub.
- `engine/contracts/controller-smoke.sh` — the acceptance harness, one named assert per CTRL-01..12, mirroring `store-smoke.sh` verbatim (set -u, cd-to-repo-root, ok/bad, inline `node -e` asserts, self-cleaning temp space, named final exit).

Purpose: Lock the executable contract the controller must satisfy. Every CTRL id maps to a concrete assert here.
Output: `pipeline.yaml`, 5 fixture files under `runs/_fixture/pipeline/`, `engine/contracts/controller-smoke.sh`.

DECISION STATED (CONTEXT manifest-availability call): approach **A** — a tiny fixture manifest set under `runs/_fixture/pipeline/` (git-whitelisted, so committed fixtures are intentional), NOT the real 11 manifests (those are Phase 3). The harness drives the FULL 7-phase loop on the fixture exactly as `run all --space=smoke` will later drive the real 11.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-run-controller-pipeline-spine/02-CONTEXT.md
@.planning/phases/02-run-controller-pipeline-spine/02-RESEARCH.md
@.planning/phases/02-run-controller-pipeline-spine/02-PATTERNS.md
@.planning/phases/02-run-controller-pipeline-spine/02-VALIDATION.md

<interfaces>
<!-- Exact contracts the harness asserts against — use these directly, do not re-derive. -->

Brick CLI contracts the controller (Plan 02) will invoke and the harness checks:

receipt-write.js (engine/bricks/receipt-write.js):
  node engine/bricks/receipt-write.js --space=<s> --spawn-id=<UNIQUE> --step=<id> --inputs=<csv> --outputs=<csv> --gate=<json>
  Writes runs/<space>/_receipts/<spawn_id>.json. WRITE-ONCE: a colliding spawn-id → exit 1.
  Receipt object: { spawn_id, step, space, inputs_hash (sha256 of sorted input bytes), inputs[], outputs[], validator_verdicts:[], gate, ts }

space-version.js (engine/bricks/space-version.js):
  node engine/bricks/space-version.js --space=<base>   → prints ONE bare line: the next free space name.
  runs/ absent or no runs/<base> → prints "<base>"; runs/<base> exists → prints "<base>-v2"; etc. READ-ONLY.

route.js (engine/hooks/route.js):
  node engine/hooks/route.js <written-file-path>   → exit 0 = pass; propagates the called validator's non-zero exit (2 = reject).
  Dispatch table: brands.json→finder+revenue; dump.json→dumper; space-map.json→validate-classifier.js; *-beliefs.json→analyzer; anything else→pass(0).
  NOTE: validate-classifier.js has 9 non-trivial reject rules — a stub space-map.json will FAIL it. Use that to prove CTRL-07 exit-2 propagation (the bad-emit fixture).

store-scaffold.js (engine/bricks/store-scaffold.js):
  node engine/bricks/store-scaffold.js --space=<s>   → scaffolds runs/<space>/ slot tree (idempotent, no-overwrite).

§5 manifest shape (SHELL-BUILD-SPEC §5): { id, reads[], writes[], scripts:{pre[],post[]}, prompt, agents, validator, gate }. {space} is the substitution token.
</interfaces>

<harness_idiom>
<!-- engine/contracts/store-smoke.sh is the EXACT skeleton. Copy it; swap the asserts. -->
set -u
cd "$(dirname "$0")/../.." || exit 1   # engine/contracts/ -> repo root
SPACE="_ctrlsmoke"                      # underscore-prefixed scratch — NOT auto-gitignored, so rm -rf both ends
for a in "$@"; do case "$a" in --space=*) SPACE="${a#--space=}" ;; esac done
FAIL=0
ok()  { echo "   PASS: $1"; }
bad() { echo "   FAIL: $1"; FAIL=1; }
rm -rf "runs/${SPACE}"                  # clean START
# ... asserts (one per CTRL id) ...
rm -rf "runs/${SPACE}"                  # clean END — leave git status clean for runs/
if [ "$FAIL" -eq 0 ]; then echo "CONTROLLER-SMOKE: ALL ASSERTS PASS ✓"; exit 0;
else echo "CONTROLLER-SMOKE: FAILED — see the named assert above"; exit 1; fi
</harness_idiom>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the real pipeline.yaml (R1 order) + the fixture manifest set</name>
  <files>pipeline.yaml, runs/_fixture/pipeline/pipeline.yaml, runs/_fixture/pipeline/manifests/fx-01-emit.json, runs/_fixture/pipeline/manifests/fx-02-gate.json, runs/_fixture/pipeline/manifests/fx-03-wave.json, runs/_fixture/pipeline/manifests/fx-bad-emit.json</files>
  <read_first>
    - .planning/phases/02-run-controller-pipeline-spine/02-PATTERNS.md (the pipeline.yaml shape + R1 order block, lines ~234-253; the fixture manifest §5 shape, lines ~212-230)
    - .planning/phases/02-run-controller-pipeline-spine/02-RESEARCH.md (Pattern 3 flat parser; Code Examples — minimal fixture manifest; Open Q1/A2 on validator:null)
    - .planning/phases/02-run-controller-pipeline-spine/02-CONTEXT.md (canonical_refs — R1 order from PART3 §1.5/§1.6, lines 70-101)
    - engine/_fixture/audit/resolve-manifest.json (the existing .json manifest fixture precedent — confirms the engine's .json convention)
    - engine/hooks/route.js (the dispatch table — confirm space-map.json → validate-classifier.js for the bad-emit fixture)
    - .gitignore (lines 20-21 — confirm runs/_fixture/** is whitelisted, so committed fixtures are intentional)
  </read_first>
  <action>
Create the real pipeline + the fixture data files.

1. `pipeline.yaml` at repo ROOT — a flat YAML `steps:` list of the 11 R1 step ids in canonical order (CTRL-10 / CTRL-02). Exact content:
```yaml
steps:
  - 00-bet-compiler
  - 01-collect
  - 02-funnel-analysis
  - 03-space-map
  - 04-voc-market-pass
  - 05-market-selection
  - 06-voc-deep-pass
  - 07-funnel-architect
  - 08-copywriter
  - 09-asset-classify
  - 10-adversarial-re-review
```
This is the production artifact. The controller (Plan 02) parses it with a 6-line flat-list splitter — zero hardcoded order. Reordering steps = editing THIS file only.

2. `runs/_fixture/pipeline/pipeline.yaml` — the FIXTURE flat id list the harness drives (CTRL-02/CTRL-10). Keep it short:
```yaml
steps:
  - fx-01-emit
  - fx-02-gate
```
(fx-03-wave and fx-bad-emit are driven by single-step `run <id>` asserts, not the `run all` walk — so they are NOT in this list. This keeps `run all` fast and the CTRL-02 order-assert clean.)

3. Four fixture manifests under `runs/_fixture/pipeline/manifests/` as `.json` (zero-dep `JSON.parse`; matches engine's `.json` fixture convention). Each is the §5 shape `{id, reads[], writes[], scripts:{pre[],post[]}, prompt, agents, validator, gate}`. `{space}` is the substitution token the controller replaces.

   a. `fx-01-emit.json` — the happy-path emit (validator:null, gate:false):
```json
{
  "id": "fx-01-emit",
  "reads": ["runs/{space}/bet-brief.md"],
  "writes": ["runs/{space}/fx-emit.json"],
  "scripts": { "pre": [], "post": [] },
  "prompt": "fixture/stub-emit.md",
  "agents": 1,
  "validator": null,
  "gate": false
}
```
   Note: `writes` a `fx-emit.json` (NOT space-map.json) so route.js passes on the no-rule basename (exit 0) — proving the explicit-call wiring + the route.js pass-path WITHOUT needing a validate-classifier-passing artifact (which is expensive per Open Q1/A2). `reads` a `bet-brief.md` (a real scaffolded slot) so preflight finds it after store-scaffold runs.

   b. `fx-02-gate.json` — same shape but `"gate": true` (CTRL-09 block-and-log). reads `runs/{space}/bet-brief.md`, writes `runs/{space}/fx-gate.json`, validator null, agents 1.

   c. `fx-03-wave.json` — same shape but `"agents": 6` (CTRL-06 wave-cap ≤5 → 2 waves). reads `runs/{space}/bet-brief.md`, writes `runs/{space}/fx-wave.json`, validator null, gate false.

   d. `fx-bad-emit.json` — writes a basename route.js validates so a stub emit FAILS the real validator, proving CTRL-07 exit-2 propagation + bounded re-spawn. Set:
```json
{
  "id": "fx-bad-emit",
  "reads": ["runs/{space}/bet-brief.md"],
  "writes": ["runs/{space}/space-map.json"],
  "scripts": { "pre": [], "post": [] },
  "prompt": "fixture/stub-emit.md",
  "agents": 1,
  "validator": null,
  "gate": false
}
```
   The mock-emit writes a `{_stub:true}` artifact; route.js dispatches `space-map.json` → `validate-classifier.js` → rejects (exit 2). The controller re-spawns ≤2 (each produces the same bad stub), then escalates + exits non-zero. This is the CTRL-07 negative assert.

Do NOT create the controller, the bin/run shim, or any prompt body here — those are Plan 02. The `prompt` field points at a fixture stub path that need not exist (the controller's mock-emit ignores it in fixture mode).
  </action>
  <verify>
    <automated>test -f pipeline.yaml && grep -q '00-bet-compiler' pipeline.yaml && grep -q '10-adversarial-re-review' pipeline.yaml && node -e 'const fs=require("fs");for(const f of ["fx-01-emit","fx-02-gate","fx-03-wave","fx-bad-emit"]){const m=JSON.parse(fs.readFileSync("runs/_fixture/pipeline/manifests/"+f+".json","utf8"));if(!("reads"in m)||!("writes"in m)||!("scripts"in m)||!("agents"in m)||!("validator"in m)||!("gate"in m))throw new Error("bad shape "+f);}console.log("OK fixtures")' && node -e 'const m=JSON.parse(require("fs").readFileSync("runs/_fixture/pipeline/manifests/fx-02-gate.json","utf8"));if(m.gate!==true)throw new Error("gate not true");if(JSON.parse(require("fs").readFileSync("runs/_fixture/pipeline/manifests/fx-03-wave.json","utf8")).agents!==6)throw new Error("agents not 6");if(JSON.parse(require("fs").readFileSync("runs/_fixture/pipeline/manifests/fx-bad-emit.json","utf8")).writes[0].indexOf("space-map.json")<0)throw new Error("bad-emit not space-map");console.log("OK variants")'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f pipeline.yaml` succeeds; `grep -c '^  - ' pipeline.yaml` returns `11` (11 step ids).
    - `grep -n '00-bet-compiler' pipeline.yaml` is line BEFORE `grep -n '10-adversarial-re-review'` (R1 order).
    - `test -f runs/_fixture/pipeline/pipeline.yaml` succeeds and contains `fx-01-emit` and `fx-02-gate`.
    - All four files under `runs/_fixture/pipeline/manifests/` parse as JSON and each carries keys `id reads writes scripts prompt agents validator gate`.
    - `fx-02-gate.json` has `gate:true`; `fx-03-wave.json` has `agents:6`; `fx-bad-emit.json` `writes` contains `space-map.json`; `fx-01-emit.json` `writes` contains `fx-emit.json` (NOT space-map.json).
    - `git check-ignore -v runs/_fixture/pipeline/pipeline.yaml` returns exit 0 and prints the matching `!runs/_fixture/**` whitelist rule (whitelisted → committed intentionally). [confirms fixture location is git-safe]
  </acceptance_criteria>
  <done>pipeline.yaml lists 11 R1 ids in order; the fixture pipeline + 4 §5-shaped JSON manifests exist under runs/_fixture/pipeline/ with the gate/wave/bad-emit variants set.</done>
</task>

<task type="auto">
  <name>Task 2: Author controller-smoke.sh — one named assert per CTRL-01..12 (authored RED)</name>
  <files>engine/contracts/controller-smoke.sh</files>
  <read_first>
    - engine/contracts/store-smoke.sh (the EXACT harness skeleton to copy — set -u, cd-to-repo-root, ok/bad, inline node -e asserts, rm -rf self-clean both ends, named final exit; STORE-03 inline-mutation pattern for negative asserts)
    - engine/contracts/h6-all.sh (the regression-suite runner — confirm the harness can run standalone)
    - .planning/phases/02-run-controller-pipeline-spine/02-VALIDATION.md (the Per-Task Verification Map — the exact assert per CTRL id, lines 43-54)
    - .planning/phases/02-run-controller-pipeline-spine/02-RESEARCH.md (Phase Requirements → Test Map lines 538-551; harness skeleton lines 461-472; CTRL-12 static-grep assert line 551)
    - engine/bricks/receipt-write.js (receipt shape the CTRL-08 assert checks — spawn_id/inputs_hash/validator_verdicts/gate; write-once exit 1)
    - engine/bricks/space-version.js (the CTRL-08 no-overwrite NAMING unit assert — prints <base>-v2 after scaffold)
    - this plan's <interfaces> and <harness_idiom> blocks
  </read_first>
  <action>
Write `engine/contracts/controller-smoke.sh` mirroring `store-smoke.sh` VERBATIM in skeleton (set -u, `cd "$(dirname "$0")/../.."`, `ok()/bad()`, `FAIL=0`, `rm -rf "runs/${SPACE}"` at start AND end, named final exit `CONTROLLER-SMOKE: ALL ASSERTS PASS ✓` / non-zero). Use `SPACE="_ctrlsmoke"` (underscore scratch — NOT auto-gitignored, so the rm -rf both-ends is mandatory to leave `git status` clean for runs/).

The harness invokes the controller as `node engine/bricks/run-controller.js <all|step> --space="$SPACE" --smoke [--pipeline=...] [--manifest-dir=runs/_fixture/pipeline/manifests]`. Before each run that needs scaffolded slots, call `node engine/bricks/store-scaffold.js --space="$SPACE"` so the fixture `reads[]` (bet-brief.md) exists (preflight must find it).

Author ONE named assert per CTRL id (the harness is RED now — the controller arrives in Plan 02; that is the intended Wave-0 state, exactly like Phase 1's STORE-02/03 asserts authored ahead of their bricks):

- **CTRL-11** (loadManifest §5 shape): `node -e` calls the controller's loader OR just asserts `node engine/bricks/run-controller.js --print-manifest=fx-01-emit --manifest-dir=runs/_fixture/pipeline/manifests` prints an object with keys `reads writes scripts prompt agents validator gate`. (The controller exposes a `--print-manifest=<id>` debug flag — Plan 02 implements it.)
- **CTRL-01** (single step, all 7 phases): scaffold, then `run fx-01-emit --space=$SPACE --smoke --manifest-dir=...`; assert exit 0 AND stdout contains 7 phase markers (PREFLIGHT, PLAN, CONTEXT, SPAWN, VALIDATE, STORE, GATE) AND a receipt landed under `runs/$SPACE/_receipts/`.
- **CTRL-04** (plan-print before run): capture stdout; assert the PLAN block line number is BEFORE the first SPAWN/STORE line (grep -n + arithmetic, or assert PLAN prints the validator+gate names ahead of any "wrote" line).
- **CTRL-05** (context embeds reads bytes): assert the controller's context-assembly emits a block containing the bytes of `bet-brief.md` (e.g. the controller prints/writes the assembled block under `--smoke` to a known location, or stdout includes the `<<<DATA ...bet-brief.md>>>` marker). Plan 02 surfaces this.
- **CTRL-06** (waves ≤5): `run fx-03-wave --space=$SPACE --smoke --manifest-dir=...`; assert plan/log shows 2 waves for agents:6 and no wave count >5 (grep for "wave 1 (5" + "wave 2 (1" or equivalent the controller prints).
- **CTRL-02** (run all walks in order): `run all --pipeline=runs/_fixture/pipeline/pipeline.yaml --manifest-dir=... --space=$SPACE --smoke`; assert exit 0 AND receipts for fx-01-emit appear before fx-02-gate (compare receipt `ts` or step order).
- **CTRL-10** (reorder = config edit): copy the fixture pipeline.yaml to a temp with reversed order, run with `--pipeline=<temp>`, assert the receipt step order follows the file (no controller edit). Clean up the temp.
- **CTRL-03** (preflight named refusal): scaffold, then `rm runs/$SPACE/bet-brief.md`, then `run fx-01-emit ...`; assert exit ≠ 0 AND stderr contains `REFUSE` AND the missing path `bet-brief.md`.
- **CTRL-07** (validator explicit + reject→re-spawn≤2→escalate): `run fx-bad-emit --space=$SPACE --smoke --manifest-dir=...`; assert (a) a validator subprocess ran (stderr/stdout shows validate-classifier output, not a silent pass) AND (b) exit ≠ 0 with an escalate line after ≤2 re-spawns.
- **CTRL-08** (no-overwrite version + receipt write-once, unique id): three sub-asserts — (a) after a happy run, `_receipts/<id>.json` exists with keys `spawn_id inputs_hash validator_verdicts gate` and `inputs_hash` matches `^[0-9a-f]{64}$`; (b) UNIT: scaffold $SPACE then `node engine/bricks/space-version.js --space=$SPACE` prints `${SPACE}-v2`; (c) UNIT: `node engine/bricks/receipt-write.js --space=$SPACE --spawn-id=dup ...` twice → second exits 1 (write-once).
- **CTRL-09** (gate block-and-log): `run fx-02-gate --space=$SPACE --smoke --manifest-dir=...`; assert stdout contains `GATE [fx-02-gate]` with `auto-approved` AND the receipt's `gate.step_gated===true`.
- **CTRL-12** (assembled from bricks, not rewritten — STATIC grep on the controller): assert `engine/bricks/run-controller.js` contains NONE of `createHash`, an inline sanitize regex `[a-z0-9._-]`/`replace(/\.\./`, a `-v\d` version scan, or a hardcoded slot-list; AND DOES contain `require('./lib/fanout-path')` AND spawns `receipt-write.js`, `space-version.js`, and `route.js`. (This assert reads the controller SOURCE — RED until Plan 02 writes it.)

Make EVERY assert tolerant of the controller-missing state so the harness fails with a clear `FAIL:` line (RED) rather than a bash crash — e.g. guard `node engine/bricks/run-controller.js` calls so a missing file produces `bad "CTRL-XX: controller missing"`, not `set -u` abort. End with the named final exit. The harness MUST `rm -rf "runs/${SPACE}"` at start and end and leave `git status` clean for `runs/`.
  </action>
  <verify>
    <automated>bash -n engine/contracts/controller-smoke.sh && grep -q 'CONTROLLER-SMOKE: ALL ASSERTS PASS' engine/contracts/controller-smoke.sh && for id in CTRL-01 CTRL-02 CTRL-03 CTRL-04 CTRL-05 CTRL-06 CTRL-07 CTRL-08 CTRL-09 CTRL-10 CTRL-11 CTRL-12; do grep -q "$id" engine/contracts/controller-smoke.sh || { echo "MISSING $id"; exit 1; }; done && grep -q 'rm -rf "runs/${SPACE}"' engine/contracts/controller-smoke.sh && echo "OK harness shape"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n engine/contracts/controller-smoke.sh` parses clean (no syntax error).
    - Each of `CTRL-01 CTRL-02 CTRL-03 CTRL-04 CTRL-05 CTRL-06 CTRL-07 CTRL-08 CTRL-09 CTRL-10 CTRL-11 CTRL-12` appears at least once in the harness (one named assert per id).
    - The harness contains `rm -rf "runs/${SPACE}"` BOTH near the top (clean start) and near the bottom (clean end).
    - The harness contains the named final line `CONTROLLER-SMOKE: ALL ASSERTS PASS ✓` and a non-zero failure branch.
    - The harness references `engine/bricks/run-controller.js` (the unit under test) and `runs/_fixture/pipeline/pipeline.yaml` (the fixture).
    - Running `bash engine/contracts/controller-smoke.sh` BEFORE Plan 02 exits NON-ZERO with at least one `FAIL:` line (authored RED) and does NOT bash-crash. [run it: `bash engine/contracts/controller-smoke.sh; echo "exit=$?"` → exit non-zero, output has `FAIL:`]
    - After running, `git status --porcelain runs/` shows NO untracked `runs/_ctrlsmoke` (self-clean works).
  </acceptance_criteria>
  <done>controller-smoke.sh parses clean, asserts all 12 CTRL ids, self-cleans both ends, and fails RED (with clear FAIL lines, no crash) before the controller exists — the executable target for Plan 02.</done>
</task>

</tasks>

<verification>
- `bash -n engine/contracts/controller-smoke.sh` → clean parse.
- `bash engine/contracts/controller-smoke.sh; echo exit=$?` → exit non-zero, output contains `FAIL:` (RED, controller not yet built), NO bash crash.
- `grep -c '^  - ' pipeline.yaml` → 11.
- All 4 fixture manifests `JSON.parse` clean with the §5 keys.
- `git status --porcelain runs/` shows no untracked `_ctrlsmoke` after a harness run (self-clean verified).
- Regression: `bash engine/contracts/h6-all.sh` and `bash engine/contracts/store-smoke.sh --space=smoke` still green (this plan adds only new files; touches no existing brick).
</verification>

<success_criteria>
- `pipeline.yaml` (root) lists 11 R1 step ids in canonical order (CTRL-10/CTRL-02 production artifact).
- A fixture pipeline + 4 §5-shaped JSON manifests (happy / gate / wave / bad-emit) exist under git-whitelisted `runs/_fixture/pipeline/`.
- `engine/contracts/controller-smoke.sh` asserts all 12 CTRL ids (one named assert each), self-cleans, and is authored RED — the executable contract Plan 02's controller must turn green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-run-controller-pipeline-spine/02-01-SUMMARY.md`
</output>
