---
phase: 01-stage-m1-s1-light-pass
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/fetch.js
autonomous: true
requirements: [TOOL-01, BET-01, TREND-01]
must_haves:
  truths:
    - "fetch.js reads its LP-hunt template (query terms + URL-path patterns) from the bet brief's fenced PIPELINE INPUTS block via a tolerant parse — NOT from a hardcoded array (D-16); the 'fixed, not agent-chosen' determinism principle is kept, only the contents become a per-run input"
    - "fetch.js performs a real Google Trends fetch (~5yr window) per brand/category term and writes a per-brand demand_trend { shape, window, source, basis } record into brands.json — the actual SOURCE that populates the D-15 field"
    - "demand_trend.shape is derived deterministically from the Trends curve and is one of steady|rising|parabolic-spike|declining|unknown (unknown when no source resolves) — fetch.js never returns unknown for every brand silently; the fad-death/parabolic-spike durability signal is live"
    - "clean.js is NOT touched by this plan — it stands as built; the corpus/<slug>/raw → corpus/<slug>/clean tree and the clean-copy-only enforcement are unchanged"
  artifacts:
    - path: "tools/fetch.js"
      provides: "Playwright per-brand fetch with brief-driven LP-hunt (PIPELINE INPUTS, tolerant parse) + a real Google Trends ~5yr source writing per-brand demand_trend into brands.json"
      contains: "demand_trend"
  key_links:
    - from: "tools/fetch.js LP-hunt template"
      to: "the bet brief's fenced PIPELINE INPUTS block (layer B of D-13)"
      via: "tolerant parse of the PIPELINE INPUTS lists (LP-hunt terms, comparable seeds, trend toggle) — read with a --brief flag, fall back gracefully if absent"
      pattern: "PIPELINE INPUTS"
    - from: "tools/fetch.js Google Trends fetch"
      to: "brands.json per-brand demand_trend { shape, window, source, basis }"
      via: "~5yr Trends pull per brand/category term → deterministic shape classification → write back into the brand row"
      pattern: "demand_trend"
---

<objective>
Apply the 2026-06-03 revision deltas to the ALREADY-BUILT `tools/fetch.js` (built by the prior 01-02 run; see 01-02-SUMMARY.md — it currently HARDCODES the LP-hunt URL-path patterns and has NO trend source). Two surgical edits:
1. **D-16** — move the LP-hunt template OUT of the hardcoded `LP_PATH_PATTERNS` array; read it from the bet brief's fenced `PIPELINE INPUTS` block (tolerant parse, layer B of D-13). Keep the "fixed, not agent-chosen" determinism principle — only the *contents* become a per-run input.
2. **D-15 (source half)** — add a real Google Trends fetch (~5yr window) per brand/category term that populates the per-brand `demand_trend` field (the field itself + shape enum are added to the schema by 01-01).

`clean.js` is in the untouched-build-that-stands list — this plan does NOT edit it. Both halves of D-15 are required: a field with no source returns `unknown` for every brand and silently disables the single most load-bearing anti-fad signal (the Gate-1 parabolic-spike kill).

Purpose: success criteria 5/6 — fetch.js reads the brief's PIPELINE INPUTS (LP-hunt terms + trend toggle), not a hardcoded template (D-16); each brand carries a populated demand_trend fed by a real Google Trends fetch (D-15), so the durability column is not empty `unknown` across the board.
Output: an edited `tools/fetch.js` (clean.js untouched).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stage-m1-s1-light-pass/01-CONTEXT.md
@.planning/phases/01-stage-m1-s1-light-pass/01-PATTERNS.md

@tools/fetch.js
@CLAUDE.md

<interfaces>
<!-- Runtime (PATTERNS.md §Runtime): Node v20.20.0, Playwright 1.59.1 GLOBAL at ~ (NOT in repo).
     No package.json, no lockfile, no local node_modules. Every script is `node tools/<x>.js <args>`,
     zero deps beyond global playwright + node builtins (fs, path). The Google Trends fetch must use the
     SAME mechanism — Playwright against the Google Trends explore page, OR an unauthenticated Trends
     endpoint hit via Playwright's request context — NO new npm dependency (no google-trends-api package). -->

<!-- SCHEMA OWNERSHIP (must match 01-01 + 01-04): demand_trend is a per-brand record.
     fetch.js (THIS plan, Google Trends source) WRITES it into the per-brand row in brands.json,
     exactly where revenue_est sits (script-populated, _note flagged). The Classifier (01-01) then
     reads it from brands.json and carries it into space-map.json per_brand[]. The validator
     (01-04 validate-classifier.js) rejects demand_trend missing / shape off-enum on space-map.json. -->
demand_trend record fetch.js writes into each brands.json brand row:
{ "shape": "steady|rising|parabolic-spike|declining|unknown", "window": "e.g. 2021-2026 (5yr)|null",
  "source": "google-trends|null", "basis": "one-line read of the curve|null" }

<!-- The bet brief's PIPELINE INPUTS fenced block (layer B of D-13) fetch.js tolerant-parses:
     a fenced ```PIPELINE INPUTS block in the brief carrying flat lists — LP-hunt terms, comparable-bet
     seed brands, trend-source toggle. fetch.js reads LP-hunt terms (+ trend toggle) from it; a messy
     brief must degrade gracefully, never crash the run. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-16 — read the LP-hunt template from the bet brief's PIPELINE INPUTS (tolerant parse), drop the hardcode</name>
  <files>tools/fetch.js</files>
  <read_first>
    - tools/fetch.js (the ALREADY-BUILT file — read whole; the hardcoded LP-hunt currently lives in the `LP_PATH_PATTERNS` array and the traceability comment block — locate with `grep -n 'LP_PATH_PATTERNS\|LP-hunt\|students|college' tools/fetch.js`)
    - .planning/phases/01-stage-m1-s1-light-pass/01-02-SUMMARY.md (records fetch.js hardcodes 11 fixed LP-hunt URL-path patterns + the query set preserved as a comment)
    - .planning/phases/01-stage-m1-s1-light-pass/01-CONTEXT.md §D-16 (the LP-hunt template becomes a per-run INPUT read from the bet brief's PIPELINE INPUTS; the determinism principle stays — fixed per run, not agent-chosen mid-run; a hardcoded template can't self-correct against a different product) and §D-13 layer B (PIPELINE INPUTS → scripts tolerant-parse)
  </read_first>
  <action>
    First locate the hardcode: `grep -n 'LP_PATH_PATTERNS\|students|college|back-to-school' tools/fetch.js`. As built, the hardcoded array is `const LP_PATH_PATTERNS = [ '/clp/', '/lp/', ... ]` (~L45-50) and the verbatim query set is preserved as a comment block (~L51-72).

    Replace the hardcoded source with a brief-driven read. Concretely:

    (a) Add a `--brief=<path>` CLI option (default `./bet-brief.md` or null) alongside the existing `--brands`/`--out` parsing (mirror the existing `opts` parsing at ~L18-24). Document it in the `--help` text.

    (b) Add a tolerant parser `parsePipelineInputs(briefPath)` that: reads the brief file if present; extracts the fenced ```PIPELINE INPUTS block (a fenced code block whose first line is `PIPELINE INPUTS`); from inside it, pulls the flat lists — `LP-hunt terms` (URL-path patterns AND/OR query terms), `comparable-bet seed brands`, `trend-source toggle`. The parse MUST be tolerant: a missing brief, a missing PIPELINE INPUTS block, or a malformed list returns sensible defaults and logs a warning — it NEVER throws/crashes (layer B rule: a messy brief degrades, never hard-fails). Return `{ lpPaths: [...], lpQueries: [...], comparableSeeds: [...], trendToggle: bool }`.

    (c) Use the parsed `lpPaths` in place of the hardcoded `LP_PATH_PATTERNS` array in the per-brand loop (the loop that iterates `for (const pattern of LP_PATH_PATTERNS)` at ~L196). If the brief supplies no LP-hunt paths, fall back to the prior built-in default set (keep the old list as a NAMED default constant, e.g. `DEFAULT_LP_PATHS`, so absence of a brief still runs the debug pass — the determinism principle holds: the set is fixed for the run, just sourced from the brief when present).

    Keep the determinism principle EXPLICIT in a comment: the LP-hunt set is FIXED for the run (not agent-chosen mid-run) — it is now a per-run planning INPUT from the brief rather than a code hardcode. Do NOT make fetch.js call an agent or a search API to pick paths dynamically.

    Do NOT touch clean.js. Do NOT add any npm dependency. Keep `--help` exiting 0.
  </action>
  <verify>
    <automated>cd /home/kyu3/PMF && node -c tools/fetch.js && node tools/fetch.js --help >/dev/null 2>&1; test $? -eq 0 && grep -q 'PIPELINE INPUTS' tools/fetch.js && grep -q -- '--brief' tools/fetch.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node -c tools/fetch.js` exits 0 (valid syntax after edit)
    - `node tools/fetch.js --help` exits 0 and the help text documents `--brief`
    - `grep -q 'PIPELINE INPUTS' tools/fetch.js` matches (the brief's fenced block is the LP-hunt source)
    - `grep -q 'parsePipelineInputs\|parseBrief' tools/fetch.js` matches (a tolerant parser exists)
    - The LP-hunt set is no longer ONLY a bare hardcode: a `--brief`-sourced path is used, with the prior list kept as a NAMED default fallback (`grep -q 'DEFAULT_LP_PATHS' tools/fetch.js` matches and the per-brand loop iterates the brief-or-default set, not a const `LP_PATH_PATTERNS` that is never overridable)
    - Tolerant-parse safety: a missing/empty brief does NOT crash — `node tools/fetch.js --brief=/nonexistent/brief.md --help` still exits 0, and the parser is wrapped so a malformed brief logs a warning rather than throwing (`grep -qi 'try' tools/fetch.js` near the parser and a warning log on parse failure)
    - Zero extra deps: `grep -E "require\('(?!playwright|fs|path|url)" tools/fetch.js` returns 0 (only playwright + node builtins)
    - clean.js untouched: `git diff --name-only` does NOT list tools/clean.js for this task's commit
  </acceptance_criteria>
  <done>fetch.js reads the LP-hunt template from the bet brief's PIPELINE INPUTS via a tolerant parser (layer B), with the prior hardcoded list kept only as a named default fallback; the determinism principle is preserved (fixed per run, not agent-chosen); no new deps; clean.js untouched.</done>
</task>

<task type="auto">
  <name>Task 2: D-15 (source half) — add a real Google Trends ~5yr fetch populating per-brand demand_trend in brands.json</name>
  <files>tools/fetch.js</files>
  <read_first>
    - tools/fetch.js (the ALREADY-BUILT file — read whole; note the per-brand loop ~L162-215, the Playwright context/launch ~L132-157, and how brands.json is read at the top ~L116-128; the demand_trend record must be WRITTEN BACK into each brand row in brands.json)
    - .planning/phases/01-stage-m1-s1-light-pass/01-02-SUMMARY.md (records the current fetch.js does homepage + LP-hunt only, NO trend source — this is the gap)
    - .planning/phases/01-stage-m1-s1-light-pass/01-CONTEXT.md §D-15 (BOTH halves required; Google Trends per brand/category term, ~5yr window; the SOURCE here populates the field 01-01 added; shape closed enum steady|rising|parabolic-spike|declining|unknown; unknown only when no source resolves — never silently unknown for every brand)
    - 01-01-PLAN.md (the sibling plan — confirm the demand_trend schema OWNERSHIP: fetch.js writes it into brands.json per-brand, the Classifier carries it to space-map.json; the validator enforces on space-map.json)
  </read_first>
  <action>
    Add a real Google Trends source that writes a per-brand `demand_trend` record into brands.json. SCHEMA OWNERSHIP (must match 01-01/01-04): fetch.js owns writing `demand_trend` into the per-brand row in `brands.json` (script-populated, the same place `revenue_est` sits). Concretely:

    (a) Add a `fetchTrend(context, term, window)` function: using the SAME Playwright `context` already created (no new dep), hit Google Trends for the term over a ~5yr window — either by navigating the Trends explore page (`https://trends.google.com/trends/explore?date=today%205-y&q=<term>`) and reading the rendered interest-over-time series, OR by using Playwright's request context against the Trends `multiline`/`comparedgeo` widget data endpoint (whichever resolves reliably — Trends is CF/anti-bot heavy, reuse the existing stealth context + CF-clear loop). Return the raw interest-over-time series (array of {date, value}) or null on failure.

    (b) Add a deterministic `classifyTrendShape(series)` function (pure arithmetic, no LLM — CLAUDE.md: deterministic → script) that maps the series to one of the CLOSED enum values: `steady | rising | parabolic-spike | declining | unknown`. Rules (deterministic, document them in a comment):
      - `null`/empty series → `unknown` (the escape valve — only here, never blanket).
      - net trend up over the window, no sharp reversal → `rising`.
      - net trend down → `declining`.
      - a sharp peak (e.g. a window-max far above the trailing/leading mean) followed by a steep drop → `parabolic-spike` (the fad-death signal — the load-bearing Gate-1 kill).
      - otherwise → `steady`.
      (Thresholds are Claude's discretion — keep them simple and documented; the debug run calibrates them.)

    (c) In the per-brand loop, after the homepage/LP-hunt fetch, call `fetchTrend` for the brand's term (brand name and/or its category term — use the brand `slug`/`brand` field; the category comes from the brief's PIPELINE INPUTS or the brands.json starting_point). Build the `demand_trend` record:
      `{ shape: classifyTrendShape(series), window: "today 5-y", source: series ? "google-trends" : null, basis: "<one-line read of the curve, e.g. 'sharp 2023 spike then -70% by 2025'>" }`
    and attach it to the brand row.

    (d) WRITE the enriched brands.json back: after the loop, write the brands array (now carrying per-brand `demand_trend`) back to the brands.json path (preserve the rest of the structure — starting_point, dropped, etc.). Use a per-brand try/catch so one Trends failure yields `shape:"unknown"` for THAT brand only and does NOT abort the batch (adlib-sweep resilient-batch pattern); log it to `_fetch-log.txt`.

    Respect the trend toggle from PIPELINE INPUTS (Task 1): if the brief's trend-source toggle is off, skip the Trends fetch and write `demand_trend.shape:"unknown", source:null` (an explicit operator opt-out, not a silent failure). Do NOT touch clean.js. Do NOT add an npm dep.
  </action>
  <verify>
    <automated>cd /home/kyu3/PMF && node -c tools/fetch.js && grep -q 'demand_trend' tools/fetch.js && grep -q 'classifyTrendShape\|fetchTrend' tools/fetch.js && grep -q 'parabolic-spike' tools/fetch.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node -c tools/fetch.js` exits 0 (valid syntax after edit)
    - `grep -q 'demand_trend' tools/fetch.js` matches (the per-brand record is built)
    - `grep -q 'fetchTrend' tools/fetch.js` and `grep -q 'classifyTrendShape' tools/fetch.js` match (a real Trends fetch + a deterministic shape classifier exist)
    - `grep -qi 'trends.google.com\|trends' tools/fetch.js` matches (an actual Google Trends source, not a stub)
    - The shape classifier emits all five enum values: `grep -Eq 'steady' tools/fetch.js && grep -Eq 'rising' tools/fetch.js && grep -Eq 'parabolic-spike' tools/fetch.js && grep -Eq 'declining' tools/fetch.js && grep -Eq 'unknown' tools/fetch.js`
    - demand_trend is WRITTEN BACK to brands.json (the per-brand source-of-record): `grep -qi 'writeFileSync' tools/fetch.js` near the brands.json write and the brands array carries demand_trend (`grep -A4 'demand_trend' tools/fetch.js | grep -q 'shape'`)
    - `unknown` is the escape valve only (per-brand failure or toggle-off), NOT a blanket default — the classifier returns a real shape for a non-null series (`grep -B2 -A2 "unknown" tools/fetch.js` shows it gated on null/empty series or toggle-off, not the unconditional return)
    - Zero extra deps: `grep -E "require\('(?!playwright|fs|path|url)" tools/fetch.js` returns 0 (no google-trends-api package)
    - clean.js untouched: it is NOT in this plan's files_modified and `git diff` shows no clean.js change
  </acceptance_criteria>
  <done>fetch.js performs a real Google Trends ~5yr fetch per brand via the existing Playwright stealth context, deterministically classifies the curve into the closed shape enum, writes a per-brand demand_trend { shape, window, source, basis } back into brands.json, returns unknown only on per-brand failure or operator toggle-off, adds no dependency, and leaves clean.js untouched — the SOURCE half of D-15 that makes the durability column live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web → fetch.js (Google Trends) | Untrusted remote HTML/JSON from trends.google.com crosses here. Playwright renders/reads it; we parse the interest-over-time series as DATA. This boundary is already covered by the existing fetch.js web→fetch posture; Trends is one more host. |
| bet brief → fetch.js (PIPELINE INPUTS) | Operator-authored brief file is read and tolerant-parsed for flat lists. Treated as DATA — string parse only, never eval'd. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | Tampering / RCE | fetch.js parsing Trends data + the brief | mitigate | Trends series + the brief PIPELINE INPUTS are treated as DATA only — JSON.parse / string ops, NO eval/Function/exec on fetched or brief content. classifyTrendShape is pure arithmetic. |
| T-01-14 | DoS (Trends rate-limit / one bad term aborts batch) | fetchTrend per-brand | mitigate | Per-brand try/catch — one Trends failure yields shape:"unknown" for that brand only and logs to _fetch-log.txt; batch continues (adlib-sweep resilient pattern). |
| T-01-04 | Elevation (page JS exec) | fetch.js Playwright render of Trends | accept | Inherits the existing crowdfund-fetch.js posture — Playwright sandboxed headless Chromium, `page.content()`/series read as a string. Local research tooling, proportionate. |
</threat_model>

<verification>
- fetch.js reads LP-hunt from the brief's PIPELINE INPUTS via a tolerant parser, prior list kept as named default (Task 1).
- fetch.js does a real Google Trends ~5yr fetch, classifies shape deterministically into the closed enum, writes per-brand demand_trend back to brands.json (Task 2).
- unknown is the escape valve only (per-brand failure / toggle-off), never blanket.
- No npm dependency added; clean.js untouched.
- Field names + ownership (demand_trend in brands.json, source:"google-trends", shape enum) match 01-01 + 01-04.
</verification>

<success_criteria>
`tools/fetch.js` now sources the LP-hunt template from the bet brief's PIPELINE INPUTS via a tolerant parse (D-16, determinism preserved via a named default fallback) and performs a real Google Trends ~5yr fetch per brand that writes a per-brand `demand_trend { shape, window, source, basis }` into brands.json with the shape on the closed enum (D-15 source half) — so the durability column is populated, not blanket `unknown`. clean.js is untouched (stands as built). Schema field names + ownership match 01-01 (field + Classifier carry) and 01-04 (enforcement) — satisfying success criterion 5 (brief-driven LP-hunt) and 6 (live demand_trend source).
</success_criteria>

<output>
After completion, create `.planning/phases/01-stage-m1-s1-light-pass/01-02-SUMMARY.md`
</output>
