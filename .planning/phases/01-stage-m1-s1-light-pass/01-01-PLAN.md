---
phase: 01-stage-m1-s1-light-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prompts/step1-light-pass.md
autonomous: true
requirements: [TOOL-01, BET-01, TREND-01, NET-01]
must_haves:
  truths:
    - "bet_type is an OPEN per-brand field (classifier-named + clustered + evidence-traced like transformation/niche/angle) — NOT a closed enum; no competitive_axis / COMPETITIVE_AXIS_ENUM token survives anywhere in the file"
    - "Each per_brand[] row carries bet_type + bet_type_basis; space-map.json lists canonical bet_types[] with raw variants (same shape as transformations[]); the Classifier owns it and the Dumper is forbidden from naming it"
    - "bet_type is populated for EVERY captured brand (live/dead/region-only) — it is a per-brand descriptor, not a per-cell saturation count, so the D-08 live-only exclusion does NOT gate population"
    - "The Finder + Space Classifier prompts each carry a <bet_brief> block injecting the operator brief VERBATIM as prose (no schema, never hook-validated) — three layers A prose→agent / B PIPELINE INPUTS→script / C output schema→hook are stated and the brief states bet (differentiator × niche × OPEN transformation slot — competitors reveal the transformation)"
    - "Each per_brand[] row carries demand_trend { shape, window, source, basis } with shape on the CLOSED enum steady|rising|parabolic-spike|declining|unknown; the Classifier carries it into space-map.json from the brands.json record fetch.js populates"
    - "The Finder casts a wide net: 'similar' = a product a buyer would cross-shop OR a brand making the same structural bet, spanning the bet-brief territories + named comparable-bet seed brands — never spec-match"
    - "The Classifier claim-typing worked examples span a RANGE of domains (each typed across all four claim_type values) and include ≥1 feature-vs-claim trap"
    - "space-map.json carries the one-line provenance note (canonical transformations are claim-categories competitors ASSERT, not validated customer transformations) and the anti-fluke floor (2+ brands at scale; 7+ day ad longevity to qualify a creative) is VISIBLE in the output"
    - "Already-landed edits stand un-regressed: no awareness/awareness_basis field survives (D-05); the verbatim D-03 sophistication ladder is in the Classifier (D-03/D-04); the saturation schema example is keyed per transformation × niche (D-08); the visits_source enum names semrush-api not similarweb-api (D-11)"
  artifacts:
    - path: "prompts/step1-light-pass.md"
      provides: "the reconciled master light-pass prompt with open bet_type (replacing competitive_axis), <bet_brief> consumption in Finder + Classifier, per-brand demand_trend, wide-net Finder, multi-domain claim-typing examples + feature-trap, and the provenance note + visible anti-fluke floor"
      contains: "bet_type"
  key_links:
    - from: "space-map.json per_brand[] bet_type + the canonical bet_types[] list"
      to: "the Classifier (AGENT 3) open-categorization step (same shape as transformations[])"
      via: "Classifier names each brand's lead bet in the space's own terms, then unifies variants into canonical bet_types[] — traceability-checked, never enum-checked (D-14)"
      pattern: "bet_type"
    - from: "the Finder + Classifier <bet_brief> blocks"
      to: "the per-run operator bet brief (prose layer A) + the fenced PIPELINE INPUTS block (layer B)"
      via: "the brief is injected verbatim as prose context; hooks only ever touch layer C (output schema)"
      pattern: "bet_brief"
    - from: "space-map.json per_brand[] demand_trend"
      to: "the brands.json demand_trend record fetch.js populates (Google Trends source, plan 01-02)"
      via: "the Classifier reads the per-brand demand_trend from brands.json and carries it into space-map.json per_brand[]"
      pattern: "demand_trend"
---

<objective>
Apply the 2026-06-03 structural-feedback REVISION deltas to the ALREADY-BUILT `prompts/step1-light-pass.md` (built by the prior 01-01 run; see 01-01-SUMMARY.md). These are SURGICAL edits to an existing file, not a rebuild. The file currently encodes the SUPERSEDED OLD D-12 (a CLOSED `competitive_axis` enum). Apply only these deltas:
1. **amend-D-12** — flip `competitive_axis` (closed enum) → `bet_type` (OPEN, classifier-named/clustered/evidence-traced); remove `COMPETITIVE_AXIS_ENUM` entirely.
2. **D-13** — add a `<bet_brief>` consumption block to the Finder + Classifier prompts (prose context, never hook-validated; three layers stated).
3. **D-15 (field half)** — add per-brand `demand_trend { shape, window, source, basis }` with a CLOSED `shape` enum to the schema + Classifier wiring (the SOURCE half is 01-02's job).
4. **D-17** — instruct the Finder to cast a wide net by substitutability AND bet-similarity.
5. **D-18** — make the Classifier's claim-typing worked examples span a RANGE of domains + add ≥1 feature-vs-claim trap.
6. **D-19** — add the provenance note to space-map.json + make the anti-fluke floor VISIBLE in output.

Do NOT regress the already-landed edits (D-05 awareness drop, D-03 ladder, D-08 per-cell saturation, D-11 semrush-api) — verify each still asserts so a re-exec is idempotent.

Purpose: success criteria 5/6/7 — agents consume a per-run bet brief (D-13); each brand carries a populated `demand_trend` field (D-15 field half); `bet_type` is OPEN with a basis and no `competitive_axis` enum remains (amend-D-12); the Finder casts a wide net (D-17). This is a prompt/schema-text edit only — it owns no scripts.
Output: an edited `prompts/step1-light-pass.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stage-m1-s1-light-pass/01-CONTEXT.md
@.planning/phases/01-stage-m1-s1-light-pass/01-PATTERNS.md

# The edit touches bet_type / demand_trend / sophistication / transformation vocabulary — definitions.md is the single source of truth (confirmed: no "bet"/"axis"/"trend" term collides with bet_type or demand_trend).
@definitions.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: amend-D-12 — flip competitive_axis (closed enum) → bet_type (OPEN), remove COMPETITIVE_AXIS_ENUM</name>
  <files>prompts/step1-light-pass.md</files>
  <read_first>
    - prompts/step1-light-pass.md (the ALREADY-BUILT file being edited — read whole; competitive_axis currently appears at the per_brand schema block, the schema prose, the closed-enums block, the CLASSIFIER hook spec, the Dumper guard, and AGENT 3 step 4 / step 4b)
    - .planning/phases/01-stage-m1-s1-light-pass/01-01-SUMMARY.md (records that competitive_axis D-12 was wired in a prior round — that is the OLD closed-enum version this task SUPERSEDES)
    - .planning/phases/01-stage-m1-s1-light-pass/01-CONTEXT.md §D-12 (AMENDED — the full open-bet_type spec) and §D-14 (closed-enum rule LOCKED: bet_type is OPEN, traceability-checked never enum-checked)
  </read_first>
  <action>
    First locate every occurrence (line numbers have shifted since build): `grep -n 'competitive_axis\|COMPETITIVE_AXIS_ENUM' prompts/step1-light-pass.md`. As of the built file these sit at the per_brand schema (~L138-139), the schema prose (~L147-156), the closed-enums block (~L162, ~L166), the CLASSIFIER hook spec (~L194-195), the Dumper guard (~L330), and AGENT 3 (~L366, ~L370-382, ~L421). Edit ALL of them. `bet_type` is OPEN — classifier-NAMED + CLUSTERED + evidence-traced, exactly like `transformation` / `niche` / `angle` already work. NO enum.

    (a) PER_BRAND SCHEMA (~L138-139) — replace these two exact lines:
      BEFORE:
        `      "competitive_axis": "function-capability-price | visual-statement | community-openness",`
        `      "competitive_axis_basis": "page-quoted signal the axis call is read off — verbatim/cited, not eyeballed",`
      AFTER:
        `      "bet_type": "the structural bet this brand leads with, NAMED in the space's own terms — OPEN, not an enum",`
        `      "bet_type_basis": "page-quoted signal the bet_type call is read off — verbatim/cited, not eyeballed",`

    (b) NEW canonical bet_types[] list — add a `bet_types` array to the space-map.json schema (place it alongside `transformations`/`niches`/`angles`, same shape), e.g. insert after the `angles` array block:
        `  "bet_types": [`
        `    { "canonical": "novel-hardware-as-lead", "raw_variants": ["the foldable e-ink device","programmable paper tablet"], "brand_count": 3 } ],`
      This mirrors `transformations[]` exactly: a canonical bet name + raw_variants traced to per-brand reads + a brand_count.

    (c) SCHEMA PROSE (~L147-156) — replace the entire `competitive_axis` prose bullet block with a `bet_type` prose block stating: `bet_type` (per brand) = the structural bet the brand LEADS with — what kind of differentiation it competes on — read off the brand's OWN positioning/page (page-readable; you do NOT need the customer's true dream/desire). It is OPEN: the Classifier names each brand's lead bet in the space's own terms, then unifies variants into canonical `bet_types[]` (same as `transformations[]`). Populated for EVERY captured brand (live/dead/region-only — it is a per-brand descriptor, NOT a live-saturation count, so D-08 live-only exclusion does not apply). `bet_type_basis` MUST quote/cite the page signal (same discipline as the sophistication evidence line — hook-checkable for traceability, never for enum membership). Feeds the Phase 2 Gate-2 structural-bet read.

    (d) CLOSED-ENUMS BLOCK (~L162, ~L166) — DELETE the entire line `COMPETITIVE_AXIS_ENUM: function-capability-price | visual-statement | community-openness   (classifier assigns one primary axis per brand; off-list = hard reject)`. Then in the "Open (captured verbatim by the dumper, clustered by the classifier)" prose line, ADD `bet_type` to the open list so it reads `claims · mechanism · niche · angle · bet_type`. In the "Classifier-assigned, hook-rejected off-list" line, REMOVE the `competitive_axis (...)` clause entirely (bet_type is OPEN, not hook-rejected off-list — it is traceability-checked, see AGENT 3).

    (e) CLASSIFIER HOOK SPEC (~L194-195) — replace `Reject any per-brand competitive_axis off COMPETITIVE_AXIS_ENUM; reject a missing/empty competitive_axis_basis when competitive_axis is set.` with: `Reject any per-brand bet_type that is null/empty or whose bet_type_basis is missing/empty; reject any canonical bet_type whose raw_variants do not trace to real per-brand reads (traceability-checked, same shape as transformation/niche/angle — NEVER enum-checked, there is no enum: D-14).`

    (f) DUMPER GUARD (~L330) — in the Dumper's no-classify clause `You never name a transformation, canonical niche, canonical angle, or competitive_axis — those are the classifier's calls.` replace `competitive_axis` with `bet_type` so the Dumper is still forbidden from naming the bet (one-job-per-executor: Classifier owns space-map.json).

    (g) AGENT 3 — SPACE CLASSIFIER (~L366, ~L370-382, ~L421):
      - At step 4 (~L366) change `a competitive_axis call (see COMPETITIVE AXIS below)` → `a bet_type call (see BET TYPE below)`.
      - Replace the entire step 4b "COMPETITIVE AXIS (per brand)" block (~L370-382) with a "BET TYPE (per brand)" block that instructs: read OFF the brand's OWN positioning/page (page-readable — decided from how the brand presents itself, WITHOUT the customer's true dream/desire) and NAME the structural bet it leads with IN THE SPACE'S OWN TERMS (do NOT pick from a fixed list — there is no enum; this is OPEN like transformation/niche/angle). Record a `bet_type_basis` quoting/citing the page signal (same discipline as the sophistication evidence line — read it off the page, do not eyeball). Populate for EVERY captured brand regardless of live status. Then unify the per-brand bet_type reads into canonical `bet_types[]` with raw_variants, exactly as you cluster transformations — variants must trace back to real per-brand reads.
      - In the AGENT 3 RULES block (~L421) replace `competitive_axis is ONE primary axis per brand, on the closed enum, with a page-quoted basis — never an eyeballed call, never off-enum.` with `bet_type is the structural bet each brand leads with, NAMED in the space's own terms (OPEN — no enum), with a page-quoted basis — never eyeballed; canonical bet_types[] must trace to real per-brand reads.`

    Do NOT introduce any "v1"/"simplified"/"for now" language. Do NOT touch awareness, the sophistication ladder, the saturation/visits_source enums, or add demand_trend/bet_brief (later tasks own those).
  </action>
  <verify>
    <automated>! grep -q 'competitive_axis' /home/kyu3/PMF/prompts/step1-light-pass.md && ! grep -q 'COMPETITIVE_AXIS_ENUM' /home/kyu3/PMF/prompts/step1-light-pass.md && grep -q '"bet_type"' /home/kyu3/PMF/prompts/step1-light-pass.md && grep -q '"bet_type_basis"' /home/kyu3/PMF/prompts/step1-light-pass.md && grep -q '"bet_types"' /home/kyu3/PMF/prompts/step1-light-pass.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'competitive_axis' prompts/step1-light-pass.md` returns 0 (every occurrence replaced)
    - `grep -c 'COMPETITIVE_AXIS_ENUM' prompts/step1-light-pass.md` returns 0 (the enum line is deleted from the closed-enums block)
    - `grep '"bet_type"' prompts/step1-light-pass.md` and `grep '"bet_type_basis"' prompts/step1-light-pass.md` both match inside the per_brand schema block (`grep -A10 '"per_brand"' prompts/step1-light-pass.md | grep -q bet_type`)
    - `grep '"bet_types"' prompts/step1-light-pass.md` matches (the canonical bet_types[] list exists in the space-map.json schema, alongside transformations/niches/angles)
    - The AGENT 3 prompt instructs naming bet_type per brand OPEN, then clustering into canonical bet_types[] (`awk '/## AGENT 3/,/## OPEN DECISIONS/' prompts/step1-light-pass.md | grep -qi 'bet_type' && awk '/## AGENT 3/,/## OPEN DECISIONS/' prompts/step1-light-pass.md | grep -qi 'open'`)
    - The Dumper no-classify guard now names bet_type, not competitive_axis (`awk '/## AGENT 2/,/## AGENT 3/' prompts/step1-light-pass.md | grep -q bet_type`)
    - The CLASSIFIER hook spec rejects null/empty bet_type + missing basis + untraceable variants and is NOT enum-checked (`grep -i 'reject any per-brand bet_type' prompts/step1-light-pass.md` matches and there is no `off COMPETITIVE_AXIS_ENUM` token left)
    - bet_type does NOT appear in the saturation or combos schema blocks (it is per-brand, not per-cell): `grep -A2 '"saturation":' prompts/step1-light-pass.md | grep -q bet_type` returns NO match
  </acceptance_criteria>
  <done>competitive_axis (closed enum) fully replaced by bet_type (OPEN) across the per_brand schema, schema prose, closed-enums block, hook spec, Dumper guard, and AGENT 3; COMPETITIVE_AXIS_ENUM removed; a canonical bet_types[] list added (same shape as transformations[]); the Classifier owns it, the Dumper is forbidden from naming it; hook check is traceability-only (D-14).</done>
</task>

<task type="auto">
  <name>Task 2: D-13 — add the <bet_brief> consumption block to the Finder + Classifier prompts</name>
  <files>prompts/step1-light-pass.md</files>
  <read_first>
    - prompts/step1-light-pass.md AGENT 1 — FINDER (~L205-250) and AGENT 3 — SPACE CLASSIFIER (~L334-424) — grep `## AGENT 1` and `## AGENT 3` for current start lines
    - .planning/phases/01-stage-m1-s1-light-pass/01-CONTEXT.md §D-13 (the full three-layer spec: A prose→agent / B PIPELINE INPUTS fenced block→script / C output schema→hook; what the brief states; the failure mode it kills) and the deferred §6 note (the brief is hand-filled from prompts/_templates/pre-research-plan.template.md; worked example runs/arduview/pre-research-plan.md)
    - CLAUDE.md (PMF) — the brief layers must be respected: A prose→agent, B PIPELINE INPUTS→script tolerant-parse, C schema→hook; hooks NEVER touch layers A/B
  </read_first>
  <action>
    Add a `<bet_brief>` block to BOTH the Finder (AGENT 1) prompt and the Space Classifier (AGENT 3) prompt. The brief is injected VERBATIM as prose context at run time; it has NO schema and is NEVER hook-validated. Insert the block near the top of each agent's fenced prompt (after the role line, before the DO/RULES), wording as follows (adapt the agent-specific line but keep the three-layer statement identical):

    For AGENT 1 — FINDER, insert:
    ```
    <bet_brief>
    The operator authors a per-run bet brief (hand-filled from prompts/_templates/pre-research-plan.template.md;
    worked example runs/arduview/pre-research-plan.md). It is injected here VERBATIM as PROSE context — it has
    no schema and is NEVER hook-validated. Read it as judgment context, never as a field contract. Three strict layers:
      (A) the prose brief → your judgment context (this block);
      (B) a fenced `PIPELINE INPUTS` block inside the brief (flat lists: LP-hunt terms, comparable-bet seed
          brands, trend-source toggle) → read by SCRIPTS with a tolerant parse, NOT by you;
      (C) the output schema (brands.json) → enforced by hooks. Hooks only ever touch layer C.
    The brief states: the BET = a differentiator × a niche × an OPEN transformation slot — the operator does
    NOT supply the transformation; competitors REVEAL it as OUTPUT. It also pins operator definitions of the
    interpretation-heavy terms (no stage re-interprets them) and lists the named comparable-bet seed brands +
    the territory set. Use the brief's territories + comparable-bet seeds to drive your wide-net search (see KEEP/NET rules).
    A messy or rich brief may degrade quality but must NEVER hard-fail the run.
    </bet_brief>
    ```

    For AGENT 3 — SPACE CLASSIFIER, insert the same block but with the closing application line changed to:
    `Use the brief's pinned definitions when you name canonical transformations/niches/angles/bet_types, and report, per structurally-similar competitor, the transformation they attach, the mechanism they actually LEAD with, whether the bet won durably, and which niche.`

    Do NOT add any schema field, enum, or hook for the bet_brief — it is prose only (layer A). Do NOT wire fetch.js here (that is 01-02 / layer B). Keep the brief OUT of the dump.json / space-map.json schema entirely.
  </action>
  <verify>
    <automated>grep -q '<bet_brief>' /home/kyu3/PMF/prompts/step1-light-pass.md && test "$(grep -c '<bet_brief>' /home/kyu3/PMF/prompts/step1-light-pass.md)" -ge 2 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '<bet_brief>' prompts/step1-light-pass.md` returns ≥2 (one in the Finder prompt, one in the Classifier prompt)
    - A `<bet_brief>` block sits inside AGENT 1 — FINDER (`awk '/## AGENT 1 — FINDER/,/## AGENT 1.5/' prompts/step1-light-pass.md | grep -q '<bet_brief>'`)
    - A `<bet_brief>` block sits inside AGENT 3 — SPACE CLASSIFIER (`awk '/## AGENT 3/,/## OPEN DECISIONS/' prompts/step1-light-pass.md | grep -q '<bet_brief>'`)
    - The block names the PIPELINE INPUTS layer and states it is script-read, not agent-read (`grep -q 'PIPELINE INPUTS' prompts/step1-light-pass.md` and `grep -qi 'hooks only ever touch layer C' prompts/step1-light-pass.md`)
    - The block states the OPEN transformation slot — operator does NOT supply the transformation (`grep -qi 'OPEN transformation slot' prompts/step1-light-pass.md`)
    - No bet_brief field appears in any JSON schema block (`grep -A20 'dump.json' prompts/step1-light-pass.md | grep -qi 'bet_brief'` returns NO match; the brief is prose only, never schema)
  </acceptance_criteria>
  <done>Both the Finder and Classifier prompts carry a <bet_brief> prose block stating the three layers (A prose→agent, B PIPELINE INPUTS→script, C schema→hook; hooks only touch C), the bet definition with an OPEN transformation slot, and the per-competitor report contract — with no schema or hook added for the brief.</done>
</task>

<task type="auto">
  <name>Task 3: D-15 (field half) — add per-brand demand_trend { shape, window, source, basis } + closed shape enum + Classifier wiring</name>
  <files>prompts/step1-light-pass.md</files>
  <read_first>
    - prompts/step1-light-pass.md — the brands.json schema (esp. the per-brand revenue_est block ~L49-57, the model for a per-brand script-populated field), the space-map.json per_brand[] block (~L134-140), the closed-enums block (~L158-166), the CLASSIFIER hook spec (~L191-195), and AGENT 3 step 4 (~L366) — grep `revenue_est`, `"per_brand"`, `Closed enums`
    - .planning/phases/01-stage-m1-s1-light-pass/01-CONTEXT.md §D-15 (the field: per-brand demand_trend { shape, window, source, basis }; shape CLOSED enum steady|rising|parabolic-spike|declining|unknown, unknown = escape valve; the SOURCE half — Google Trends — is 01-02's job, NOT here) and §D-14 (demand_trend.shape is an explicitly LISTED closed enum)
    - 01-02-PLAN.md (the sibling plan) — confirm where fetch.js writes the demand_trend record so the schema OWNERSHIP matches: fetch.js populates the per-brand demand_trend in brands.json (script-owned, same place revenue_est sits); the Classifier reads it from brands.json and carries it into space-map.json per_brand[]
  </read_first>
  <action>
    Add a per-brand `demand_trend` field. SCHEMA OWNERSHIP (must be identical across 01-01/01-02/01-04): `demand_trend` is a per-brand record. `fetch.js` (01-02, Google Trends source) writes it into the per-brand row in `brands.json` (script-owned, exactly where `revenue_est` already sits — a `_note` flags it as script-populated). The Classifier then reads each brand's `demand_trend` from `brands.json` and carries it into `space-map.json` `per_brand[]` so the Phase 2 skill reads it off the one per-brand record. State this ownership explicitly in the schema comment.

    (a) brands.json SCHEMA — in the per-brand object (after the `revenue_est { ... }` block, ~L57), add a `demand_trend` block mirroring the revenue_est `_note` pattern (script-populated, agents never write it):
    ```
        "demand_trend": {
          "_note": "populated by fetch.js (Google Trends ~5yr per brand/category term), NOT an agent",
          "shape": "steady | rising | parabolic-spike | declining | unknown",
          "window": "the trend window measured, e.g. '2021-2026 (5yr)' | null",
          "source": "google-trends | null",
          "basis": "one-line read of the curve the shape is called off — e.g. 'sharp 2023 spike then -70% by 2025' | null"
        }
    ```

    (b) space-map.json per_brand[] — add the same `demand_trend` field to each per_brand row (the Classifier carries it from brands.json), placed adjacent to bet_type:
    ```
          "demand_trend": { "shape": "one of DEMAND_TREND_SHAPE_ENUM", "window": "string|null", "source": "google-trends|null", "basis": "string|null" },
    ```

    (c) CLOSED-ENUMS BLOCK — add a new enum line (demand_trend.shape IS a closed enum per D-14):
    `DEMAND_TREND_SHAPE_ENUM: steady | rising | parabolic-spike | declining | unknown   (unknown = escape valve when no source resolves; off-list = hard reject)`

    (d) AGENT 3 — SPACE CLASSIFIER — in step 4 (the per-brand listing), add an instruction: carry each brand's `demand_trend` from its brands.json record into per_brand[]; do NOT recompute it (it is a fetch.js Google-Trends read, not your judgment); if a brand's `demand_trend.shape` is `unknown`, list it `unknown` — never guess a shape. Add a one-line note that demand_trend is the load-bearing fad-death / parabolic-spike durability signal the Phase 2 Gate-1 kill reads.

    Do NOT add the Google Trends FETCH logic here (that is 01-02). Do NOT add the demand_trend-missing hook reject here (that is 01-04). This task is the FIELD + Classifier wiring only.
  </action>
  <verify>
    <automated>grep -q '"demand_trend"' /home/kyu3/PMF/prompts/step1-light-pass.md && grep -q 'DEMAND_TREND_SHAPE_ENUM: steady | rising | parabolic-spike | declining | unknown' /home/kyu3/PMF/prompts/step1-light-pass.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'DEMAND_TREND_SHAPE_ENUM: steady | rising | parabolic-spike | declining | unknown' prompts/step1-light-pass.md` matches (the closed shape enum is present, verbatim, all five values incl. unknown)
    - `"demand_trend"` appears in BOTH the brands.json per-brand block and the space-map.json per_brand block (`grep -c '"demand_trend"' prompts/step1-light-pass.md` returns ≥2)
    - The brands.json demand_trend block names fetch.js / Google Trends as the populator and lists shape/window/source/basis (`grep -A6 '"demand_trend"' prompts/step1-light-pass.md | grep -qi 'google' && grep -A6 '"demand_trend"' prompts/step1-light-pass.md | grep -q 'parabolic-spike'`)
    - The AGENT 3 prompt instructs carrying demand_trend from brands.json without recomputing (`awk '/## AGENT 3/,/## OPEN DECISIONS/' prompts/step1-light-pass.md | grep -qi 'demand_trend'`)
    - The schema comment states ownership: fetch.js writes it to brands.json, the Classifier carries it to space-map.json (`grep -qi 'populated by fetch.js' prompts/step1-light-pass.md`)
  </acceptance_criteria>
  <done>A per-brand demand_trend { shape, window, source, basis } exists in both brands.json (fetch.js-populated, _note flagged) and space-map.json per_brand[] (Classifier-carried); shape is on the new closed DEMAND_TREND_SHAPE_ENUM; AGENT 3 carries it without recomputing; ownership matches 01-02/01-04.</done>
</task>

<task type="auto">
  <name>Task 4: D-17 wide-net Finder + D-18 multi-domain claim-typing examples + feature-trap + D-19 provenance note & visible anti-fluke floor</name>
  <files>prompts/step1-light-pass.md</files>
  <read_first>
    - prompts/step1-light-pass.md AGENT 1 — FINDER KEEP BAR / RELEVANCE-DROP (~L218-232), AGENT 3 step 1b claim-typing worked examples (~L349-360) + the RULES worked-examples bullet (~L418-420), the space-map.json schema block (~L113-156), and the OPEN DECISIONS block (~L428-437) — grep `KEEP BAR`, `LAYER DISCIPLINE`, `Worked examples`
    - .planning/phases/01-stage-m1-s1-light-pass/01-CONTEXT.md §D-17 (wide net: "similar" = a product a buyer would cross-shop OR a brand making the same structural bet; spanning bet-brief territories + named comparable-bet seeds; never spec-match; an empty comparable pool reads identically to a failed bet), §D-18 (claim-typing examples must span a RANGE of domains, each typed across all four claim_type values + ≥1 feature-vs-claim trap), §D-19 (provenance note verbatim + visible anti-fluke floor: 2+ brands at scale, 7+ day ad longevity)
  </read_first>
  <action>
    Three judgment/text edits (no new schema field beyond the D-19 provenance/floor surfacing).

    (a) D-17 WIDE-NET FINDER — in AGENT 1 — FINDER, add a NET rule near the KEEP BAR (~L225-232). Insert:
    ```
    NET (what counts as "similar" — wide by substitutability AND bet-similarity, never by spec match):
      A brand belongs if EITHER is true:
        - SUBSTITUTABILITY — a buyer choosing this product would also cross-shop it, OR
        - BET-SIMILARITY — it makes the SAME structural bet (see the bet brief), even in a different form/spec.
      Span the FULL set of bet-brief territories + the named comparable-bet seed brands from the brief.
      Do NOT filter by spec match. Rationale: a bet validates only if structurally-similar bets are in the
      pool — an EMPTY comparable-bet pool reads identically to a FAILED bet (a false-negative that silently
      kills a live opportunity). When in doubt on a bet-similar brand, keep it (still log a relevance line).
    ```
    Keep the existing RELEVANCE-DROP rule (slop still drops) — the NET rule WIDENS what is bet-similar, it does NOT license padding with junk. Make the two read consistently.

    (b) D-18 MULTI-DOMAIN CLAIM-TYPING EXAMPLES + FEATURE-TRAP — in AGENT 3 step 1b, REPLACE the single-domain worked examples (the focus/skincare-only set at ~L351-359) so the four claim_type values are each illustrated across a RANGE of domains likely in the pool (e.g. skincare, supplements, sleep/wellness, productivity hardware, fitness), AND include ≥1 explicit feature-vs-claim trap. Example shape to inline:
    ```
      WORKED EXAMPLES (span domains — do NOT anchor on one product's vocabulary):
        - direct    — "removes wrinkles" (skincare) · "fall asleep faster" (sleep) · "blocks distractions" (productivity).
        - enlarged  — "removes wrinkles in 14 days" (skincare) · "asleep in under 20 min" (sleep) · "2x deep-work hours" (productivity).
        - mechanism — "via retinol microspheres" (skincare) · "via magnesium glycinate" (supplement) · "via an amber distraction-free display" (hardware).
        - enhanced  — "the ONLY retinol clinically shown to…" (skincare) · "the only magnesium with 3 published RCTs" (supplement).
      FEATURE-vs-CLAIM TRAP (the inoculation): a striking FEATURE is NOT a claim and is NOT typed —
        "a 4.5mm titanium body / 16K-pressure stylus" is a FEATURE/mechanism shown as spec, NOT promoted to a
        claim — sitting beside a real outcome-claim "write for 40 days on one charge" (that is the typed claim).
        Miscounting a headline feature as a claim inflates the stage/saturation read — do NOT do it.
    ```
    Also update the AGENT 3 RULES worked-examples bullet (~L418-420) to reference these multi-domain examples rather than only the e-ink/paper-feel ones (keep the paper-feel example as one entry, add the cross-domain framing). No schema change.

    (c) D-19 PROVENANCE NOTE + VISIBLE ANTI-FLUKE FLOOR:
      - Add a one-line provenance note to the space-map.json schema (place it in the schema prose under the space-map.json block, ~L144): `"_provenance": "Canonical transformations are claim-categories competitors ASSERT, not validated customer transformations — true transformation is a later VOC/review-mine finding."` (add it both as a schema field comment in the JSON block AND as a one-line prose note so it survives).
      - Make the anti-fluke floor VISIBLE in the output: add to each `combos[]` entry (or the per-cell saturation read) a visible `anti_fluke` surfacing — at minimum a schema field/comment stating the floor and where it shows: `"anti_fluke": { "brands_at_scale": "count of brands at scale in this cell (floor: 2+)", "qualifying_creatives": "count of creatives with run_length_days >= 7 (floor: 7+ days to qualify a creative)" }`. Enforcement stays decision-time (Phase 2 / D-09 — Phase 1 never hard-gates); this task only makes the floor VISIBLE in the JSON rather than only reconstructable. Add a one-line prose note that the floor is surfaced-not-enforced (D-09).

    Do NOT regress the already-landed edits. Do NOT touch bet_type, demand_trend, or the bet_brief (other tasks own those).
  </action>
  <verify>
    <automated>grep -qi 'an EMPTY comparable-bet pool reads identically' /home/kyu3/PMF/prompts/step1-light-pass.md && grep -qi 'FEATURE-vs-CLAIM TRAP' /home/kyu3/PMF/prompts/step1-light-pass.md && grep -q 'Canonical transformations are claim-categories competitors ASSERT' /home/kyu3/PMF/prompts/step1-light-pass.md && grep -qi 'anti_fluke' /home/kyu3/PMF/prompts/step1-light-pass.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The Finder carries the NET rule with substitutability OR bet-similarity (`awk '/## AGENT 1 — FINDER/,/## AGENT 1.5/' prompts/step1-light-pass.md | grep -qi 'bet-similarity'` and `grep -qi 'an EMPTY comparable-bet pool reads identically' prompts/step1-light-pass.md`)
    - The existing RELEVANCE-DROP rule still survives (slop still drops): `grep -q 'RELEVANCE-DROP' prompts/step1-light-pass.md`
    - The claim-typing examples span multiple domains AND include a feature-trap (`grep -qi 'FEATURE-vs-CLAIM TRAP' prompts/step1-light-pass.md` and the four claim_type lines each show ≥2 domains — `awk '/WORKED EXAMPLES/,/FEATURE-vs-CLAIM TRAP/' prompts/step1-light-pass.md | grep -c '·'` returns ≥4)
    - The provenance note is present verbatim (`grep -q 'Canonical transformations are claim-categories competitors ASSERT, not validated customer transformations' prompts/step1-light-pass.md`)
    - The anti-fluke floor is visible in the schema (`grep -qi 'anti_fluke' prompts/step1-light-pass.md` and `grep -qi '7' prompts/step1-light-pass.md | : ; grep -qi 'run_length_days >= 7\|7+ days' prompts/step1-light-pass.md`) and a prose note states it is surfaced-not-enforced (D-09): `grep -qi 'surfaced.*not.*enforced\|never hard-gate' prompts/step1-light-pass.md`
  </acceptance_criteria>
  <done>The Finder casts a wide net by substitutability AND bet-similarity with the empty-pool false-negative rationale (D-17); the claim-typing examples span a range of domains with a feature-vs-claim trap (D-18); space-map.json carries the verbatim provenance note and a VISIBLE anti-fluke floor (2+ brands at scale; 7+ day ad longevity) surfaced-not-enforced (D-19) — all while RELEVANCE-DROP and the already-landed edits stand un-regressed.</done>
</task>

<task type="auto">
  <name>Task 5: Idempotency guard — verify the already-landed edits (D-05, D-03, D-08, D-11) still stand</name>
  <files>prompts/step1-light-pass.md</files>
  <read_first>
    - 01-01-SUMMARY.md (records the already-landed edits: awareness stripped D-05, D-03 ladder inlined, saturation keyed per cell D-08, visits_source = semrush-api D-11)
    - prompts/step1-light-pass.md (the edited file after Tasks 1-4)
  </read_first>
  <action>
    This is a NON-MUTATING verification task — confirm Tasks 1-4 did not regress the prior round's landed edits. Run the greps below. If ANY fails, the editing tasks regressed a prior edit — fix the regression in place (restore the prior edit) and re-run. Do NOT add any new content; this task only guards idempotency of a re-execution.
      - D-05: `grep -c -i 'awareness' prompts/step1-light-pass.md` returns 1 (only the OPEN-DECISIONS rationale line survives; no schema/enum/hook/Dumper awareness)
      - D-03: `grep -q 'SOPHISTICATION (per combo cell = transformation' prompts/step1-light-pass.md` and `grep -q 'stage = the highest claim_type tier that 2' prompts/step1-light-pass.md`
      - D-08: the saturation schema example carries a niche key (`grep -Eq '"saturation": \[ \{ "transformation": "[^"]+", "niche":' prompts/step1-light-pass.md`)
      - D-11: `grep -q 'semrush-api' prompts/step1-light-pass.md` and `! grep -q 'similarweb-api' prompts/step1-light-pass.md`
  </action>
  <verify>
    <automated>test "$(grep -c -i 'awareness' /home/kyu3/PMF/prompts/step1-light-pass.md)" = "1" && grep -q 'SOPHISTICATION (per combo cell = transformation' /home/kyu3/PMF/prompts/step1-light-pass.md && grep -q 'stage = the highest claim_type tier that 2' /home/kyu3/PMF/prompts/step1-light-pass.md && grep -Eq '"saturation": \[ \{ "transformation": "[^"]+", "niche":' /home/kyu3/PMF/prompts/step1-light-pass.md && grep -q 'semrush-api' /home/kyu3/PMF/prompts/step1-light-pass.md && ! grep -q 'similarweb-api' /home/kyu3/PMF/prompts/step1-light-pass.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - D-05: `grep -c -i 'awareness'` returns exactly 1
    - D-03: the SOPHISTICATION block header and the mechanical-rule line both still match
    - D-08: the saturation schema example still carries the per-cell niche key
    - D-11: semrush-api present, similarweb-api absent
    - (If any failed, the regression was fixed in place and the greps now pass.)
  </acceptance_criteria>
  <done>All four already-landed edits (D-05/D-03/D-08/D-11) are confirmed un-regressed after the revision deltas — a re-execution of this plan is idempotent against the prior round's work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — prompt-text edit only) | This plan edits a markdown prompt/schema file. No code, no fetched data, no credentials, no execution surface. The bet_brief is prose injected at run time (layer A), never executed; its PIPELINE INPUTS (layer B) are script-parsed in 01-02, not here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | prompts/step1-light-pass.md | accept | Prompt/schema-text edit under git; any bad edit is caught by the acceptance-criteria greps and reviewable in diff. No runtime execution. |
| T-01-02 | Information disclosure | DR-marketing KB | mitigate | D-02 enforced: no KB decision-procedure text inlined into agent prompts; the bet_brief is operator prose injected at run time, not KB. |
| T-01-06 | Tampering (prose-as-contract) | the <bet_brief> block | mitigate | D-13 enforced: the brief is layer-A prose only — NO schema, NO hook touches it; hooks only ever validate layer C (output JSON). A messy brief degrades quality, never hard-fails or injects into the schema. Acceptance criterion asserts no bet_brief field exists in any JSON schema block. |
</threat_model>

<verification>
- competitive_axis (closed enum) fully replaced by bet_type (OPEN); COMPETITIVE_AXIS_ENUM removed; canonical bet_types[] list added; Classifier owns it, Dumper forbidden (Task 1).
- <bet_brief> prose block in both Finder + Classifier; three layers stated; OPEN transformation slot; no schema/hook for the brief (Task 2).
- per-brand demand_trend { shape, window, source, basis } in brands.json (fetch.js-owned) + space-map.json per_brand[] (Classifier-carried); DEMAND_TREND_SHAPE_ENUM closed (Task 3).
- Finder wide-net (substitutability OR bet-similarity, empty-pool false-negative); multi-domain claim examples + feature-trap; provenance note + visible anti-fluke floor (Task 4).
- D-05 / D-03 / D-08 / D-11 prior edits un-regressed (Task 5).
- File still reads as one runnable prompt.
</verification>

<success_criteria>
`prompts/step1-light-pass.md` now: carries an OPEN per-brand `bet_type` + `bet_type_basis` and a canonical `bet_types[]` list with NO `competitive_axis`/`COMPETITIVE_AXIS_ENUM` token (amend-D-12); injects a `<bet_brief>` prose block into the Finder + Classifier with the three layers and the OPEN transformation slot (D-13); carries a per-brand `demand_trend { shape, window, source, basis }` with a closed shape enum, owned by fetch.js in brands.json and carried by the Classifier into space-map.json (D-15 field half); instructs a wide-net Finder by substitutability AND bet-similarity (D-17); spans claim-typing examples across domains with a feature-trap (D-18); carries the verbatim provenance note + a visible anti-fluke floor (D-19) — with the prior D-05/D-03/D-08/D-11 edits intact. Schema field names + ownership match 01-02 (demand_trend source) and 01-04 (bet_type + demand_trend enforcement).
</success_criteria>

<output>
After completion, create `.planning/phases/01-stage-m1-s1-light-pass/01-01-SUMMARY.md`
</output>
