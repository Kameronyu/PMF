---
phase: 01-stage-m1-s1-light-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prompts/step1-light-pass.md
autonomous: true
requirements: [TOOL-01]
must_haves:
  truths:
    - "step1-light-pass.md is one runnable prompt — scaffold + Finder + Roster Verifier + Dumper + Space Classifier in a single readable flow"
    - "No awareness/awareness_basis field survives anywhere in the file (schema, enum list, hook spec, Dumper prompt)"
    - "The Space Classifier carries the verbatim D-03 sophistication ladder, derived per combo cell from the claim_type distribution"
    - "claim_type and the sophistication stage are the same ladder — a stage call is justified by the cell's claim_type distribution"
    - "The space-map.json saturation schema example is keyed per transformation × niche (carries a niche key per row), not pooled by transformation (D-08)"
    - "The brands.json visits_source enum lists semrush-api as the primary API source, not similarweb-api (D-11)"
    - "Every captured competitor carries a page-evidenced competitive_axis on the closed enum (function-capability-price | visual-statement | community-openness), assigned by the Classifier with a page-quoted competitive_axis_basis (D-12)"
  artifacts:
    - path: "prompts/step1-light-pass.md"
      provides: "the reconciled master light-pass prompt (collector half), awareness-stripped, D-03 inlined, schema reconciled to D-08/D-11, competitive_axis added per D-12"
      contains: "SOPHISTICATION (per combo cell = transformation"
  key_links:
    - from: "AGENT 3 — SPACE CLASSIFIER claim_type typing (step 1b)"
      to: "the inlined D-03 sophistication ladder"
      via: "stage = highest claim_type tier 2+ live in-geo brands deploy in the cell"
      pattern: "stage = the highest claim_type tier that 2"
    - from: "space-map.json saturation schema example"
      to: "the per-combo-cell saturation prose (~L144) + Classifier hook (reject saturation computed across cells)"
      via: "the schema example carries a niche key per saturation row so it cannot train pooled output"
      pattern: "saturation.*niche"
    - from: "AGENT 3 — SPACE CLASSIFIER per-brand call (step 4 / 4b)"
      to: "the space-map.json per_brand[] competitive_axis field + COMPETITIVE_AXIS_ENUM"
      via: "the Classifier assigns one primary page-read axis per brand with a page-quoted basis (D-12)"
      pattern: "competitive_axis"
---

<objective>
Reconcile `prompts/step1-light-pass.md` into one runnable prompt and apply the CONTEXT inlines + schema reconciliations: (1) strip the dropped `awareness` field (D-05) consistently in all four places it appears, (2) inline the verbatim D-03 sophistication-determination ladder into the Space Classifier, (3) fix the `saturation` schema example so it is keyed per combo cell (transformation × niche) instead of pooled (D-08), (4) fix the `visits_source` enum to list `semrush-api` instead of `similarweb-api` (D-11), and (5) add the per-competitor `competitive_axis` (closed enum) + `competitive_axis_basis` to the per_brand schema and wire the Classifier to assign it page-evidenced (D-12). This is a prompt/judgment-text + schema-text edit — NOT a code change. It owns no scripts.

Purpose: Success criterion 1 — the prompt must emit brands.json + dump.json + space-map.json with the pitch binding, per-combo-cell saturation, revenue_est, claim_type (direct/enlarged/mechanism/enhanced), the sophistication read derived FROM the typed claims (D-03/D-04), and a per-brand competitive_axis read off the page (D-12 — the Gate-2 transparency-axis input the Phase 2 market-selection skill reads). D-05 awareness is collected-but-not-useful noise (the InkLeaf failure) and must be removed cleanly. The master spec is the upstream contract every downstream script and the Phase 2 skill read — a pooled saturation example, a wrong visits_source enum, or a missing competitive_axis trains the wrong output even though Plan 03/04 emit/enforce correctly, so the spec text must match the locked decisions.
Output: an edited `prompts/step1-light-pass.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stage-m1-s1-light-pass/01-CONTEXT.md
@.planning/phases/01-stage-m1-s1-light-pass/01-PATTERNS.md

# This edit touches the sophistication / claim_type vocabulary — definitions.md is the single source of truth.
@definitions.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip awareness (D-05) from all four locations</name>
  <files>prompts/step1-light-pass.md</files>
  <read_first>
    - prompts/step1-light-pass.md (the file being edited — read whole)
    - .planning/phases/01-stage-m1-s1-light-pass/01-CONTEXT.md §D-05 (the rationale: per-creative awareness never rolls into space-map.json; a brand runs creatives across many awareness stages; per-creative tag is not decision-useful)
    - .planning/phases/01-stage-m1-s1-light-pass/01-PATTERNS.md §"prompts/step1-light-pass.md" edit #2 and §hooks "D-05 impact" (lists the four locations + the hook-rule removals)
  </read_first>
  <action>
    Remove `awareness` and `awareness_basis` everywhere they appear. Four locations (verify current line numbers with `grep -n -i awareness prompts/step1-light-pass.md` before editing — the file grew when the D-12 competitive_axis block was added, so the historical ~L90/L150/L180/L311 anchors have shifted):
    1. dump.json schema (the creative object): delete the two lines `"awareness": "one of AWARENESS_ENUM",` and `"awareness_basis": "observed | inferred",`. Leave `angle_raw`, `canonical_angle`, `angle_basis` intact (angle is NOT dropped — only awareness).
    2. Closed-enums block: delete the `AWARENESS_ENUM: ...` line. Then update the prose: remove `awareness` from the "Closed (dumper picks from the enum...)" list so it reads `channel · lane`.
    3. DUMPER hook spec: in the `DUMPER:` hook bullet, delete `Reject `awareness` off-enum.` and change `Reject if `angle_basis`/`awareness_basis` missing.` to `Reject if `angle_basis` missing.`
    4. Dumper prompt AGENT 2: delete the bullet `- awareness: pick ONE from {unaware, problem-aware, solution-aware, product-aware, most-aware}.` and edit the `angle_basis / awareness_basis` bullet down to `angle_basis: "observed" if the copy states it; "inferred" if you judged it.` The Dumper-prompt DEFINITIONS line currently says "The ONLY closed-set label you pick here is awareness." — since awareness is gone, the Dumper now picks NO closed-set label. Rewrite to: "You pick NO closed-set label here — you EXTRACT claims / mechanism / niche_raw / angle_raw / problem_um_raw verbatim." (KEEP the clause already present that the Dumper "never names a transformation, canonical niche, canonical angle, or competitive_axis — those are the classifier's calls" — that is the D-12 guard, do NOT remove it.) Also drop the "claimless cold ad's awareness is almost always inferred" clause and the `A creative always has a niche-target read + angle + awareness` line in the dump.json comment → drop `+ awareness`.
    Do NOT introduce a "static for now" or "v2" note — awareness is removed, not deferred-in-place. Per D-05 the real awareness read is a Phase 2 per-funnel deep-dive — note that in one line in the OPEN DECISIONS section so the rationale survives, e.g. "Awareness dropped from the light pass (D-05) — per-creative tag isn't decision-useful for the market-pick map; real awareness read is a Phase 2 per-funnel job."
  </action>
  <verify>
    <automated>test "$(grep -c -i 'awareness' /home/kyu3/PMF/prompts/step1-light-pass.md)" = "1"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c -i 'awareness' prompts/step1-light-pass.md` returns 1 (the single OPEN-DECISIONS rationale line is the only surviving mention; schema/enum/hook/Dumper-prompt mentions all gone)
    - `grep -i 'AWARENESS_ENUM' prompts/step1-light-pass.md` returns 0 matches
    - `grep -i 'awareness_basis' prompts/step1-light-pass.md` returns 0 matches
    - `grep 'angle_basis' prompts/step1-light-pass.md` still returns matches (angle survives — only awareness was removed)
    - `grep 'competitive_axis' prompts/step1-light-pass.md` still returns matches (the D-12 Dumper guard "you do not classify competitive_axis" survives the awareness edit)
  </acceptance_criteria>
  <done>Awareness/awareness_basis removed from schema, enum block, DUMPER hook spec, and Dumper prompt; angle + competitive_axis fields untouched; one rationale line retained in OPEN DECISIONS.</done>
</task>

<task type="auto">
  <name>Task 2: Inline the verbatim D-03 sophistication ladder into the Space Classifier</name>
  <files>prompts/step1-light-pass.md</files>
  <read_first>
    - prompts/step1-light-pass.md AGENT 3 — SPACE CLASSIFIER (esp. step 1b claim_type typing and step 4 / 4b per-brand calls) — grep `## AGENT 3` for the current start line
    - .planning/phases/01-stage-m1-s1-light-pass/01-CONTEXT.md §D-03 (the EXACT verbatim ladder text to inline, the fenced block) and §D-04 (claim_type and the stage call are the same ladder)
    - definitions.md §"Market sophistication" (~L213-262, L324 levers matrix, L374-375 the 5+ rule) — the inline traces here; confirm the inlined text is consistent with the source before committing
  </read_first>
  <action>
    Paste the D-03 ladder VERBATIM (copy it character-for-character from 01-CONTEXT.md, the fenced block beginning `SOPHISTICATION (per combo cell = transformation × niche)` and ending `...the claim(s) + brand count that set the stage.`) into AGENT 3 — SPACE CLASSIFIER, physically adjacent to step 1b (claim_type typing) / step 4 (per-brand sophistication call). Place it as a labeled block the prompt instructs the classifier to apply when it makes the per-brand "Stage 1-5 + evidence" call. (Note: step 4b is already the COMPETITIVE AXIS block from D-12 — place the sophistication ladder near the sophistication call, do not collide with or displace the competitive_axis block.)

    The inlined block must contain, verbatim, all five stage definitions, the "Mechanical rule: stage = the highest claim_type tier that 2+ LIVE in-geo brands deploy in the cell", the "5+ RULE" (5+ live brands making the same `direct` claim → discounted, cell is ≥Stage 2 and claim-saturated), and the "Evidence line MUST cite the claim(s) + brand count that set the stage."

    Wire D-04: edit the per-brand sophistication-call instruction (step 4) so the call is explicitly derived from the cell's `claim_type` distribution — change "a sophistication call (Stage 1-5 + evidence)" to "a sophistication call (Stage 1-5) read off the cell's claim_type distribution per the SOPHISTICATION block below — the evidence line cites the claim(s) + brand count that set the stage; never eyeball the stage." This is the same ladder as the claim_type field (D-04). Honor D-08: the ladder already counts only LIVE in-geo brands — confirm the inlined text keeps "LIVE, in-geography brands" wording so dead/region-only brands don't inflate the stage.

    Do NOT load any DR-marketing KB decision-procedure text (D-02) — only this definitional ladder goes in. Do NOT paraphrase or "simplify" the ladder; it is load-bearing and must be verbatim.
  </action>
  <verify>
    <automated>grep -q 'stage = the highest claim_type tier that 2' /home/kyu3/PMF/prompts/step1-light-pass.md && grep -q 'SOPHISTICATION (per combo cell = transformation' /home/kyu3/PMF/prompts/step1-light-pass.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E 'Stage [1-5] —' prompts/step1-light-pass.md | wc -l` returns 5 (all five stage lines present)
    - `grep 'SOPHISTICATION (per combo cell = transformation' prompts/step1-light-pass.md` matches (the block header is present, verbatim)
    - `grep '5+ RULE' prompts/step1-light-pass.md` matches
    - `grep 'stage = the highest claim_type tier that 2' prompts/step1-light-pass.md` matches (the mechanical rule, verbatim)
    - The block sits inside the AGENT 3 — SPACE CLASSIFIER section (appears after the line `## AGENT 3 — SPACE CLASSIFIER`), not in AGENT 1/2
    - No DR-marketing KB filenames (carl-weische, spencer-origins, mark-builds-brands) appear in the prompt (`grep -iE 'weische|spencer-origins|mark-builds' prompts/step1-light-pass.md` returns 0 — D-02 keeps KB out of runtime agent context)
  </acceptance_criteria>
  <done>The verbatim D-03 ladder lives in AGENT 3; the per-brand stage call is wired to read off the cell's claim_type distribution (D-04); no KB decision-procedure text leaked in (D-02); the D-12 competitive_axis block is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile two schema enums to the locked decisions — per-cell saturation (D-08) + semrush-api visits_source (D-11)</name>
  <files>prompts/step1-light-pass.md</files>
  <read_first>
    - prompts/step1-light-pass.md space-map.json schema block (esp. the `saturation` example and the per-combo-cell prose) and AGENT 3 step 5 ("Saturation: count brands per COMBO CELL") — grep `"saturation":` and `visits_source` for current lines
    - .planning/phases/01-stage-m1-s1-light-pass/01-CONTEXT.md §D-08 (live saturation counts are per combo cell = transformation × niche, never pooled — the anti-noise rule) and §D-11 (Semrush Trends API is PRIMARY → `visits_source:"semrush-api"`; SimilarWeb-manual is the fallback; SimilarWeb-API is NOT a source)
  </read_first>
  <action>
    Two surgical schema-text reconciliations. Both fix the WRITTEN CONTRACT in the master spec so it matches the locked decisions — the spec is the upstream source every downstream consumer (Plan 03 revenue-est.js, Plan 04 validate-classifier.js, the Phase 2 market-selection skill) reads. The downstream scripts already emit the correct shape; the spec must stop contradicting them.

    FIX A — saturation schema example is pooled, contradicts D-08 (BLOCKER). The `saturation` array example is keyed by transformation only:
    `  "saturation": [ { "transformation": "focus-productivity", "brand_count": 7, "saturated": true } ]`
    This is exactly the cross-cell pooling D-08 forbids — and it directly contradicts the prose ("Saturation = brand_count within a combo cell (transformation × niche), never pooled across cells") and the Classifier hook ("Reject saturation computed across cells (must be per combo cell)"). The schema EXAMPLE trains the model; a pooled example trains pooled output. Replace the line with a per-cell-keyed example that carries a `niche` key per row, e.g.:
    `  "saturation": [ { "transformation": "focus-productivity", "niche": "students", "brand_count": 3, "saturated": false } ]`
    (Use `brand_count: 3` / `saturated: false` so the example is internally consistent with the per-cell `combos` example which already shows `focus-productivity × students` at `brand_count: 3` — do not invent a contradicting count. The values are illustrative; the load-bearing change is the presence of the `niche` key per saturation row.) Then confirm the prose and the AGENT 3 step-5 instruction ("count brands per COMBO CELL (transformation × niche). NEVER pool across cells.") still read consistently with the now-fixed example — they already do; do not weaken them. If AGENT 3 step text anywhere says "count brands per transformation" without the niche pairing, fix it to "per combo cell (transformation × niche)".

    FIX B — visits_source enum names similarweb-api, but D-11 locks semrush-api (BLOCKER). The brands.json `inputs` schema has:
    `"visits_source": "similarweb-manual | similarweb-api | proxy | review-proxy | null",`
    D-11 makes Semrush Trends API the PRIMARY source (`visits_source:"semrush-api"`); SimilarWeb-manual is only the fallback and there is NO SimilarWeb-API source (auto-browser SimilarWeb scraping is ToS-banned). Replace `similarweb-api` with `semrush-api` so the enum reads:
    `"visits_source": "similarweb-manual | semrush-api | proxy | review-proxy | null",`
    Keep `similarweb-manual`, `proxy`, `review-proxy`, `null` exactly as-is — only the one token changes. Then check the revenue-est.js scaffold spec: if its prose mentions traffic sources, ensure it names "Semrush Trends API (`visits_source:"semrush-api"`)" as primary and "operator-pasted SimilarWeb free-tier (`visits_source:"similarweb-manual"`)" as fallback, consistent with D-11. Make the minimal edit needed for consistency; do NOT add multi-login wiring detail (D-11 leaves that to the implementer).

    Do NOT touch any other schema field, the awareness edits (Task 1), the sophistication ladder (Task 2), or the competitive_axis field (Task 4). This task is exactly two token-level enum/example reconciliations plus their consistency check.
  </action>
  <verify>
    <automated>grep -Eq '"saturation": \[ \{ "transformation": "[^"]+", "niche": "[^"]+"' /home/kyu3/PMF/prompts/step1-light-pass.md && grep -q 'semrush-api' /home/kyu3/PMF/prompts/step1-light-pass.md && ! grep -q 'similarweb-api' /home/kyu3/PMF/prompts/step1-light-pass.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The `saturation` array example contains a `"niche"` key inside the row (single-line form matches `"saturation": \[ \{ "transformation":.*"niche":`) — the example is keyed per transformation × niche, not pooled
    - `grep 'similarweb-api' prompts/step1-light-pass.md` returns 0 matches (the SimilarWeb-API token is gone)
    - `grep 'semrush-api' prompts/step1-light-pass.md` returns ≥1 match (the Semrush-API token is now in the visits_source enum)
    - `grep 'similarweb-manual' prompts/step1-light-pass.md` still returns matches (the manual fallback is preserved)
    - The per-combo-cell saturation prose ("never pooled across cells") and the Classifier hook ("Reject saturation computed across cells") are unchanged or strengthened, not weakened
  </acceptance_criteria>
  <done>The saturation schema example carries a per-cell `niche` key (D-08, no longer pooled, no longer contradicts its own prose/hook); the visits_source enum names `semrush-api` and drops `similarweb-api` (D-11); the manual fallback and all other enum tokens are untouched; revenue-est.js scaffold prose names Semrush as primary if it references sources.</done>
</task>

<task type="auto">
  <name>Task 4: Add per-competitor competitive_axis (closed enum) + page-quoted basis, assigned by the Classifier (D-12)</name>
  <files>prompts/step1-light-pass.md</files>
  <read_first>
    - prompts/step1-light-pass.md space-map.json `per_brand[]` block (grep `"per_brand"`), the closed-enums block (grep `COMPETITIVE_AXIS_ENUM`), the CLASSIFIER hook spec (grep `competitive_axis` in the Hooks section), and AGENT 3 step 4 / step 4b (grep `## AGENT 3` then `COMPETITIVE AXIS`)
    - .planning/phases/01-stage-m1-s1-light-pass/01-CONTEXT.md §D-12 (the full spec: closed enum verbatim, page-readable rule, basis-required, populated for every brand regardless of live status, Classifier-assigned NOT Dumper, hook reject rules, debug-pass enum-extension note)
    - .planning/phases/01-stage-m1-s1-light-pass/01-PATTERNS.md §"prompts/step1-light-pass.md" edit list (competitive_axis is item 6) and the closed-enum block
    - definitions.md — confirm there is NO pre-existing "axis"/"positioning" term competitive_axis would collide with (it is a new per-brand descriptor; record the naming choice in the SUMMARY)
  </read_first>
  <action>
    Add the per-competitor `competitive_axis` (closed enum) + `competitive_axis_basis` per D-12. This is the Gate-2 transparency-axis input the Phase 2 market-selection skill reads. Place it in the per_brand schema (the per-brand, Classifier-written structure — one-writer-per-file is respected because the Classifier solely owns space-map.json; the Dumper never touches it). Concretely:

    (a) SCHEMA — in the `space-map.json` `per_brand[]` block, add two fields per brand row:
    `"competitive_axis": "function-capability-price | visual-statement | community-openness",`
    `"competitive_axis_basis": "page-quoted signal the axis call is read off — verbatim/cited, not eyeballed",`
    Add a definition comment under the space-map.json schema block listing the three enum values VERBATIM with their one-line glosses (function-capability-price = competes on what it does / specs / features / cost; visual-statement = competes as a visual showpiece / object-as-statement / aesthetic identity; community-openness = competes on community, openness, transparency), and stating: populated for EVERY captured brand (live/dead/region-only — it is a per-brand descriptor, NOT a live-saturation count, so D-08 live-only exclusion does not apply); Classifier-assigned; basis must quote/cite the page signal.

    (b) CLOSED-ENUMS BLOCK — add `COMPETITIVE_AXIS_ENUM: function-capability-price | visual-statement | community-openness` to the closed-enums block, and add competitive_axis to the "Classifier-assigned, hook-rejected off-list" prose line (alongside claim_type) noting "one primary page-read axis per brand, with a page-quoted basis — see AGENT 3".

    (c) CLASSIFIER (AGENT 3) PROMPT — wire the Classifier to assign competitive_axis per brand: it picks ONE primary axis from the enum, read OFF the brand's own positioning/page (page-readable — decided from how the brand presents itself, WITHOUT needing the customer's true dream/desire), and records a `competitive_axis_basis` that QUOTES/CITES the page signal (same discipline as the sophistication evidence line: read it off the page, do not eyeball). State it is populated for every captured brand regardless of live status. Add the debug-pass note: if a brand's dominant axis fits none of the three values, flag it in `notes` — that is debug-pass signal to extend the enum, not a license to guess. Place this as a per-brand step adjacent to step 4 (the per-brand listing).

    (d) DUMPER GUARD — confirm the Dumper prompt's "you never name a transformation, canonical niche, canonical angle, or competitive_axis — those are the classifier's calls" guard is present (one-job-per-executor: the Dumper must NOT classify; competitive_axis is the Classifier's judgment read). If absent, add competitive_axis to the existing Dumper no-classify clause.

    Do NOT touch awareness (Task 1), the sophistication ladder (Task 2), or the saturation/visits_source enums (Task 3) in this task. competitive_axis is a NEW per-brand field, not a per-cell count — do NOT add it to the saturation/combo blocks.
  </action>
  <verify>
    <automated>grep -q 'COMPETITIVE_AXIS_ENUM: function-capability-price | visual-statement | community-openness' /home/kyu3/PMF/prompts/step1-light-pass.md && grep -q '"competitive_axis"' /home/kyu3/PMF/prompts/step1-light-pass.md && grep -q '"competitive_axis_basis"' /home/kyu3/PMF/prompts/step1-light-pass.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'COMPETITIVE_AXIS_ENUM: function-capability-price | visual-statement | community-openness' prompts/step1-light-pass.md` matches (the closed enum is present, verbatim, all three values)
    - `grep '"competitive_axis"' prompts/step1-light-pass.md` and `grep '"competitive_axis_basis"' prompts/step1-light-pass.md` both match inside the per_brand schema block (`grep -A8 '"per_brand"' prompts/step1-light-pass.md | grep -q competitive_axis`)
    - competitive_axis does NOT appear inside the saturation or combos schema blocks (it is per-brand, not per-cell): `grep -A2 '"saturation":' prompts/step1-light-pass.md | grep -q competitive_axis` returns NO match
    - The AGENT 3 — SPACE CLASSIFIER prompt instructs assigning competitive_axis per brand with a page-quoted basis (`awk '/## AGENT 3/,/## /' prompts/step1-light-pass.md | grep -qi 'competitive.axis'`)
    - The Classifier-assigned-off-list prose line names competitive_axis alongside claim_type (`grep -i 'Classifier-assigned' prompts/step1-light-pass.md | grep -q competitive_axis`)
    - The Dumper no-classify guard names competitive_axis (`awk '/## AGENT 2/,/## AGENT 3/' prompts/step1-light-pass.md | grep -q competitive_axis`) — one-job-per-executor: Dumper must not classify it
  </acceptance_criteria>
  <done>The per_brand schema carries competitive_axis (closed enum) + competitive_axis_basis; COMPETITIVE_AXIS_ENUM is in the closed-enums block; the Classifier (AGENT 3) assigns it page-evidenced per brand for every captured brand; the Dumper guard forbids it from classifying — one-writer-per-file (Classifier owns space-map.json) and one-job-per-executor both respected (D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — prompt-text edit only) | This plan edits a markdown prompt/schema file. No code, no fetched data, no credentials, no execution surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | prompts/step1-light-pass.md | accept | Prompt/schema-text edit under git; any bad edit is caught by the acceptance-criteria greps and reviewable in diff. No runtime execution. |
| T-01-02 | Information disclosure | DR-marketing KB | mitigate | D-02 enforced: no KB decision-procedure text inlined into agent prompts; acceptance criterion greps for KB filenames returning 0. |
</threat_model>

<verification>
- All four awareness locations stripped; angle + competitive_axis fields intact (Task 1).
- D-03 ladder inlined verbatim in AGENT 3; five stages + mechanical rule + 5+ rule present (Task 2).
- Per-brand stage call wired to claim_type distribution (D-04) (Task 2).
- saturation schema example keyed per transformation × niche, no longer pooled (D-08) (Task 3).
- visits_source enum names semrush-api, not similarweb-api (D-11) (Task 3).
- per_brand schema carries competitive_axis (closed enum) + page-quoted basis; COMPETITIVE_AXIS_ENUM in the enum block; Classifier assigns it; Dumper guard forbids it (D-12) (Task 4).
- File still reads as one runnable prompt (scaffold → 4 agents → open decisions).
</verification>

<success_criteria>
`prompts/step1-light-pass.md` is awareness-free (except one rationale line), carries the verbatim per-cell sophistication ladder in the Space Classifier, has its saturation schema example keyed per combo cell (matching its own prose + the Classifier hook + Plan 04's validator), names `semrush-api` as the primary traffic source in the visits_source enum (matching Plan 03's revenue-est.js emit), and carries a per-competitor `competitive_axis` (closed enum) + page-quoted `competitive_axis_basis` assigned by the Classifier (D-12 — the Gate-2 transparency-axis input Plan 04's validator enforces and the Phase 2 skill reads) — satisfying success criterion 1's "claim_type (direct/enlarged/mechanism/enhanced)" + the per-combo-cell saturation read, and keeping the master spec consistent with locked decisions D-08, D-11, and D-12.
</success_criteria>

<output>
After completion, create `.planning/phases/01-stage-m1-s1-light-pass/01-01-SUMMARY.md`
</output>
