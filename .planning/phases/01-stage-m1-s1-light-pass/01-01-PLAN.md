---
phase: 01-stage-m1-s1-light-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prompts/step1-light-pass.md
autonomous: true
requirements: [TOOL-01]
must_haves:
  truths:
    - "step1-light-pass.md is one runnable prompt — scaffold + Finder + Roster Verifier + Dumper + Space Classifier in a single readable flow"
    - "No awareness/awareness_basis field survives anywhere in the file (schema, enum list, hook spec, Dumper prompt)"
    - "The Space Classifier carries the verbatim D-03 sophistication ladder, derived per combo cell from the claim_type distribution"
    - "claim_type and the sophistication stage are the same ladder — a stage call is justified by the cell's claim_type distribution"
  artifacts:
    - path: "prompts/step1-light-pass.md"
      provides: "the reconciled master light-pass prompt (collector half), awareness-stripped, D-03 inlined"
      contains: "SOPHISTICATION (per combo cell = transformation"
  key_links:
    - from: "AGENT 3 — SPACE CLASSIFIER claim_type typing (step 1b)"
      to: "the inlined D-03 sophistication ladder"
      via: "stage = highest claim_type tier 2+ live in-geo brands deploy in the cell"
      pattern: "stage = the highest claim_type tier that 2"
---

<objective>
Reconcile `prompts/step1-light-pass.md` into one runnable prompt and apply the two surgical CONTEXT inlines: strip the dropped `awareness` field (D-05) consistently in all four places it appears, and inline the verbatim D-03 sophistication-determination ladder into the Space Classifier. This is a prompt/judgment-text edit — NOT a code change. It owns no scripts.

Purpose: Success criterion 1 — the prompt must emit brands.json + dump.json + space-map.json with the pitch binding, per-combo-cell saturation, revenue_est, and claim_type (direct/enlarged/mechanism/enhanced), with the sophistication read derived FROM the typed claims (D-03/D-04). D-05 awareness is collected-but-not-useful noise (the InkLeaf failure) and must be removed cleanly.
Output: an edited `prompts/step1-light-pass.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stage-m1-s1-light-pass/01-CONTEXT.md
@.planning/phases/01-stage-m1-s1-light-pass/01-PATTERNS.md

# This edit touches the sophistication / claim_type vocabulary — definitions.md is the single source of truth.
@definitions.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip awareness (D-05) from all four locations</name>
  <files>prompts/step1-light-pass.md</files>
  <read_first>
    - prompts/step1-light-pass.md (the file being edited — read whole)
    - .planning/phases/01-stage-m1-s1-light-pass/01-CONTEXT.md §D-05 (the rationale: per-creative awareness never rolls into space-map.json; a brand runs creatives across many awareness stages; per-creative tag is not decision-useful)
    - .planning/phases/01-stage-m1-s1-light-pass/01-PATTERNS.md §"prompts/step1-light-pass.md" edit #2 and §hooks "D-05 impact" (lists the four locations + the hook-rule removals)
  </read_first>
  <action>
    Remove `awareness` and `awareness_basis` everywhere they appear. The pattern-mapper confirmed exactly four locations:
    1. dump.json schema (~L90-91): delete the two lines `"awareness": "one of AWARENESS_ENUM",` and `"awareness_basis": "observed | inferred",` from the creative object. Leave `angle_raw`, `canonical_angle`, `angle_basis` intact (angle is NOT dropped — only awareness).
    2. Closed-enums block (~L150): delete the `AWARENESS_ENUM: unaware | problem-aware | solution-aware | product-aware | most-aware` line. Then update the prose at ~L154-156: remove `awareness` from the "Closed (dumper picks from the enum...)" list so it reads `awareness ·`→ gone, leaving `channel · lane`.
    3. DUMPER hook spec (~L180): in the `DUMPER:` hook bullet, delete `Reject `awareness` off-enum.` and change `Reject if `angle_basis`/`awareness_basis` missing.` to `Reject if `angle_basis` missing.`
    4. Dumper prompt AGENT 2 (~L311, L314-315): delete the bullet `- awareness: pick ONE from {unaware, problem-aware, solution-aware, product-aware, most-aware}.` and edit the `angle_basis / awareness_basis` bullet down to `angle_basis: "observed" if the copy states it; "inferred" if you judged it.` Also fix the line ~L320-321 "The ONLY closed-set label you pick here is awareness." → since awareness is gone, the Dumper now picks NO closed-set label (it only extracts open fields + picks channel/lane upstream is the Finder's job). Rewrite to: "You pick NO closed-set label here — you EXTRACT claims / mechanism / niche_raw / angle_raw / problem_um_raw verbatim." Also drop the "claimless cold ad's awareness is almost always inferred" clause and the `A creative always has a niche-target read + angle + awareness` line in the dump.json comment (~L72) → drop `+ awareness`.
    Do NOT introduce a "static for now" or "v2" note — awareness is removed, not deferred-in-place. Per D-05 the real awareness read is a Phase 2 per-funnel deep-dive — note that in one line in the OPEN DECISIONS section so the rationale survives, e.g. "Awareness dropped from the light pass (D-05) — per-creative tag isn't decision-useful for the market-pick map; real awareness read is a Phase 2 per-funnel job."
  </action>
  <verify>
    <automated>test "$(grep -c -i 'awareness' /home/kyu3/PMF/prompts/step1-light-pass.md)" = "1"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c -i 'awareness' prompts/step1-light-pass.md` returns 1 (the single OPEN-DECISIONS rationale line is the only surviving mention; schema/enum/hook/Dumper-prompt mentions all gone)
    - `grep -i 'AWARENESS_ENUM' prompts/step1-light-pass.md` returns 0 matches
    - `grep -i 'awareness_basis' prompts/step1-light-pass.md` returns 0 matches
    - `grep 'angle_basis' prompts/step1-light-pass.md` still returns matches (angle survives — only awareness was removed)
  </acceptance_criteria>
  <done>Awareness/awareness_basis removed from schema, enum block, DUMPER hook spec, and Dumper prompt; angle fields untouched; one rationale line retained in OPEN DECISIONS.</done>
</task>

<task type="auto">
  <name>Task 2: Inline the verbatim D-03 sophistication ladder into the Space Classifier</name>
  <files>prompts/step1-light-pass.md</files>
  <read_first>
    - prompts/step1-light-pass.md AGENT 3 — SPACE CLASSIFIER (~L325-370, esp. step 1b claim_type typing at ~L340-351 and step 4/5 at ~L356-358)
    - .planning/phases/01-stage-m1-s1-light-pass/01-CONTEXT.md §D-03 (the EXACT verbatim ladder text to inline, lines 34-61) and §D-04 (claim_type and the stage call are the same ladder)
    - definitions.md §"Market sophistication" (~L213-262, L324 levers matrix, L374-375 the 5+ rule) — the inline traces here; confirm the inlined text is consistent with the source before committing
  </read_first>
  <action>
    Paste the D-03 ladder VERBATIM (copy it character-for-character from 01-CONTEXT.md lines 34-61, the fenced block beginning `SOPHISTICATION (per combo cell = transformation × niche)` and ending `...the claim(s) + brand count that set the stage.`) into AGENT 3 — SPACE CLASSIFIER, physically adjacent to step 1b (claim_type typing) / step 4 (per-brand sophistication call). Place it as a labeled block the prompt instructs the classifier to apply when it makes the per-brand "Stage 1-5 + evidence" call.

    The inlined block must contain, verbatim, all five stage definitions, the "Mechanical rule: stage = the highest claim_type tier that 2+ LIVE in-geo brands deploy in the cell", the "5+ RULE" (5+ live brands making the same `direct` claim → discounted, cell is ≥Stage 2 and claim-saturated), and the "Evidence line MUST cite the claim(s) + brand count that set the stage."

    Wire D-04: edit the per-brand sophistication-call instruction (step 4, ~L357) so the call is explicitly derived from the cell's `claim_type` distribution — change "a sophistication call (Stage 1-5 + evidence)" to "a sophistication call (Stage 1-5) read off the cell's claim_type distribution per the SOPHISTICATION block below — the evidence line cites the claim(s) + brand count that set the stage; never eyeball the stage." This is the same ladder as the claim_type field (D-04). Honor D-08: the ladder already counts only LIVE in-geo brands — confirm the inlined text keeps "LIVE, in-geography brands" wording so dead/region-only brands don't inflate the stage.

    Do NOT load any DR-marketing KB decision-procedure text (D-02) — only this definitional ladder goes in. Do NOT paraphrase or "simplify" the ladder; it is load-bearing and must be verbatim.
  </action>
  <verify>
    <automated>grep -q 'stage = the highest claim_type tier that 2' /home/kyu3/PMF/prompts/step1-light-pass.md && grep -q 'SOPHISTICATION (per combo cell = transformation' /home/kyu3/PMF/prompts/step1-light-pass.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Stage 1 —' ... 'Stage 5 —'`: all five stage lines present (`grep -E 'Stage [1-5] —' prompts/step1-light-pass.md | wc -l` returns 5)
    - `grep 'SOPHISTICATION (per combo cell = transformation' prompts/step1-light-pass.md` matches (the block header is present, verbatim)
    - `grep '5+ RULE' prompts/step1-light-pass.md` matches
    - `grep 'stage = the highest claim_type tier that 2' prompts/step1-light-pass.md` matches (the mechanical rule, verbatim)
    - The block sits inside the AGENT 3 — SPACE CLASSIFIER section (appears after the line `## AGENT 3 — SPACE CLASSIFIER`), not in AGENT 1/2
    - No DR-marketing KB filenames (carl-weische, spencer-origins, mark-builds-brands) appear in the prompt (`grep -iE 'weische|spencer-origins|mark-builds' prompts/step1-light-pass.md` returns 0 — D-02 keeps KB out of runtime agent context)
  </acceptance_criteria>
  <done>The verbatim D-03 ladder lives in AGENT 3; the per-brand stage call is wired to read off the cell's claim_type distribution (D-04); no KB decision-procedure text leaked in (D-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — prompt-text edit only) | This plan edits a markdown prompt file. No code, no fetched data, no credentials, no execution surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | prompts/step1-light-pass.md | accept | Prompt-text edit under git; any bad edit is caught by the acceptance-criteria greps and reviewable in diff. No runtime execution. |
| T-01-02 | Information disclosure | DR-marketing KB | mitigate | D-02 enforced: no KB decision-procedure text inlined into agent prompts; acceptance criterion greps for KB filenames returning 0. |
</threat_model>

<verification>
- All four awareness locations stripped; angle fields intact.
- D-03 ladder inlined verbatim in AGENT 3; five stages + mechanical rule + 5+ rule present.
- Per-brand stage call wired to claim_type distribution (D-04).
- File still reads as one runnable prompt (scaffold → 4 agents → open decisions).
</verification>

<success_criteria>
`prompts/step1-light-pass.md` is awareness-free (except one rationale line), carries the verbatim per-cell sophistication ladder in the Space Classifier, and the sophistication call is derived from claim_type — satisfying success criterion 1's "claim_type (direct/enlarged/mechanism/enhanced)" + the per-combo-cell saturation read.
</success_criteria>

<output>
After completion, create `.planning/phases/01-stage-m1-s1-light-pass/01-01-SUMMARY.md`
</output>
