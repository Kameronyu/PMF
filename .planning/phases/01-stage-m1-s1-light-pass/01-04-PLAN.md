---
phase: 01-stage-m1-s1-light-pass
plan: 04
type: execute
wave: 2
depends_on: ["01-01", "01-03"]
files_modified:
  - tools/hooks/validate-classifier.js
autonomous: true
requirements: [TOOL-01, BET-01, TREND-01]
must_haves:
  truths:
    - "validate-classifier.js rejects a per_brand entry whose bet_type is null/empty, OR whose bet_type_basis is missing/empty, OR any canonical bet_type whose raw_variants do not trace to real per-brand reads (traceability-checked, same shape as transformation/niche/angle)"
    - "validate-classifier.js does NOT enum-check bet_type — there is no enum (D-14); 'off-enum' is never a rejection reason; COMPETITIVE_AXIS_ENUM and the competitive_axis reject rules are removed entirely"
    - "validate-classifier.js rejects a per_brand entry with demand_trend missing, and rejects a demand_trend.shape off the closed enum steady|rising|parabolic-spike|declining|unknown"
    - "The other three validators (validate-finder.js, validate-dumper.js, validate-revenue.js) and settings.json + route.js are NOT touched — they stand as built and are not regressed"
  artifacts:
    - path: "tools/hooks/validate-classifier.js"
      provides: "open bet_type traceability reject (replacing the competitive_axis enum reject) + demand_trend-missing / shape-off-enum reject"
      contains: "bet_type"
  key_links:
    - from: "tools/hooks/validate-classifier.js bet_type rule"
      to: "space-map.json per_brand[].bet_type + bet_type_basis + the canonical bet_types[] list"
      via: "reject null/empty bet_type, missing basis, and canonical bet_types whose variants don't trace to per-brand reads — never enum-check (D-14)"
      pattern: "bet_type"
    - from: "tools/hooks/validate-classifier.js demand_trend rule"
      to: "space-map.json per_brand[].demand_trend.shape"
      via: "reject demand_trend missing + shape off DEMAND_TREND_SHAPE_ENUM"
      pattern: "demand_trend"
---

<objective>
Apply the 2026-06-03 revision deltas to the ALREADY-BUILT `tools/hooks/validate-classifier.js` (built by the prior 01-04 run; see 01-04-SUMMARY.md — it currently enforces the SUPERSEDED closed `competitive_axis` enum). Two surgical edits to this ONE validator:
1. **amend-D-12** — replace the `competitive_axis` enum reject with the OPEN `bet_type` rule: reject a per_brand entry whose `bet_type` is null/empty, OR whose `bet_type_basis` is missing/empty, OR any canonical `bet_type` whose variants don't trace to real per-brand reads (traceability check, same shape as transformation/niche/angle). It must NOT reject "off-enum" — there is no enum (D-14). Remove `COMPETITIVE_AXIS_ENUM` checks.
2. **D-15** — reject a brand with `demand_trend` missing, and reject a `demand_trend.shape` off the closed enum `steady|rising|parabolic-spike|declining|unknown`.

The other three validators + settings.json + route.js are in the untouched-build-that-stands list — this plan does NOT touch them.

Purpose: success criterion 7 — bet_type is an OPEN classifier-named/clustered/evidence-traced field enforced by traceability (not enum), and the per-brand demand_trend the durability check depends on is enforced present + on-enum. The hook is the backstop that the 01-01 schema (open bet_type, demand_trend field) and the 01-02 source (Google Trends demand_trend) are actually honored at write time.
Output: an edited `tools/hooks/validate-classifier.js` (the other validators untouched).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stage-m1-s1-light-pass/01-CONTEXT.md
@.planning/phases/01-stage-m1-s1-light-pass/01-PATTERNS.md

@tools/hooks/validate-classifier.js
@prompts/step1-light-pass.md
@CLAUDE.md

<interfaces>
<!-- Runtime: Node v20.20.0, zero deps (fs/path only). Validator reads the written space-map.json,
     parses it as DATA, applies reject rules, exits 2 + stderr on violation, 0 silently on pass. -->

<!-- SCHEMA OWNERSHIP (must match 01-01 + 01-02):
     - bet_type / bet_type_basis: OPEN per-brand fields in space-map.json per_brand[]; a canonical
       bet_types[] list (same shape as transformations[]) also in space-map.json. NO enum (D-14) —
       checked for non-null + basis + traceability, NEVER for enum membership.
     - demand_trend { shape, window, source, basis }: per-brand; carried into space-map.json per_brand[]
       by the Classifier (fetch.js wrote it to brands.json). shape on a CLOSED enum. -->
DEMAND_TREND_SHAPE_ENUM: steady | rising | parabolic-spike | declining | unknown

<!-- This validator owns the file (space-map.json) that carries BOTH bet_type and the carried demand_trend,
     so BOTH new reject rules live in validate-classifier.js (matching the ownership chosen in 01-01). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: amend-D-12 — replace the competitive_axis enum reject with the OPEN bet_type traceability reject; remove COMPETITIVE_AXIS_ENUM</name>
  <files>tools/hooks/validate-classifier.js</files>
  <read_first>
    - tools/hooks/validate-classifier.js (the ALREADY-BUILT file — read whole; competitive_axis currently appears at the header comment ~L9-10, the `COMPETITIVE_AXIS_ENUM` const ~L32, and Rules 6-7 in the per_brand loop ~L141-155 — locate with `grep -n 'competitive_axis\|COMPETITIVE_AXIS_ENUM' tools/hooks/validate-classifier.js`)
    - .planning/phases/01-stage-m1-s1-light-pass/01-04-SUMMARY.md (records validate-classifier.js rejects competitive_axis off-enum + missing basis — that is the OLD closed-enum version this task SUPERSEDES)
    - .planning/phases/01-stage-m1-s1-light-pass/01-CONTEXT.md §D-12 AMENDED (the hook reject: bet_type null/empty OR basis missing/empty OR canonical bet_type variants don't trace; does NOT reject off-enum — there is no enum) and §D-14 (open categorization is traceability-checked, NEVER enum-checked)
    - 01-01-PLAN.md (the sibling — confirm the space-map.json shape the validator checks: per_brand[].bet_type + .bet_type_basis, and the canonical bet_types[] list with raw_variants tracing to per-brand reads)
  </read_first>
  <action>
    First locate: `grep -n 'competitive_axis\|COMPETITIVE_AXIS_ENUM' tools/hooks/validate-classifier.js`. As built, these sit at the header comment (~L9-10), the const `COMPETITIVE_AXIS_ENUM = new Set([...])` (~L32), and Rules 6-7 inside the per_brand loop (~L141-155).

    (a) DELETE the const (~L32): remove the entire line `const COMPETITIVE_AXIS_ENUM = new Set(['function-capability-price', 'visual-statement', 'community-openness']);`.

    (b) HEADER COMMENT (~L9-10) — replace the two lines:
      BEFORE:
        `//   6. D-12: per_brand[] competitive_axis off COMPETITIVE_AXIS_ENUM.`
        `//   7. D-12: missing or empty competitive_axis_basis when competitive_axis is set.`
      AFTER:
        `//   6. amend-D-12: per_brand[] bet_type null/empty, OR bet_type_basis missing/empty (OPEN field — traceability-checked, NEVER enum-checked: D-14).`
        `//   7. amend-D-12: any canonical bet_types[] entry whose raw_variants do not trace to real per-brand bet_type reads.`

    (c) REPLACE Rule 6 + Rule 7 in the per_brand loop (~L141-155 — the whole `if (brand.competitive_axis !== undefined ...)` block) with the OPEN bet_type rule. New logic (no enum membership test anywhere):
    ```
        // Rule 6 (amend-D-12): bet_type is an OPEN field — reject null/empty, NEVER enum-check (D-14).
        if (
          brand.bet_type === undefined ||
          brand.bet_type === null ||
          typeof brand.bet_type !== 'string' ||
          brand.bet_type.trim() === ''
        ) {
          violations.push(`REJECT: brand "${label}" bet_type is null/empty — every captured brand must carry a named bet_type (OPEN field, named in the space's own terms)`);
        } else {
          // Rule 7 (amend-D-12): bet_type_basis must be present + non-empty when bet_type is set.
          if (
            !brand.bet_type_basis ||
            typeof brand.bet_type_basis !== 'string' ||
            brand.bet_type_basis.trim() === ''
          ) {
            violations.push(`REJECT: brand "${label}" bet_type is set ("${brand.bet_type}") but bet_type_basis is missing or empty — bet_type call requires page-quoted evidence`);
          }
        }
    ```

    (d) ADD a canonical-bet_types traceability rule alongside the existing Rule 1 (the transformation/niche/angle raw_variants check, ~L52-82) — mirror it exactly for `data.bet_types`:
    ```
    const betTypes = data.bet_types || [];
    for (let i = 0; i < betTypes.length; i++) {
      const b = betTypes[i];
      const label = b.canonical || `bet_types[${i}]`;
      const variants = b.raw_variants;
      if (!Array.isArray(variants) || variants.length === 0) {
        violations.push(`REJECT: bet_type "${label}" has zero raw_variants — canonical bet_type must trace to real per-brand reads (traceability-checked, never enum-checked: D-14)`);
      }
    }
    ```

    There must be NO `bet_type` enum/Set membership check anywhere — bet_type is OPEN (D-14). Do NOT touch the other validators, settings.json, or route.js.
  </action>
  <verify>
    <automated>cd /home/kyu3/PMF && node -c tools/hooks/validate-classifier.js && ! grep -q 'competitive_axis' tools/hooks/validate-classifier.js && ! grep -q 'COMPETITIVE_AXIS_ENUM' tools/hooks/validate-classifier.js && grep -q 'bet_type' tools/hooks/validate-classifier.js && grep -q 'bet_type_basis' tools/hooks/validate-classifier.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node -c tools/hooks/validate-classifier.js` exits 0 (valid syntax after edit)
    - `grep -c 'competitive_axis' tools/hooks/validate-classifier.js` returns 0 and `grep -c 'COMPETITIVE_AXIS_ENUM' tools/hooks/validate-classifier.js` returns 0 (both fully removed)
    - The validator rejects null/empty bet_type and missing bet_type_basis (`grep -q 'bet_type is null/empty' tools/hooks/validate-classifier.js` and `grep -q 'bet_type_basis is missing' tools/hooks/validate-classifier.js`)
    - The validator has a canonical bet_types[] traceability rule mirroring the transformation/niche/angle raw_variants check (`grep -q 'data.bet_types\|betTypes' tools/hooks/validate-classifier.js` and `grep -qi 'raw_variants' tools/hooks/validate-classifier.js`)
    - NO enum membership check on bet_type exists (`grep -i 'bet_type' tools/hooks/validate-classifier.js | grep -qi 'new Set\|ENUM.has\|off.*ENUM'` returns NO match — bet_type is OPEN, traceability-only)
    - Functional smoke (bet_type): a space-map.json per_brand entry with `bet_type:null` → validate-classifier.js exits 2; a valid bet_type with empty bet_type_basis → exits 2; a valid bet_type + non-empty basis (and otherwise-valid file) → exits 0 on that rule; ANY non-empty bet_type string passes the enum check because there is no enum (e.g. `bet_type:"some-novel-bet-name"` is NOT rejected for being off-list).
    - The other three validators are untouched: `git diff --name-only` for this task lists only tools/hooks/validate-classifier.js
  </acceptance_criteria>
  <done>validate-classifier.js rejects null/empty bet_type, missing/empty bet_type_basis, and untraceable canonical bet_types[] variants — with NO enum check (D-14); COMPETITIVE_AXIS_ENUM and the competitive_axis rules are gone; the other validators stand untouched.</done>
</task>

<task type="auto">
  <name>Task 2: D-15 — reject demand_trend missing + demand_trend.shape off the closed enum</name>
  <files>tools/hooks/validate-classifier.js</files>
  <read_first>
    - tools/hooks/validate-classifier.js (the file post-Task-1 — the per_brand loop ~L137-165 is where the demand_trend rule joins the bet_type rule; the CLAIM_TYPE_ENUM Set ~L31 is the pattern to mirror for a new closed enum const)
    - .planning/phases/01-stage-m1-s1-light-pass/01-CONTEXT.md §D-15 (a hook rejecting the demand_trend field missing; shape closed enum steady|rising|parabolic-spike|declining|unknown) and §D-14 (demand_trend.shape IS an explicitly LISTED closed enum — so unlike bet_type, shape IS enum-checked)
    - 01-01-PLAN.md + 01-02-PLAN.md (confirm ownership: the Classifier carries demand_trend into space-map.json per_brand[]; THIS validator owns space-map.json so the demand_trend reject lives here)
  </read_first>
  <action>
    Add the demand_trend reject to validate-classifier.js (it owns space-map.json, which carries the Classifier-carried per_brand demand_trend — matching the 01-01 ownership choice).

    (a) Add a closed-enum const near the existing CLAIM_TYPE_ENUM (~L31), mirroring its Set pattern:
    `const DEMAND_TREND_SHAPE_ENUM = new Set(['steady', 'rising', 'parabolic-spike', 'declining', 'unknown']);`

    (b) Inside the per_brand loop (the same `for` loop carrying the bet_type rule from Task 1), add the demand_trend rule:
    ```
        // D-15: demand_trend must be present, and shape must be on the closed enum.
        const dt = brand.demand_trend;
        if (dt === undefined || dt === null || typeof dt !== 'object') {
          violations.push(`REJECT: brand "${label}" demand_trend is missing — every captured brand must carry a demand_trend (the fad-death/durability signal); shape "unknown" is allowed but the field must exist`);
        } else if (!DEMAND_TREND_SHAPE_ENUM.has(dt.shape)) {
          violations.push(`REJECT: brand "${label}" demand_trend.shape "${dt.shape}" off DEMAND_TREND_SHAPE_ENUM (steady|rising|parabolic-spike|declining|unknown)`);
        }
    ```
    Note the asymmetry vs bet_type: `demand_trend.shape` IS enum-checked (it is an explicitly listed closed enum, D-14), whereas `bet_type` is NOT (it is OPEN). `shape:"unknown"` is a LEGAL value (the escape valve) — do NOT reject it; only reject a MISSING demand_trend field or an off-enum shape.

    Do NOT touch the other validators, settings.json, or route.js. Keep `--help` working (exit 0).
  </action>
  <verify>
    <automated>cd /home/kyu3/PMF && node -c tools/hooks/validate-classifier.js && grep -q 'DEMAND_TREND_SHAPE_ENUM' tools/hooks/validate-classifier.js && grep -q 'demand_trend is missing' tools/hooks/validate-classifier.js && grep -q 'parabolic-spike' tools/hooks/validate-classifier.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node -c tools/hooks/validate-classifier.js` exits 0
    - A closed shape enum const exists with all five values (`grep -q "DEMAND_TREND_SHAPE_ENUM = new Set(\['steady', 'rising', 'parabolic-spike', 'declining', 'unknown'\])" tools/hooks/validate-classifier.js`)
    - The validator rejects a missing demand_trend (`grep -q 'demand_trend is missing' tools/hooks/validate-classifier.js`) and an off-enum shape (`grep -q 'off DEMAND_TREND_SHAPE_ENUM' tools/hooks/validate-classifier.js`)
    - `shape:"unknown"` is NOT rejected (it is in the enum Set — the escape valve is legal): the smoke below confirms
    - Functional smoke (demand_trend): a space-map.json per_brand entry with no demand_trend → exit 2; a demand_trend with `shape:"fad"` (off-enum) → exit 2; a demand_trend with `shape:"unknown"` → passes that rule (exit 0 if otherwise valid); a demand_trend with `shape:"parabolic-spike"` → passes that rule
    - The other three validators untouched: `git diff --name-only` lists only tools/hooks/validate-classifier.js
  </acceptance_criteria>
  <done>validate-classifier.js rejects a missing demand_trend and a demand_trend.shape off the closed DEMAND_TREND_SHAPE_ENUM, while allowing shape:"unknown" as the legal escape valve; the rule lives in the validator that owns space-map.json (matching 01-01 ownership); the other validators stand untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-written space-map.json → validate-classifier.js | The just-written JSON is read and parsed as DATA by the validator (JSON.parse + property reads + Set membership). No execution of contents. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Tampering / RCE | validate-classifier.js parsing space-map.json | mitigate | Validator treats input as DATA only — JSON.parse + property reads + Set.has, NO eval/Function/dynamic require on contents. The edits add only string/Set checks. |
| T-01-11 | Repudiation (silent bad output) | the bet_type / demand_trend gate | mitigate | This edit IS the mitigation: the hook rejects an unevidenced bet_type (no basis), an untraceable canonical bet_type, a missing demand_trend, and an off-enum shape — instead of trusting the Classifier prompt. Non-zero exit + stderr gives a readable rejection. |
| T-01-15 | Tampering (over-broad reject) | bet_type enum-check regression | mitigate | Acceptance criterion asserts NO enum membership check on bet_type (D-14) — an accidental enum check would wrongly reject valid open bet names; the grep guards against it. |
</threat_model>

<verification>
- validate-classifier.js rejects null/empty bet_type, missing basis, untraceable canonical bet_types[] — with NO enum check (amend-D-12 / D-14) (Task 1).
- COMPETITIVE_AXIS_ENUM + competitive_axis rules removed entirely (Task 1).
- validate-classifier.js rejects missing demand_trend + off-enum shape; allows shape:"unknown" (D-15) (Task 2).
- Functional smokes prove: bet_type:null rejected, off-list bet name accepted, missing demand_trend rejected, off-enum shape rejected, shape:"unknown" accepted.
- The other three validators + settings.json + route.js untouched.
</verification>

<success_criteria>
`tools/hooks/validate-classifier.js` now enforces the OPEN bet_type contract (reject null/empty bet_type, missing/empty bet_type_basis, untraceable canonical bet_types[] — NEVER enum-checked, COMPETITIVE_AXIS_ENUM gone, per amend-D-12 / D-14) and the demand_trend contract (reject missing demand_trend + shape off the closed DEMAND_TREND_SHAPE_ENUM, allowing shape:"unknown" — per D-15) on space-map.json — the validator that owns that file, matching the 01-01 schema and 01-02 source ownership. The other validators stand untouched — completing success criterion 7 (bet_type OPEN + traceable; demand_trend required + on-enum).
</success_criteria>

<output>
After completion, create `.planning/phases/01-stage-m1-s1-light-pass/01-04-SUMMARY.md`
</output>
