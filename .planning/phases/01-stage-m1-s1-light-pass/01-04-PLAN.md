---
phase: 01-stage-m1-s1-light-pass
plan: 04
type: execute
wave: 2
depends_on: ["01-01", "01-03"]
files_modified:
  - .claude/settings.json
  - tools/hooks/validate-finder.js
  - tools/hooks/validate-dumper.js
  - tools/hooks/validate-classifier.js
  - tools/hooks/validate-revenue.js
autonomous: true
requirements: [TOOL-01]
must_haves:
  truths:
    - "A PostToolUse hook fires on each agent's Write and rejects off-contract output instead of trusting the prompt"
    - "The Dumper hook rejects any claims[] string that is not a verbatim substring of that brand's clean/ corpus (kills hallucinated/paraphrased claims)"
    - "The Classifier hook rejects saturation computed across cells (must be per combo cell) and any claim_type off the closed enum"
    - "The Dumper hook does NOT reference awareness (dropped per D-05)"
    - "The Revenue hook rejects a value_usd_monthly missing method+confidence, and traffic_formula when monthly_visits is null"
  artifacts:
    - path: ".claude/settings.json"
      provides: "the net-new hooks.PostToolUse block matching Write → node validators"
      contains: "PostToolUse"
    - path: "tools/hooks/validate-dumper.js"
      provides: "verbatim-substring + no-classification + no-awareness reject rules"
      min_lines: 25
    - path: "tools/hooks/validate-classifier.js"
      provides: "per-cell-saturation + claim_type-enum + claim_count reject rules"
      min_lines: 25
  key_links:
    - from: ".claude/settings.json hooks.PostToolUse"
      to: "tools/hooks/validate-*.js"
      via: "matcher Write → command node tools/hooks/validate-<agent>.js"
      pattern: "validate-"
    - from: "tools/hooks/validate-dumper.js"
      to: "corpus/<slug>/clean/*.md"
      via: "string-match each claims[] entry against the clean corpus"
      pattern: "clean"
---

<objective>
Wire the net-new PostToolUse rejection hooks the master spec specs (~L176-190). Add a `hooks` block to `.claude/settings.json` (which today has only `permissions.allow`) matching `Write`, invoking small per-agent Node validator scripts in `tools/hooks/`. Each validator reads the just-written JSON, applies its reject rules, exits non-zero on violation. This is the H1 gate brick: "the did-it-classify-correctly problem is solved by hooks that reject bad output, not by trusting the prompt."

Purpose: Success criterion 2 — "the rejection-hook JSON" must exist so a run is reproducible and self-enforcing. Depends on Plan 01 (final prompt enums + awareness removed → the Dumper hook must NOT validate awareness) and Plan 03 (revenue-est.js method/confidence semantics the REVENUE hook mirrors).
Output: `.claude/settings.json` hooks block + four validator scripts under `tools/hooks/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stage-m1-s1-light-pass/01-CONTEXT.md
@.planning/phases/01-stage-m1-s1-light-pass/01-PATTERNS.md

@.claude/settings.json
@prompts/step1-light-pass.md
@CLAUDE.md

<interfaces>
<!-- Runtime: Node v20.20.0, zero deps. Validators are plain node, fs/path only.
     settings.json today contains ONLY permissions.allow — there is NO hooks block. This is net-new.
     Claude Code PostToolUse hook shape (matcher on tool name, command runs the validator;
     non-zero exit + stderr message = rejection). The hook receives the tool input via the
     documented hook stdin/env; the validator reads the written file path and parses its JSON. -->

<!-- Closed enums the hooks validate against (from prompts/step1-light-pass.md, AFTER Plan 01's awareness removal): -->
CHANNEL_ENUM:    dtc | marketplace | crowdfunding
CLAIM_TYPE_ENUM: direct | enlarged | mechanism | enhanced
<!-- AWARENESS_ENUM is GONE (D-05) — the Dumper hook MUST NOT check awareness. -->

<!-- revenue_est shape the REVENUE hook checks (must match Plan 03's revenue-est.js output):
     value_usd_monthly present ⇒ method + confidence present; method:"traffic_formula" ⇒ inputs.monthly_visits != null -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the four per-agent validator scripts in tools/hooks/</name>
  <read_first>
    - prompts/step1-light-pass.md "Hooks (PostToolUse on each agent's Write)" block (~L176-190) — the verbatim reject rules per agent; AND the closed-enum block (post-Plan-01, awareness removed)
    - .planning/phases/01-stage-m1-s1-light-pass/01-PATTERNS.md §".claude/settings.json PostToolUse hooks" (the net-new pattern: PostToolUse→Write→Node validator, per-agent for one-job-per-brick; the verbatim reject rules; "D-05 impact" — DUMPER hook loses the awareness rules)
    - .planning/phases/01-stage-m1-s1-light-pass/01-CONTEXT.md §D-04 (stage call hook-checkable against claim_type distribution), §D-05 (awareness dropped), §D-08 (live-only counts)
    - tools/adlib-one.js / tools/crowdfund-fetch.js (the standalone-node + fs/path idiom to mirror — zero deps)
  </read_first>
  <action>
    Create four standalone Node validators under `tools/hooks/`, each `node`-runnable with zero deps beyond fs/path. Each reads the just-written JSON file (path from the hook input), parses it, applies its reject rules, and on ANY violation prints a clear `REJECT: <rule> — <detail>` to stderr and exits non-zero (exit 2). On pass, exit 0 silently. Keep them per-agent (one job per brick).

    **tools/hooks/validate-finder.js** (FINDER hook, spec ~L187): reject if any brand's `channel` is off CHANNEL_ENUM (`dtc|marketplace|crowdfunding`) or `lane` off `major|crowdfunding|marketplace`; reject any brand row missing `url` or `sells_observed`.

    **tools/hooks/validate-dumper.js** (DUMPER hook, spec ~L176-180, POST-D-05): reject if any creative has `canonical_niche != null` or `canonical_angle != null`, or any pitch `transformation != null` (the dumper must not classify). **Reject if any pitch `claims[]` string is not a verbatim substring of that brand's clean corpus** — load `corpus/<slug>/clean/*.md` (concatenate the clean files for the dump's `slug`), and for each claims[] string assert it appears as a substring (this is the load-bearing gate against hallucinated/paraphrased claims). Reject if `angle_basis` is missing. **DO NOT check awareness or awareness_basis** — those fields are dropped (D-05); referencing them is a defect.

    **tools/hooks/validate-classifier.js** (CLASSIFIER hook, spec ~L181-184): reject any assigned `canonical` transformation/niche/angle with zero raw variants tracing to real dumps; **reject saturation computed across cells** (saturation entries must be keyed per combo cell = transformation × niche, not pooled — assert each saturation/combo entry carries both a transformation AND a niche, never a bare transformation-only pooled count); reject any `claim_type` off CLAIM_TYPE_ENUM (`direct|enlarged|mechanism|enhanced`); reject a combo missing `claim_count` or `enhanced_claim_count`; reject `enhanced_claim_count > claim_count`. (D-04 backstop: a per-brand `sophistication` stage with no claim evidence line is suspect — at minimum require the `sophistication` string is non-empty when combos exist.)

    **tools/hooks/validate-revenue.js** (REVENUE hook, spec ~L185-186, mirrors Plan 03 revenue-est.js): for each brand with a non-null `revenue_est.value_usd_monthly`, reject if `method` or `confidence` is missing; reject `method:"traffic_formula"` when `inputs.monthly_visits` is null (must be `review_proxy`); reject if `value_usd_monthly` is the literal string `"PENDING"` (D-10 never-fabricate).

    Treat the read JSON as DATA only — no `eval`/`Function`/`require` of file contents (untrusted-data rule). Add a `--help` to each (exit 0).
  </action>
  <verify>
    <automated>cd /home/kyu3/PMF && for f in validate-finder validate-dumper validate-classifier validate-revenue; do node -c tools/hooks/$f.js || exit 1; done; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All four `node -c tools/hooks/validate-*.js` exit 0 (valid syntax)
    - `grep -ci 'awareness' tools/hooks/validate-dumper.js` returns 0 (D-05: Dumper hook must NOT reference awareness)
    - `grep -q 'clean' tools/hooks/validate-dumper.js` and `grep -qi 'substring\|includes\|indexOf' tools/hooks/validate-dumper.js` match (verbatim-substring check against clean corpus)
    - `grep -q 'canonical_niche' tools/hooks/validate-dumper.js` and `grep -q 'transformation' tools/hooks/validate-dumper.js` match (no-classification reject)
    - validate-classifier.js references both per-cell saturation and the claim_type enum: `grep -qi 'saturation\|combo' tools/hooks/validate-classifier.js` and `grep -Eq 'direct|enlarged|mechanism|enhanced' tools/hooks/validate-classifier.js`
    - `grep -q 'traffic_formula' tools/hooks/validate-revenue.js` and `grep -q 'review_proxy\|confidence' tools/hooks/validate-revenue.js` match
    - Functional smoke (dumper): tmp dump.json with a claims[] string NOT present in a tmp clean corpus → `node tools/hooks/validate-dumper.js <path>` exits non-zero; a dump with all claims as verbatim substrings → exits 0.
    - Functional smoke (classifier): a space-map.json with a saturation entry keyed by transformation only (no niche) → validate-classifier.js exits non-zero.
    - No validator does `eval(`/`new Function(`/`require(<dynamic>)` on file contents (`grep -E 'eval\(|new Function' tools/hooks/*.js` returns 0)
  </acceptance_criteria>
  <done>Four zero-dep per-agent validators enforce the spec reject rules; the Dumper validator does verbatim-substring matching against clean/ and references no awareness; the Classifier validator rejects cross-cell saturation + off-enum claim_type; the Revenue validator mirrors the never-fabricate logic.</done>
</task>

<task type="auto">
  <name>Task 2: Add the hooks.PostToolUse block to .claude/settings.json</name>
  <read_first>
    - .claude/settings.json (whole file — today only `permissions.allow`; the hooks block is net-new — do NOT clobber permissions)
    - .planning/phases/01-stage-m1-s1-light-pass/01-PATTERNS.md §".claude/settings.json PostToolUse hooks" (the pattern to establish)
    - prompts/step1-light-pass.md the parenthetical at ~L189-190 ("Hook configs are JSON in settings — written after these prompts are approved")
  </read_first>
  <action>
    Add a `hooks` block to `.claude/settings.json` WITHOUT touching the existing `permissions.allow` array. Use the Claude Code `hooks.PostToolUse` shape: a matcher on the `Write` tool, with a `command` that runs the appropriate Node validator on the written file.

    Because all four agents write via the same `Write` tool, route by output-path so the right validator fires:
    - a Write whose path ends in `brands.json` → `node tools/hooks/validate-finder.js` then `node tools/hooks/validate-revenue.js` (brands.json carries both Finder fields and the revenue_est block)
    - a Write whose path ends in `dump.json` → `node tools/hooks/validate-dumper.js`
    - a Write whose path ends in `space-map.json` → `node tools/hooks/validate-classifier.js`
    Implement the routing in the hook `command` (e.g. a small inline shell/case on the file path, or a single `tools/hooks/route.js` dispatcher the command calls — Claude's discretion; keep it zero-dep Node/shell). Each validator exiting non-zero must block/flag the Write per the PostToolUse contract.

    Validate the resulting settings.json is valid JSON. Preserve every existing `permissions.allow` entry verbatim. Add `Bash(node tools/hooks/*)` and `Bash(node tools/*.js *)` to `permissions.allow` if needed so the hooks/scripts can run.
  </action>
  <verify>
    <automated>cd /home/kyu3/PMF && node -e "const s=require('./.claude/settings.json'); if(!s.hooks||!s.permissions||!s.permissions.allow) process.exit(1); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/settings.json` is valid JSON (`node -e "require('./.claude/settings.json')"` exits 0)
    - `node -e "const s=require('./.claude/settings.json'); process.exit(s.hooks?0:1)"` exits 0 (hooks block present)
    - `node -e "const s=require('./.claude/settings.json'); process.exit(s.permissions&&s.permissions.allow&&s.permissions.allow.length>0?0:1)"` exits 0 (permissions.allow preserved, not clobbered)
    - `grep -q 'PostToolUse' .claude/settings.json` matches
    - `grep -q 'validate-dumper' .claude/settings.json` and `grep -q 'validate-classifier' .claude/settings.json` and `grep -q 'validate-finder' .claude/settings.json` and `grep -q 'validate-revenue' .claude/settings.json` all match (all four wired)
    - The matcher targets the Write tool (`grep -q 'Write' .claude/settings.json` within the hooks block)
  </acceptance_criteria>
  <done>settings.json carries a net-new hooks.PostToolUse block routing Write-by-path to the four validators; existing permissions.allow preserved; valid JSON.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-written JSON → validators | The just-written JSON is read and parsed as DATA by the validators. |
| clean corpus → dumper validator | clean/*.md (derived from untrusted fetched copy) is string-matched, not executed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Tampering / RCE | validators parsing agent/corpus JSON+md | mitigate | Validators treat input as DATA only — `JSON.parse` + string `.includes`, NO `eval`/`Function`/dynamic `require` on contents. Acceptance criterion greps confirm. |
| T-01-11 | Repudiation (silent bad output) | the gate itself | mitigate | This whole plan IS the mitigation: hooks reject off-contract output (hallucinated claims, cross-cell saturation, fabricated revenue) instead of trusting the prompt. Non-zero exit + stderr message gives a readable rejection. |
| T-01-12 | Tampering | .claude/settings.json permissions | mitigate | Edit adds a hooks block only; acceptance criterion asserts permissions.allow is preserved non-empty and JSON stays valid. |
</threat_model>

<verification>
- Four validators parse (node -c) and enforce their spec reject rules; dumper does verbatim-substring vs clean/; no awareness anywhere; classifier rejects cross-cell saturation.
- settings.json has a valid hooks.PostToolUse block routing all four validators by path; permissions preserved.
- Functional smokes prove a bad dump and a cross-cell saturation map are both rejected.
</verification>

<success_criteria>
The rejection-hook JSON + validator scripts exist and self-enforce the contract (verbatim claims, per-cell saturation, typed claims, never-fabricated revenue, no awareness) — completing success criterion 2's "rejection-hook JSON" and backstopping criterion 3's "no cross-cell saturation pooling / no layer conflation."
</success_criteria>

<output>
After completion, create `.planning/phases/01-stage-m1-s1-light-pass/01-04-SUMMARY.md`
</output>
