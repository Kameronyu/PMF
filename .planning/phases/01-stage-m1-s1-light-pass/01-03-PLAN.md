---
phase: 01-stage-m1-s1-light-pass
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/dedupe.js
  - tools/revenue-est.js
  - tools/adlib-one.js
autonomous: true
requirements: [TOOL-01]
must_haves:
  truths:
    - "Finder brand rows surfaced by two lanes merge into one row by domain (found_by unioned, richest channel kept)"
    - "Every brand carries a revenue_est computed by arithmetic, never by an LLM"
    - "revenue_est always carries method + confidence; a null monthly_visits forces method:review_proxy, never a shipped PENDING/fabricated number"
    - "adlib-one.js writes structured ads/<brand>.json via --out, preserving page-ID resolution (no keyword-collision noise)"
  artifacts:
    - path: "tools/dedupe.js"
      provides: "domain-key brand merge → clean brands.json"
      min_lines: 25
    - path: "tools/revenue-est.js"
      provides: "deterministic value_usd_monthly = monthly_visits × cvr × aov, with method/confidence + never-fabricate logic"
      min_lines: 30
    - path: "tools/adlib-one.js"
      provides: "page-ID-resolved Meta Ad Library pull, now --out-parameterized + JSON-emitting"
      contains: "pickAdvertiser"
  key_links:
    - from: "tools/revenue-est.js"
      to: "brands.json revenue_est.method + confidence"
      via: "never emit value_usd_monthly without method+confidence; null visits → review_proxy"
      pattern: "review_proxy"
    - from: "tools/dedupe.js"
      to: "brands.json (deduped)"
      via: "normalized-host group + found_by union"
      pattern: "toLowerCase"
---

<objective>
Build the two transform/arithmetic scripts of the layer-3 scaffold — `tools/dedupe.js` (domain-key brand merge) and `tools/revenue-est.js` (deterministic revenue arithmetic with the never-fabricate discipline) — and apply the light touch-up to the existing `tools/adlib-one.js` (parameterize its hard-coded output path + emit structured `ads/<brand>.json`). All three are scripts (CLAUDE.md: dedupe/score are deterministic — never agents; "no LLM ever computes revenue").

Purpose: Success criterion 2 — the remaining layer-3 scripts exist so a run is reproducible. revenue-est.js carries the D-09/D-10/D-11 revenue contract; adlib-one.js preserves the page-ID resolution that kills the InkLeaf keyword-collision noise (D-07).
Output: `tools/dedupe.js`, `tools/revenue-est.js`, touched-up `tools/adlib-one.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stage-m1-s1-light-pass/01-CONTEXT.md
@.planning/phases/01-stage-m1-s1-light-pass/01-PATTERNS.md

@tools/adlib-one.js
@tools/crowdfund-fetch.js
@CLAUDE.md

<interfaces>
<!-- Runtime: Node v20.20.0, zero local deps. dedupe.js + revenue-est.js need only fs/path (NO playwright).
     adlib-one.js already requires playwright (keep). -->

<!-- brands.json revenue_est block these scripts read/write (from prompts/step1-light-pass.md schema): -->
"revenue_est": {
  "value_usd_monthly": null,
  "method": "traffic_formula | review_proxy | disclosed | null",
  "confidence": "high | medium | low",
  "inputs": { "monthly_visits": null,
              "visits_source": "similarweb-manual | semrush-api | proxy | review-proxy | null",
              "cvr_assumption": 0.02, "aov_usd": null, "aov_source": "observed-price | estimate | null" },
  "notes": "string"
}
<!-- brand row fields dedupe.js touches: { slug, url, channel, lane, found_by[] }
     price_points[] (verbatim '$329' strings) is the aov_usd source for revenue-est.js. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build tools/dedupe.js — merge brands.json by domain</name>
  <files>tools/dedupe.js</files>
  <read_first>
    - tools/adlib-one.js L25-26 (the normalize-key idiom: `s.toLowerCase().replace(/[^a-z0-9]+/g,'')` — the only analog)
    - tools/crowdfund-fetch.js L28-43 (CLI-arg parse pattern to copy)
    - .planning/phases/01-stage-m1-s1-light-pass/01-PATTERNS.md §"tools/dedupe.js" (job + the domain-key merge + no-LLM constraint)
    - prompts/step1-light-pass.md DETERMINISTIC SCAFFOLD item 4 + Finder dedup rule (~L234) + OPEN DECISIONS dedup keys (~L381: brands by domain)
  </read_first>
  <action>
    Create `tools/dedupe.js` as `node tools/dedupe.js [--in=brands.json] [--out=brands.json] [--help]`, zero deps beyond fs/path.

    Job: read brands.json, merge rows that share a normalized domain into one row. Domain key = the host of `row.url`, normalized with the adlib-one.js idiom: extract host (strip protocol/path/`www.`), then `.toLowerCase().replace(/[^a-z0-9]+/g,'')`. Group rows by this key. For each group:
    - Union `found_by[]` across all rows in the group (dedupe the array).
    - Keep the RICHEST `channel` — prefer the most specific: `crowdfunding` > `dtc` > `marketplace` (a brand with a crowdfunding block is the richest record). Keep that row's `crowdfunding` block if present.
    - Keep the first non-empty `url`, `product_observed`, `sells_observed`, `relevance`, `price_points`.
    Emit a clean brands.json preserving the top-level shape (`starting_point`, `brands[]`, `dropped[]`). Pass `dropped[]` through untouched.

    This is pure set/string logic — a script, never an agent (CLAUDE.md one-job-per-brick: the keep-bar judgment was the Finder's; the mechanical merge is deterministic). NO LLM call, NO network. Add `--help` (exit 0). Idempotent: running it twice on already-deduped brands.json yields the same output.
  </action>
  <verify>
    <automated>cd /home/kyu3/PMF && node -c tools/dedupe.js && node tools/dedupe.js --help; test $? -eq 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node -c tools/dedupe.js` exits 0; `node tools/dedupe.js --help` exits 0
    - `grep -q 'toLowerCase' tools/dedupe.js` and `grep -q "replace(/\[\^a-z0-9\]" tools/dedupe.js` match (the normalize-key idiom inherited)
    - `grep -q 'found_by' tools/dedupe.js` matches (found_by union logic present)
    - `grep -c "require('playwright')" tools/dedupe.js` returns 0 (no network/LLM — pure transform)
    - Functional smoke: feed a 2-row brands.json where both rows have host `acme.com` (one `dtc`, one `crowdfunding`) → output has exactly 1 brand row, channel `crowdfunding`, found_by unioned. Verify with a tmp file + `node -e` length check.
  </acceptance_criteria>
  <done>tools/dedupe.js merges brand rows by normalized domain, unions found_by, keeps richest channel, idempotent, no network/LLM.</done>
</task>

<task type="auto">
  <name>Task 2: Build tools/revenue-est.js — deterministic revenue with never-fabricate logic</name>
  <files>tools/revenue-est.js</files>
  <read_first>
    - .planning/phases/01-stage-m1-s1-light-pass/01-PATTERNS.md §"tools/revenue-est.js" (NO analog — use the spec formula + D-09/D-10/D-11 verbatim; the method/confidence logic block)
    - .planning/phases/01-stage-m1-s1-light-pass/01-CONTEXT.md §D-09 (collected+attached, never a hard gate), §D-10 (always method+confidence; null visits → review_proxy/low or explicit null; NEVER ship PENDING), §D-11 (Semrush Trends API primary visits_source:"semrush-api", multi-login wiring left to implementer; SimilarWeb-manual fallback; NO auto-browser SimilarWeb scraping)
    - prompts/step1-light-pass.md DETERMINISTIC SCAFFOLD item 5 (the formula + worked example 300,000 × 0.02 × $60 = $360,000/mo) and the REVENUE hook spec (~L185-186)
  </read_first>
  <action>
    Create `tools/revenue-est.js` as `node tools/revenue-est.js [--in=brands.json] [--out=brands.json] [--help]`, zero deps beyond fs/path. NO LLM computes revenue — it is arithmetic (D-09).

    For each brand row, compute `revenue_est.value_usd_monthly` per the VERBATIM spec formula:
      `value_usd_monthly = monthly_visits × cvr_assumption × aov_usd`
      - `cvr_assumption` default = 0.02 (stored as an assumption, never a measured fact — keep the field).
      - `aov_usd` derived from `row.price_points` (parse the first `$NNN` numeric; set `aov_source:"observed-price"`). If no price_points → leave aov null, `aov_source:"estimate"` only if operator supplies `--aov=`; else null.
      - `monthly_visits` read from `inputs.monthly_visits` (operator-populated upstream from the Semrush/SimilarWeb source; this script does NOT fetch traffic — no auto-browser scraping per D-11).

    Never-fabricate method/confidence logic (D-10 — the load-bearing rule, mirror the spec block verbatim):
      - ALWAYS emit `method` + `confidence` alongside any `value_usd_monthly`.
      - If `monthly_visits` is a real number AND `aov_usd` resolved → `method:"traffic_formula"`, confidence from source (`semrush-api`→medium, `similarweb-manual`→medium, `proxy`→low).
      - If `monthly_visits` is null → DO NOT use traffic_formula. Use `method:"review_proxy"`: `value ≈ review_count × category_multiplier × aov_usd`, `confidence:"low"`, `visits_source:"review-proxy"`. If neither visits nor a review proxy is available → `value_usd_monthly: null` with `method:null` (explicit null is allowed).
      - NEVER write the string "PENDING" as a value (the InkLeaf failure: SimilarWeb PENDING on all 31 records). A fake number is forbidden; explicit null is fine.

    revenue_est is collected and attached to every brand — it is NEVER a hard roster gate here (D-09); the $300–500K floor is Phase 2's call. Do not drop a brand for low/null revenue.

    Resilient per-brand loop (adlib-sweep pattern), `--help` (exit 0). Worked-example self-test sanity: 300000 × 0.02 × 60 = 360000.
  </action>
  <verify>
    <automated>cd /home/kyu3/PMF && node -c tools/revenue-est.js && node tools/revenue-est.js --help; test $? -eq 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node -c tools/revenue-est.js` exits 0; `node tools/revenue-est.js --help` exits 0
    - `grep -q 'review_proxy' tools/revenue-est.js` and `grep -q 'traffic_formula' tools/revenue-est.js` match
    - `grep -q '0.02' tools/revenue-est.js` matches (cvr default)
    - `grep -ci 'PENDING' tools/revenue-est.js` returns 0 OR only appears in a comment forbidding it (`grep -i 'PENDING' tools/revenue-est.js` must not assign PENDING as a value — confirm by reading the match)
    - `grep -c "require('playwright')" tools/revenue-est.js` returns 0 (no fetch — no auto-browser SimilarWeb scraping, D-11)
    - Functional smoke: tmp brands.json, one brand with `inputs.monthly_visits:300000` + `price_points:["$60"]` → output `value_usd_monthly` ≈ 360000, `method:"traffic_formula"`, has `confidence`. Second brand with `monthly_visits:null` → `method` is `review_proxy` or null, `value_usd_monthly` is a number-or-null, NEVER "PENDING", and `confidence` present when a value exists.
  </acceptance_criteria>
  <done>tools/revenue-est.js computes value_usd_monthly by arithmetic, always emits method+confidence, routes null visits to review_proxy (never PENDING/fabricated), never gates the roster, no traffic fetch.</done>
</task>

<task type="auto">
  <name>Task 3: Touch up tools/adlib-one.js — parameterize --out + emit ads/<brand>.json</name>
  <files>tools/adlib-one.js</files>
  <read_first>
    - tools/adlib-one.js (whole file — the working page-ID-resolution script to touch up, NOT rewrite)
    - .planning/phases/01-stage-m1-s1-light-pass/01-PATTERNS.md §"tools/adlib-one.js" (the ONLY allowed changes: parameterize OUT path L20, JSON-emit instead of/alongside _adv.txt; preserve pickAdvertiser L25-51, forcedPageId, two-pass protocol, cookie-dismiss)
    - .planning/phases/01-stage-m1-s1-light-pass/01-CONTEXT.md §D-07 (ad volume counts only if attributable to the target product — page-ID resolution is what enforces this; the InkLeaf "TCL TVs = 260 ads" failure)
    - prompts/step1-light-pass.md DETERMINISTIC SCAFFOLD item 3 (adlib-one.js → ads/<brand>.json contract)
  </read_first>
  <action>
    Touch up `tools/adlib-one.js` — do NOT rewrite. The light pass works; only the two changes PATTERNS.md sanctions:

    1. Parameterize the hard-coded output path. Replace `const OUT = '/home/kyu3/PMF/runs/eink-tablets/adlibrary';` (L20) with an `--out=` flag (default `./ads`), following the crowdfund-fetch.js arg-parse pattern. This also fixes the legacy path drift (PATTERNS.md path-convention note: canonical home is now `ads/` per the spec contract, not `runs/eink-tablets/`).
    2. Emit structured `ads/<brand>.json` (the scaffold contract) in addition to / instead of the `_adv.txt` text dump. The JSON must carry at minimum: `{ slug, brand_query, status, resolved_advertiser:{name,pageId,followers}|null, active_ad_count, library_ids_loaded }`. Keep the `.png` screenshot and (optionally) the `.txt` for debugging.

    PRESERVE verbatim (do NOT regress — these are the load-bearing anti-noise pieces):
    - `pickAdvertiser` (L25-51) scoring + the `score >= 30` threshold (kills keyword-collision noise — D-07).
    - `forcedPageId` 3rd-CLI-arg override (L15, L77-78).
    - Two-pass re-resolve-to-view_all_page_id protocol; `count='0'` on "no ads match" (absence of ads is data).
    - Cookie-consent dismissal loop (L71-74).

    Do NOT touch the FB Ad Library URL construction or the typeahead `li[role=option]` page-ID scraping. Add a `--help` path (exit 0). The CLI stays positional `<slug> "<brand>" [pageId]` + the new `--out=`.
  </action>
  <verify>
    <automated>cd /home/kyu3/PMF && node -c tools/adlib-one.js && grep -q 'pickAdvertiser' tools/adlib-one.js && grep -q 'forcedPageId' tools/adlib-one.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node -c tools/adlib-one.js` exits 0 (still valid after edit)
    - `grep -c "runs/eink-tablets/adlibrary" tools/adlib-one.js` returns 0 (hard-coded legacy path removed)
    - `grep -q "'--out'" tools/adlib-one.js || grep -q 'opts.out' tools/adlib-one.js` matches (output path parameterized)
    - `grep -q 'ads' tools/adlib-one.js` and `grep -q '.json' tools/adlib-one.js` match (emits ads/<brand>.json)
    - `grep -q 'pickAdvertiser' tools/adlib-one.js` and `grep -q 'score >= 30' tools/adlib-one.js` match (page-ID resolution preserved)
    - `grep -q 'forcedPageId' tools/adlib-one.js` matches (override preserved)
    - `grep -q 'no ads match' tools/adlib-one.js` matches (absence-of-ads-is-data preserved)
  </acceptance_criteria>
  <done>adlib-one.js takes --out, emits structured ads/<brand>.json, legacy hard-coded path gone; page-ID resolution / forcedPageId / two-pass / cookie-dismiss all preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-supplied JSON → dedupe/revenue-est | brands.json is operator-controlled, treated as data. |
| FB Ad Library → adlib-one.js | Untrusted remote page; page-ID resolution + score threshold filter noise. |
| Semrush/SimilarWeb credentials (D-11) | API keys for traffic — must not be hardcoded/committed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Information disclosure | Semrush API keys (D-11) | mitigate | revenue-est.js does NOT fetch traffic and stores NO keys — monthly_visits arrives pre-populated in brands.json. Any future multi-login wiring (left to implementer) must read keys from env/gitignored config, never hardcode/commit. Documented as the constraint. |
| T-01-07 | Spoofing / data poisoning | adlib-one.js advertiser resolution | mitigate | `pickAdvertiser` score>=30 threshold + page-ID resolution prevents keyword-collision brands inflating ad counts (D-07). Preserved verbatim. |
| T-01-08 | Tampering (fabricated revenue) | revenue-est.js | mitigate | Never-fabricate logic: method+confidence always emitted; null visits → review_proxy/null, never "PENDING"/fake number (D-10). Mirrored by the REVENUE hook in Plan 04. |
| T-01-09 | ToS / ban risk | traffic acquisition | mitigate | NO auto-browser SimilarWeb scraping introduced (D-11); revenue-est.js does no fetching at all. Accepted: Meta Ad Library + crowdfunding fetches inherit existing rate/stealth patterns. |
</threat_model>

<verification>
- All three scripts parse and respond to --help (exit 0).
- dedupe.js merges by domain, unions found_by, keeps richest channel.
- revenue-est.js: method+confidence always present; null visits → review_proxy/null; no PENDING; no fetch.
- adlib-one.js: --out parameterized, emits JSON, page-ID resolution + override + two-pass preserved, legacy path gone.
</verification>

<success_criteria>
The remaining layer-3 transform/arithmetic scripts exist as zero-dep Node scripts; revenue carries the D-09/D-10/D-11 contract; adlib-one.js emits the spec's ads/<brand>.json while preserving page-ID resolution — completing success criterion 2's script set (alongside Plan 02's fetch/clean).
</success_criteria>

<output>
After completion, create `.planning/phases/01-stage-m1-s1-light-pass/01-03-SUMMARY.md`
</output>
