---
phase: 01-stage-m1-s1-light-pass
plan: 05
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03", "01-04"]
files_modified:
  - corpus/
  - brands.json
  - space-map.json
  - .planning/phases/01-stage-m1-s1-light-pass/01-DEBUG-RUN-NOTES.md
autonomous: false
requirements: [TOOL-01]
must_haves:
  truths:
    - "Running the reconciled prompt + scripts on a real T/P/N produces brands.json + per-brand dump.json + space-map.json end-to-end"
    - "space-map.json has saturation keyed per combo cell (transformation × niche) — no cross-cell pooling"
    - "No layer conflation survives: features are not typed as claims, mechanisms are not transformations"
    - "Every revenue_est record carries method + confidence; none ship PENDING"
    - "Kam scans a bucketed sample and confirms letters / themes / typed claims are assigned the way he would"
  artifacts:
    - path: "space-map.json"
      provides: "the classified space map from the reference run (the Phase 2 skill's input contract)"
      contains: "saturation"
    - path: ".planning/phases/01-stage-m1-s1-light-pass/01-DEBUG-RUN-NOTES.md"
      provides: "the debug-pass findings: what broke, what the methodology surfaced, gaps to fold (or confirm clean)"
      min_lines: 15
  key_links:
    - from: "the reconciled step1-light-pass.md + tools/*.js + hooks"
      to: "space-map.json"
      via: "Finder → Verifier → fetch/clean/dedupe/adlib/revenue → Dumper → Classifier, hooks gating each Write"
      pattern: "saturation"
---

<objective>
Run the reconciled light pass end-to-end on ONE real reference T/P/N as a DEBUG PASS — "first run on a space is a debug pass to break the methodology, not produce a deliverable" (D-01 anchor). Prove the pipeline emits a clean structured run (brands.json + dump.json + space-map.json) with no cross-cell saturation pooling and no layer conflation (success criterion 3), then have Kam scan a bucketed sample and confirm the letters / themes / typed claims are assigned the way he would (success criterion 4).

Purpose: Success criteria 3 & 4 — the acceptance gate is UAT (STATE.md: "Verification is UAT, not unit tests — run on a reference space and Kam reads the output"). Do NOT over-build ahead of this run; capture what the debug pass surfaces.
Output: a populated `corpus/` + `space-map.json`, a `01-DEBUG-RUN-NOTES.md`, and Kam's confirmation (or a flagged gap list).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stage-m1-s1-light-pass/01-CONTEXT.md
@.planning/phases/01-stage-m1-s1-light-pass/01-PATTERNS.md

@prompts/step1-light-pass.md
@definitions.md
@CLAUDE.md

<interfaces>
<!-- Pipeline (step1-light-pass.md):
     Finder → brands.json → Roster Verifier → [human ok] →
     tools/fetch.js + tools/clean.js + tools/adlib-one.js → corpus/<slug>/{raw,clean}, ads/<slug>.json →
     tools/dedupe.js (brands.json) + tools/revenue-est.js (revenue_est) →
     Dumper (per brand) → corpus/<slug>/dump.json → Space Classifier → space-map.json
     Hooks (Plan 04) gate each agent Write by output path. -->
<!-- space-map.json saturation entries MUST be keyed per combo cell (transformation × niche), never pooled. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Pick the reference T/P/N for the debug run</name>
  <decision>Which real product / transformation / niche seed to run the debug pass on.</decision>
  <action>Present the three seed options to Kam, take his pick, and record the chosen `<PRODUCT>` / `<CATEGORY>` seed in 01-DEBUG-RUN-NOTES.md. No pipeline runs until the seed is locked.</action>
  <context>
    The reference space should be one Kam can judge by eye (he knows it well enough to confirm letter/theme/claim_type assignments — success criterion 4). InkLeaf's eink-tablets run is RETIRED and quarantined — do NOT use it as the corpus (explicitly not a UAT rebuild target). Pick a live space. Per STATE.md the top candidate markets are analog-wellness organizer and ADHD-student tool. The seed shape is product-start (the Finder takes product = "<PRODUCT>", category "<CATEGORY>").
  </context>
  <options>
    <option id="adhd-student">
      <name>ADHD-student focus tool</name>
      <pros>Kam has deep avatar knowledge; rich live competitor set across DTC + crowdfunding; good claim_type variety to stress the sophistication ladder.</pros>
      <cons>Adjacent to the retired eink space — must keep the corpus fresh, not pull quarantined data.</cons>
    </option>
    <option id="analog-wellness">
      <name>Analog-wellness organizer</name>
      <pros>Distinct from the retired run; wellness framing exercises problem-UM clustering.</pros>
      <cons>Possibly thinner ad presence → fewer creatives to type.</cons>
    </option>
    <option id="other">
      <name>A different space Kam names at runtime</name>
      <pros>Kam picks the space he can judge most confidently.</pros>
      <cons>none</cons>
    </option>
  </options>
  <read_first>
    - .planning/STATE.md (candidate markets; InkLeaf retired)
    - .planning/phases/01-stage-m1-s1-light-pass/01-CONTEXT.md §specifics (the debug-pass anchor)
  </read_first>
  <acceptance_criteria>
    - A single product + category seed is recorded in 01-DEBUG-RUN-NOTES.md (the `<PRODUCT>` / `<CATEGORY>` the Finder runs on)
    - The chosen space is NOT the quarantined eink-tablets InkLeaf run
  </acceptance_criteria>
  <resume-signal>Select: adhd-student, analog-wellness, or other (name the space)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run the pipeline end-to-end and self-audit for pooling / layer conflation</name>
  <files>brands.json, space-map.json, corpus/, .planning/phases/01-stage-m1-s1-light-pass/01-DEBUG-RUN-NOTES.md</files>
  <read_first>
    - prompts/step1-light-pass.md (the full reconciled pipeline — Finder, Roster Verifier, the DETERMINISTIC SCAFFOLD invocation order, Dumper, Space Classifier with the inlined D-03 ladder)
    - tools/fetch.js, tools/clean.js, tools/dedupe.js, tools/revenue-est.js, tools/adlib-one.js (the scripts to invoke, with their --flags)
    - tools/hooks/validate-*.js + .claude/settings.json (the gates that fire on each Write)
    - .planning/phases/01-stage-m1-s1-light-pass/01-CONTEXT.md §D-06/D-07/D-08 (live-vs-dead/region flagging; ad attributability; live-only saturation counts) and §D-03/D-04 (the sophistication ladder read off claim_type)
    - run-retrospective.md (the InkLeaf failures this run guards against: pooled saturation, claim=feature conflation, SimilarWeb PENDING) — read on demand if the run surfaces a regression
  </read_first>
  <action>
    Run the full pipeline on the chosen seed, in pipeline order:
    1. FINDER: run AGENT 1 (step1-light-pass.md) on the product/category → write brands.json. The FINDER hook fires on Write.
    2. ROSTER VERIFIER: run AGENT 1.5 adversarial cross-check → apply its slop_flags/missing_brands to brands.json (human ok per the prompt's "human ok → proceed"). Keep flagged dead/region-only brands IN brands.json (D-06 flag-don't-drop) with `status` + `market_presence`.
    3. SCRIPTS: `node tools/dedupe.js --in=brands.json --out=brands.json` (merge by domain); `node tools/fetch.js --brands=brands.json --out=corpus` (homepage + LP-hunt → corpus/<slug>/raw); `node tools/clean.js --in=corpus --out=corpus` (raw → clean); `node tools/adlib-one.js <slug> "<brand>" --out=ads` per ad-flagged brand (page-ID-resolved ad pull — D-07 attributable-to-product only); `node tools/revenue-est.js --in=brands.json --out=brands.json` (attach revenue_est; populate inputs.monthly_visits from the operator's Semrush/SimilarWeb source first, or leave null → review_proxy/null, never PENDING — D-10).
    4. DUMPER: run AGENT 2 per brand against corpus/<slug>/clean/ ONLY → corpus/<slug>/dump.json. The DUMPER hook fires (verbatim-substring check, no-classification, no-awareness).
    5. SPACE CLASSIFIER: run AGENT 3 over ALL dump.json → space-map.json, applying the inlined D-03 ladder per combo cell. The CLASSIFIER hook fires (per-cell saturation, claim_type enum, claim_count sanity).

    Then SELF-AUDIT space-map.json before handing to Kam — confirm the three structural fixes hold:
    - Saturation keyed per combo cell (transformation × niche), never a bare transformation-only pooled count (D-08). Live + in-geo brands only in the counts; dead/region-only listed but excluded.
    - No layer conflation: a feature ("thinnest 4.5mm", "16K stylus") is NOT typed as a claim; a mechanism alone is not a transformation; "paper-like feel" reads as an ANGLE not a transformation.
    - Every revenue_est carries method + confidence; grep the output for "PENDING" → must be absent.

    If a hook rejects a Write, that is the gate WORKING — read the REJECT message, fix the agent output (or the hook if the rule is wrong), re-run that stage. Capture every break in 01-DEBUG-RUN-NOTES.md (this is a debug pass — surfacing gaps is the deliverable). Do NOT over-build to pre-empt gaps that haven't appeared (D-01 anchor).
  </action>
  <verify>
    <automated>cd /home/kyu3/PMF && test -f brands.json && test -f space-map.json && node -e "const m=require('./space-map.json'); const combos=m.combos||[]; const pooled=combos.filter(c=>!c.transformation||!c.niche); if(JSON.stringify(m).includes('PENDING')){console.error('PENDING present');process.exit(1)} if(pooled.length){console.error('combo missing transformation or niche');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `brands.json` and `space-map.json` exist at repo root (the run completed end-to-end)
    - At least one `corpus/<slug>/clean/*.md` exists and at least one `corpus/<slug>/dump.json` exists (fetch→clean→dump ran)
    - `space-map.json` saturation/combos are keyed per transformation × niche — `node -e` check above passes (no combo missing transformation or niche → no cross-cell pooling)
    - `grep -c 'PENDING' brands.json space-map.json` returns 0 (never-fabricate held — D-10)
    - `grep -L '"method"' brands.json` is empty (every revenue_est carries method; confidence present where a value exists)
    - 01-DEBUG-RUN-NOTES.md records: the seed, which hooks fired/rejected, any layer-conflation or pooling caught + fixed, and a "clean | gaps-to-fold" verdict
    - No awareness field in any dump.json (`grep -rc awareness corpus/*/dump.json` returns 0 — D-05 held through the run)
  </acceptance_criteria>
  <done>The pipeline ran end-to-end on the reference seed; space-map.json is per-cell (no pooling), layer-discipline holds, revenue is never-fabricated, and the debug findings are recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Kam scans a bucketed sample and confirms the assignments</name>
  <files>.planning/phases/01-stage-m1-s1-light-pass/01-DEBUG-RUN-NOTES.md</files>
  <action>Surface the space-map.json + a 2-3 brand dump.json sample to Kam, walk the verification steps below, and record his verdict (confirmed, or specific mis-assignments to fold) in 01-DEBUG-RUN-NOTES.md.</action>
  <what-built>
    The reconciled light pass ran on the reference seed and produced space-map.json: canonical transformations / niches / angles, per-combo-cell saturation, typed claims (direct/enlarged/mechanism/enhanced), and a per-brand sophistication stage read off the claim_type distribution.
  </what-built>
  <how-to-verify>
    1. Open `space-map.json` and a 2-3 brand `corpus/<slug>/dump.json` sample.
    2. For a bucketed sample of creatives, confirm the TRANSFORMATION clustering is how you'd group them (no feature-as-transformation, no angle-as-transformation — e.g. "paper-like feel" should be an angle, not a transformation).
    3. Confirm the claim_type assignments match your read of the sophistication ladder: a bare promise = `direct`, quantified/specified = `enlarged`, outcome-tied-to-named-how = `mechanism`, mechanism+superlative = `enhanced`. Spot-check 5-10 typed claims.
    4. Confirm each combo cell's sophistication STAGE matches the ladder — the stage = the highest claim_type tier 2+ live in-geo brands deploy in that cell; the evidence line cites the claim(s) + brand count.
    5. Confirm saturation reads per cell (a wide-open territory where a competitor left does NOT read as saturated because dead brands are excluded — D-08).
    6. If anything is assigned a way you would NOT assign it, note it specifically (which creative, which field, your call) — this drives whether the inline examples need expanding (the deferred "4th gray area").
  </how-to-verify>
  <read_first>
    - .planning/phases/01-stage-m1-s1-light-pass/01-CONTEXT.md §D-03 (the ladder), §specifics (the differentiation-floor frame)
    - definitions.md §"Market sophistication" + §"Claim"/"Enhanced claim"
  </read_first>
  <acceptance_criteria>
    - Kam reviewed a bucketed sample of transformations / themes / typed claims
    - Any disagreement is recorded specifically in 01-DEBUG-RUN-NOTES.md (creative + field + Kam's call), OR Kam confirms the assignments are how he would make them
    - The verdict (confirmed / fold these fixes) is written to 01-DEBUG-RUN-NOTES.md
  </acceptance_criteria>
  <resume-signal>Type "confirmed" if the letters/themes/typed claims are assigned the way you would; otherwise list the specific mis-assignments to fold.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web → fetch.js / adlib-one.js | Untrusted competitor sites + FB Ad Library fetched during the run. |
| fetched copy → Dumper agent | Untrusted ad/page copy is fed (as clean text) to the LLM Dumper. |
| Semrush/SimilarWeb credentials (D-11) | Traffic source; multi-login keys must not be committed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | Tampering / injection | fetched copy → Dumper prompt | mitigate | Fetched copy is treated as DATA: the Dumper extracts verbatim substrings, and the DUMPER hook string-matches every claim against the clean corpus (rejects injected/hallucinated content). No fetched content is eval'd or executed. |
| T-01-14 | Information disclosure | Semrush keys during the run | mitigate | Keys (if multi-login wired) live in env/gitignored config, never committed; brands.json + corpus committed must carry no secrets — verify before any commit. |
| T-01-15 | ToS / ban | traffic + ad fetches | accept/mitigate | No auto-browser SimilarWeb scraping (D-11). Meta Ad Library + crowdfunding fetches inherit existing stealth/rate posture (proportionate for local research tooling). |
| T-01-16 | Repudiation | silent bad classification | mitigate | The Plan-04 hooks gate every Write; the debug-run self-audit + Kam's human-verify checkpoint are the final backstops against pooled saturation / layer conflation / fabricated revenue slipping through. |
</threat_model>

<verification>
- Pipeline ran end-to-end: brands.json + corpus/<slug>/{clean,dump.json} + space-map.json all exist.
- space-map.json saturation/combos are per transformation × niche (no pooling); no PENDING; every revenue_est has method.
- No awareness in any dump.json (D-05 held).
- Debug findings recorded; Kam confirmed the bucketed sample (or listed fixes to fold).
</verification>

<success_criteria>
A real reference run produces a clean per-cell space map with no cross-cell saturation pooling and no layer conflation (success criterion 3), and Kam confirms the letters / themes / typed claims are assigned the way he would (success criterion 4) — closing the Phase 1 acceptance gate. Any surfaced gap is recorded for a follow-up gap-closure pass rather than silently absorbed (the debug-pass discipline).
</success_criteria>

<output>
After completion, create `.planning/phases/01-stage-m1-s1-light-pass/01-05-SUMMARY.md`
</output>
