# HANDOFF — PMF Shell Build (autonomous) — branch `pmf-shell-build`

> Read this first on resume. Everything needed to continue the autonomous 6-phase build without re-discovery. eink-phase0-run is untouched. **DO NOT push.**

## Mission
Runnable SHELL of the PMF marketing pipeline (Steps 0–10) on STUB prompts, mechanism not content. 6 phases. Source of truth: `basis/build-base/ONE-SHOT-SHELL.md` (+ `SHELL-BUILD-SPEC.md`, `architecture/PART0`, `PART3 §8/§9/§5.2`). **Reuse root `/engine` bricks, never rewrite.** Quality profile = Opus agents.

## Conductor recipe (thin dispatcher — repeat per phase N = 3→6)
1. **Dispatch ONE isolated bg agent** (`general-purpose`, model opus, `run_in_background`). Its job, fully autonomous (no prompts, atomic commits, NO push): write a minimal infra `NN-CONTEXT.md` with a `<canonical_refs>` block pointing at that phase's basis sources → `Skill(gsd-plan-phase N)` → `Skill(gsd-execute-phase N --no-transition)` → `Skill(gsd-code-review N)` → if findings `Skill(gsd-code-review-fix N --auto)` → re-confirm all gates → report a structured build report. Include a Wave-0 acceptance harness `engine/contracts/<phase>-smoke.sh` (authored RED, code turns it GREEN).
2. **Re-arm a Monitor** on git commits; exit when `NN-VERIFICATION.md` appears + re-run gates.
3. On verification passed → **launch the xhigh adversarial verify Workflow** (below) with phase-specific targets.
4. **Extract synthesis with python** (raw_decode the object containing `synthesis` from the workflow `.output`; print per-verdict status/severity/false_green + synthesis.overall/blockers/concerns/summary). NEVER `Read` the raw `.output` (overflows context).
5. If synthesis has blockers / `false_green_risk` → dispatch a targeted fix agent, re-confirm. If clean → next phase. Every phase's xhigh verify has caught real bugs — keep running it.

## Reusable xhigh verify Workflow
`scriptPath`: `/home/kyu3/.claude/projects/-home-kyu3-PMF/fd245313-7588-4b80-917e-cc766741a9b4/workflows/scripts/phase-verify-xhigh-wf_196524c9-968.js`
Invoke: `Workflow({scriptPath, args:{phase, phaseDir, gateCmd, targets:[{id, prompt}]}})`. `args` parsed object-or-string (defensive). Spawns 5 adversarial verifiers at `effort:'xhigh'` + skeptical synthesis. Each target prompt = "adversarially verify X; try to BREAK it / find false-green; create only `runs/_vfy*` temp, clean up, touch no committed file." Synthesis marks `clean` only if no fail/high/critical/false_green.

## Gates (must stay green every phase)
- `bash engine/contracts/store-smoke.sh --space=smoke`  (Phase 1 STORE-01..05)
- `bash engine/contracts/controller-smoke.sh`           (Phase 2 CTRL-01..12)
- `bash engine/contracts/h6-all.sh`                     (engine regression, 14/14)

## Progress
- **PHASE 1 DONE ✓** Artifact Store. `engine/bricks/{store-scaffold,space-version,receipt-write}.js`, `engine/bricks/lib/fanout-path.js`, `engine/contracts/store-smoke.sh`. xhigh-verify caught 1 high (receipt silent-overwrite) + 2 true-green; all fixed.
- **PHASE 2 DONE ✓** Run-Controller. `engine/bricks/run-controller.js` (7-phase loop + loadManifest + parsePipeline + runAll), `pipeline.yaml` (repo ROOT, steps 0..10 R1), `bin/run` (CLI), `engine/contracts/controller-smoke.sh`, fixtures `runs/_fixture/pipeline/`. 12/12 verified; reuse-not-rewrite confirmed. xhigh-verify found 2 medium + 2 cheap — **ALL FIXED** (F1 wave-COUNT upper bound, agents=1e9 no longer hangs; F2 non-file `reads[]` → named preflight REFUSE; F3 controller-smoke strict ordering assert; F4 non-object manifest → named REFUSE; recorded in `02-XVERIFY-FIX.md`). Gates confirmed green: controller-smoke PASS, store-smoke + h6-all no-regression.
- **PHASE 3 DONE ✓** Step Manifests ×11. `engine/manifests/00..10-*.json` (real set; controller needed ZERO change — `DEFAULT_MANIFEST_DIR` was already `engine/manifests`, `pipeline.yaml` already R1), `engine/contracts/manifest-smoke.sh` (RED-first; MANIFEST-01..04 + WIRE-01/02 + graph integrity). Graph proven orphans=0/dangling=0; gate set exactly {0,1,5,7,8,10}. xhigh-verify: 4/5 content verdicts clean (wiring genuinely sound); 1 medium blocker on the HARNESS (producer-blind flat-set false-green) — FIXED (`4d819c3`): MANIFEST-02 rewritten to ordered/producer-id-bound (write-write dup, back/self-edge, wrong-producer-terminal, rogue-file all now RED-fail; mutation-proven; recorded in `03-XVERIFY-FIX.md`). 4 gates green.
- **PHASE 4 DONE ✓** Prompt Stubs ×11 + STUB-mode mock emits. `prompts/00..10-*.md` (bet-compiler envelope, marked drop-in `# BODY` + ```` ```stub-emit ```` block per step), multi-write emits in `run-controller.js` (minimal additive `readStubEmit`/`writeOne`/array `mockEmit` + REFUSE guards; Mode A fixtures preserved), `engine/contracts/stub-smoke.sh` (STUB-01..04 + WIRE-03 + determinism). `run all --space=smoke` already completes end-to-end clean. xhigh-verify: 5/5 content sound (26 writes materialize, WIRE-03 real two-tier all 5 axes w/ full raw→canonical trace, byte-deterministic); 1 HIGH blocker on the HARNESS (stub-smoke over-claimed coverage: hollow `{}` passed, emit-vs-seed indistinct, WIRE-03 transform-axis-only, determinism only space-map, no slot-fidelity) — FIXED (`bb4d92a`): shape map + non-seed + all-5-axis collapse/trace + full-artifact determinism + slot fidelity; 6 mutations RED-proven; recorded in `04-XVERIFY-FIX.md`.
- **ENGINE FLAKE FIXED (`5045565`)** — pre-existing: `h5-e2e` (inside h6-all) embed hops go live to Voyage when `VOYAGE_API_KEY` set → intermittent "rag output not JSON" (~3/5). Pinned the coherence harness to the engine's deterministic stub backend (`unset VOYAGE_API_KEY` inside `h5-e2e.sh`); 14/14 every run, verified 5x with key live. Live-Voyage path intact for real operation. **User granted full control over pre-existing engine code; "maximalist cleanest" mandate.**
- **PHASE 5 DONE ✓** Validators, Gates & Receipts. New `engine/hooks/validate-shape.js` (generic presence + top-level-shape + non-hollow + drift-guard) + `engine/contracts/output-shapes.json` (per-output contract keys); `route.js` dispatch extended to ALL ~14 outputs (silent-pass tail removed); `validate-classifier.js` WIRE-03 trace-back + axis-presence; controller preflight hollow-input refusal (VALID-02) + gate-decision + `validator_verdicts` threaded into receipt (VALID-04/05); `receipt-write.js` `--verdicts` flag; 11 manifests' `validator` bound to route.js (reads/writes/gate/prompt UNCHANGED); `engine/contracts/validate-smoke.sh` (6th gate). xhigh-verify: 5/5 sound, **zero false-green, zero blockers** (validators genuinely execute/reject/gate; run-all green is real). 3 non-blocking gaps closed (`d7ba9c1`/`eacfece`/`448e364`): MEDIUM regression-net hole (validate-shape generic gate now RED-pinned by a non-brands mutation + controller-escalation assert), VALID-02 strengthened (preflight reuses validate-shape at the reads[] seam — refuses `[{}]`/`{k:null}`/nested-empty/junk-.md), `__MD__` scaffold-placeholder reject implemented; recorded in `05-XVERIFY-FIX.md`. 6 gates green.
- **PHASE 6 BUILD DONE ✓ (xhigh verify running; milestone lifecycle pending).** Smoke Run & DoD. `engine/contracts/smoke-dod.sh` (the 7th gate, SMOKE-01..05 + determinism, asserted at the RUN grain over a fresh `run all --smoke`: 11 receipts in canonical order, all verdicts pass, all 26 writes materialized, gates logged + 64-hex chain, zero orphan/dangling over the real run); `engine/bricks/run-controller.js` `runAll` **pipeline-completeness preflight** (SMOKE-05 mechanism — dropped/reordered producer → named `pipeline incomplete/misordered` refusal before any spawn; external pipeline-entry inputs allowed via canonical manifest-dir scan, so a dropped INTERNAL producer is caught but a seeded entry like bet-brief.md is not false-refused). RED→GREEN proven. Regression caught+fixed during build: first preflight cut refused the fixture pipelines (external bet-brief.md reads) → controller-smoke CTRL-02/CTRL-10 red → fixed via the external-entry classification (NOT by weakening fixtures). **All 7 gates green; committed smoke run clean + runs/smoke byte-intact; pipeline.yaml/manifests/prompts frozen.** Commits `00942ff` (harness RED), `e5ca778` (preflight GREEN), `0eda506` (SMOKE-01..05 marked complete — all 38 v1 reqs now Complete). **BUILT INLINE** in the main session after 2 background build agents died on API transport drops (ConnectionRefused / stream-watchdog); followed the surviving `06-01-PLAN.md` task-by-task with per-task commits. xhigh verify = task `wdjcb0dty`.
- **Remaining: milestone lifecycle** — after the Phase-6 xhigh verify passes (+ any fix): `gsd-audit-milestone` → `gsd-complete-milestone` (CONFIRM WITH USER before complete-milestone — it archives the milestone). Content-job deferrals (NOT this milestone): real field-level seam schemas (CONTENT-02), real prompt bodies (CONTENT-01), prompt-division 1-vs-N (CONTENT-03), marketing truth/thresholds (CONTENT-04), Phase B (PHASEB-01).

## Deferred items (HONOR in the owning phase — recorded, not dropped)
- **Phase 5 / VALID-02:** refuse empty/non-null-CONTENT load-bearing input (Phase-2 preflight only checks existence + is-file).
- **Phase 5 / VALID-05:** populate operator-gate decision in the receipt (currently `{step_gated, decision:null}`).
- **Phase 6 / SMOKE-05:** pipeline-completeness / canonical-order assertion (controller walks `pipeline.yaml` as-is; a DROPPED step exits 0 — the DoD smoke must catch it as a dangling input).

## Remaining phases (requirements)
- **Phase 3 — Step Manifests ×11:** MANIFEST-01..04 + WIRE-01 (Step0 emits `asset-classify/CLAIM-LIST.json`, Step9 reads it) + WIRE-02 (Step7 reads `bet-brief.md` AND `product-intake.md`). 11 declarative manifests; reads/writes per `PART3 §5.2`; `gate:true` on steps 0,1,5,7,8,10. Likely create real `engine/manifests/` set (doesn't exist yet — controller currently runs only the fixtures).
- **Phase 4 — Prompt Stubs ×11 + mock emits:** STUB-01..04 + WIRE-03 (Step2 raw per-funnel angle/claim/transformation; Step3 canonicalizes). Envelope template = `basis/build-base/skills/bet-compiler/SKILL.md`. STUB mode emits a contract-shaped artifact.
- **Phase 5 — Validators/Gates/Receipts:** VALID-01..05 + the 2 deferred Phase-5 items above.
- **Phase 6 — Smoke Run & DoD:** SMOKE-01..05. `run all --space=smoke` clean end-to-end; zero orphan/dangling; + deferred SMOKE-05. Then milestone lifecycle: `gsd-audit-milestone` → `gsd-complete-milestone`.

## Conventions
no-overwrite-v1 (receipts write-once; whole-space `-vN`). Build agents commit atomically (per build mandate) but DON'T push. Wave-0 RED-then-green harness. Engine bricks invoked as subprocesses, not re-implemented.

## Open non-build item
`.claude/settings.local.json` permissions allowlist NOT yet written (for hands-off overnight; connection died before it was created). If wanted: read existing → merge → allow `Bash, Task, Monitor, Workflow, Write, Edit, Skill, Read, TodoWrite`. It's gitignored.

## To resume
Phases 1–6 BUILD DONE — **all 7 gates green** (store-smoke, controller-smoke, h6-all [deterministic], manifest-smoke, stub-smoke, validate-smoke, smoke-dod) and **all 38 v1 requirements Complete**. The Phase-6 xhigh adversarial verify (`wdjcb0dty`) was the last thing launched: read its synthesis (python-extract the `synthesis` object — don't Read the raw .output), and if it has blockers/false_green dispatch a targeted fix, else the SHELL BUILD IS COMPLETE. Then milestone lifecycle: `gsd-audit-milestone` → (CONFIRM WITH USER) → `gsd-complete-milestone`. Memory file `project_pmf_shell_milestone.md` also has the high-level state.
