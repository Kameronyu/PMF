---
status: reference
role: Cold audit of Reviewer A - Collection (Space Classifier) vs the redesign. One of the four segment audits behind PART4.
read-with:
  - reference/reviews/PART4--review-propagation-audit-and-agent-building-skills.md
  - architecture/PART3--architecture-design.md
supersedes: []
---

> **What this is:** per-segment audit detail behind PART4. **Read by:** the Job 2 (determination-tests) session.

# AUDIT — Reviewer A / Collection (Space Classifier) vs PART 3 redesign

**Auditor posture:** cold adversarial. I did not write the architecture. Default to prosecution; obsolescence must be earned. Targeted reviewer output: **Reviewer A — A-COLLECTION DIAGNOSIS** (reviews file lines ~176–307). Architecture under audit: `PART3--architecture-design.md` + `PART0--pipeline-flow.md`; as-ran failures (do-not-re-flag) from `asran-repo-report.md` §3/§9; operator annotations cross-referenced from `PART1--dependency-ordered-map.md`.

**Calibration applied (VERSION DELTA).** Under R1 (§1.5) the Space Classifier is no longer a raw-dump cranker; deep funnel analysis runs first on the full roster, and the Space Map is reborn as the **STEP-3 synthesizer** over analyzed-funnel records + quantitative rollups (§1.5, §6.4). VOC is added as a demand currency (§5). A test registry with named-but-empty slots is created (§7.3). **The operator has SETTLED the transformation-source direction** (PART1 D2; P5/P20/A31/P2): "Transformation can absolutely be derived from competition as long as the input and analysis is mechanical enough" (P5). This narrows several findings: the LIVE residue is **not** "VOC is the only source," it is "the determination is still definition-plus-vibes, not a mechanical test with named inputs." Decision-level findings persist where the decision still exists; artifact-wiring findings (parse bugs, missing output fields) are mostly moot because §1.1 root-causes them and the synthesizer reorder changes how/whether the field is produced.

---

## SECTION 1 — SEVERITY-RANKED GAP LIST

Legend: **annotated?** = was the reviewer finding covered by an adjacent operator `<annotate>`/P# (high propagation likelihood) or un-annotated (highest loss risk). **version** = LIVE / OBSOLETE-justified / FALSE-MOOT. **prop** = addressed / partial / absent.

---

### GAP-1 (SEV: CRITICAL) — DECISION 7 + DECISION 5/6: `bet_type` / `sophistication` / `mechanism` typing still lack a LICENSING SOURCE; `basis`-field rule supplies a place to write an assertion, not a datum that licenses it. **[FALSE-MOOT TARGET #1 — confirmed LIVE]**

- **Reviewer finding (quoted):** DECISION 7 — *"The claim that structural bet is page-readable without knowing the true desire is defensible … reading the *structural bet* off observable positioning, while deferring the *true transformation* to VOC, is a clean separation"* → RULE-SOUNDNESS **SURVIVED, narrowly**; but DATA-SUFFICIENCY **CANNOT ASSESS** because *"No `bet_types[]` array and no per-brand `bet_type` / `bet_type_basis` appear in the delivered `space-map.json`."* And DECISION 6: *"Calling a spec a 'mechanism' and then computing ownability on it … launders a feature into a UM-availability signal"* → RULE-SOUNDNESS **VIOLATION**.
- **operator-annotated?** YES, heavily. DECISION 7's `<annotate>` is the long "we need mechanical application of the rules … How do we verify whether another company fits the bet that we are making?" note. DECISION 6 carries three inline `<annotate>` agreements ("agreeV"; "mechanism classification should be given a test / made more mechanical"; "we do want to track all product features, but just needs to be separate from proposed mechanism"). DECISION 2's `<annotate>` ("Do they buy it to look at, buy a cool gift, look cool, be in the know") is the same root.
- **version-status:** LIVE. The Space Classifier's typing decisions did not disappear under R1 — §6.4 keeps "the same single owner of light-pass judgment," and §1.5/§6.4 explicitly retain transformation clustering, claim typing, niche/angle/**bet_type** clustering, and sophistication as its jobs. The synthesizer reads structured funnel rows instead of raw dumps, but it STILL ASSIGNS bet_type and sophistication. So the failure class (a property asserted from competitor surface without a licensing test) can recur at the synthesizer seam exactly as before.
- **prop:** PARTIAL. The architecture's answer is the `basis`-field rule: §6.4 change (3) — *"Emits per-property `basis` fields universally (claim-quoted evidence per call — extends the existing `bet_type_basis` pattern to every judgment)."* **But a `basis` field is a slot to WRITE an assertion's cited evidence into; it is NOT a licensing datum that says when the assertion is permitted.** The reviewer's exact charge against DECISION 7 was that bet_type is "read off the page WITHOUT needing to know the customer's true dream/desire" — the `basis` field records *which page text was read*, not *whether that page text licenses the bet_type call*. The actual licensing mechanism is deferred: §6.4 change (1) — *"Every classification it makes … runs through the D3 test slots once Job 2 writes them (○); until then it carries the run's known failure as a warning label."* §7.3 lists the slots (`bet-fit`, `mechanism-vs-feature` + believability gate, `sophistication`) but **content is ○ Job 2**. So: structure exists (a named registry slot), content deferred = PARTIAL.
- **architecture location checked:** §6.4 (changes 1+3), §7.3 (registry slots), §7.2 (Space Classifier row), §6.1 (Bet Compiler — `bet_type` + basis now also a bet-brief field, §6.1 contract list). NOTE: bet_type now ALSO appears upstream in the Bet Compiler contract (§6.1: "`bet_type` + basis"), so the *licensing-source* question is split across two seams (bet-brief and synthesizer) without either being given the test — which makes the residue slightly worse, not better, because it is now ambiguous which stage owns the bet_type determination test.
- **resulting flaw if not addressed:** The STEP-3 synthesizer emits `bet_type`, `sophistication`, and mechanism-ownability with a cited `basis` string but no procedure gating whether the citation actually supports the label. A spec typed as a "mechanism" (the reviewer's "Long battery life" / "Premium high-density display" examples) still gets ownability computed on it and inflates white-space. Market Selection Gate 2 (§6.5, "differentiating axis wired to OPEN bet_type … and mechanisms_in_play ownability") then gates on a laundered differentiator. This lands on the **Space Map synthesizer (§6.4)** and propagates to **Market Selection Gate 2/3 (§6.5)**. It is genuinely live but **CAN be absorbed at the Job-2 build session** — the registry slot exists; it does not need new architecture, it needs the operator to write the mechanism-vs-feature + bet-fit + sophistication test content. This is the operator's own stated next job ("mechanism classification should be given a test").

---

### GAP-2 (SEV: HIGH) — DECISION 2 + PHASE-2 "second weakest join": transformation token is a *possession-desire mislabeled as a transformation*; the determination is still definition-plus-clustering, not a test. **[VOC version-triaged carefully]**

- **Reviewer finding (quoted):** DECISION 2 — *"`novelty-object-own` is not an outcome that happens to the buyer — it is *the act of purchase itself* … reads as a *desire-to-possess* dressed as a transformation … a non-transformation promoted to transformation."* DATA-SUFFICIENCY: *"Clustering three unlike outcome-promises into one canonical 'novelty-object-own' is asserted, not demonstrated. The missing datum: a stated rule for *why* these three roll up to one transformation rather than three."* PHASE 2: *"the placeholder chosen to stand in its place is mislabeled against `definitions.md`, and that mislabel is carried as the decision's `run_transformation`."* THREAD CHECK net: *"Broken — possession-desire mislabeled (degrading)."* Tagged **"Most insidious finding (survives a careless read)."*
- **operator-annotated?** YES. DECISION 2 inline: "agree. It would be what happens after that. Do they buy it to look at, buy a cool gift, look cool, be in the know of cutting edge tech." PHASE-2 A24/A25: "i wonder what caused this classification to go south"; "i seriously doubt that these brands became successful if the true transformation their marketing sold was purely (own a cool object.) no way."
- **version-status:** LIVE, but **NARROWED** by the operator's D2 settle. The reviewer's framing leaned on S1/S8 ("true transformation is VOC-found"). The operator has explicitly overruled that as his own non-DR authorship (P20: "the doc that said this is the doc I wrote its not even a true DR source of truth"; P5: derivable from competition if mechanical). PART3 §2 re-verifies this against the full KB and CONFIRMS it (§2 item 1: "No passage anywhere in the 65 files requires transformation to be derived from VOC exclusively"). **So the residue is NOT "VOC was the only fix."** The residue is the part the operator AGREES is live and is the real defect: the clustering/labeling produced a non-transformation because the determination was "definition + the agent's own clustering judgment, no named inputs, no test" (confirmed in §1.1 A24-evidence: "the classifier ran on `definitions.md` definitions plus its own clustering judgment — no named inputs, no test, no DR bundle").
- **prop:** PARTIAL. The transformation test is a named registry slot — §7.3: `transformation` (D2 ○) — and §6.4 change (1) routes the synthesizer's transformation clustering through it once Job 2 writes it. §10 confirms "the transformation test itself (D2 direction settled ●, design ○)." VOC is added as corroboration (§5.1 Product 1 §1 transformation signal; §5.2 routes voc-market-signal §2 vocabulary to the synthesizer "for naming/clustering cells"). So both levers the operator named (a mechanical test + VOC vocabulary to ground naming) have STRUCTURE in the design — but the test CONTENT that would make `novelty-object-own`-style roll-ups impossible is ○ Job 2. Structure exists, content deferred = PARTIAL.
- **architecture location checked:** §6.4 (change 1, carries the run's `novelty-object-own` failure as an explicit warning label — quoted: "A24's `novelty-object-own` rolled three unlike outcome-promises into one label"), §7.3 (`transformation` slot), §5.1/§5.2 (VOC grounding), §10 (D2 routed), §2 item 1 (KB re-verification), PART1 D2 (operator settle).
- **resulting flaw if not addressed:** Until Job 2 writes the transformation test, the STEP-3 synthesizer can again cluster heterogeneous claim-categories into one mislabeled `run_transformation` and hand it to Market Selection as `run_transformation` (§6.5 decision input). The reviewer's "competitive-set-change" sub-test (a Divoom pixel-art display and a Pocket Operator synth do not share a competitive set) is NOT yet a named procedure anywhere — it is exactly the kind of test the registry slot must hold. **Needs the operator at Job 2** (it is a marketing-truth call about what licenses a transformation label), but does NOT need new architecture. NOT falsely-mooted: the warning-label honesty (§6.4) prevents silent recurrence but does not prevent recurrence.

---

### GAP-3 (SEV: HIGH) — DECISION 8 / PHASE-2 FATAL: durability-validation **CLASS** (not just the Trends parse bug) — does "unknown" still silently pass a gate?

- **Reviewer finding (quoted):** DECISION 8 — *"The delivered `space-map.json` contains **no `demand_trend` field anywhere** … the single most important supply-side validation for this exact product is, by the operator's own stated standard, silently disabled."* → DATA-SUFFICIENCY **VIOLATION (FATAL)**. RULE-SOUNDNESS SURVIVED (the "never guess; list unknown" rule is sound). THREAD CHECK: *"the #1 product risk's signal is missing."* PHASE-2 names it the **"Most fatal single finding."**
- **operator-annotated?** YES, and it is the deepest annotation in the segment (A21): "what are we actually going to assess the trend of? … The trend of people looking for the transformation? … the CAGR of the market? How is this mechanically calculated, not left up to an agent that decides what is trend without knowing what thing it needs to verify is trending … Look for this pattern of failure elsewhere in this project." Plus the data-sufficiency `<annotate>`: "data sufficiency may either be a wiring thing … or … whether or not the agent that starts searching for data is even asking for the right things."
- **version-status:** SPLIT — **parts OBSOLETE, the core LIVE.**
  - The *parse bug* (the proximate cause of the empty column) is OBSOLETE / already-absorbed: §1.1 root-causes it as wiring ("`fetch.js extractTrendSeries()` cannot parse Google Trends' deferred-XHR payload → `demand_trend.shape = "unknown"` for all 20 brands"); asran §9 #8 says the same. Do NOT re-flag the parse bug. §6.5 lists "repaired `demand_trend` (the Trends fix is implementation, already root-caused §1.1)."
  - The *durability-validation CLASS* — "what trend, of what, computed how" — is **LIVE**. This is the operator's A21 question and it is **not** answered by repairing the fetch. The architecture confirms this: §7.3 has a `trend/durability` slot with **content ○ Job 2**; §10 routes "trend determination or demand determination mechanics" to Job 2/3; PART0 STEP 4 GAP-2 even reframes part of the demand question (could-our-product-satisfy delta) as unspecced.
- **prop:** PARTIAL, with one genuinely sharp residual hole. The reviewer's prosecutable question — *can "unknown" still silently pass a gate?* — is the right adversarial probe. PART3 answer: §6.5 **Refuse condition** = *"`demand_trend` shape unknown above tolerance, or `voc-market-signal` absent when the run declares VOC active (soft-gate mode stays available as an explicit operator override, never a silent default — the D-08 lesson)."* This is a real, named gate that did NOT exist as-ran (as-ran ran SOFT-GATE MODE D-08 with all 6 cells carried through despite unknown trend — asran §2b, §9 #8). **However:** "unknown above tolerance" defers the *tolerance threshold* to ○ Job 2/4, and the soft-gate override is explicitly preserved. So the as-ran failure mode (unknown silently passing because soft-gate carried everything) is converted from a silent default into a **logged operator override** — but it is NOT eliminated; it is made explicit. That is the correct architectural posture (P9), not a gap. The genuine residue is narrower: **what `demand_trend` measures is still ○** — repairing the fetch makes the field populated, but A21's "trend of WHAT" (transformation-seeking vs niche-growth vs CAGR vs competitor-growth-rate) is undefined, so a *populated-but-measuring-the-wrong-thing* trend could pass the repaired gate. Structure exists, content deferred = PARTIAL.
- **architecture location checked:** §1.1 (parse bug root-cause — absorbed), §6.5 (refuse condition + repaired input), §7.3 (`trend/durability` slot, content ○), §10 (routed Job 2/3), asran §9 #8 / §2b (do-not-re-flag the wiring).
- **resulting flaw if not addressed:** If Job 2 does not define *what is being trended*, the repaired fetch produces a populated `demand_trend` that the synthesizer carries and Market Selection Gate 1 (§6.5, "trend durability via demand_trend.shape") gates on — but it may be trending the wrong keyword (e.g., brand-name search volume rather than transformation-seeking), so the gate passes a fad as durable. Lands on **Market Selection Gate 1**. **Needs the operator at Job 2** (A21 is a list of marketing-truth options only the operator can pick among). The *silent-pass* sub-risk the reviewer probed is largely closed by §6.5's refuse condition; the *measuring-the-wrong-thing* sub-risk is fully live.

---

### GAP-4 (SEV: MEDIUM) — DECISION 3: niche (`edc-aesthetic-collectors`) undertraced — no venue evidence, no raw_variants; "is this one niche or a Frankenstein stapling of two."

- **Reviewer finding (quoted):** DECISION 3 — RULE-SOUNDNESS CANNOT ASSESS: *"whether 'edc-aesthetic-collectors' is ONE niche or a stapling of two … is unestablished. Missing datum: the venue/community evidence that makes this a single niche."* DATA-SUFFICIENCY VIOLATION: *"The niche label carries no `raw_variants` in the cell the way mechanisms and claims do. The prompt's own RULE — 'Every canonical label must trace to raw variants actually present in the dumps' — is not visibly satisfied for the niche label."*
- **operator-annotated?** YES. DECISION 3 inline: "ok, we should put this test in then" (re the venue test). Adjacent A8/A14-style note: "we can study the niche in so many different ways. Do they have signal for this transformation … shared values beliefs / PMBDE's across this niche regardless of transformation." Also S6 `<annotate>`: "should we run a venue test just to look for venues? How would this work what step would this be in. again, what mechanical inputs would determine this."
- **version-status:** LIVE. Niche clustering remains a Space Classifier/synthesizer job (§6.4, §7.2 "niche venue/reading test"). The class can recur.
- **prop:** PARTIAL → leaning ADDRESSED-in-structure. The venue test the operator asked to "put in" is now a named registry slot — §7.3: `niche` (venue + reading + in/exclusion A9) and a standalone `venue` slot; §7.2 Space Classifier row lists "niche venue/reading test." VOC supplies the evidence base — §5.1 Product 1 §3 "Niche identity baseline … venue census (the venue test's evidence base — A8/A14)." The `basis`-field-universally rule (§6.4 change 3) would force the niche canonical to carry cited raw_variants, directly answering the "no raw_variants" data-sufficiency violation. So this finding is better-covered than the bet_type/transformation ones: structure (slot + basis field + VOC evidence base) exists; only test content is ○ Job 2.
- **architecture location checked:** §7.3 (`niche` + `venue` slots), §7.2 (Space Classifier row), §6.4 (basis-field rule covers the raw_variants gap), §5.1 Product 1 §3 (venue census).
- **resulting flaw if not addressed:** Until Job 2 writes the venue test, the synthesizer can again emit a niche canonical with no venue evidence and no raw_variants, and Market Selection gates a cell whose niche may be a Frankenstein of two communities (changing the saturation read per S7). Lands on **Space Map synthesizer → Market Selection**. **Can be absorbed at Job 2**; the basis-field rule already partially closes the raw_variants half at the architecture level (it does not wait for Job 2 — §6.4 change 3 is a committed ◆-adjacent design, not deferred content).

---

### GAP-5 (SEV: MEDIUM) — DECISION 4 & 6 / S3: claim-vs-feature and mechanism-vs-feature conflation — is there now a REAL test, or still "definitions + interpretation"?

- **Reviewer finding (quoted):** DECISION 4 — RULE-SOUNDNESS SURVIVED but DATA-SUFFICIENCY VIOLATION: *"'studio quality sound' is arguably a feature-as-spec, not an outcome-claim — the typing may itself be the inflation the inoculation warns against. This is a self-inconsistency: the rule is sound (survives) but the application contradicts the rule."* DECISION 6 — *"space-wide `mechanisms_in_play[]` clusters include genuine mechanisms … but ALSO non-mechanisms: 'Long battery life' … and 'Premium high-density display' … Those are FEATURES/specs."*
- **operator-annotated?** YES. DECISION 6 inline ("agreeV"; the long note redefining mechanism as "the pathway in which your product achieves the transformation, which must pass a believability gate before we use a mechanism … We might need to have some of these downstream analyzers ALSO read outputs and work through them with me"). DECISION 4 itself is un-annotated, but the operator's P5 note ("if transformation comes from reading the claims, the claims must be accurately labeled as well, as in product features should not be claims") covers the same class directly.
- **version-status:** LIVE. Claim-typing and mechanism clustering remain synthesizer jobs (§6.4, §7.2). Class recurs.
- **prop:** PARTIAL — and this is the cleanest "named-slot-with-believability-gate" case, so judge it carefully. The registry has BOTH halves the operator named: §7.3 `claim-type` slot AND `mechanism-vs-feature` (+ believability gate parameterized by niche, A37). §7.2 Space Classifier row lists "claim-typing, mechanism-vs-feature + believability gate." This is more than "definitions + interpretation" *as a structure* — the believability gate is exactly the operator's redefinition ("must pass a believability gate before we use a mechanism"). **But** the gate's parameters and the claim-vs-feature procedure are ○ Job 2. So: is a named-but-empty slot "addressed in principle" or a live hole? **Verdict: addressed-in-principle for the structure, live-hole for the content.** The architecture has done everything it can without the operator: it named the slot, attached the believability gate, parameterized it by niche, and routed content to Job 2. It cannot write the test for him because that is marketing truth (P6/§10). So this is a legitimate PARTIAL, not an ABSENT — the operator's own annotation ("work through them with me") confirms he expects to co-author it.
- **architecture location checked:** §7.3 (`claim-type` + `mechanism-vs-feature` + believability gate), §7.2 (Space Classifier row), §6.4 (basis-field on every typing call), §6.9 chief test 2 (believability re-tested downstream at copy stage — P13).
- **resulting flaw if not addressed:** Until Job 2, the synthesizer still types "studio quality sound" as an enlarged claim and "Long battery life" as a mechanism, inflating both the sophistication stage and the ownable-mechanism (white-space) count that Market Selection Gate 2/3 reads. Lands on **synthesizer → Market Selection Gate 2/3**. **Needs the operator at Job 2** (he explicitly wants to "go down the list and classify" with output examples). The believability-gate structure being pre-wired is a genuine improvement over as-ran; do not call it absent.

---

### GAP-6 (SEV: LOW / mostly OBSOLETE) — DECISION 5: `sophistication_stage` field absent from the chosen cell.

- **Reviewer finding (quoted):** DECISION 5 — RULE-SOUNDNESS SURVIVED; DATA-SUFFICIENCY CANNOT ASSESS: *"NO `sophistication_stage` field appears on the cell in `space-map.json` … The load-bearing Gate-3 field the prompt calls 'load-bearing' is, in the delivered artifact, absent for the decided cell."*
- **operator-annotated?** No direct annotation on DECISION 5 (the stage *mechanics* are un-annotated; the *mechanism-vs-feature inputs* to the stage are annotated under D6).
- **version-status:** Largely OBSOLETE as a *field-location* finding, LIVE only as part of GAP-5's typing-inputs problem. asran §5 / §9 #9 establishes that sophistication DOES exist in the run — on `per_brand[]` not `combos[]` ("these three fields are PRESENT in the run, just not at the cell grain"). The reviewer was working from the filtered chosen-cell artifact and read the cell-grain absence as a total absence. Under R1 the synthesizer emits a hybrid schema (§1.5 STEP 3, output schema ○) and §6.4 basis-field rule forces a basis on every judgment including sophistication. The *field-presence/grain* defect is an artifact-wiring issue the redesign's open schema + synthesizer reorder dissolves.
- **prop:** ADDRESSED (structurally) — the synthesizer's output schema is being designed fresh (§1.5 STEP 3, ○) so the cell-vs-per-brand grain mismatch is a schema decision the build session makes, not an inherited bug; GAP-1 (standalone vs pair-keyed quantification, §1.6) is the operator's explicit instruction to fix exactly this axis-grain question. The only LIVE remainder is the *input quality* to the stage call (GAP-5), not the field's existence.
- **architecture location checked:** asran §5/§9 #9 (field exists on per_brand), §1.5 STEP 3 (open schema), §1.6 GAP-1 (standalone quantification), §6.4 (basis rule).
- **resulting flaw if not addressed:** Minimal as a standalone — the stage will be emitted. The real risk is inherited from GAP-5 (the stage is computed off mistyped claims/mechanisms). Do not re-flag the field-absence as new; it is an artifact-wiring finding the reorder moots.

---

### GAP-7 (SEV: LOW) — DECISION 9 HAZARD: thin chosen cell stripped of its strongest demand evidence by the bet-exclusion logic.

- **Reviewer finding (quoted):** DECISION 9 — RULE-SOUNDNESS SURVIVED; **HAZARD (degrading):** *"the cell that the pipeline DECIDES ON … is simultaneously (a) thin and (b) stripped of its strongest demand evidence … `anti_fluke.qualifying_creatives:0` … A qualifying-creative count of zero in the decided cell is a red flag the artifact surfaces but does not resolve."*
- **operator-annotated?** YES — a long DECISION 9 `<annotate>`: "why did this ever happen? What is the design that excludes it? … I need to see what the bet logic actually is. What is the exclusion what is the cell, what is the cell used for? … bet needs to control for NTP." Plus the comparable-bet `<annotate>`: "make sure the bets are calibrated to control for niche and transformation. So like a bet that works for one niche cannot be determined to reach for another."
- **version-status:** SPLIT. The `qualifying_creatives:0` wiring cause is OBSOLETE/absorbed — §1.1 root-causes it ("Ad-longevity signal was never lifted from raw `ads/*.json` … `anti_fluke.qualifying_creatives = 0` everywhere while Flipper Zero's raw file holds 53 recovered ads with a 238-day top run"); asran §9 #11. Do NOT re-flag the empty creative count. The *bet-logic design question* (how exclusion interacts with thin cells; how bets control for NTP) is LIVE and is precisely what the operator asked to see.
- **prop:** PARTIAL/ADDRESSED. (a) The wiring is fixed: §6.5 lists "lifted ad-longevity (`qualifying_creatives` fed from raw ads)." (b) R1 directly attacks the thin-cell problem: §1.5 says the reorder "resolves several earlier worries (A22 thin-map …)" because ad-volume/spend/longevity are now quantified per transformation/angle on the full roster BEFORE the map, so a decided cell is no longer 3-brands-thin by construction. (c) The bet-logic/NTP-control design is moved upstream to the Bet Compiler contract — §6.1: "N/T/P similarity controls as three separate criteria sets (● P8) … functional-mechanism-equivalence definition for bet-evidence transfer (P19) … comparable-bet seeds each with fit-verification fields (A23 'how do we verify whether another company fits the bet')." That is a direct, named answer to the operator's "what is the bet logic / how do we verify fit" — content ○ Job 1.
- **architecture location checked:** §1.1 (qualifying_creatives root-cause — absorbed), §6.5 (lifted ad-longevity), §1.5 (thin-map resolved by reorder), §6.1 (bet-fit + NTP-control contract), asran §9 #11 (do-not-re-flag).
- **resulting flaw if not addressed:** Low. The reorder + ad-longevity lift substantially dissolve the thin-cell hazard; the bet-fit verification is a named Job-1 contract slot. Residual: the *interaction rule* between bet-exclusion and a still-thin cell is not itself a written test — but the operator owns this at Job 1. **Can be absorbed at Job 1.**

---

### GAP-8 (SEV: LOW, structural-honesty residue) — DECISION 1 / PHASE-2 weakest join: the over-claim guard is "a sentence, not a mechanism."

- **Reviewer finding (quoted):** DECISION 1 — RULE-SOUNDNESS SURVIVED (the provisional-flag tradeoff is DR-defensible); **BUT** *"the condition's enforcement is only a label … That guard is a *sentence*, not a mechanism."* PHASE-2: *"the reasoning is sound; the produced evidence does not instantiate the reasoning."*
- **operator-annotated?** YES — A6/A12-class notes ("we need to put VOC in this system going forward … i think that demand validation and these gates for the market selector should be fed VOC, full stop"); A12 ("how VOC and the supply side research are gonna go together to select the NTP pairing").
- **version-status:** LIVE but heavily mitigated, and the *direction* is operator-settled (the provisional/claimed-transformation scoping is the correct posture per P5; the defect was enforcement, not the tradeoff).
- **prop:** ADDRESSED (structurally). The "sentence not a mechanism" critique is answered by the whole P3 (contract-or-refuse) + P8 (orchestrator-run validators) machinery and the test registry: the over-claim guard becomes (i) the synthesizer's required `basis` field per judgment (§6.4), (ii) the transformation test gating the label (§7.3), and (iii) Market Selection's refuse condition + per-currency evidence rows (§6.5). The "evidence does not instantiate the reasoning" gap (the whole PHASE-2 verdict) is the exact thing §1.1 + P3 + the validators-as-blocking-steps redesign targets — the as-ran "every stage improvised over nulls" is named as the driver of P3.
- **architecture location checked:** §3 P3/P8, §6.4 (basis field), §6.5 (refuse + evidence rows), §7.3 (transformation test), §1.1 (null-input run as the P3 motivator).
- **resulting flaw if not addressed:** Low — the architecture's central thesis (de-trust the orchestrator, contract-or-refuse, evidence rows) is a direct structural answer. The residue is that the *transformation test itself* (the strongest mechanism) is ○ Job 2 — i.e., this collapses back into GAP-2. No separate action; tracked under GAP-2.

---

## SECTION 2 — OBSOLETE: RESOLVED BY REDESIGN

These reviewer findings targeted structures that are GONE *and* whose failure class cannot recur in the new shape. Each is justified; none re-flagged.

1. **"One agent cranks ALL raw dumps" (the segment's framing premise, line 180: *"reads ALL dumps — the only judgment stage in the light pass"*).** OBSOLETE. R1 dissolves the light pass (§1.5: "Light pass — dissolved") and reborns the Space Classifier as the STEP-3 synthesizer that "reads compact structured rows (one per funnel, plus aggregate tables), never the raw 40-funnels-of-copy" (§1.5 scale check; §6.4). The *raw-dump-cranking* failure class cannot recur because the input is now analyzed-funnel records, not raw copy. (Note: this moots the *input mechanism*, NOT the *classification decisions* — those persist, per GAPs 1–5.)

2. **DECISION 8 — the Trends parse bug as proximate cause of the empty `demand_trend` column.** OBSOLETE / already-absorbed. §1.1 root-causes it as wiring (`extractTrendSeries()` deferred-XHR); asran §9 #8 documents it; §6.5 marks the fix "implementation, already root-caused." Re-flagging the parse bug would violate the do-not-re-flag instruction. (The durability *class* — what to trend — remains LIVE: GAP-3.)

3. **DECISION 5 — `sophistication_stage` field absent from the cell** (as a field-location defect). OBSOLETE. asran §5/§9 #9: the field exists on `per_brand[]`; the reviewer read a filtered-artifact grain mismatch as total absence. The synthesizer's fresh open schema (§1.5 STEP 3) + GAP-1 standalone-quantification instruction (§1.6) dissolve the grain question. (Input-quality to the stage remains LIVE: GAP-5.)

4. **DECISION 7 / DECISION 5 / DECISION 9 — "field X absent from delivered `space-map.json`" data-sufficiency CANNOT-ASSESS verdicts** (bet_type fields, stage, demand_trend-presence, etc.), *as artifact-instantiation findings*. OBSOLETE as instantiation findings: these were "the prompt promised it, the delivered JSON omitted it" gaps. asran §9 #9/#11 shows several (bet_type, mechanisms_in_play, sophistication) actually ARE present on per_brand/top-level; the rest are emitted by a freshly-designed synthesizer schema. The *RULE-level* charges underneath them (no licensing test) survive as GAP-1/GAP-5 — those are NOT obsolete. Only the "the JSON didn't contain it" layer is moot.

5. **`anti_fluke.qualifying_creatives:0` as an unresolved red flag (DECISION 9 hazard, data side).** OBSOLETE / absorbed. §1.1 + asran §9 #11 root-cause the empty count (ad-longevity never lifted from raw `ads/`); §6.5 fixes it ("qualifying_creatives fed from raw ads"). The thin-cell *design* hazard is separately tracked (GAP-7) but the zero-count artifact is a closed wiring bug.

---

## SECTION 3 — VALIDATED: KEEP THESE (sound findings the redesign must not regress)

The reviewer found these MOVES sound. The redesign currently honors each; flagging them so a future revision does not regress them.

1. **DECISION 1 — the provisional/claimed-transformation scoping is DR-defensible AT THIS STAGE** (RULE SURVIVED). The reviewer affirmed that reading *claimed* transformations off competitors for the *sophistication* purpose is law-sanctioned, provided it is honestly scoped as provisional. The operator independently settled this (P5/P20). **Keep:** §6.4's explicit warning-label posture and §2's KB re-verification preserve exactly this honesty. Do not let a future revision quietly re-assert `run_transformation` as validated.

2. **DECISION 4 — the claim-typing SCHEMA and the FEATURE-vs-CLAIM TRAP inoculation are law-conformant** (RULE SURVIVED). The reviewer confirmed `direct/enlarged/mechanism/enhanced` maps cleanly to `definitions.md`/`angle.md`, and "mechanism alone is not a claim; it becomes claim_type 'mechanism' only when bound to an outcome" is verbatim-in-spirit. **Keep:** the registry's `claim-type` slot must preserve this schema and the trap, not loosen it.

3. **DECISION 5 — the mechanical sophistication rule (stage = highest claim_type tier 2+ live in-geo brands deploy; 5+ rule)** is "a faithful operationalization" (RULE SURVIVED). **Keep:** §6.5 Gate 3 and the `sophistication` registry slot must keep the *mechanical* rule; the only fix needed is clean typing inputs (GAP-5), not a new rule.

4. **DECISION 7 — reading the structural bet off observable positioning while deferring true transformation to VOC is "a clean separation the law supports"** (RULE SURVIVED, via Hormozi Actions-Only filter). **Keep:** the bet_type determination must stay anchored to court-admissible page signal (the `basis` field) — the LIVE gap (GAP-1) is the missing *test*, not the page-readability principle, which is sound.

5. **DECISION 8 — "never recompute or guess; list unknown" is correct DR logic** (RULE SURVIVED). **Keep:** §6.5's refuse-on-unknown-above-tolerance and the no-guess discipline. Do not regress to soft-gate-as-silent-default (the as-ran D-08 failure).

6. **DECISION 9 — keeping bet-evidence brands separate from cell saturation ("they prove the bet, not the cell") is logically clean**, and the single-comparable→dry-test-only override is "the Tradeoff Framework done consciously" (RULE SURVIVED). **Keep:** §6.1's separation of bet-fit from cell membership and the dry-test-not-commitment backstop.

7. **The PHASE-2 anti-glaze principle itself** — *"Internal coherence of the prompt is not a defense"* / *"the reasoning is sound; the produced evidence does not instantiate the reasoning."* This is the single most valuable methodological finding. **Keep:** it is institutionalized as P3 (contract-or-refuse) + P8 (orchestrator-run validators) + the §1.1 null-input lesson. Any future revision that ships a "rigorous prompt" without an enforcing validator regresses this.

---

## SECTION 4 — SHORTLIST: HIGHEST-SEVERITY FINDINGS STILL LIVE AND NEVER PROPAGATED INTO A MECHANISM

Ranked. Each is LIVE, and each is at best PARTIAL (named registry slot / R2 GAP = "structure exists, content deferred"); none has a *working mechanism* yet. All four are absorbable at the named build session without re-architecting — but they are the holes a build session must not skip.

1. **`bet_type` and `sophistication` have no licensing test — the `basis` field is a write-slot, not a license. [GAP-1 / FALSE-MOOT TARGET #1 — confirmed live]** The Space Classifier is reborn as the STEP-3 synthesizer but still ASSIGNS bet_type and sophistication; §6.4's universal-`basis`-field rule supplies a place to cite evidence, not a procedure that says when the label is permitted. Made slightly worse by bet_type now also living in the Bet Compiler contract (§6.1) with no clear owner of the determination test. → **Routed: Job 2 (registry slots `bet-fit`, `sophistication`, `mechanism-vs-feature`) + Job 1 (Bet Compiler bet_type basis).** Damage: laundered differentiator/stage reaches Market Selection Gate 2/3 (§6.5).

2. **The transformation determination is still definition-plus-clustering, not a mechanical test — `novelty-object-own`-class mislabels can recur. [GAP-2]** VOC grounding (§5) and the warning label (§6.4) reduce silent recurrence, and the operator's D2 settle correctly narrows the fix away from "VOC-only" — but the `transformation` registry slot's content is ○ Job 2, including the reviewer's competitive-set-change sub-test which exists nowhere yet. → **Routed: Job 2 (`transformation` test, co-designed with Job 3 VOC per §10).** Damage: a mislabeled `run_transformation` flows into Market Selection (§6.5) as the decision token.

3. **The durability-validation CLASS — "trend of WHAT" — is undefined; a repaired fetch can populate a trend that measures the wrong thing and pass the gate. [GAP-3]** The parse bug is correctly absorbed (do-not-re-flag); §6.5's refuse-on-unknown closes the *silent-pass* sub-risk; but A21's question (transformation-seeking vs niche-growth vs CAGR vs competitor-growth) is ○ Job 2/3. → **Routed: Job 2/3 (`trend/durability` slot).** Damage: Market Selection Gate 1 passes a fad as durable on a wrong-keyword trend — and the operator flagged this is a "look for this pattern elsewhere" systemic risk.

4. **claim-vs-feature / mechanism-vs-feature conflation — believability gate is named and pre-wired, but its parameters and the claim-vs-feature procedure are ○ Job 2. [GAP-5]** This is the best-covered of the four *structurally* (the believability gate matches the operator's own redefinition and is parameterized by niche in §7.3), so it is a legitimate PARTIAL, not ABSENT — but until Job 2 writes the procedure, specs are still typed as mechanisms/claims and inflate both the stage and the white-space count. → **Routed: Job 2 (`claim-type` + `mechanism-vs-feature` slots), with operator co-authoring per his "work through them with me" annotation.** Damage: inflated sophistication stage + ownable-mechanism count at synthesizer → Market Selection Gate 2/3.

**Auditor's bottom line.** Reviewer A — Collection was unusually law-faithful, and the redesign earned most of its obsolescence honestly: the raw-dump-cranking premise is genuinely dissolved (§1.5), and every artifact-wiring finding (Trends parse, qualifying_creatives, field grain) is root-caused in §1.1 / asran §9 and must not be re-flagged. **The four LIVE residues are all the same shape** — a classification decision (transformation, bet_type/sophistication, durability, claim/mechanism typing) that the operator agrees is still "definition + vibes," for which the architecture has correctly built the *slot* (registry §7.3, basis fields §6.4, refuse conditions §6.5) but deferred the *test content* to Job 2/3 with the operator. **None requires re-opening the architecture; all four require the operator at the Job-2/Job-3 build sessions**, which the design itself routes them to (§10) and which the operator's own annotations demand ("work through them with me"; "what mechanical inputs would determine this"). FALSE-MOOT #1 is confirmed: the synthesizer rebirth relocated the bet_type/sophistication assertion seam but did not supply it a licensing datum — the `basis` field is necessary-but-not-sufficient.
