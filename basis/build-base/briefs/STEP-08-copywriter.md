---
status: brief
role: Per-step build brief for STEP 8 — Copywriter + Copy Chief. Points (not copies) to the exact slices the build session needs.
read-with:
  - INDEX.md
step: 8
covers: Copywriter (constrained writer, built to spec) + Copy Chief (per-line test battery, greased-slide loop, ≤2 rounds → ★ operator). Writes the funnel copy under the locked architect brief.
---

> **What this is:** the reading list + duties for the session that builds the **Copywriter** and the **Copy Chief**. Open this, follow the pointers, build — nothing is copied, every line points into the base. **Start with §"What was already run" — the copywriter SPEC is the highest-leverage input** (the copywriter was never properly built in the as-ran run, so the spec it is built *to* is the as-ran reference). The loop ends at ★ operator after ≤2 chief rounds (P9).

## In one line
The Copywriter is a **constrained writer** built to `15-SPEC-copywriter.md` — brief's angle beats RAG's angle, blocked ports absolute, grade 6–7, greased-slide craft, "steal frameworks, not words" — fed the locked architect brief + slot-scoped verbatim RAG from the voc-bank copy bank. The Copy Chief reads the draft **per line** and runs five detachable tests (does-something, believability/install, greased-slide next-thought, readability, **register/Command+F**), loops ≤2 rounds, then ★ operator.

## ★ Start here — what was already run (highest leverage)
This step was **never properly built** in the as-ran run — so the highest-leverage input is the **spec the copywriter is built to**, plus the as-ran run's headline copy failure.
- **The spec (build target):** `pmf3/15-SPEC-copywriter.md` — the admitted copywriter spec (parent folder; catalogued in `reference/EXTERNAL-INPUTS-MAP.md` §B). **Read this first** — it is the behavior authority: constrained writer, locked format rules, blocked ports, greased-slide, grade 6–7, the OPERATOR VERIFICATION MANDATE (which already requires slot-scoped RAG). It does **not** contain the Copy Chief as a separate testing agent — that is new topology from A35/A34.
- **The as-ran headline failure (what the build must fix):** `reference/as-ran-repo/asran-repo-report.md` is the reader's guide to the run that conceded "NO VOC was run, so Spencer's Command+F verbatim check is unrunnable — word choices here are plausible, not verified against buyer language."
- (Do **not** re-prosecute the as-ran architect-writes-copy role collapse — it happened only because the copywriter was unbuilt; closed once both stages exist.)

## Then build from (authoritative — in this order)
1. **`architecture/PART0--pipeline-flow.md` → STEP 8b (Copywriting)** — the job logic (ingests the locked architect brief + slot-scoped verbatim from the copy bank + the funnel index; writes under the brief's angle; chief reads per line and loops; emits drafted funnel copy → ★ operator; feeds asset classification + the adversarial re-review). (STEP 8a VOC language bank is the copy bank it RAGs — a VOC-build concern, not this build's.)
2. **`architecture/PART3--architecture-design.md`:**
   - **§6.9** — the Copywriter + Copy Chief card. *The spine of the build.* Copywriter built to the spec; refuse on missing brief section fields (RAG-empty sections written conservative + flagged). Copy Chief = separate agent, the **five detachable tests**, loop contract (per-line verdicts `{line, test, verdict, predicted_next_thought?, evidence}`; **≤2 rounds then operator**). Models: copywriter quality, chief cheap-to-mid.
   - **§6.0** — soundness duties rows for Copywriter (grounds copy in voc-bank + blocked-ports **MR-005**; carries RAG-empty sections) and Copy Chief (register/Command+F = `CANNOT-EVALUATE` when voc-bank absent — **carried, not PASS**).
   - **§7.2** — knowledge scope: copywriting--spencer + --carl (primary craft), copywriting--mark (body structure), hooks--spencer (open-loop law); **excludes** hormozi copywriting + strategy law (the chief must not re-architect).
   - **§7.3** — chief-side registry slots: `line-does-something`, `believability-install`, `readability`, `register`. Content ○ Job 2; **slots** fixed.
   - **§4.3** — the splitting verdict: Copy Chief NEW AGENT (● A35); the line-reader (P17) is ABSORBED into the chief's greased-slide test, detachable later.
3. **`architecture/PART1--dependency-ordered-map.md` → D8** — by annotation:
   - **A35** (D8) — the copy-chief: tests whether each line **does something**, is believable for installing the belief ("i will ship this by aug 30" fails), the greased-slide dumb-consumer prediction, grade 6–7 "telepathic comprehension."
   - **A34** (D3/D7/D8) — the next-thought prediction loop routed back to the writer.
   - **P13** (D3/D8) — the **feature-as-believed-mechanism** test (maximum-strength / 10g-melatonin): features sell when they are believable mechanisms for the transformation at that awareness level.
4. **`architecture/PART2--build-order-roadmap.md` → Job 8 (D8)**; gates stacked: architect output real + voc-bank exists + Job-2 test patterns exist. Build order unchanged. This build wires the writer + chief + loop; Job 2 fills the chief's test content.

## Marketing-soundness duties (non-negotiable — read first)
- `standards/BUILDER-DIRECTIVE.md` → `standards/SPEC-marketing-soundness.md` → `standards/marketing-rule-register.md`. Apply **PART3 §6.0**.
- Every load-bearing chief verdict carries a `grounding_ref` + `carried_risks[]`; the register/Command+F check reports `PASS | FAIL | CANNOT-EVALUATE` and **must return CANNOT-EVALUATE (never PASS) over an empty/absent voc-bank**.
- **MR-005** (blocked ports — a debut claims only what it can back) is absolute in the copywriter. The verbatim **custody chain** (P7) ends here: the Command+F register check is its terminal gate — copy vocabulary must exist in buyer language.

## Adversarial findings this build must honor (`reference/reviews/`)
Read **`AUDIT-funnel.md`** (the copy section) in full; the load-bearing ones:
- **GAP A (FATAL at copy) — Command+F register-check runnability.** The single fatal copy charge. Mechanism is named (chief test #5 + voc-bank-exists gate), but runnability hinges on (a) the **VOC soft-gate toggle** — the same D-08 override that produced the as-ran VOC-absent run — and (b) the copy-bank content (○ on three counts: GAP-3, GAP-5, §5.3 schemas) being built *after* the copywriter per §11 sequencing. **A green register-check over an empty corpus is worse than the honest as-ran "unrunnable."** Honor the L3 rule (see Open decisions).
- **GAP F — feature-as-believed-mechanism test** (P13): chief test #2 names it but content is ○ Job 2; until then it falls back to a generic assert-vs-install vibe and can't catch a spec line that doesn't function as a believed mechanism for this niche.
- **AUDIT-reviewerB B5** — no "claim vs product-fact" check: a physical-capability caption ("the screen is clear / pixels float") has no test binding it to a product-fact / CLAIM-LIST ledger. The chief battery is detachable-by-design and could gain such a test, or the asset CLAIM-LIST is declared the source of truth.
- **KEEP (do not regress):** outcomes-over-features + no-over-claim craft (SURVIVED); blocked-ports absolute; grade 6–7 readability; "steal frameworks, not words."

## Open decisions touching this step (`OPEN-DECISIONS.md`)
- **B2 / L3** — **"VOC present" as a HARD precondition for the copy stage** (no soft-gate path into copy) + sequence this Job 8 build **behind Job 3's pass-2 bank contract**. **Highest-value here** — without it the chief signs a green register-check over an empty corpus.
- **A1/A2/A3** — the three build-time meta-skills; the chief's empty tests are the `grounding-and-refuse` "empty test = REFUSE, never PASS" case; the chief's I/O is a `contract-congruence` target.
- **Dependencies that are NOT this build's call:** the copy-bank's phrase×frequency shape (R2 GAP-3/GAP-5) → **Job 3 (VOC build)**; the chief's believability/register test content → **Job 2**. Wire the I/O so these drop in; do not hard-code them.

## Done =
The Copywriter writes under the **locked architect brief** with **slot-scoped verbatim RAG**, blocked ports absolute, grade 6–7, refusing/flagging RAG-empty sections; the Copy Chief runs its **five per-line tests**, returns **`CANNOT-EVALUATE` (never PASS) on register/Command+F when the voc-bank is absent**, loops **≤2 rounds then ★ operator** — with "VOC present" wired as a hard precondition (pending the L3 ruling) and every chief test whose content it can't yet set left as a wired ○ slot pointing at Jobs 2/3.
