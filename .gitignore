# Raw HTML corpus (large, reproducible from fetch.js)
corpus/*/raw/

# Ad library screenshots (binary, large)
ads/*_adv.png

# Python virtual environment (PEP 668 — use .venv/bin/python explicitly)
.venv/
