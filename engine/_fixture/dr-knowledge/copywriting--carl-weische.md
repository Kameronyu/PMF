# copywriting--carl-weische.md — DR KNOWLEDGE FIXTURE STUB (engine smoke only)

Placeholder. The real DR knowledge is an external, operator-supplied wire-time input (the old ~/knowledge/dr-marketing content is NOT vendored). This stub exists so the inject-*-dr smokes can prove the bundler reads --dr-dir.
